# Change Log

All structural and performance optimizations must be recorded here.

| Date | Summary | Files Changed | Reason | Risk Level | Follow-up Verification |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-06-02 | footer: enlarge the back-to-top text button `CLICK HERE TO GET TOP` (`.ak-btt-text`/`.ak-btt` in `sections/ak-footer.liquid`). **CSS-only size/padding change.** Dev-approved + live-promoted. | `sections/ak-footer.liquid`, `docs/change-log.md`, `docs/runtime-test-checklist.md`, `docs/source-of-truth.md` | Declan wanted the footer back-to-top control visibly bigger (it rendered ~10.45 px desktop / ~9.35 px mobile because the site root font-size is ~11 px). Four CSS-value edits only in `sections/ak-footer.liquid`: `.ak-btt` padding `6px 10px` → **`8px 14px`** (proportional tap target); `.ak-btt-text` desktop `font-size:0.95rem` → **`1.6rem`** (~17.6 px) and `letter-spacing:0.18em` → **`0.14em`**; mobile (`@media max-width:680px`) `font-size:0.85rem` → **`1.2rem`** (~13.2 px) and `letter-spacing:0.16em` → **`0.14em`**. Runtime-verified on dev: text stays one line (208 px desktop / 156 px mobile, ~344 px headroom — no wrap, no horizontal overflow), click scrolls to top (400→0), `:focus-visible` magenta outline present. **Visible copy `CLICK HERE TO GET TOP`, `id="AkBackToTop"`, `aria-label="Back to top"`, the scroll-to-top JS, `:focus-visible` outline, hover/focus `#ff33ff`, reduced-motion behavior, footer layout/divider/copyright, Mystery Box iframe/customer flow, product cards, cart, header/nav, `sections/ak-top.liquid`, `layout/theme.liquid`, particle code, app embeds, Shopify admin, and JSON templates were untouched.** Footer is a shared section on 8 templates → one edit updates all. | Low (CSS-only, four values in one self-contained section; copy/id/aria/scroll-JS/focus/reduced-motion preserved; runtime-verified desktop + mobile). | Pushed to dev `185910067480` (Declan visually approved). Scoped live push to `184615043352` via `--only sections/ak-footer.liquid`. Verify on live `/pages/the-collective`, `/pages/club-hub`, `/pages/narrative`, `/pages/2025`, `/pages/badmusic-2025`, `/pages/mystery-box`, `/collections`, `/collections/master-collection`: footer renders, button reads exactly `CLICK HERE TO GET TOP`, visibly larger, one line, no overflow, click scrolls to top, focus-visible outline + hover `#ff33ff`, Mystery Box iframe loads (no interaction). No new console errors (only pre-existing/app-side `video-player`, `shop.app` CSP, crate `THREE.WebGLState`). |
| 2026-06-02 | nav: enlarge the CandyFacts desktop header/nav icon (`.ak-top-candy-img` in `sections/ak-top.liquid`). **Visual-only CandyFacts icon sizing change.** Dev-approved + live-promoted. | `sections/ak-top.liquid`, `docs/change-log.md`, `docs/runtime-test-checklist.md`, `docs/source-of-truth.md` | Declan wanted the CandyFacts image button in the site-wide `ak-top` nav (the right tile of the BAD+Candy duo, linking to `/pages/mystery-box`) visibly bigger on desktop. Two CandyFacts-scoped CSS edits only: `.ak-top-candy-img` `transform` `scale(1.28)` → **`scale(1.52)`**, and `.ak-top-candy` padding `clamp(6px,1vw,10px)` → **`clamp(4px,0.8vw,8px)`** (candy padding now remains `clamp(4px,0.8vw,8px)`). `transform`/`padding` only → no layout reflow: tile footprint, `--duo-h`, `.ak-top-duo-tile` height, and grid all unchanged, so MONIKER does not move. Measured on dev: CandyFacts ~241×195 @1440 (was ~198×159 at the original 1.28); MONIKER clearance 11/8/8 px @1440/1280/1024 (no overlap); at ≤980 px the grid is 2-col and MONIKER drops to its own row (overlap structurally impossible). Pre-existing note: in the 768–980 px band the wide candy image's bounding box overlaps BAD's box (geometry that existed at 1.28 too; candyfacts.png transparent margins reduce the visible collision; no page overflow). **BAD icon, MONIKER label, `/pages/mystery-box` route, `request.path` active-state, mobile drawer (`.ak-top-mobile-image img` 120px), Mystery Box iframe/customer flow, product cards, cart, footer, `layout/theme.liquid`, particle code, app embeds, Shopify admin, and JSON templates were untouched.** `.ak-top-candy:active` press-shrink (`scale(1.15)`) left as-is. | Low (two CandyFacts-scoped CSS values in one section file; no reflow; BAD/MONIKER/route/JS preserved; runtime-verified at 1440/1280/1024/970 on dev). | Pushed to dev `185910067480` (Declan visually approved). Scoped live push to `184615043352` via `--only sections/ak-top.liquid`. Verify on live `/collections/master-collection`, `/pages/club-hub`, `/pages/mystery-box` (iframe loads, untouched, candy `is-active`), `/` (splash): CandyFacts visibly larger, BAD unchanged, MONIKER not moved/overlapped, candy links `/pages/mystery-box`, mobile drawer unchanged, cart renders. No new console errors (only the pre-existing `video-player` registry warning + a prior 404). |
| 2026-06-02 | nav: rename the site-wide nav/menu label from `CLUBHOUSE` to `MONIKER` (desktop tile + mobile drawer in `sections/ak-top.liquid`). **Visible label-only change.** Dev-approved + live-promoted. | `sections/ak-top.liquid`, `docs/change-log.md`, `docs/runtime-test-checklist.md`, `docs/source-of-truth.md` | Declan wanted the visible "club hub" nav tile to read MONIKER. Changed only the inner text of the two visible label instances — line 358 (desktop `.ak-top-label` tile) and line 368 (mobile drawer link) — from `CLUBHOUSE` to `MONIKER`. The `href="/pages/club-hub"`, the `request.path == '/pages/club-hub'` active-state conditionals, the `ak-top-tile`/`ak-top-contact` classes, the `data-text` glint (derived at runtime from `textContent`, so it auto-updates to MONIKER), and all header/nav/cart/menu/drawer JS are byte-identical. Net diff: 2 insertions / 2 deletions. **The `/pages/club-hub` route and active-state logic were preserved; `sections/ak-top.liquid` is the only storefront file changed.** No Mystery Box / Mystery Crate, product-card, cart, footer, `layout/theme.liquid`, particle code, app embeds, Shopify admin, page handle, or JSON templates touched. | Low (two inner-text string swaps in one section file; route/handle/active-state/classes/JS preserved; runtime-verified on dev + live). | Pushed to dev `185910067480` (Declan visually approved). Scoped live push to `184615043352` via `--only sections/ak-top.liquid`. Verify on live `/pages/club-hub` (MONIKER tile `is-active`), `/collections/master-collection`, `/pages/mystery-box` (iframe loads, untouched), `/` (splash, no nav): desktop tile + mobile drawer read MONIKER, both link to `/pages/club-hub`, other nav labels unchanged, MENU drawer opens/closes, cart renders. No new console errors. |
| 2026-05-28 | products: add tag-driven NEW badge (red comic burst sticker). Triggered by Shopify product tag `new` (case-insensitive). Removes the old hardcoded handle-based NEW system. Dev-approved + live-promoted. | `snippets/card-product.liquid`, `sections/main-collection-product-grid.liquid`, `assets/base.css`, `docs/change-log.md`, `docs/runtime-test-checklist.md`, `docs/source-of-truth.md` | Two NEW indicators were in play. The theme already had a hardcoded handle allow-list (`candyfacts-mystery-box-combo`, `candyfacts-zombie-slayer-trucker-hat`, `candyfacts-label-t-shirt`) in `sections/main-collection-product-grid.liquid` that added `grid__item--new` to the `<li>` and rendered a magenta-glow `::after` pseudo-element NEW — section-scoped, master-collection only, no admin control, easy to drift out of sync. Declan wanted (a) admin-controllable trigger via the standard Shopify product tag, (b) site-wide reach across every `card-product` surface, (c) a louder comic-pop visual. **Liquid (`snippets/card-product.liquid`):** added two `{%- liquid -%}` assigns (`ak_card_tags_lower = card_product.tags | join: '|' | downcase | split: '|'` and `ak_card_is_new_tag = false; if ak_card_tags_lower contains 'new'`) — whole-tag case-insensitive match that won't false-positive on `news`/`renew`. Added an `{%- elsif ak_card_is_new_tag and card_product.available -%}` NEW branch to BOTH `.card__badge` blocks (no-media + main media paths). Priority chain preserved: sold-out (first, unchanged) → NEW (second, new) → sale (third, unchanged). Span markup: `<span class="badge badge--bottom-left badge--new ak-badge-new">NEW</span>`. **Old-system removal (`sections/main-collection-product-grid.liquid`):** deleted the `ak_new_handles = '…' | split: ','` assign, deleted the per-iteration `ak_is_new = false / if ak_new_handles contains product.handle / assign ak_is_new = true` block, deleted the `{% if ak_is_new %} grid__item--new{% endif %}` class condition (the `<li>` now reads `class="grid__item"`), and deleted the scoped `.grid__item--new .card-wrapper::after` CSS block + its `@media (max-width: 749px)` companion. No Dawn grid/facets/sorting/pagination/card layout/sold-out diagonal banner CSS touched. **Comic-burst styling (`assets/base.css`):** new rule `.card-wrapper .badge--new.ak-badge-new` next to the base `.badge { ... }` baseline so cascade-order wins. Red `#e3192c` fill with a soft `radial-gradient(circle at 50% 42%, rgba(255,244,168,0.22) → transparent 55%)` cream inner highlight. 32-vertex `clip-path: polygon(...)` 16-spike comic burst silhouette (inner radius ~0.62 → deep valleys for dramatic spikes). Oversized cream-white `#fff8e1` Impact uppercase `2.3rem` `NEW` with 9-direction `text-shadow` simulating a thick black stroke. Layered `filter: drop-shadow(0 0 0 #111) drop-shadow(3px 3px 0 #111) drop-shadow(0 5px 6px rgba(0,0,0,0.45))` for comic-paper outline + offset drop + soft lift (drop-shadow over box-shadow because clip-path clips box-shadow). `transform: rotate(-12deg)` playful tilt. `width/height: 6.6rem` desktop / `4.6rem` mobile (≤480px), `pointer-events: none`, `z-index: 22`. Lives inside Dawn's `.card__badge.top.left` Grid placement → top-left corner of card. **Mystery Box / Mystery Crate iframe / `customer_id` handoff / prestart / release shell, `sections/main-product.liquid`, every cart section, `sections/ak-top.liquid`, `sections/ak-footer.liquid`, `layout/theme.liquid`, all particle code, all JSON templates, all Shopify admin / product / tag / app-embed state — UNTOUCHED.** No JS added, no images added, no animation. | Medium-low (3 storefront files; shared snippet reaches 7 card-rendering sections so blast radius is wide, but the change is additive in the snippet, removal-only in the section, and CSS-only in the asset; sold-out / sale chain byte-identical; runtime-verified on dev + scoped live push). | Scoped dev push to `185910067480` succeeded. Scoped live push to `184615043352` via `--only` for the three theme files. Open `/collections/master-collection` and `/collections/candyfacts` on live → only `candyfacts-mystery-box-combo` (the currently `new`-tagged product) shows the red comic burst, other products show no badge, no `grid__item--new` class in DOM, no magenta `::after` NEW. Old system fully gone. Sold-out / sale badges unchanged on any product that has those states. Mystery Box iframe/customer flow untouched (no interaction performed). Mobile 375 / 320 px sweep. Console — only pre-existing `video-player` CustomElementRegistry warning, no new errors. Adding the `new` tag to a product in Admin makes the badge appear; removing it makes it disappear. |
| 2026-05-28 | footer: simplify back-to-top control to a text-only button reading `CLICK HERE TO GET TOP`. Dev-approved + live-promoted. | `sections/ak-footer.liquid`, `docs/change-log.md`, `docs/runtime-test-checklist.md` | The previous control was a 64×64 rounded-square ring with an SVG chevron and a small "Back to Top" caption underneath, plus a launch-pulse keyframe + chevron-lift keyframe triggered by an `is-launching` class toggle in JS. Declan wanted a simpler text-only affordance with the exact visible copy `CLICK HERE TO GET TOP`. Single-file scoped change to `sections/ak-footer.liquid` only. Replaced the `<span class="ak-btt-ring">` + `<svg class="ak-btt-arrow">` + `<span class="ak-btt-label">Back to Top</span>` button inner markup with a single `<span class="ak-btt-text">CLICK HERE TO GET TOP</span>`. Removed now-unused CSS: `.ak-btt-ring`, `.ak-btt-ring::before`, `.ak-btt-arrow`, `.ak-btt-label`, hover/active/launching variants, and the `@keyframes akBttPulse` + `@keyframes akBttLaunch` blocks. Removed the dead `btn.classList.add('is-launching')` + 520 ms `setTimeout` toggle in the inline JS (the keyframes it triggered no longer exist). Added a slim `.ak-btt-text` rule (Impact, uppercase, 0.95rem, letter-spacing 0.18em) plus a hover/focus rule that turns the text AmeriKid magenta `#ff33ff`. Preserved: `id="AkBackToTop"`, `aria-label="Back to top"`, `:focus-visible` magenta outline (scaled to 4 px offset / 6 px radius for the text target), `window.scrollTo({top:0,behavior:b})` + `document.body.scrollTop=0` + `document.documentElement.scrollTop=0` triple-fallback, reduced-motion guard (smooth → auto, color transition off), the 680 px responsive break (text drops to 0.85rem / letter-spacing 0.16em), the `.ak-footer` shell + divider + copyright row + `<footer role="contentinfo">` semantics. Net change: 20 insertions, 111 deletions (−91 lines). **No Mystery Box, product card, cart, `layout/theme.liquid`, `sections/ak-top.liquid`, or particle code touched. No JSON templates touched. No app embed or admin change.** | Low (single self-contained section file; behavior contract preserved; CSS removals are confined to the deleted ring/arrow/label DOM that no longer exists; JS scroll fallback unchanged). | Push to development theme `185910067480` → Declan visually approved. Live dry-run against `184615043352` → clean (only `sections/ak-footer.liquid` in the upload set). Live push to `184615043352` → verify footer renders on `/pages/the-collective`, `/pages/club-hub`, `/pages/narrative`, `/pages/2025`, `/pages/badmusic-2025`, `/pages/mystery-box`, `/collections`, `/collections/master-collection`; `CLICK HERE TO GET TOP` text visible; click scrolls to top (smooth desktop, instant reduced-motion); keyboard tab shows magenta `:focus-visible` outline; hover/focus turns text magenta; mobile 375 / 320 px does not wrap badly; Mystery Box iframe still loads + `customer_id` handoff intact; no new console errors. |
| 2026-05-22 | mystery-box: hide theme particle canvas (`ak-mb-canvas`) via CSS-only rule on `/pages/mystery-box`. **Development-theme batch only — NOT live.** | `templates/page.mystery-box.json`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The last drawing non-header theme canvas was `ak-mb-canvas` (ambient particle background in `custom_liquid_mysterybox_embed`). A safety analysis found it shares the embed's SINGLE main IIFE and combined guard (`if(!embed || !iframe || !canvas || !frame) return;`) with the Mystery Crate iframe + `customer_id` handoff + iframe-`src` construction (which run AFTER the guard), with no standalone particle IIFE and no particle-only early-return (the only `if(reduced)` branch still draws a static frame). So a JS guard-disable/early-return would risk breaking iframe loading / customer flow and was explicitly NOT used. The ONLY low-risk path — a **CSS-only hide** — was implemented: added `#ak-mysterybox-embed .ak-mb-canvas { display: none !important; }` to the embed's `<style>` block (higher specificity than the base `.ak-mb-canvas{display:block}`, so it wins). Precise char-diff confirms the ONLY change is the +223-char CSS insertion; zero JS/iframe/`customer_id` bytes changed. The canvas element stays in the DOM (its rAF JS still runs harmlessly) but is `display:none` (not composited → no visible particles, no paint cost). **Mystery Crate iframe/app, `customer_id`/purchased/preview/email-link flow, prestart, release shell all untouched.** No customer/checkout/claim interaction performed. No replacement effects. **Revert:** `git revert <this-commit>`, or remove the `#ak-mysterybox-embed .ak-mb-canvas { display: none !important; }` rule. | Low (CSS-only, additive, scoped; no JS/iframe/customer-flow touched; verified char-diff = CSS insert only; runtime-confirmed canvas display:none + iframe loads). | Push to development theme `185910067480` only. Open `/pages/mystery-box` on the dev preview → `ak-mb-canvas` no longer renders a particle background (computed `display:none`, `offsetParent` null), background plain; iframe loads (`src=https://amerikid-mystery-crate.vercel.app`); `ak-mb-frame` + prestart/lock overlay present; `ak-top` header inert. Verify (read-only, no claim/checkout) the public preview path loads and the `customer_id` handoff still builds the iframe src. Confirm CandyFacts unchanged (combo product `ak-product-field` inert, `/collections/candyfacts` `ak-collection-field` inert). Mobile 375/320 px. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-22 | homepage: restore pre-prototype landing (`templates/index.json` only). **Development-theme batch only — NOT live.** Hybrid revert: removes the game-landing prototype, re-enables the old splash + bouncing kid, keeps the homepage particle background OFF. | `templates/index.json`, `docs/change-log.md`, `docs/source-of-truth.md` | The live-promotion readiness review flagged that the unshipped branch bundled a homepage game-landing prototype (`9ed6224`: `templates/index.json` + new `sections/ak-home-game-landing.liquid`) that would replace the live homepage on a full push. Declan asked to restore the pre-prototype homepage without undoing any particle-removal work. The prototype commit had ALSO disabled the old homepage's `ak-collection-bg` (particle background), `ak-landing` (splash), and `custom_liquid_xPURQD` (bouncing kid) — so a wholesale revert to the pre-prototype `index.json` (`83bf952`) would have reintroduced the homepage particle background. Instead, a **hybrid revert** was made in `templates/index.json` ONLY: (1) removed the `ak-home-game-landing` section object + its `order` entry; (2) re-enabled `ak-landing` (removed its `"disabled": true`); (3) re-enabled `custom_liquid_xPURQD` (removed its `"disabled": true`); (4) **kept `ak-collection-bg` `"disabled": true`** so no homepage particles return. `sections/ak-home-game-landing.liquid` left in the repo but dormant (only `index.json` referenced it; not deleted, per preservation-first). No section/JS/CSS files touched; no particle-removal commit reverted. Resulting homepage order: `ak-collection-bg` (disabled) → `ak-landing` (on) → `custom_liquid_xPURQD` (on). Verified on the local working-tree preview (`127.0.0.1:9292`): title "AMERIKID", splash + bouncing kid render, no game-landing, no `ak-cbg-cv` canvas, zero canvases (plain black). (Note: the dev-theme preview briefly showed Shopify's "Something went wrong" from a stale intermediate theme-dev sync of the rapid mid-edit saves; the explicit `shopify theme push` re-uploads the clean final files and resolves it.) | Low (single template file; re-enables sections that were active and working pre-prototype; valid JSON; no particle background reintroduced; local working-tree render confirmed). | Push to development theme `185910067480` only. Open `/` on the dev preview → old fullscreen splash (video/image) renders, bouncing kid logo bounces, click-through to `/pages/mystery-box` works, NO game-menu prototype, NO particle background (no `ak-cbg-cv` canvas, plain black), `prefers-reduced-motion` instant nav. Confirm all other routes unchanged (every particle lane still inert; `ak-mb-canvas` still protected). Mobile 375/320 px. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-22 | particles: disable **ak-top site-wide header** background (`ak-top-canvas` / `#ak-top-cv-{id}` in `sections/ak-top.liquid`, included by 31 templates). **FINAL particle lane — all non-protected particle backgrounds are now complete on dev.** **Development-theme batch only — NOT live.** | `sections/ak-top.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | `ak-top` is the shared site-wide header/nav section (929 lines, 31 templates) — the highest-blast-radius lane and the last remaining particle system. Its single main IIFE (line 380) contains BOTH the header/nav/cart/menu/drawer/label behavior AND the particle code; the header behavior (dock/nav micro-animations, cart hover, `cart:updated` count listener, `openDrawer`/`closeDrawer`/click-outside, label `data-text` setup) all initializes BEFORE the particle guard at line 550 (`if(!cv || prm) return;`), and the particle nested IIFE (line 552: canvas/getContext/dust/floaters/shooters/rAF/IO) follows it. A top-of-IIFE early return would break the header and was explicitly NOT used. One scoped **guard-disable** (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): `if(!cv || prm) return;` → `if(!cv || prm || true) return;`, routing every visitor into the section's own already-tested reduced-motion no-particle path — a known-good state where the header fully works. The `<canvas class="ak-top-canvas" id="ak-top-cv-{id}" aria-hidden>` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed on dev:** header shell, nav labels, MENU drawer open (verified via click), cart button, `cart:updated` count, cart hover, dock/nav micro-animations, `akTopGlint` CSS sheen, `akTopCartBob` CSS bounce, mobile drawer. Mystery Box's protected `ak-mb-canvas` confirmed STILL DRAWING and untouched (different file). Prior ak-top perf-pass edits (DPR cap, glint blur removal, contrast removal) untouched. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **Revert:** `git revert <this-commit>`, or change `if(!cv || prm || true) return;` back to `if(!cv || prm) return;`. | Low (mechanism: one guard line reusing the proven reduced-motion no-particle path; header/nav/cart/menu/drawer code runs before the guard and is byte-identical; drawer open verified). Site-wide blast radius (31 templates) so QA spans representative routes. | Push to development theme `185910067480` only. Open representative routes (`/collections/master-collection`, `/products/candyfacts-mystery-box-combo`, `/cart`, `/pages/club-hub`, `/pages/narrative`, `/pages/the-collective`, `/collections`) on the dev preview → header background plain solid black, no visible particle field, `ak-top-canvas` present but blank (default 300×150, 0 drawn pixels). Verify header shell + nav labels + glint sheen render; open/close the MENU drawer; click-outside closes; cart button + live cart count work; cart bob animates; mobile 375/320 px drawer. Confirm `/pages/mystery-box` outer shell + iframe intact and protected `ak-mb-canvas` still drawing (no crate-flow interaction). Confirm all prior-lane page canvases remain inert. Console — no new errors. **All particle lanes complete on dev.** Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-22 | particles: disable **cart page** backgrounds (`ak-cart-field` in `sections/main-cart-items.liquid` + `ak-cart-footer-field` in `sections/main-cart-footer.liquid` → `/cart`). **Development-theme batch only — NOT live.** | `sections/main-cart-items.liquid`, `sections/main-cart-footer.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The cart page (`/cart` → `templates/cart.json` → `main-cart-items` + `main-cart-footer`) drew TWO ambient dust/floaters/shooters canvases behind the cart content. Each lives in a dedicated, clearly-labeled standalone `<script>` IIFE that is canvas-only (`getElementById('ak-cart-canvas-…'|'ak-cart-footer-canvas-…')` + guards, then particle setup) and fully separate from the cart functionality (line items, quantity, remove, cart form, subtotal/totals, checkout, dynamic/additional checkout buttons, cart notes, discounts, cart-errors, empty-cart state, AJAX updates) which is Dawn Liquid + Dawn's external cart JS, and from the cart drawer/notification snippets (which have NO canvas). Two scoped changes (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): an early `return;` inserted as the first statement inside each particle IIFE, so neither seeds dust/floaters/shooters, sizes its canvas, or starts its rAF loop / IO. Both `<canvas>` elements stay in the DOM but draw nothing (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed on dev in BOTH empty and item-present states** (a harmless test item was added via the add permalink — no order — then cleared via `/cart/clear.js`): empty-cart state, line item, quantity input, remove button, subtotal ($60), checkout button, dynamic checkout, `ak-top` header particles. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **With cart done, `ak-top` is the ONLY remaining particle lane (saved for last).** **Revert:** `git revert <this-commit>`, or remove the `return;` line from each cart particle IIFE. | Low (two one-line early `return;`s in self-contained, labeled, canvas-only IIFEs; cart functionality is separate Dawn code; drawer/notification have no canvas; verified empty + item-present on dev). Cart is checkout-adjacent so QA included a real test item. | Push to development theme `185910067480` only. Open `/cart` on the dev preview (empty) → background plain solid black, no visible particle field, `ak-cart-field` + `ak-cart-footer-field` present but blank (default 300×150, 0 drawn pixels), `ak-top` header particles intact, empty-cart state + checkout/subtotal render. Add a harmless item (no order) → line item, quantity stepper, remove button, subtotal, checkout button, dynamic/additional checkout buttons render; change quantity + remove work; cart drawer opens from product add-to-cart; cart notification works. Confirm `/pages/club-hub` (ak-clubhub-field inert), `/collections` Archive (ak-archive-canvas inert), `/pages/narrative` (akfeed-field inert), `/pages/mystery-box` outer shell all unchanged. Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-22 | particles: disable **Clubhouse page** background (`ak-clubhub-field` in `snippets/club-hub.liquid` → `/pages/club-hub`). **Development-theme batch only — NOT live.** | `snippets/club-hub.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The Clubhouse page (`/pages/club-hub` → `page.club-hub.json` → `custom_liquid_4tbt7q` = `{% render 'club-hub' %}`) drew an ambient dust/floaters/shooters canvas (`ak-clubhub-field`) behind its content. The particle code lives in `snippets/club-hub.liquid` (3012 lines) inside a dedicated, clearly-labeled standalone `<script>` block (`<!-- Ambient particles + shooting stars system -->`) — an IIFE that is canvas-only (`getElementById('ak-clubhub-field')` + `querySelector('.ak-clubhub')`, guards, then particle setup) and fully separate from the Clubhouse content/form/captcha logic (`dropsForm`/`dropsBtn`/`findToken`/auto-submit/`hCaptcha`), which lives in prior `<script>` blocks that close before it. One scoped change (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): an early `return;` inserted as the first statement inside the particle IIFE, so it never seeds dust/floaters/shooters, never sizes the canvas, never starts its rAF loop / IO. The `<canvas class="ak-clubhub-field" id="ak-clubhub-field" aria-hidden>` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed:** `.ak-clubhub` content/cards, links, footer, and (in separate untouched scripts) the drops form + captcha (`dropsForm`/`dropsBtn`/`findToken`/auto-submit/`hCaptcha` — render conditionally by customer/drops state), `ak-top` header particles. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **Note:** this large snippet had a few-second theme-dev sync lag (dev preview briefly showed the old drawing canvas, then caught up to blank); verified neutralized on both local working-tree (`127.0.0.1:9292`) and dev theme. **Revert:** `git revert <this-commit>`, or remove the `return;` line from the particle IIFE in `snippets/club-hub.liquid`. | Low (one-line early `return;` in a self-contained, labeled, canvas-only IIFE; form/captcha/content logic is in separate script blocks; no content/CSS touched; runtime-verified on local + dev). | Push to development theme `185910067480` only. Open `/pages/club-hub` on the dev preview → background plain solid black, no visible particle field, `ak-clubhub-field` present but blank (default 300×150, 0 drawn pixels), `ak-top` header particles intact. Verify `.ak-clubhub` content/cards/links/footer render. In a logged-in / drops-active state, verify the drops form + captcha + buttons still surface and submit (separate untouched scripts). Confirm `/collections` Archive (ak-archive-canvas inert), `/pages/narrative` (akfeed-field inert), `/products/candyfacts-mystery-box-combo` (ak-product-field inert), `/pages/mystery-box` outer shell all unchanged. Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-21 | particles: disable **Archive page** background (`ak-archive-canvas`/`#AkArchiveCanvas` in `sections/ak-archive.liquid` → `/collections`). **Development-theme batch only — NOT live.** | `sections/ak-archive.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The Archive page (`/collections` → `list-collections.json` → `ak-archive` section) drew an ambient dust/floaters/shooters canvas behind the cassette-card deck. **Unlike other lanes, the section's single `<script>` contains BOTH the card-stack deck engine and the particle code.** The deck engine (`springTo`/`springStops`/`layout()`/drag/`pointerdown`/tap→`window.location.href`/`data-href`/arrows/keyboard/anim-lock + 700ms safety timer) initializes BEFORE the particle guard at line 729 (`if(!cv||prm) return;`); the particle code is a nested IIFE that follows it. A top-of-IIFE early return would break the deck, so it was explicitly NOT used. Instead, one scoped **guard-disable** (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): `if(!cv||prm) return;` → `if(!cv||prm||true) return;`, which routes every visitor into the section's own already-tested reduced-motion no-particle path — a known-good state where the deck works fully. The `<canvas class="ak-archive-canvas" id="AkArchiveCanvas" aria-hidden>` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed:** 18 deck cards, 18 `data-href` links (real routes, sample `/collections/uh-oh-ghoul-aid-tee`), prev/next arrows, footer, cassette CSS, titles, mobile fan, `ak-top` header particles. Deck-engine code byte-identical (only the guard line changed; 40 deck tokens intact). No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **Revert:** `git revert <this-commit>`, or change `if(!cv||prm||true) return;` back to `if(!cv||prm) return;`. | Low (reuses the section's own proven reduced-motion no-particle path; deck engine runs before the guard and is byte-identical; only the guard line changed; runtime-verified — canvas blank, deck + routes + arrows intact). | Push to development theme `185910067480` only. Open `/collections` on the dev preview → background plain solid black, no visible particle field, `ak-archive-canvas` present but blank (default 300×150, 0 drawn pixels), `ak-top` header particles intact. Verify the cassette-card deck renders (cards read as cassette cases, trailing strips visible); **drag a card**, **tap/click a card → navigates to its collection**, **prev/next arrows**, **keyboard arrows** all work; titles readable; mobile 375/320 px fan intact; footer renders. Confirm `/pages/narrative` (akfeed-field inert), `/products/candyfacts-mystery-box-combo` (ak-product-field inert), `/collections/master-collection` (ak-collection-field inert), `/pages/mystery-box` outer shell all unchanged. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-21 | particles: disable **Narrative page** background (`akfeed-field` in `templates/page.narrative.json` → `/pages/narrative`). **Development-theme batch only — NOT live.** | `templates/page.narrative.json`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The Narrative page's particle system was `akfeed-field` — the theme's largest (runtime 1304×5543) and only un-optimized canvas (DPR not capped, high counts, `shadowBlur`, `filter: contrast(1.18) saturate(1.08)`, no offscreen cancellation). It lives in the active `custom_liquid_7HLMyz` inside a dedicated, clearly-labeled standalone `<script>` block (`<!-- ✅ ADDED: PARTICLE SYSTEM SCRIPT -->`) — an IIFE scoped to `.ak-twitter-feed-wrapper`, fully separate from the feed/composer logic (`postTweet`/`makeTweet`/`baseTweets` + input/button listeners live in a prior `<script>` that closes before it). One scoped change (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): an early `return;` inserted as the first statement inside the particle IIFE, so it never seeds dust/floaters/shooters, never sizes the canvas, never starts its rAF loop / IO. The `<canvas class="akfeed-field" aria-hidden>` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed:** feed wrapper + 146 tweet cards, `baseTweets`/`makeTweet`/`postTweet`, composer input/button, Enter-to-post, typing-loader/dots, scroll-reveal entrance, footer, layout, copy, CSS, `ak-top` header particles. **Live composer test passed** — typed into `#akTweetInput`, clicked `#akTweetButton`, post container went 35→36 (prepend works; Narrative posts are client-side/in-memory only, so the test card clears on reload). No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **Revert:** `git revert <this-commit>`, or remove the `return;` line from the particle IIFE in `custom_liquid_7HLMyz`. | Low (one-line early `return;` in a self-contained, labeled, canvas-only IIFE; posting/composer is a separate script block, proven functional via the post test; no CSS/feed code touched; runtime-verified). | Push to development theme `185910067480` only. Open `/pages/narrative` on the dev preview → background plain solid black, no visible particle field, `akfeed-field` canvas present but blank (default 300×150, 0 drawn pixels), `ak-top` header particles intact. Verify feed wrapper + tweet cards render; type a post and press Enter/click button → new post prepends; typing-loader dots animate; scroll up/down → entrance animations fire; footer renders. Confirm `/products/candyfacts-mystery-box-combo` (ak-product-field inert), `/collections/master-collection` (ak-collection-field inert), `/pages/the-collective` (de-particled), `/pages/mystery-box` outer shell all unchanged. Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable **product page** background (`ak-product-field` in `sections/main-product.liquid` → all product pages). **Development-theme batch only — NOT live.** | `sections/main-product.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The remaining product-page particle system was `ak-product-field` — an ambient dust/floaters/shooting-star canvas drawn behind the product. It lives in `sections/main-product.liquid` inside a dedicated, clearly-labeled standalone `<script id="ak-product-particles-{{ section.id }}">` IIFE (`<!-- BEGIN: Ambient Particle System -->`) that is canvas-only (queries `MainProduct-{id}` + `ak-product-field-{id}`, guards, then particle setup) and fully separate from the product form, media gallery, variant selectors, variant image switcher, quantity, Add to Cart, payment buttons, price, pickup, accordions, sticky behavior, CSS `{%- style -%}` blocks, and schema. One scoped change (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): an early `return;` inserted as the first statement inside that IIFE, so it never seeds dust/floaters/shooters, never sizes the canvas, never starts its rAF loop. The `<canvas class="ak-product-field" aria-hidden>` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **`main-product.liquid` is the theme's most fragile file (2800+ lines, "do not refactor") — only the one-line `return;` was added; no other code, CSS, or schema touched.** **`ak-back-field` was already disabled** in the product templates (`custom_liquid_6wWQFN`, 2026-04-05) and remains untouched/disabled. **Scope:** all products use this one shared section, so one edit covers every product page; verified on two distinct products. **Preserved and confirmed:** media gallery + 9/8 images, variant selector, quantity, product form, Add to Cart, price, payment buttons (combo product), variant image switching, layout, `ak-top` header particles. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **Revert:** `git revert <this-commit>`, or remove the `return;` line from the `ak-product-particles` IIFE in `sections/main-product.liquid`. | Low (one-line early `return;` in a self-contained, labeled, canvas-only IIFE; no refactor of the fragile file; Dawn/AmeriKid purchase-flow code untouched; runtime-verified on two products + a collection control). Elevated context only because it's the conversion page in the most fragile file — mitigated by minimal additive change + deep QA. | Push to development theme `185910067480` only. Open `/products/candyfacts-mystery-box-combo` and one other product on the dev preview → background plain solid black, no visible particle field, `ak-product-field` canvas present but blank (default 300×150, 0 drawn pixels), `ak-back-field` absent, `ak-top` header particles intact. Click through the full purchase flow: open media gallery / lightbox, switch a variant (confirm variant image switching), change quantity, press Add to Cart (cart updates), confirm price + dynamic checkout/payment buttons render, pickup availability + accordions if present, sticky behavior, related products on scroll (lazy-loaded). Confirm `/collections/master-collection` + `/collections/candyfacts` (ak-collection-field inert), `/pages/the-collective`, Bad Music, `/pages/mystery-box` outer shell all unchanged. Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable **default collection grid** background (`ak-collection-field` in `sections/main-collection-product-grid.liquid` → `/collections/master-collection` + all default-template collections). **Development-theme batch only — NOT live.** | `sections/main-collection-product-grid.liquid`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The Products/master-collection page's remaining particle system was `ak-collection-field` — an ambient dust/floaters/shooting-star canvas drawn behind the product grid. It is NOT `ak-collection-bg` (the default `templates/collection.json` never had that section; its black background already comes from the global net). `ak-collection-field` lives in the shared `sections/main-collection-product-grid.liquid` inside a single standalone `<script>(function(){…})()` IIFE that is canvas-only (queries `.section-{id}-padding` + `#ak-collection-field-{id}`, guards, then particle setup) and fully separate from Dawn's grid/cards/facets/sorting/pagination markup + external JS. One scoped change (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): an early `return;` inserted as the first statement inside the IIFE, so it never seeds dust/floaters/shooters, never sizes the canvas, never starts its rAF loop. The `<canvas class="ak-collection-field">` element stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **Scope:** the grid section is active only on the published default `collection.json` (+ 7 unpublished builder remnants); it is DISABLED on all 18 named/Bad-Music collection templates, so they never rendered `ak-collection-field` and are unaffected — one shared-section edit covers `/collections/master-collection`, `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve` at once with zero template edits. **Preserved and confirmed:** product cards, images, titles, prices, sold-out badges, product links, hover states, facets/filtering, sorting, pagination, layout, section settings/schema, `ak-top` header particles. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed (master-collection 28 cards/28 links/70 prices; candyfacts 6 cards). **Revert:** `git revert <this-commit>`, or remove the `return;` line from the IIFE in `sections/main-collection-product-grid.liquid`. | Low (one-line early `return;` in a self-contained canvas-only IIFE; Dawn grid/facets/sorting/pagination untouched — separate Liquid + external JS; named collections unaffected; runtime-verified on two pages). | Push to development theme `185910067480` only. Open `/collections/master-collection` on the dev preview → background plain solid black, no visible particle field, `ak-collection-field` canvas present but blank (default 300×150, 0 drawn pixels), `ak-top` header particles intact, product grid renders (cards/images/titles/prices/badges/links), hover states normal, filtering/sorting/pagination work where available, layout unchanged. Spot-check `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve` (same shared section → identically inert). Confirm `/collections/uh-oh-tee` and other named collections still show only `ak-top` (grid disabled there). Confirm product pages (`ak-product-field`), `/pages/the-collective`, Bad Music, `/pages/mystery-box` outer shell all unchanged. Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable **Life/Style page** backgrounds (`templates/page.json` → `/pages/the-collective`). Disables `ak-collection-bg` AND neutralizes all three module ambient particle canvases (Adventures `.ak-adv-field`, Kid Radio `.ak-kr-field`, Channels `.ak-chan-field`); all module content + title animations preserved. **Development-theme batch only — NOT live.** | `templates/page.json`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | Life/Style was the last template with `ak-collection-bg` active and the most particle-dense remaining page (4 in-scope canvases: the full-viewport background + one ambient field per module). Four scoped local changes (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): (1) `ak-collection-bg` section flagged `"disabled": true`; (2–4) each module's ambient particle block guard changed from `if(canvas && !reduced){` to `if(false && canvas && !reduced){`. **Guard approach, not early-return:** unlike `bv-field`/`ak-bg-canvas` (standalone IIFEs), these particle blocks share scope with module interaction code (card-tilt listeners, etc. run in the same function), so flipping the guard false disables ONLY the particle drawing while leaving sibling code intact — and routes every visitor into the already-supported reduced-motion (no-particles) code path, a known-good tested state. The `.ak-adv-field`/`.ak-kr-field`/`.ak-chan-field` canvases remain in the DOM but are never sized/drawn (runtime: default 300×150, 0 drawn pixels). **Preserved and confirmed:** Adventures/Kid Radio/Channels modules + video cards, 26 YouTube embeds/facades, 16 glass cards (`.ak-holo-card`), playing states, Adventures 3D tilt (`perspective`), Kid Radio wave bar, module layouts, the ambient title animations `akAdvFlicker`/`akKrSignal`/`akChanTuneIn` (intended final design — NOT removed), `ak-footer`, nav/cart, `ak-top` header particles. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. **This completes `ak-collection-bg` removal across every template that used it on dev.** **Revert:** `git revert <this-commit>`, or change each `if(false && canvas && !reduced){` back to `if(canvas && !reduced){` and/or remove `"disabled": true` from `ak-collection-bg`. | Low (one section-disable flag + three guard-disables equivalent to the proven reduced-motion path; no content/video/title-animation code removed, no CSS added, no shared/section/`layout/theme.liquid`/Mystery Crate files touched; runtime-verified). Cosmetic note: Life/Style loses its continuous-field look (the intended direction). | Push to development theme `185910067480` only. Open `/pages/the-collective` on the dev preview → background plain solid black, NO `ak-cbg-cv-…` canvas, `.ak-adv-field`/`.ak-kr-field`/`.ak-chan-field` present but blank (default 300×150, no dust/floaters/shooting stars), `ak-top` header particles intact. Verify all three modules render; play a YouTube video in Adventures + Kid Radio; hover an Adventures card (3D tilt); Kid Radio wave bar animates; Channels cards/links work; section title shimmer animations still run; glass cards styled; footer Back-to-Top present. Confirm `/pages/2025`, `/pages/badmusic-2025`, `/collections/bad-music` remain complete; `/pages/mystery-box` outer shell intact; `/collections/master-collection` (`ak-collection-field`) and product pages (`ak-product-field`) unchanged (separate later lanes). Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable **main Bad Music page** backgrounds (`templates/page.badmusic.json` → `/pages/2025`, the TV + cassette page). Disables `ak-collection-bg` AND neutralizes the TV-hero ambient `ak-bg-canvas` particle layer; TV hero + YouTube player + cassette shelf fully preserved. **Development-theme batch only — NOT live.** | `templates/page.badmusic.json`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | Final Bad Music page lane. `/pages/2025` had two ambient particle layers: the site-wide `ak-collection-bg` and the TV hero's OWN ambient canvas (`ak-bg-canvas` — dust/floaters/shooting stars, in the IIFE explicitly commented "Ambient background particles (isolated, does not touch TV logic)" inside `custom_liquid_dx4GNc`). Two scoped local changes (verified on the local `shopify theme dev` preview which auto-syncs to dev `185910067480`): (1) `ak-collection-bg` section flagged `"disabled": true`; (2) the ambient IIFE made a no-op via an early `return;` right after its reduced-motion guard, so it never seeds dust/floaters/shooters, never sizes the canvas, never starts its rAF loop. The `<canvas class="ak-bg-canvas" aria-hidden>` stays in the DOM but draws nothing (runtime: default 300×150, 0 drawn pixels). **TV/cassette preserved and confirmed:** `#amerikid-tv-hero`, `ak-screen`/`ak-bezel`, `ak-yt-holder` (YouTube player — separate from the canvas), `ak-poster`, controls/knobs/`ak-timeline`/scanlines, `ak-caption` (2025 caption), BAD MUSIC heading (`custom_liquid_bm_heading`), `ak-bm-album-link` cassette shelf, left cassette link `/pages/badmusic-2025`, right Coming Soon cassette, cassette `<video>` MP4, `ak-footer`. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed. After this commit `ak-collection-bg` remains active only on `templates/page.json` (Life/Style). **Revert:** `git revert <this-commit>`, or remove the `return;` from the ambient IIFE (restores `ak-bg-canvas` particles) and/or the `"disabled": true` from `ak-collection-bg` (restores background layer). | Low (one section-disable flag + one scoped JS no-op in a self-contained, explicitly-isolated ambient IIFE; no TV/cassette/YT-player code removed, no CSS added, no shared/section/`layout/theme.liquid`/`ak-bm-album-link.liquid`/Mystery Crate files touched; runtime-verified). | Push to development theme `185910067480` only. Open `/pages/2025` on the dev preview → background solid black, NO `ak-cbg-cv-…` canvas, `ak-bg-canvas` present but blank (no dust/floaters/shooting stars), `ak-top` header particles intact. Verify TV hero renders + plays (press play → YouTube loads), 2025 caption above TV, red BAD MUSIC banner, BAD MUSIC heading, two-cassette shelf, left cassette navigates to `/pages/badmusic-2025`, right cassette not clickable, left cassette MP4 plays on scroll-in. Confirm `/pages/badmusic-2025`, `/collections/bad-music` remain complete; `/pages/mystery-box` outer shell intact; `/pages/the-collective` (Life/Style) unchanged (still has `ak-collection-bg`). Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable Bad Music **2025 vinyl page** backgrounds (`templates/page.badmusic-2025.json` → `/pages/badmusic-2025`). Disables `ak-collection-bg` AND neutralizes the page-specific `bv-field` ambient particle canvas; vinyl UI fully preserved. **Development-theme batch only — NOT live.** | `templates/page.badmusic-2025.json`, `docs/change-log.md`, `docs/source-of-truth.md`, `docs/runtime-test-checklist.md`, `docs/known-fragile-areas.md` | The earlier `ak-collection-bg` disable on this template removed the site-wide background layer, but the vinyl section's OWN ambient canvas (`bv-field`: dust + floaters + shooting stars inside `custom_liquid_QcCtY6`) was still drawing a starfield, which the user reported as remaining particle background. Two scoped local changes (both verified on the local `shopify theme dev` preview): (1) `ak-collection-bg` section flagged `"disabled": true`; (2) the `initBadVinylAmbient()` IIFE made a no-op via an early `return;` immediately after the function brace, so it never seeds dust/floaters/shooters, never sizes the canvas, and never starts its rAF loop. The `<canvas class="bv-field">` element stays in the DOM but draws nothing (runtime: default 300×150 size, 0 non-transparent pixels). **Vinyl UI/interactions preserved and confirmed:** 12 records/cards, album art (`cd-art`), `BAD MUSIC / 2025 / THE ALBUM` intro, seek bars, record spin (`vinylSpin`/`startSpin`), hover/tilt (`initDiscTilt`), play/pause + `hyperLaunch` start-FX, titles/artists. No CSS stars/noise/gradients/textures or replacement particle system added. User visually confirmed the result. After this commit `ak-collection-bg` remains active only on `page.json` (Life/Style) and `page.badmusic.json` (`/pages/2025`). **Revert:** `git revert <this-commit>`, or remove the `return;` from `initBadVinylAmbient()` (restores vinyl ambient particles) and/or the `"disabled": true` from `ak-collection-bg` (restores background layer). | Low (one scoped JS no-op + one section-disable flag; self-contained ambient IIFE, no vinyl code removed, no CSS added, no shared/section/`layout/theme.liquid`/Mystery Crate files touched; runtime-verified on local preview). | Push to development theme `185910067480` only. Open `/pages/badmusic-2025` on the dev preview → background solid black, NO `ak-cbg-cv-…` canvas, `bv-field` canvas present but blank (no dust/floaters/shooting stars), `ak-top` header particles intact. Click several records → spin, hover/tilt, play/pause, `hyperLaunch` bloom, and seek all work; album art + intro + titles render. Confirm `/pages/2025` (`page.badmusic.json`) is unchanged → `ak-collection-bg` + TV-hero `ak-bg-canvas` still drawing (deliberately deferred). Confirm `/collections/bad-music` unchanged (done). Mobile 375/320 px sweep. Console — no new errors. Do NOT promote to live theme `184615043352` until the user explicitly approves. |
| 2026-05-19 | particles: disable `ak-collection-bg` on the Bad Music **collection** (`templates/collection.bad-music.json` → `/collections/bad-music`). **Development-theme batch only — NOT live.** Completes the full collection-template `ak-collection-bg` removal lane on dev. | `templates/collection.bad-music.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Bad Music collection was the one named collection deliberately held back from the named-collection lane (batches 1–9, `6af484c`…`bc49d22`) as its own pilot. Same proven one-line `"disabled": true` flag added to the existing `ak-collection-bg` section instance; section not deleted, `order` array unchanged, no other settings touched. User visually verified local preview before commit. **Scope:** 1 file (plus docs). `ak-collection-bg` is now disabled on every collection template that ever used it (18 collection templates + `templates/index.json` homepage-prototype lane). **Still intentionally active — separate later lanes, NOT bundled here:** `templates/page.json` (Life/Style), `templates/page.badmusic.json`, `templates/page.badmusic-2025.json`. The Bad Music **page** templates (`page.badmusic.json`, `page.badmusic-2025.json`) remain their own later lane and were not touched. Default/master `collection.json` uses `ak-collection-field` (not `ak-collection-bg`) and is a separate lane. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from `templates/collection.bad-music.json`. | Low (mechanism is verbatim-identical to the prior nine batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no `ak-top` change, no Mystery Crate change). | Push to development theme `185910067480` only. Open `/collections/bad-music` on the dev preview → body solid black behind content, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, Bad Music collection content blocks (banner, `image_with_text_HdyHR4`, `collage_y3rQ6F`, custom-liquid bands) still rendered, product/collection layout + titles + images + prices + badges + hover states intact. Confirm the Bad Music **pages** are untouched: `/pages/badmusic` (TV hero `ak-bg-canvas`) and `/pages/2025` / badmusic-2025 (`bv-field`) → `ak-collection-bg` still present and particles still drawing (deliberately excluded). Open `/pages/the-collective` (Life/Style) → `ak-collection-bg` still present (excluded). Open `/collections` (Archive), `/cart`, a product page, default `/collections/master-collection`, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on `/collections/bad-music`. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme. Do NOT promote to live theme `184615043352` until the user explicitly approves live promotion. | 
| 2026-05-19 | particles: batch 9 (final named-collection batch) — disable `ak-collection-bg` on `templates/collection.uhoh.json` and `templates/collection.siwyc.json`. **Development-theme batch only — NOT live. Completes the named-collection `ak-collection-bg` removal lane on dev.** | `templates/collection.uhoh.json`, `templates/collection.siwyc.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Ninth and final batch of the named-collection particle-removal lane, following batches 1–8 (`6af484c`+`0314dc9`, `ff74121`, `f685cd1`, `70bb986`, `9b3e79a`, `b2dee24`, `ed0e16f`, `b87f245`), all observed clean on dev. Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. **Route mapping note:** `collection.uhoh.json` → `/collections/uh-oh-tee`, `collection.siwyc.json` → `/collections/say-it-with-your-chest-1` (file suffix ≠ admin handle in both cases; mappings confirmed via the sitemap-anchored probe and re-verified as active controls during the batch-8 QA). **Lane complete:** all 17 collection templates that ever rendered `ak-collection-bg` are now disabled on dev EXCEPT `collection.bad-music.json` (held by the Bad Music constraint as its own later lane). `templates/index.json` is also disabled (separate homepage-prototype lane). `ak-collection-bg` remains active only on `collection.bad-music.json` (→ /collections/bad-music), `page.json` (Life/Style), `page.badmusic.json`, and `page.badmusic-2025.json`. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior eight batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change; both mismatched handles pre-confirmed so no route surprise). | Push to development theme `185910067480` only. Open `/collections/uh-oh-tee` and `/collections/say-it-with-your-chest-1` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (UH-OH Tee `collage_wk7wbC` + `custom_liquid_Gxbhbp` + `image_with_text_BmitcX`, Say It With Your Chest `collage_CbyeWk` + `custom_liquid_DtpQyd` + `image_with_text_ViXUMi`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open `/collections/bad-music` → `ak-cbg-cv-…` canvas still present, particles still drawing (deliberately excluded). Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h. Future separate lanes (each its own pilot): Bad Music (`collection.bad-music.json` + `page.badmusic*` canvases), Life/Style `page.json`, default `collection.json`, Archive, Narrative, Club Hub, cart, product pages, `ak-top` — and eventually a live-theme promotion of the accumulated set once everything is observed clean on dev. Do NOT promote to live theme `184615043352` yet. |
| 2026-05-19 | particles: batch 8 — disable `ak-collection-bg` on `templates/collection.katskan.json` and `templates/collection.travellercap.json`. **Development-theme batch only — NOT live.** | `templates/collection.katskan.json`, `templates/collection.travellercap.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Eighth batch of the pilot-first particle-removal rollout, following batches 1–7 (`6af484c`+`0314dc9`, `ff74121`, `f685cd1`, `70bb986`, `9b3e79a`, `b2dee24`, `ed0e16f`), all observed clean on dev. Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. **Route mapping note:** `collection.katskan.json` → `/collections/kat-skan-tees`, `collection.travellercap.json` → `/collections/traveller-cap` (file suffix ≠ admin handle in both cases; mappings confirmed via the sitemap-anchored handle→suffix probe and re-verified as active controls during the batch-7 QA). **Pilot scope:** 15 of 22 published Collections now particle-disabled on dev; `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled — 2 remaining non-Bad-Music collection templates** (`uhoh` → /collections/uh-oh-tee, `siwyc` → /collections/say-it-with-your-chest-1) **plus Bad Music excluded** (`collection.bad-music.json` → /collections/bad-music), default `collection.json`, Life/Style `page.json`, Bad Music page templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior seven batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change; both mismatched handles pre-confirmed so no route surprise). | Push to development theme `185910067480` only. Open `/collections/kat-skan-tees` and `/collections/traveller-cap` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Kat Skan `collage_CCAHbf` + `custom_liquid_KEGbVR` + `image_with_text_7eAa4q`, Traveller Cap `collage_CQYkqh` + `custom_liquid_GiKHPC` + `image_with_text_PwckKM`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open the last two out-of-pilot named collections (`/collections/uh-oh-tee`, `/collections/say-it-with-your-chest-1`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until the named-collection lane is complete and observed clean on dev. |
| 2026-05-19 | particles: batch 7 — disable `ak-collection-bg` on `templates/collection.kidzyn.json` and `templates/collection.loveamerica.json`. **Development-theme batch only — NOT live.** | `templates/collection.kidzyn.json`, `templates/collection.loveamerica.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Seventh batch of the pilot-first particle-removal rollout, following batches 1–6 (`6af484c`+`0314dc9`, `ff74121`, `f685cd1`, `70bb986`, `9b3e79a`, `b2dee24`), all observed clean on dev. Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. **Route mapping note:** `collection.kidzyn.json` → `/collections/kid-zyn`, `collection.loveamerica.json` → `/collections/love-amerikid` (file suffix ≠ admin handle in both cases; mappings confirmed via the sitemap-anchored handle→suffix probe and re-verified as active controls during the batch-6 QA). **Pilot scope:** 13 of 22 published Collections now particle-disabled on dev; `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled — 4 remaining non-Bad-Music collection templates** (`katskan` → /collections/kat-skan-tees, `siwyc` → /collections/say-it-with-your-chest-1, `travellercap` → /collections/traveller-cap, `uhoh` → /collections/uh-oh-tee) **plus Bad Music excluded** (`collection.bad-music.json` → /collections/bad-music), default `collection.json`, Life/Style `page.json`, Bad Music page templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior six batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change; both mismatched handles pre-confirmed so no route surprise). | Push to development theme `185910067480` only. Open `/collections/kid-zyn` and `/collections/love-amerikid` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Kid Zyn `collage_bqfxam` + `custom_liquid_6EPpLB` + `image_with_text_UqQJcD`, Love Amerikid `collage_xbVmnV` + `custom_liquid_TF7mUe` + `image_with_text_XKBPC8`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/kat-skan-tees`, `/collections/traveller-cap`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until the named-collection lane is complete and observed clean on dev. |
| 2026-05-19 | particles: batch 6 — disable `ak-collection-bg` on `templates/collection.uh-oh-ghoul-aid-tee.json` and `templates/collection.election.json`. **Development-theme batch only — NOT live.** | `templates/collection.uh-oh-ghoul-aid-tee.json`, `templates/collection.election.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Sixth batch of the pilot-first particle-removal rollout, following the clean Weed pilot (`6af484c` + `0314dc9`), Kandy + Skate-Tables (`ff74121`), Stones + Aperikid (`f685cd1`), Bucket + GPR (`70bb986`), and Solo + Watermelon (`9b3e79a`). Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. **Route mapping note:** `collection.uh-oh-ghoul-aid-tee.json` → `/collections/uh-oh-ghoul-aid-tee` (clean handle=suffix); `collection.election.json` → `/collections/2024-election-tee` (file suffix `election` ≠ admin handle `2024-election-tee`; mapping confirmed via the sitemap-anchored handle→suffix probe and re-verified as an active control during the Solo + Watermelon QA). **Pilot scope:** 11 of 22 published Collections now particle-disabled on dev; `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled:** remaining 6 active-`ak-cbg` collection templates (`bad-music`, `katskan` → /collections/kat-skan-tees, `kidzyn` → /collections/kid-zyn, `loveamerica` → /collections/love-amerikid, `siwyc` → /collections/say-it-with-your-chest-1, `travellercap` → /collections/traveller-cap, `uhoh` → /collections/uh-oh-tee), default `collection.json`, Life/Style `page.json`, Bad Music templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior five batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change; mismatched election handle pre-confirmed so no route surprise). | Push to development theme `185910067480` only. Open `/collections/uh-oh-ghoul-aid-tee` and `/collections/2024-election-tee` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Ghoul-Aid `collage_y3rQ6F` + `custom_liquid_Gxbhbp` + `image_with_text_HdyHR4`, Election `collage_zNaYei` + `image_with_text_VWetem` + `rich_text_FxK773`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/kid-zyn`, `/collections/love-amerikid`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until at least one more batch is observed clean on dev. |
| 2026-05-19 | particles: batch 5 — disable `ak-collection-bg` on `templates/collection.solo.json` and `templates/collection.watermelon.json`. **Development-theme batch only — NOT live.** | `templates/collection.solo.json`, `templates/collection.watermelon.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Fifth batch of the pilot-first particle-removal rollout, following the clean Weed pilot (`6af484c` + `0314dc9`), Kandy + Skate-Tables (`ff74121`), Stones + Aperikid (`f685cd1`), and Bucket + GPR (`70bb986`). Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. Both targets are clean handle=suffix mappings (`solo` → `collection.solo.json`, `watermelon` → `collection.watermelon.json`) confirmed via the sitemap-anchored probe, and both were verified as still-active `ak-cbg` controls during the Bucket + GPR QA pass. **Pilot scope:** 9 of 22 published Collections now particle-disabled on dev (`/collections/` weed, kandy, skate-tables, stones, aperikid, bucket, gpr, solo, watermelon); `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled:** remaining 8 active-`ak-cbg` collection templates (the 7 mismatched-handle templates: election, katskan, kidzyn, loveamerica, siwyc, travellercap, uhoh; plus `uh-oh-ghoul-aid-tee` which is clean handle=suffix; plus Bad Music), default `collection.json`, Life/Style `page.json`, Bad Music templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior four batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change). | Push to development theme `185910067480` only. Open `/collections/solo` and `/collections/watermelon` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Solo `custom_liquid_Nwjmdt` + shared `collage_y3rQ6F` + `image_with_text_HdyHR4`, Watermelon `collage_9C3tfG` + `custom_liquid_YpWKgz` + `image_with_text_gwBt3A`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/uh-oh-ghoul-aid-tee`, `/collections/2024-election-tee`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped to Solo + Watermelon. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until at least one more batch is observed clean on dev. |
| 2026-05-19 | particles: batch 4 — disable `ak-collection-bg` on `templates/collection.bucket.json` and `templates/collection.gpr.json`. **Development-theme batch only — NOT live.** | `templates/collection.bucket.json`, `templates/collection.gpr.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Fourth batch of the pilot-first particle-removal rollout, following the clean Weed pilot (`6af484c` + `0314dc9`), the Kandy + Skate-Tables batch (`ff74121`), and the Stones + Aperikid batch (`f685cd1`). Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. Both targets selected from the sitemap-anchored handle→suffix probe (2026-05-19) as confirmed clean handle=suffix mappings (`bucket` → `collection.bucket.json`, `gpr` → `collection.gpr.json`) and confirmed-active `ak-cbg` rendering as out-of-pilot controls during the Stones + Aperikid QA pass. **Pilot scope:** 7 of 22 published Collections now particle-disabled on dev (`/collections/weed`, `/collections/kandy`, `/collections/skate-tables`, `/collections/stones`, `/collections/aperikid`, `/collections/bucket`, `/collections/gpr`); `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled:** remaining 10 active-`ak-cbg` collection templates, default `collection.json`, Life/Style `page.json`, Bad Music templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the prior three batches, all observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change). | Push to development theme `185910067480` only. Open `/collections/bucket` and `/collections/gpr` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Bucket `collage_wyLqVQ` + `custom_liquid_3ir9Jz` + `image_with_text_HdyHR4`, GPR `collage_7b93HX` + `image_with_text_Nqr6kh`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/watermelon`, `/collections/solo`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped to Bucket + GPR. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until at least one more batch is observed clean on dev. |
| 2026-05-19 | particles: batch 3 — disable `ak-collection-bg` on `templates/collection.stones.json` and `templates/collection.aperikid.json`. **Development-theme batch only — NOT live.** | `templates/collection.stones.json`, `templates/collection.aperikid.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Third batch of the pilot-first particle-removal rollout, following the clean Weed pilot (`6af484c` + `0314dc9`) and the Kandy + Skate-Tables batch (`ff74121`). Same one-line `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. Both targets selected from a sitemap-anchored handle→template fingerprint probe that confirmed clean handle=suffix mapping (`stones` → `collection.stones.json`, `aperikid` → `collection.aperikid.json`) and active `ak-cbg` rendering pre-flip. User visually verified local preview on Stones + Aperikid before commit: content area renders plain black under `ak-top`, header/nav particles preserved (expected — `ak-top` is a separate canvas), product grid + per-Collection hero blocks intact. **Pilot scope:** 5 of 22 published Collections now particle-disabled on dev (`/collections/weed`, `/collections/kandy`, `/collections/skate-tables`, `/collections/stones`, `/collections/aperikid`); `templates/index.json` is also disabled but is the separate homepage-prototype lane. **Not bundled:** remaining 12 active-`ak-cbg` collection templates, default `collection.json`, Life/Style `page.json`, Bad Music templates, Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the 2026-05-18 Weed pilot and the 2026-05-19 Kandy/Skate batch, both observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change). | Push to development theme `185910067480` only. Open `/collections/stones` and `/collections/aperikid` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, per-Collection hero blocks still rendered (Stones `image_with_text_BPTYhQ` + `collage_CbyeWk`, Aperikid `image_with_text_HdyHR4` + `custom_liquid_HKKeGK`), product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/bucket`, `/collections/gpr`, `/collections/watermelon`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped to Stones + Aperikid. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged. Mobile 375/320 px sweep on both pilot routes. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until at least one more batch is observed clean on dev. |
| 2026-05-19 | particles: batch 2 — disable `ak-collection-bg` on `templates/collection.kandy.json` and `templates/collection.skate.json`. **Development-theme batch only — NOT live.** | `templates/collection.kandy.json`, `templates/collection.skate.json`, `docs/change-log.md`, `docs/source-of-truth.md` | Second batch of the pilot-first particle-removal rollout, following the clean Weed pilot (commits `6af484c` + `0314dc9`). Mechanism is identical: one `"disabled": true` flag added to each template's existing `ak-collection-bg` section instance. Section file `sections/ak-collection-bg.liquid` and the global 3-rule black safety net in `layout/theme.liquid` are unchanged. Targets chosen as low/medium-traffic named collections that are structurally identical to Weed (Pattern A JSON, `ak-collection-bg` first in `order`, `ak-top` present). User-verified local preview on Skate before commit: content area renders plain black under `ak-top`, header/nav particles preserved (expected — `ak-top` is a separate canvas). **Pilot scope:** 3 of 22 active `ak-collection-bg`-using templates now disabled (`collection.weed.json`, `collection.kandy.json`, `collection.skate.json`); `templates/index.json` is also disabled but is a separate homepage-prototype lane, not part of the particle-removal rollout. **Not bundled:** remaining 17 collection templates + default `collection.json` + Life/Style `page.json` + Bad Music `page.badmusic.json` + Bad Music 2025 `page.badmusic-2025.json` + Mystery Box outer shell. **Revert:** `git revert <this-commit>` or remove the `"disabled": true` line from each file. | Low (mechanism is verbatim-identical to the 2026-05-18 Weed pilot, which has been observed clean on the dev theme; no new CSS, no Liquid, no section-file edits, no `layout/theme.liquid` change, no Mystery Crate change). | Push to development theme `185910067480` only. Open `/collections/kandy` and `/collections/skate` on the dev preview → body solid black behind the product grid, no `ak-cbg-cv-…` canvas in the DOM, no white leak, `ak-top` header particles intact, product cards rendering with Impact-uppercase-no-underline titles, square ratio, hover states, sold-out badges. Open at least one out-of-pilot named collection (e.g., `/collections/election`, `/collections/stones`) → `ak-cbg-cv-…` canvas still present, particles still drawing — confirms the batch is scoped to Kandy + Skate. Open `/pages/the-collective`, `/pages/narrative`, `/collections` (Archive), `/cart`, a product page, and `/pages/mystery-box` outer shell → all unchanged from the post-Weed-pilot state. Mobile 375/320 px sweep on `/collections/kandy` and `/collections/skate`. DevTools console — no new errors beyond the pre-existing `video-player` double-define and PopClip noise. Watch dev theme for ≥24 h before considering broader rollout. Do NOT promote to live theme `184615043352` until at least one more batch is observed clean on dev. |
| 2026-05-18 | particles: pilot — disable `ak-collection-bg` on `templates/collection.weed.json` only, plus lift the proven 3-rule black-background CSS package into `layout/theme.liquid` as a global safety net. **Development-theme pilot only — NOT live.** | `layout/theme.liquid`, `templates/collection.weed.json` | One-page pilot validating the safest path for site-wide `ak-collection-bg` removal after a prior 21-template batch attempt produced white-background regressions. Read-only Chrome DevTools postmortem identified the root cause: `sections/ak-collection-bg.liquid` was not only drawing particles, it was also injecting a 3-rule CSS package that made the page solid black — `html, body { background:#000 !important }` + `#MainContent { background: transparent !important }` + `#MainContent .gradient { background: transparent !important; background-image: none !important }`. Disabling the section without preserving that package exposed Dawn's body white. Fix: lift the identical 3-rule package into `layout/theme.liquid` (inserted as a new `<style>` block immediately after the existing `{% endstyle %}` so source order beats Dawn's body-via-scheme rule, with `!important` as defensive belt-and-braces). Then flip `"disabled": true` on the `ak-collection-bg` instance in a single low-traffic collection template (`collection.weed.json`) as a one-page pilot. DevTools simulation across `/collections/weed`, `/collections/kandy`, `/pages/the-collective`, `/pages/mystery-box`, and `/cart` confirmed body stays solid black, no white leak, all out-of-pilot canvases (`ak-top-canvas`, `ak-cbg` on the other 20 templates, `ak-collection-field`, `ak-product-field`, `ak-archive-canvas`, `ak-cart-field`, `ak-cart-footer-field`, `ak-mb-canvas`, per-module Life/Style canvases, BAD Vinyl `bv-field`, Bad Music TV `ak-bg-canvas`, Narrative `akfeed-field`, Clubhouse, Contact, Newsletter) still rendering. Mystery Crate iframe, frame, prestart, release, shell-fx, lock overlay, `customer_id` flow, `_preview_hat_id` all verified untouched on `/pages/mystery-box`. Homepage prototype `9ed6224` preserved. **Pilot scope:** 2 files only; 1 of 22 active `ak-collection-bg`-using templates disabled. **Not yet bundled:** remaining 17 collection templates + default `collection.json` + Life/Style `page.json` + Bad Music `page.badmusic.json` + Bad Music 2025 `page.badmusic-2025.json` (those wait for development-theme observation + Bad Music local-preview verification, since `/pages/badmusic` and `/pages/2025` 404 on live). **Revert:** `git checkout -- layout/theme.liquid templates/collection.weed.json`. | Low (CSS package is verbatim-identical to a 4-line block previously embedded in `sections/ak-collection-bg.liquid` that has been in production on 22 templates without regression). | Push to development theme `184629133592` only. Open `/collections/weed` on the development storefront → body should be solid black behind product cards, no white leak above/below/between cards, nav header (`ak-top`) still drifting with starfield particles, product cards rendering normally with Impact-uppercase-no-underline titles. Open another collection (e.g., `/collections/kandy`) → unchanged, full ambient starfield still mounted. Open `/pages/the-collective` → Life/Style modules + glass cards + 3D tilt + hero + ambient particles all intact. Open `/pages/mystery-box` → iframe loads, prestart appears, lock overlay state respected, bunker shell + LED + chin telemetry + scanlines visible, customer_id flow works. Open `/cart` → cart line items, totals, Checkout button, drawer all functional; add an item and confirm checkout flow reaches Shopify-hosted checkout. Mobile 375/320 px sweep on `/collections/weed`. `prefers-reduced-motion: reduce` toggle — particles already gated off, page still legible. DevTools Console clean. Watch development theme for ≥24h. Mystery Crate behavior must match production. If clean, next pilot batch adds `page.badmusic.json` + `page.badmusic-2025.json` for local-preview verification (those routes don't resolve on live). Only after Bad Music confirms clean should the third batch flip the remaining 17 collection templates + the default `collection.json` + Life/Style `page.json`. Do NOT promote to live theme `184615043352` until all three batches are observed clean on dev. |
| 2026-05-12 | ak-top: remove `filter:blur(.15px)` from `.ak-top-label::after` (next smallest-safe site-wide scroll-lag pass) | `sections/ak-top.liquid` | Post-deploy validation after the `ak-top` DPR-cap pass showed site-wide scroll lag persisted: all `ak-top`-using routes measured 24–43 FPS while the homepage (no `ak-top`) measured 72.5 FPS, isolating `ak-top` as the dominant shared compositor cost. Live in-page isolation (temporary DOM toggles, all reverted) measured `akTopGlint` killed → +7.9 FPS recovered on `/collections/master-collection`. The `.ak-top-label::after` glint pseudo-element runs 5 simultaneous infinite instances (one per nav label) and stacks three expensive per-frame operations: animated `background-position` (paint-triggering), `mix-blend-mode: screen` (stacking context), and `filter:blur(.15px)` (full GPU filter pass). 0.15 px is below pixel-snap threshold on every modern display — the blur is an anti-aliasing micro-trick that is visually imperceptible but routes the layer through the GPU filter pipeline every frame on 5 simultaneous pseudos. Removing only the `filter:blur(.15px);` declaration from line 197 of `sections/ak-top.liquid` skips that filter pass site-wide (every page using `ak-top` — 18+ templates) with zero perceptible visual change. **Preserved unchanged:** `background-position` keyframe animation (`akTopGlint`, line 198 + line 200), `opacity:.22` base, `opacity:.08 → .22 → .26 → .12 → .08` keyframe progression, `mix-blend-mode: screen`, `-webkit-background-clip:text` + `background-clip:text` shine clip, linear-gradient sheen, `background-size:260% 260%`, base `background-position:-120% 0%`, hover rule `.ak-top-tile:hover .ak-top-label::after,.ak-top-tile.is-active .ak-top-label::after{opacity:.44;filter:blur(0px)}` (left intact — the `filter:blur(0px)` override is now vacuous against no base filter but harmless and kept to minimize diff scope), `.ak-top-label::before` gradient/shimmer (unchanged), particle counts (dust 400–800, floaters 200–400), DPR cap=1 (shipped 2026-05-12 in prior pass), `mix-blend-mode: screen` on the canvas, `akTopCartBob`, shooter cadence, `IntersectionObserver` cancel/restart, nav drawer, cart button, every other selector and rule in `sections/ak-top.liquid`. **Not bundled into this pass:** `ak-product-field` `filter: contrast(1.1)` removal (page-specific lane, not site-wide), `ak-top` canvas particle reductions (exploratory, not precedent-backed), `ak-collection-field` dust ceiling (measurement-disqualified), Mystery Box outer-shell animation stack (protected). **Mystery Crate completely untouched:** no Mystery Crate file edited, no `page.mystery-box.json`, no `sections/ak-mb-*.liquid`, no iframe src, no `_preview_hat_id`, no `customer_id`, no crate-repo file. **Revert:** in `sections/ak-top.liquid` line 197, restore the declaration to `mix-blend-mode:screen;opacity:.22;filter:blur(.15px);`. | Low (lowest visual risk of any glint reduction option — the 0.15 px blur is below the perceptual threshold on every modern display). | Run `shopify theme dev` locally and load `/collections/master-collection`, a representative product page, `/cart`, `/pages/the-collective` (Life/Style), `/pages/2025` (Bad Music), `/pages/narrative`, `/pages/club-hub`, and `/pages/mystery-box` outer shell. Eyeball the `ak-top` nav header — the glint sheen should still sweep across each label every ~2.85 s with the same character, brightness, and gradient color. Compare to a pre-change screenshot if available; expect no visible difference. Verify hover state on nav tiles still brightens the glint (`opacity:.44`) — the hover `filter:blur(0px)` override is left in place. Verify mobile drawer, cart button, cart-bob, logo entrance animation, and the underlying canvas particle field all unchanged. Re-run the FPS measurement script previously used on `/collections/master-collection` (idle for 300 frames) — expect baseline to lift from the post-DPR ~43 FPS toward higher (the in-page `akTopGlint` kill measured at +7.9 FPS as upper bound; this option only removes the filter, not the entire glint, so realistic gain is a fraction of that, expected ~+2–4 FPS). On real retina/Atlas hardware (DPR ≥ 2) the saved filter work is proportionally larger than the headless 800×600 test shows. If FPS gain is significant and site-wide scroll feels better, the next still-safe lane is `ak-top` dust ceiling 800 → 600 (matches the 800→600 floater cut already shipped on `ak-collection-field`). If gain is small, the next candidate becomes the more visually-risky glint option B (`background-position` animation → opacity-only) or reducing simultaneous glints. Verify no new console errors. Verify Mystery Box outer shell unchanged (no Mystery Crate file touched). |
| 2026-05-12 | ak-top: cap canvas DPR from 2 to 1 (measured highest-confidence next compositor pass) | `sections/ak-top.liquid` | Live Chrome DevTools measurement on `https://amerikid.ca/collections/master-collection` showed idle FPS at 41.4 with average frame gap 24.18 ms and zero long-tasks — confirming the bottleneck is compositor/paint, not JS. In-page isolation (temporary DOM `display:none` toggles, all reverted) measured the contribution of each suspect surface: hiding `ak-top-canvas` recovered +23.8 FPS (~8.8 ms/frame); hiding `ak-collection-field` recovered only +2.3 FPS (~1.3 ms/frame); cleanly killing `akTopGlint` recovered +3.2 FPS (~1.6 ms/frame); hiding both canvases recovered +31.9 FPS (page reached 73 FPS). `ak-top-canvas` is the dominant single contributor. Root cause located: `sections/ak-top.liquid` is the only major canvas in the theme that did NOT receive the DPR-cap pass — file read `dpr = Math.min(window.devicePixelRatio || 1, 2)` on both the initial declaration (line 555) and the `resize()` reassignment (line 574), while every other audited canvas (`ak-product-field` Phase 2 2026-04-04, `ak-collection-field` Phase 1 2026-04-05, `ak-archive-canvas`, `ak-collection-bg`, `ak-mb-canvas`) was already hardcoded to `dpr = 1`. On retina hardware (DPR ≥ 2 — most laptops, all iPhones, all iPads), `ak-top-canvas` was drawing a 1600×1530 backing buffer (~2.45 MP) instead of 800×765 (~0.61 MP) — a 4× fill-rate cost on the always-on header that renders across every page using `ak-top` (18+ templates). Both DPR expressions changed to literal `1`, exactly matching the proven pattern used on the other five canvases. **Preserved unchanged:** every other line of `sections/ak-top.liquid` — particle counts (dust 400–800, floaters 200–400), `akTopGlint` keyframe/animation (5 simultaneous instances on `.ak-top-label::after`), `akTopCartBob` keyframe/animation, `mix-blend-mode: screen` on `.ak-top-canvas`, canvas inset/size CSS, shooter cadence, accent-floater `glowSize`, `IntersectionObserver` cancel/restart, `ResizeObserver`, reduced-motion branch, nav drawer, cart button, glass nav header layout, logo entrance + hover, all 18 templates that include this section. **Mystery Crate completely untouched:** no Mystery Crate files, no `page.mystery-box.json`, no `sections/ak-mb-prestart.liquid`, no `sections/ak-mb-release.liquid`, no iframe src, no `customer_id`, no `_preview_hat_id`, no crate-repo file. `sections/main-collection-product-grid.liquid` also not touched in this pass — the previously planned dust ceiling `2000 → 1500` was measurement-disqualified and not implemented. **Revert:** in `sections/ak-top.liquid`, change line 555 from `var w = 0, h = 0, dpr = 1;` back to `var w = 0, h = 0, dpr = Math.min(window.devicePixelRatio || 1, 2);` and line 574 from `dpr = 1;` back to `dpr = Math.min(window.devicePixelRatio || 1, 2);`. | Low | Push to development theme `184629133592` first. Open `/collections/master-collection` and a representative ak-top route (any product page, cart, Life/Style, Archive, Narrative, Bad Music, Clubhouse, Mystery Box) on Chrome desktop and Chrome mobile (≤480 px); compare side-by-side against a pre-change screenshot. Verify ak-top header band, glass nav drawer, cart button, dust particles, floaters with accent glow, shooting stars, and `akTopGlint` label sheen all render. Particles should still look soft and acceptable — slight reduction in pixel crispness at extreme zoom on retina is expected and proven safe across 5 prior canvas DPR-cap passes. Re-measure `/collections/master-collection` with the same in-page FPS measurement script used to identify the bottleneck — expect baseline FPS to lift from ~41 toward 55–65 (closer to the +23.8 FPS observed when the canvas was hidden entirely, but not all the way since canvas is still drawing at half resolution). Capture a fresh real-browser Web Vitals `onINP` measurement and compare against the 1296 ms field P75 / 1640–1656 ms lab — expect material reduction in presentation delay. Verify Mystery Box outer shell unchanged (no Mystery Crate file touched). Verify no new console errors. Verify cart drawer interactions, header drawer open/close, logo entrance animation, cart bob, glint sheen all behave as before. If visual regression appears (unlikely given precedent), revert per the **Revert** path above. If INP drop is significant, the secondary `akTopGlint` simplification (`+3.2 FPS measured`) becomes the next candidate; if INP drop is modest, the next pass should investigate why PageFly is loading 8+ scripts on a non-PageFly collection page (separate lane). |
| 2026-04-16 | ak-collection-field: reduce desktop floater ceiling 800 → 600 (smallest-safe next collection-page compositor pass) | `sections/main-collection-product-grid.liquid` | Lag-reduction lane for `/collections/master-collection`. The prior `ak-top` filter removal was kept as a harmless micro-optimization but did not produce a major felt lag win, so this pass targets the next largest compositor contributor on the route — the `ak-collection-field` canvas owned by `sections/main-collection-product-grid.liquid`. The canvas is ~4,421 px tall on master-collection, so both the dust ceiling (2,000) and the floater ceiling (800) are hit on desktop. Floaters are the more expensive particle type (accent floaters carry `shadowBlur` glow; dust does not since Phase 1). Dropping the floater ceiling from 800 to 600 on line 504 of `sections/main-collection-product-grid.liquid` is a desktop-only reduction — mobile's `densityScale()` of 0.62 already keeps the floater count (~580 on typical phones) below the new 600 cap, so mobile visual density is unchanged. 25 % fewer accent-glowing floaters on desktop compared to the prior state. Matches `ak-back-field`'s proven desktop floater ceiling of 600 (shipped 2026-04-04) exactly. **Preserved unchanged:** dust count formula and ceiling (still 800–2,000 with `densityScale`), `densityScale()` mobile/desktop split (0.62 / 1.0), DPR hardcoded to 1, `mix-blend-mode: screen`, canvas inset/size CSS, shooter cadence (`rand(2400, 7200)` ms), shooter spawn logic, accent-floater `glowSize` `rand(8, 18)`, dust `shadowBlur`-off state, `IntersectionObserver` cancel/restart, reduced-motion `display:none` hide, color palette (white/purple/blue/yellow), particle kinds (dot/pill/blob), dust/floater color pickers, section background forced black, product card plate removal CSS, NEW marker CSS, filter state (still no `filter: contrast(1.1)`, removed Phase 2 2026-04-05). Adjacent comment on line 503 updated to reflect the new history (`520, 1800` → `200, 800` (Phase 1) → `200, 600` (this pass)). **Mystery Crate completely untouched:** no Mystery Crate files, no `page.mystery-box.json`, no wrapper canvas, no iframe src, no `customer_id`, no `_preview_hat_id`, no crate-repo file. `sections/ak-top.liquid` also not touched in this pass. **Revert:** in `sections/main-collection-product-grid.liquid` line 504, change `200, 600` back to `200, 800`. Line 503 comment can be reverted or left as-is (it is historical annotation only). | Low | Open `/collections/master-collection` on Chrome desktop and compare side-by-side against a before screenshot — starfield should still read as dense and alive; accent-glowing dots, pills, and blobs should still appear throughout the grid, just ~25 % less dense. On mobile (375/320 px), starfield density should be byte-for-byte unchanged because mobile was below the old ceiling already. Capture a fresh real-browser Web Vitals `onINP` measurement on the collection page and compare against the 1640–1656 ms baseline and against the post-ak-top-filter-removal measurement (if recorded). Expect a measurable reduction in presentation delay/paint cost on desktop, proportional to accent-glow reduction. Verify other routes using this section (none — `sections/main-collection-product-grid.liquid` is the only host of `ak-collection-field`) and confirm no visual change on other collection pages that use `templates/collection.json`. Verify no change on `/pages/mystery-box`, `/products/candyfacts-mystery-box-combo`, homepage, Life/Style, Archive, Narrative, Bad Music, Clubhouse, Mystery Box pages (all unrelated to this canvas). Verify shooter stars still fire occasionally on the collection page. Verify reduced-motion hides the canvas as before. Verify `IntersectionObserver` cancel/restart still idles the canvas when scrolled past. Verify no new console errors, no layout shift, no missing assets. If desktop density looks too sparse, revert to 800; if INP drop is small and visually fine, the next smallest-safe candidate is the dust ceiling 2,000 → 1,500 or adopting `ak-back-field`'s full mobile branch. |
| 2026-04-16 | ak-top: remove `.ak-top-canvas` CSS `filter: contrast(1.1)` (Phase 2 — smallest-safe collection-page compositor pass) | `sections/ak-top.liquid` | Lag-reduction lane for `/collections/master-collection`. Real-browser Web Vitals `onINP` measurement put interaction delay at 1640–1656ms, dominated by presentation delay (paint/compositor), not JS processing time. `.ak-top-canvas` is always-on on every collection page (and 17 other templates) and stacks `mix-blend-mode: screen` on top of `filter: contrast(1.1)` — the filter adds a per-frame GPU pass on the full always-visible top canvas. Removed the single `filter:contrast(1.1);` declaration from the `.ak-top-canvas` CSS block. **Preserved unchanged:** `mix-blend-mode: screen`, `position:absolute`, `inset:0`, `width:100%`, `height:100%`, `pointer-events:none`, `z-index:0`, and everything else in `sections/ak-top.liquid` — canvas element, `akTopCartBob` keyframes/animation, `akTopGlint` keyframes/animation on nav tiles, particle JS loop, DPR cap, dust/floater/shooter counts, IntersectionObserver cancel/restart, `ak-top-wrap::after` fade, nav drawer, cart button, glass nav, starfield behavior. Precedent: identical filter removal shipped for `ak-collection-field` (2026-04-05, Phase 2), `bv-field` (2026-04-09), and all three Life/Style content canvases (2026-04-05) with no reported visual regression. `sections/main-collection-product-grid.liquid` (`ak-collection-field` canvas) already had its filter removed in Phase 2, so the collection page's other major canvas lane is already clean. Mystery Box wrapper, Mystery Crate iframe, `ak-mb-canvas` filter, `_preview_hat_id` handoff, purchased/email-link access flow, and all commerce logic completely untouched — no Mystery Crate file edited. **Revert:** in `sections/ak-top.liquid`, inside the `.ak-top-canvas{ ... }` block, re-add `  filter:contrast(1.1);` directly below `mix-blend-mode:screen;`. | Low | Open `/collections/master-collection` on Chrome (desktop and mobile) and compare side-by-side with a before screenshot. Confirm ak-top header band, glass nav, cart button, particle field (dust + floaters + shooting stars) all still render normally. Screen blend is preserved so bright particles still pop against the dark background; the filter was only a subtle contrast boost, so particle density, color, and glow should look visually indistinguishable. Capture a real-browser INP measurement using Web Vitals `onINP` (not synthetic `dispatchEvent`) for scroll + click on the collection page and compare against the 1640–1656ms baseline — expect a measurable reduction in presentation delay. If presentation delay does not drop materially, the dominant lane is elsewhere and the next smallest-safe pass should target another compositor contributor (e.g. product-grid stacking contexts, ak-top's infinite `akTopCartBob` / `akTopGlint` keyframes, or the collection-page second canvas). Verify all 17 other templates using ak-top (cart + product + Life/Style + Mystery Box + Archive + Narrative + Bad Music + Clubhouse) still render normally. Verify Mystery Box page completely unchanged — no Mystery Crate file touched in this lane. Verify reduced-motion branches unchanged. No new console errors, no layout shift. |
| 2026-04-16 | Life/Style: reduce `.ak-holo-card` backdrop-filter blur radius 56px → 24px (Option A, smallest-safe lag-reduction pass) | `templates/page.json` | Lag-reduction lane for `/pages/the-collective`. Gemini runtime picture flagged `backdrop-filter` on Life/Style glass cards as a strong Chrome scroll-jank contributor. Approved implementation: Option A from the prior repo-planning pass — numeric reduction of the blur radius only. Changed all 8 occurrences of `blur(56px)` to `blur(24px)` inside the three ID-scoped Life/Style module blocks: (1) Adventures `custom_liquid_X46wQm` / `#akmod-adventures-yt .ak-holo-card` — 2 instances (`-webkit-backdrop-filter` + `backdrop-filter`). (2) Kid Radio `custom_liquid_kkxqR3` / `#ak-kidradio-holo .ak-holo-card` — 2 instances. (3) Channels `custom_liquid_PRGR7i` / `#ak-chan-holo .ak-chan-banner` + `#ak-chan-holo .ak-holo-card` — 4 instances (both `-webkit-backdrop-filter` + `backdrop-filter` on each rule). **Preserved unchanged:** all `saturate(...)` values (`saturate(170%)` on Kid Radio, `saturate(210%)` on Adventures and Channels), all `contrast(112%)` values, the aura `::before` `filter: blur(90px)` gradient (different property, not a backdrop-filter), background, border, box-shadow, opacity, transforms, transitions, hover states, 3D tilt, `transform-style:preserve-3d`, `perspective`, reduced-motion branches, playing-state rules, specular highlights, shimmer film, grid, IDs, selectors, DOM, iframes, canvases, and JS. No browser gating added. No YouTube facade bundled in. Radius dominates blur GPU cost roughly by radius squared on most Chromium implementations — a 56 → 24 reduction is a ~5× drop on the blur pass while keeping the frosted read visibly present. Mystery Crate fully untouched. No changes to `assets/base.css`, `sections/newsletter2.liquid`, `sections/contact-form.liquid`, `sections/ak-archive.liquid`, `snippets/club-hub.liquid`, `templates/page.contact.json`, or any other page template. **Revert:** in `templates/page.json`, change `blur(24px)` back to `blur(56px)` across all 8 occurrences inside the three Life/Style `custom_liquid` blocks. | Low | Open `/pages/the-collective` on Chrome (desktop and mobile). Compare against a saved before-screenshot: Adventures, Kid Radio, and Channels cards should still read as glass (slight frost still visible but less diffuse than before). Confirm Adventures 3D hover tilt on cards 2–6 still fires on hover. Confirm starfield canvases (Adventures, Kid Radio, Channels, ak-collection-bg, ak-top) still render. Confirm YouTube iframes still load and play. Confirm banner card (Channels) visual treatment still present. Confirm playing-state color wash (Adventures amber, Kid Radio magenta) still fires when a video plays. Confirm `page.contact.json` page visually unchanged. Capture fresh Chrome DevTools performance trace scrolling Life/Style — verify blur work drops relative to the before trace. If blur still dominates frame cost, plan Option B (mobile gate) or C (strip saturate+contrast). If `ThirdParties`/`ForcedReflow` now dominate, plan the YouTube facade lane. Confirm Mystery Box page completely unchanged (no file touched in that lane). |
| 2026-04-15 | Mystery Box: Phase 5.0 Dossier / Eyes-Only framing (envelope + watermarks + title rails + subtitle cartridge + plate memo + disclaimer ruling) | `sections/ak-mb-prestart.liquid` | Additive cosmetic framing layer over the existing prestart overlay. Six coordinated devices, zero behavior change, zero copy change, zero state change, zero animation added, purple `#ff33ff` only. **(1) Overlay envelope** — new `.ak-mb-prestart__envelope` absolute element at `inset:16px` (desktop) / `12px` (640) / `10px` (380), z-index 2 (above watermarks, below inner content), 1px rule `rgba(255,51,255,0.16)` with inset + outer box-shadow. Four corner bracket children `.ak-mb-prestart__env-corner--tl/tr/bl/br` render L-shaped magenta brackets via `::before` (2px horizontal arm) + `::after` (2px vertical arm), 22×22px desktop / 18×18 640 / 14×14 380, `0 0 6px rgba(255,51,255,0.45)` glow. Deliberately larger and crisper than the shell-fx register marks behind the iframe so the overlay reads as a separate document layer. **(2) Watermark document bands** — two `.ak-mb-prestart__watermark--top`/`--bottom` siblings at z-index 1 (behind envelope, above atmospheric haze). Sans-serif 700-weight uppercase, `rgba(255,51,255,0.05)` alpha, 14px desktop / 11px 640 / 9px 380, 0.5em tracking. Top reads `CLASSIFIED // MYSTERY BOX` repeating, bottom reads `EYES ONLY // SPIN PROTOCOL` repeating. Purely decorative, `aria-hidden="true"`, `user-select:none`, `pointer-events:none`. Very low opacity so the classified-document read is felt without fighting hero legibility. **(3) Title rails** — new `.ak-mb-prestart__title-frame` wraps the h2, `max-width:560px` (desktop) / `420px` (640) / `300px` (380), padding `14px 0 10px`, with `::before`/`::after` absolute 1px magenta hairlines (linear-gradient transparent → 0.35 → 0.55 → 0.35 → transparent) pinning the title top and bottom. Title now reads as a mastered callout on the page. **(4) Subtitle cartridge** — `.ak-mb-prestart__subtitle::before`/`::after` re-specified from the Tier 1 inline 4×8 rectangles to absolutely-positioned 10px-wide bracket hardware: left bracket `border-right:none`, right bracket `border-left:none`, `height:calc(100% - 6px)`, `border:1px solid rgba(255,51,255,0.72)`, `0 0 6px rgba(255,51,255,0.3)` glow, `position:absolute; top:50%; transform:translateY(-50%)`. Subtitle padding becomes `7px 30px 6px`. Overrides (not adds to) the Tier 1 declarations via cascade order — Tier 1 values are shadowed. Reads as a status cartridge bracketing `START // SPIN` / `ENTER // ACCESS`. **(5) Plate memo right tab** — new `.ak-mb-prestart__plate-tab-r` absolutely positioned at `top:-7px; right:18px` (desktop) / `14px` (640) / `12px` (380). Same background/type/color/line-height family as the existing top-left `.ak-mb-prestart__plate-label`, 10px mono, 0.3em letter-spaced, renders a decorative `· · ·` glyph, text-shadow `0 0 5px rgba(255,51,255,0.4)`. Plate now reads as a two-terminal memo strip. Additionally: internal dashed dividers between briefing lines via `.ak-mb-prestart__plate-body .ak-mb-prestart__brief-line:not(:last-child){ padding-bottom:6px; border-bottom:1px dashed rgba(255,51,255,0.1); }`. **(6) Disclaimer signature ruling** — `.ak-mb-prestart__disclaimer` picks up a very fine repeating vertical tick pattern via `repeating-linear-gradient(90deg, transparent 0, transparent 6px, rgba(255,51,255,0.055) 6px, rgba(255,51,255,0.055) 7px)` layered atop the existing dark gradient, and `border-top` color raised to `rgba(255,51,255,0.28)`. Tier 1 pulse-dot + right bar bookends preserved on top. Turns the disclaimer into a designed console/signature footer. **Architecture:** All new DOM is `aria-hidden="true"` (envelope + 4 corners + 2 watermarks + title-frame wrapper + plate-tab-r = 9 new nodes). No new animations. No JS behavior change. Watermarks/envelope/title-frame compete for no z-index with inner content (inner still at z-index 3). No reduced-motion additions needed. All changes live in `sections/ak-mb-prestart.liquid` only. Zero edits to `templates/page.mystery-box.json`, zero edits to `sections/ak-mb-release.liquid`, zero iframe interaction, zero crate-repo changes, zero Shopify admin/Flow changes. Shell continuity (Phase 3.0), Tier 1 (Phase 4.0), atmospheric layers (Phase 1.5 / 2.0), public/purchased state branching, dismiss behavior, iframe pointer-events restore, chin telemetry swap all untouched. Held for possible later pass: large rotated `AUTHORIZED` / `OPEN FILE` stamp, CTA rail extensions reaching the envelope, stronger purchased-only elevation, boot-sequence staged mount. | Very Low | Verify 1px magenta envelope framing inset ~16px from overlay edges (12 at 640, 10 at 380) with four magenta L-shaped corner brackets at each corner — noticeably larger/crisper than the shell-fx register marks inside the iframe. Verify `CLASSIFIED // MYSTERY BOX` watermark along top, `EYES ONLY // SPIN PROTOCOL` along bottom, both purple, very low alpha, readable only on inspection, not fighting hero legibility. Verify `MYSTERY BOX` title flanked by two magenta hairline rails (top + bottom, gradient-faded to transparent at edges). Verify `START // SPIN` / `ENTER // ACCESS` subtitle now sits inside magenta bracket hardware (cartridge), replacing the Tier 1 small rectangles. Subtitle copy unchanged. Verify HOW IT WORKS / YOUR ACCESS plate now shows a decorative `· · ·` tab at top-right balancing the top-left label. Verify subtle dashed magenta dividers between briefing lines (not after last line). Verify disclaimer strip picks up fine vertical tick background and a slightly brighter top border — Tier 1 pulse dot (left) and bar (right) still intact on top. Verify purple `#ff33ff` only — no gold/amber/green/cyan added. Verify all new elements are `aria-hidden="true"` (screen reader still reads kicker → title → subtitle → plate label → brief lines → CTA → disclaimer). Verify no new animations (reduced-motion users see the same framing as everyone else). Verify START / ACCESS SPINS dismiss still works, iframe pointer-events restore, chin telemetry swaps to `CRATE // ONLINE`. Verify mobile 640/380 responsive: envelope inset tightens, corners shrink (18→14), watermark shrinks (14→11→9), title rails shorten (max-width 560→420→300), cartridge padding tightens, plate right-tab shifts inward. Verify no overflow or clipping on 375/320px. Verify no new console errors, no new canvases, no layout shift. |
| 2026-04-15 | Mystery Box: Phase 4.0 Tier 1 prestart hierarchy refinements (kicker + subtitle + brief-line tiers + disclaimer bookends + focus echo) | `sections/ak-mb-prestart.liquid` | Pure CSS typographic/bookend/focus pass over the existing prestart overlay. Zero DOM change, zero JS change, zero behavior change, zero copy change. **(1) Kicker authority** — `.ak-mb-prestart__kicker` picks up 1px magenta hairlines above/below via `border-top: 1px solid rgba(255,51,255,0.22)` + `border-bottom: 1px solid rgba(255,51,255,0.10)`, padding `4px 14px 3px`, alpha raised from 0.42 to 0.62, tracking tightened 0.34em → 0.32em, font-size pinned at 10.5px. Reads as stamped document header. **(2) Subtitle bookends** — leading/trailing 4×8px magenta rectangles via `.ak-mb-prestart__subtitle::before`/`::after` with `box-shadow: 0 0 5px rgba(255,51,255,0.5)`, 10px margin off the text. Bridges title→plate with visible terminal hardware. **(3) Briefing-line hierarchy** — `:nth-child()` selectors inside `.ak-mb-prestart__plate-body` create a three-tier read. Line 1: 14px, alpha 0.96, chevron `text-shadow: 0 0 6px rgba(255,51,255,0.6)`, `padding-bottom: 2px`. Line 2 unchanged. Line 3: 11px, alpha 0.6, chevron alpha 0.55. Purchased 2-line variant benefits from line-1 emphasis with line 2 unchanged. Inline `COMBO PACK` link styling untouched. **(4) Disclaimer bookends** — `.ak-mb-prestart__disclaimer::before` renders a 6px magenta pulse dot at `left:14px` with the existing serial-dot shadow family and reuses `akMbPrestartPulse` 4s keyframe. `::after` renders a 4×6px magenta bar at `right:14px`. Mirrors the top serial strip as a matched console footer bookend. Disclaimer text content unchanged. **(5) Keyboard focus echo** — `.ak-mb-prestart__inner:has(.ak-mb-prestart__cta--primary:focus-visible)` selector brightens `.ak-mb-prestart__plate::after` background to `rgba(255,51,255,0.92)` and lifts `.ak-mb-prestart__subtitle` border-top to `rgba(255,51,255,0.82)`, border-bottom to `rgba(255,51,255,0.28)`, color to `#ff66ff`. Subtitle carries new `transition: border-color 160ms ease-out, color 160ms ease-out` for smooth in/out response. Desktop keyboard users get a full-screen coordinated response matching hover treatment. Responsive: all new rules have matching scaled sizes at `@media (max-width:640px)` (kicker 9.5px, bookends 3×7, line 1 12.5px, line 3 10px, dot 5×5) and `@media (max-width:380px)` (kicker 9px, bookends 3×6, line 1 11.5px, line 3 9.5px, dot 4×4). Reduced-motion: `.ak-mb-prestart__disclaimer::before` animation killed inside existing reduced-motion block with opacity pinned to 0.85. Focus-echo rules animate no keyframes (color/opacity/border-color only) so they remain active for reduced-motion users. Zero edits to `templates/page.mystery-box.json`, zero edits to `sections/ak-mb-release.liquid`, zero iframe interaction, zero crate-repo changes, zero Shopify admin/Flow changes. Shell continuity architecture (Phase 3.0) untouched. Phase 1.5 / 2.0 atmospheric animations untouched. | Very Low | Verify `— AMERIKID —` kicker now sits between 1px magenta hairlines and reads as a stamped document header. Verify `START // SPIN` / `ENTER // ACCESS` subtitle has small magenta 4×8px blocks flanking the text with subtle glow. Verify public briefing plate reads in three distinct tiers: line 1 bigger/brighter with glowing chevron, line 2 baseline with inline COMBO PACK link, line 3 smaller/dimmer. Verify purchased plate's 2 lines get line-1 emphasis with line 2 unchanged. Verify bottom disclaimer has a magenta pulse dot 14px from left (matches top serial dot cadence) and a small magenta bar 14px from right. Verify disclaimer text copy unchanged. Verify keyboard tab to CTA brightens plate top accent to near-full magenta AND subtitle border-top/color in smooth 160ms transition, reverses smoothly on blur. Verify hover on CTA still works. Verify mobile 640/380 breakpoints scale proportionally. Verify reduced-motion: disclaimer dot pulse disabled (static 0.85), focus echo still operates. Verify no new DOM nodes (Tier 1 is pseudo-element-only), no new canvases, no new console errors, no layout shift, no behavior drift. Verify START dismiss and purchased ACCESS SPINS dismiss both still work. Verify inline COMBO PACK link still navigates. |
| 2026-04-15 | Mystery Box: Phase 3.0 Shell Continuity (atmosphere survives prestart dismiss) | `sections/ak-mb-prestart.liquid` | Persistent atmospheric + telemetry layer mounted directly on `.ak-mb-frame` at z-index 3 (above iframe/stage, below prestart/lock/bevel). New DOM `.ak-mb-shell-fx` is injected by the prestart section's JS as first child of `.ak-mb-frame` via a new `initShellFx(frame, initialChinText)` function, idempotent via `frame.dataset.akMbShellFxInit` guard. **Six persistent children:** (1) **scanline continuity** at stage inset 8/8/14/8 (desktop) / 6/6/10/6 (mobile), stripes `rgba(255,255,255,0.04)` — about half the prestart scanline opacity so it doesn't fight iframe content readability, reuses existing `akMbScanScroll` keyframe at 14s linear. (2) **grain continuity** same inset, SVG fractal-noise at opacity 0.045 (no `mix-blend-mode` — neutral over arbitrary iframe colors), reuses `akMbGrainDrift` keyframe at 18s linear. (3) **phosphor hairline** 1px magenta line at top stage edge `rgba(255,51,255,0.22)` with new `akMbShellHair` keyframe (7s ease-in-out, 0.55↔1.0 opacity). (4) **corner register marks** — four `.ak-mb-shell-fx__reg` L-brackets 8×8px (desktop) / 6×6px (mobile) positioned 3px inside each stage corner, magenta lines `rgba(255,51,255,0.55)` at 0.42 element opacity, no animation. (5) **chin telemetry** — mono-caps status strip at `left:12px; bottom:3px` (desktop) / `left:8px; bottom:2px` (mobile), 8.5px/7.5px, purple `rgba(255,51,255,0.56)`, with 6×8px blinking cursor via new `akMbChinCursor` keyframe (1.1s steps(1)). Initial wording: `CRATE // LINK ACTIVE` (public) or `CRATE // ACCESS GRANTED` (purchased). Text swaps to `CRATE // ONLINE` in the existing `dismiss()` timeout callback once the overlay is removed. (6) **LED life** — `.ak-mb-frame::after` overridden via cascade (same selector, prestart section loads after the embed so it wins) with stacked animations: new `akMbLedBreathe` 5s ease-in-out on opacity (0.68↔1.0) + new `akMbLedFlicker` 27s linear on `filter:brightness()` — two animations target different properties so both apply without conflict. Flicker pattern: brightness(1) default with two micro-dips near 98.8% and 99.3% of cycle (≈150ms double-blink every 27s). LED color, size, position unchanged. **Architecture:** All new CSS and JS additive inside `sections/ak-mb-prestart.liquid`. Zero edits to `templates/page.mystery-box.json` custom_liquid string. Zero edits to `sections/ak-mb-release.liquid`. Zero iframe interaction, zero iframe src changes, zero postMessage, zero audio. Release gate and `isPurchased` detection unchanged and shared with prestart. `pointer-events:none` on all shell-fx children so iframe remains fully interactive. Reduced-motion kills all Phase 3.0 animations, hairline snaps to 0.55, chin cursor stays solid, LED animation disabled. Mobile 480px breakpoint matches existing chin/padding geometry. | Low | Verify `.ak-mb-shell-fx` mounts as first child of `.ak-mb-frame` (DevTools Elements). During prestart: shell-fx hidden under opaque overlay (correct). After clicking START: shell-fx visible over iframe. Verify subtle scanlines scroll over iframe (half-opacity of prestart scanlines). Verify faint grain drift over iframe. Verify top-edge magenta hairline breathes 7s. Verify four L-bracket register marks inside each stage corner. Verify chin telemetry reads `CRATE // LINK ACTIVE` (public, no `customer_id` param) or `CRATE // ACCESS GRANTED` (purchased, `?customer_id=test`). Verify chin cursor blinks ~1.1s. Verify chin text swaps to `CRATE // ONLINE` 200ms after START click. Verify green LED breathes 5s + rare brief flicker every ~27s. Verify iframe still fully interactive (pointer events pass through). Verify scanlines/grain paint inside screen area only, not bezel. Verify mobile 375/320px — insets 6/6/10/6, reg marks 6×6, chin 7.5px, no overflow. Verify purple-only palette (no new accents). Verify reduced-motion: all Phase 3.0 animations stop. Verify no new console errors. Verify no new canvases. Verify no layout shift. |
| 2026-04-15 | Mystery Box: Phase 2.0 visibility tuning for prestart atmospheric effects | `sections/ak-mb-prestart.liquid` | Four CSS value-level adjustments to make Phase 1.5 ambient effects actually perceptible in real browsing. **(1) CTA glow fix:** Removed `overflow:hidden` from `.ak-mb-prestart__cta` (was clipping the `::after` glow shadow) and changed `::after` `z-index` from `-1` to `0` (was rendering behind opaque button background). Glow pulse now visibly radiates around button. Labels at `z-index:1` remain above. `::before` gradient sheen at `z-index:0` unaffected (transparent except for top gradient). **(2) Scanline visibility:** Stripe opacity `rgba(255,255,255,0.038)` → `rgba(255,255,255,0.07)`. Now above casual perception threshold — reads as CRT horizontal lines scrolling down, not invisible shimmer. **(3) Root haze visibility:** Resting gradient center `rgba(255,51,255,0.04)` → `rgba(255,51,255,0.08)`. Keyframe range `0.04→0.09` → `0.06→0.18`. Purchased override `rgba(255,51,255,0.06)` → `rgba(255,51,255,0.10)`. **(4) Plate flicker visibility:** `::after` height `1px` → `2px`, background `rgba(255,51,255,0.32)` → `rgba(255,51,255,0.55)`, keyframe range `0.08–0.38` → `0.12–0.58`, reduced-motion static opacity `0.18` → `0.28`. No new keyframes. No new DOM. No JS changes. No copy changes. No behavior changes. Title glow, vignette breathing, grain drift, state class architecture, cadence logic all unchanged. | Very Low | Verify CTA glow pulse is now visible as a faint magenta halo around the primary button. Verify glow doesn't obscure button text. Verify CTA hover/active/focus still work. Verify scanlines are clearly visible as horizontal stripes scrolling downward (CRT feel, not too strong). Verify text readability unaffected by scanlines. Verify purple haze is visible as soft central tint (not foggy). Verify plate top-edge flicker reads as a 2px magenta accent that breathes irregularly. Verify no content clipping on CTA after overflow:hidden removal. Verify purchased state proportionally brighter. Verify mobile 375/320px — no overflow, no clipping. No console errors. |
| 2026-04-15 | Mystery Box: Phase 1.5 atmospheric styling for prestart overlay | `sections/ak-mb-prestart.liquid` | Additive CSS-only ambient animation pass bringing the prestart overlay to life as a COD Zombies / bunker-monitor terminal. Seven new animation layers: (1) scanline scroll — `background-position-y` on `__scanline`, 12s linear infinite, opacity bumped from 0.012 to 0.038. (2) grain drift — `background-position` on `__grain`, 14s linear infinite, opacity bumped from 0.048 to 0.08. (3) vignette breathing — opacity pulse on `__vignette`, 7s ease-in-out infinite. (4) title glow pulse — `text-shadow` bloom on `__title`, 6s ease-in-out, peaks at 32px/60px magenta spread. (5) CTA glow pulse — `::after` pseudo on `__cta--primary`, 5s ease-in-out, pulsing `box-shadow` bloom. (6) plate top-edge flicker — `::after` pseudo on `__plate`, 6s ease-in-out, irregular opacity pattern with micro-dip at 42%. (7) root atmospheric haze — `::before` pseudo on `.ak-mb-prestart`, radial gradient pulse, 8s ease-in-out. **State differentiation** via CSS modifier class: JS now sets `ak-mb-prestart--public` or `ak-mb-prestart--purchased` on root div. Public reads colder/eerier (default slower cadences). Purchased reads more energized (faster durations 3.5–10s, slightly brighter haze/glow). Both states use only `#ff33ff` purple family — no new accent colors. 8 new `@keyframes` declarations. Reduced-motion block updated to kill all Phase 1.5 animations. **One JS change:** `overlay.className` now includes state modifier class. Zero DOM structure changes. Zero copy changes. Zero iframe changes. No crate-repo changes. | Low | Verify scanlines scroll downward (slow, subtle). Verify grain drifts laterally (barely perceptible). Verify vignette breathes (edge darkness pulses). Verify title glow pulses magenta (6s cycle). Verify CTA outer glow pulses (faint, not distracting). Verify plate top edges flicker subtly. Verify faint purple haze behind content. Verify public state (`--public` class) reads colder/slower. Verify purchased state (`--purchased` class, `?customer_id=test`) reads more energized/faster. Verify no green/amber/other accents — purple only. Verify reduced-motion kills all ambient animations. Verify hover states on CTA still work over glow pulse. Verify no layout shift. Verify mobile 375/320px — animations still render, no overflow. No console errors. |
| 2026-04-15 | Mystery Box: finalize public prestart How It Works copy | `sections/ak-mb-prestart.liquid` | Final wording pass on public prestart briefing lines. Line 2 now describes combo pack contents: `THE COMBO PACK INCLUDES 1 MYSTERY CRATE HAT + 1 CANDYFACTS SHIRT.` (inline `COMBO PACK` link preserved). Line 3 now explains spin access path: `BUY THE COMBO PACK FIRST. ACCESS 2 SPINS THROUGH YOUR EMAIL CONFIRMATION LINK.`. Line 1 `START HERE TO SPIN THE CRATE.` unchanged. Subtitle, button, disclaimer all unchanged. **Purchased branch unchanged** — byte-for-byte identical. **No CSS changes.** **No JS logic changes.** No iframe changes. No crate-repo changes. Pushed to dev + live. | Very Low | Verify public prestart: line 2 reads `THE COMBO PACK INCLUDES 1 MYSTERY CRATE HAT + 1 CANDYFACTS SHIRT.` with inline magenta link. Line 3 reads `BUY THE COMBO PACK FIRST. ACCESS 2 SPINS THROUGH YOUR EMAIL CONFIRMATION LINK.`. Line 1 unchanged. Button `START / MYSTERY BOX`. Disclaimer `START HERE · OR COMBO PACK · 2 SPINS`. Purchased prestart unchanged. Mobile no overflow. No console errors. |
| 2026-04-15 | Mystery Box: remove "free" / "preview" wording from public prestart | `sections/ak-mb-prestart.liquid` | Wording-only correction to the public prestart branch. All instances of "free" and "preview" removed. Brief line 1 `SPIN THE CRATE. 1 FREE SPIN INCLUDED.` → `START HERE TO SPIN THE CRATE.`. Brief line 3 `FREE-SPIN RESULTS CAN STILL CONTINUE THROUGH THE COMBO PACK.` → `RESULTS CAN STILL CONTINUE THROUGH THE COMBO PACK.`. Button sublabel `1 FREE SPIN` → `MYSTERY BOX`. Aria-label updated to match. Disclaimer `1 FREE SPIN · OR COMBO PACK · 2 SPINS` → `START HERE · OR COMBO PACK · 2 SPINS`. Brief line 2 (inline COMBO PACK link) unchanged. Subtitle `START // SPIN` unchanged. Single-button structure unchanged. Inline link CSS unchanged. **Purchased branch unchanged** — byte-for-byte identical. **No CSS changes.** **No JS logic changes.** No iframe changes. No crate-repo changes. | Very Low | Verify public prestart: no "free" or "preview" wording anywhere. Button reads `START / MYSTERY BOX`. Lines read `START HERE TO SPIN THE CRATE.` / `BUY THE COMBO PACK FIRST TO GET 2 SPINS.` / `RESULTS CAN STILL CONTINUE THROUGH THE COMBO PACK.`. Disclaimer reads `START HERE · OR COMBO PACK · 2 SPINS`. Inline link still works. Purchased prestart unchanged. Mobile 375/320px — no overflow. No console errors. |
| 2026-04-15 | Mystery Box: single START button + inline combo link for public prestart | `sections/ak-mb-prestart.liquid` | Public prestart simplified from dual-CTA (Enter + Buy Combo Pack) to single centered `START / 1 FREE SPIN` button. Secondary `BUY COMBO PACK` `<a>` button removed entirely. Combo Pack path preserved as an inline `<a>` hyperlink inside briefing line 2 (`BUY THE <a>COMBO PACK</a> FIRST TO GET 2 SPINS`), linking to combo product page. New CSS added for `.ak-mb-prestart__brief-line a`: magenta accent color (`#ff33ff`), underline with 3px offset, hover brightens to `#ff66ff` with thicker underline, `focus-visible` matches hover. Subtitle `ENTER // EXPLORE` → `START // SPIN`. Briefing copy rewritten: line 1 `SPIN THE CRATE. 1 FREE SPIN INCLUDED.`, line 2 `BUY THE COMBO PACK FIRST TO GET 2 SPINS.` (inline link), line 3 `FREE-SPIN RESULTS CAN STILL CONTINUE THROUGH THE COMBO PACK.`. Disclaimer `1 FREE PREVIEW · OR COMBO PACK · 2 REAL SPINS` → `1 FREE SPIN · OR COMBO PACK · 2 SPINS`. Word "preview" removed from all public copy. Dismiss ID `akMbPrestartStart` stays on START button. Flexbox container naturally centers single child (same as purchased state). **Purchased branch unchanged** — byte-for-byte identical. **No JS logic changes.** No iframe changes. No crate-repo changes. | Very Low | Verify public prestart (no URL params): single centered `START / 1 FREE SPIN` button, no secondary button. Verify START dismisses overlay. Verify inline `COMBO PACK` link in briefing line 2 is magenta and underlined. Verify link navigates to combo product page. Verify link hover brightens. Verify subtitle `START // SPIN`. Verify disclaimer `1 FREE SPIN · OR COMBO PACK · 2 SPINS`. Verify purchased prestart (`?customer_id=test`) unchanged. Verify mobile 375/320px — centered, no overflow. No console errors. |
| 2026-04-15 | Mystery Box: reorder public prestart CTAs — Enter primary, Buy Combo secondary | `sections/ak-mb-prestart.liquid` | Public prestart CTA order and visual weight swapped. **Enter** (`<button>`, dismisses overlay) moved to left position and promoted to `--primary` style (brighter border-top, stronger glow, visually larger). **Buy Combo Pack** (`<a>`, navigates to combo product) moved to right position and demoted to `--secondary` style (dimmer, supporting). Briefing copy tightened to lead with Enter: line 1 `ENTER FOR ONE FREE PREVIEW SPIN.`, line 2 `OR BUY THE COMBO PACK FOR 2 REAL SPINS.`, line 3 `PREVIEW RESULTS CHECK OUT THROUGH THE COMBO PACK.` Subtitle `ENTER // EXPLORE` unchanged. CTA labels: Enter reads `ENTER / ONE PREVIEW SPIN`, Buy Combo reads `BUY COMBO PACK / 2 REAL SPINS`. Disclaimer `1 FREE PREVIEW · OR COMBO PACK · 2 REAL SPINS`. Dismiss ID `akMbPrestartStart` stays on Enter button (position-independent). **Purchased branch unchanged** — byte-for-byte identical. **No CSS changes.** **No JS logic changes.** No iframe changes. No crate-repo changes. | Very Low | Verify public prestart (no URL params): `ENTER / ONE PREVIEW SPIN` is left and visually bolder (primary style). `BUY COMBO PACK / 2 REAL SPINS` is right and dimmer (secondary style). Verify Enter dismisses overlay. Verify Buy Combo navigates to combo product. Verify briefing lines lead with Enter. Verify purchased prestart unchanged. Verify mobile 375/320px — no overflow. No console errors. |
| 2026-04-15 | Combo product: accept preview-hat metadata from URL | `sections/main-product.liquid` | Additive inline `<script>` at end of `main-product.liquid`, gated by `{% if product.handle == 'candyfacts-mystery-box-combo' %}`. Reads `preview_hat_id` from URL query params. If present, injects `<input type="hidden" name="properties[_preview_hat_id]" value="...">` into the product add-to-cart form. If absent, script early-returns — zero effect on normal purchases. The `_` prefix hides the property from customer-facing order confirmations (Shopify convention). Form target scoped to `#MainProduct-{{ section.id }} [data-type="add-to-cart-form"]`. No changes to `snippets/buy-buttons.liquid` (shared). No changes to other product pages (Liquid handle guard). No Flow changes. No wrapper/prestart/iframe changes. No crate-repo changes. | Very Low | Open combo product page with no URL params → add to cart → verify order has no `_preview_hat_id` property (normal purchase unchanged). Open combo product page with `?preview_hat_id=test-hat-123` → DevTools Elements: verify hidden input `properties[_preview_hat_id]` with value `test-hat-123` inside the form → add to cart → verify line-item property `_preview_hat_id: test-hat-123` appears in Shopify admin order details (hidden from customer confirmation). Open any other product page with `?preview_hat_id=test` → verify NO hidden input injected (Liquid guard scopes to combo handle only). Verify product page layout, canvas, gallery, add-to-cart button all unchanged. No console errors. |
| 2026-04-15 | Mystery Box: dual-CTA public prestart — Buy Combo + Enter preview | `sections/ak-mb-prestart.liquid` | Public prestart wrapper updated from single buy-first `<a>` to two-button layout. **(1) Buy Combo Pack** (`<a>`, `--primary` style): navigates to `https://amerikid.ca/products/candyfacts-mystery-box-combo`, does NOT dismiss overlay, no `id="akMbPrestartStart"`. **(2) Enter** (`<button>`, `--secondary` style): carries `id="akMbPrestartStart"`, triggers existing dismiss handler, removes overlay, restores `iframe.style.pointerEvents = 'auto'`, exposes crate preview-spin flow. Public copy updated: subtitle `ENTER // PURCHASE` → `ENTER // EXPLORE`. 3 briefing lines rewritten for dual-path (buy combo for 2 real spins, or enter to preview, preview results check out through combo). Disclaimer `COMBO PACK · 2 SPINS · EMAIL ACCESS` → `COMBO PACK · 2 SPINS · OR 1 FREE PREVIEW`. **Purchased branch unchanged** — `ACCESS SPINS / PURCHASED ACCESS` single `<button>` with dismiss ID, all purchased copy, all purchased behavior byte-for-byte identical. **No CSS changes** — existing `--primary` and `--secondary` styles already had full hover/active/focus-visible states. **No JS logic changes** — existing `querySelector('#akMbPrestartStart')` + null guard + `dismiss()` function already supports this pattern. No iframe embed changes. No iframe src changes. No release overlay changes. No crate-repo changes. No customer_id handoff changes. | Very Low | Verify public prestart (no URL params): two buttons visible — `BUY COMBO PACK / 2 REAL SPINS` (left, primary style) and `ENTER / 1 FREE PREVIEW SPIN` (right, secondary style). Verify Buy Combo Pack navigates to combo product page (overlay does NOT dismiss). Verify Enter dismisses overlay (200ms fade), iframe becomes interactive, crate preview-spin flow loads. Verify purchased prestart (`?customer_id=test`): single `ACCESS SPINS / PURCHASED ACCESS` button, dismiss works, no second button. Verify mobile 375/320px — two buttons wrap or fit, no overflow. Verify no console errors. |
| 2026-04-13 | Mystery Box: prestart wrapper visual refinement + state branching | `sections/ak-mb-prestart.liquid` | Multi-pass CSS/JS refinement of the prestart entry overlay inside the Mystery Box iframe frame. **(1) Purchased-state branching:** Added `isPurchased` detection via `new URLSearchParams(window.location.search).has('customer_id')` — URL-only, deliberately NOT using Liquid `{{ customer.id }}` to avoid false positives for unrelated signed-in customers. Public state shows: subtitle `ENTER // PREVIEW`, plate label `HOW IT WORKS`, 2 briefing lines, single CTA `SPIN / FREE PREVIEW`, disclaimer `FREE PREVIEW · 1 SPIN · NO PURCHASE REQUIRED`. Purchased state (email-link arrivals with `customer_id` URL param) shows: subtitle `ENTER // ACCESS`, plate label `YOUR ACCESS`, 2 briefing lines, single CTA `ACCESS SPINS / PURCHASED ACCESS`, disclaimer `PURCHASED ACCESS · 2 SPINS · NON-REFUNDABLE`. **(2) Public state simplification:** Removed BUY COMBO secondary CTA — single centered SPIN button in public state. Data plate hidden in both states. **(3) CTA centering:** Switched `.ak-mb-prestart__ctas` from `display:grid; grid-template-columns:1.35fr 1fr` to `display:flex; justify-content:center`. Added `min-width:220px` to button. **(4) Composition scale-up (CSS-only):** Enlarged the entire prestart layout to fill more of the iframe area. Root padding `28px 32px` → `20px 28px`. Inner max-width `560px` → `680px`, gap `16px` → `22px`. Title `58px` → `72px`. Serial strip font `9px` → `10px`, padding widened. Kicker `10px` → `11px`. Subtitle `10px` → `11px`. Plate padding enlarged. Brief line font `11.5px` → `12.5px`. CTA min-height `60px` → `66px`, label `20px` → `22px`. Disclaimer `8.5px` → `9px`. Tablet breakpoint proportionally adjusted (title `42px` → `48px`, gap `13px` → `16px`). Small mobile unchanged. **No crate behavior changes.** No iframe src changes. No release overlay changes. No DOM structure changes. | Very Low | Verify public prestart: SPIN / FREE PREVIEW centered CTA, 2 briefing lines, no BUY COMBO button. Verify purchased prestart (add `?customer_id=test` to URL): ACCESS SPINS / PURCHASED ACCESS centered CTA, 2 briefing lines. Verify composition fills more of the iframe area (larger title, wider plate, more generous spacing). Verify mobile 375/320px — no overflow, no clipping. Verify dismiss works in both states. Verify iframe loads behind overlay. No console errors. |
| 2026-04-09 | Mystery Box: forward guest `customer_id` from page URL into iframe src | `templates/page.mystery-box.json` | Smallest safe theme-only fix for the guest email-CTA handoff bug. In `custom_liquid_mysterybox_embed`'s inline `<script>` IIFE, the iframe-src builder previously read `customer_id` **only** from the Liquid server variable `{{ customer.id }}`, which is empty for guest visitors. It also read `demo` from `pageParams`, but never read `customer_id` from `pageParams`. The order-confirmation email CTA correctly delivered users to `/pages/mystery-box?customer_id=...&syclid=...`, but the JS silently discarded the URL's `customer_id`, so the final iframe src was rebuilt as bare `https://amerikid-mystery-crate.vercel.app` and the crate fell into preview instead of the purchased path. **Change:** one additive line inserted directly after the existing `var customerId = "{{ customer.id }}";` declaration: `if (!customerId) customerId = pageParams.get("customer_id") || "";`. Liquid-first priority preserved (signed-in users never overridden by URL). Existing empty-check gate preserved (direct guest visits with no URL param still produce a no-param src and preview). `demo=1` passthrough preserved (untouched). `syclid` intentionally not forwarded (scope creep, not used by crate). Hardcoded fallback `<iframe src>` tag untouched. `LOCKED_DEFAULT`, sizing logic, canvas particle system, and init guard all untouched. `sections/ak-mb-release.liquid` and `sections/ak-mb-prestart.liquid` not touched. No visual change. No crate-repo change. One-line additive patch inside a JSON-escaped `custom_liquid` string. | Low | Signed-in: open `/pages/mystery-box` while logged in → iframe src should be `...vercel.app?customer_id=<your id>` → crate enters purchased mode. Guest CTA: open an order-confirmation email CTA link containing `?customer_id=...&syclid=...` in a logged-out browser → iframe src should now include that `customer_id` → crate enters purchased mode (was previously falling into preview). Guest direct visit: open `/pages/mystery-box` with no query params logged out → iframe src should be bare `...vercel.app` → crate previews as before. Demo: open `/pages/mystery-box?demo=1` → iframe src should include `demo=1` (and `customer_id` if signed-in or supplied via URL). Verify no visual drift on the wrapper (bunker monitor shell, lock overlay hidden post-release, canvas particles render). No new console errors. |
| 2026-04-09 | Landing page: bouncing kid K1 motion quality + K2 glow and presence | `templates/index.json` | Two accepted polish passes on the homepage bouncing kid logo (`custom_liquid_xPURQD`). **(K1) Motion quality:** Replaced per-frame `dx=2.2, dy=2.2` increment with delta-time-based movement. `SPEED=132` px/sec (matches old 2.2 px/frame at 60fps). `lastTime` tracking via `requestAnimationFrame(now)`. Delta capped at 100ms to prevent jumps after tab-switch. Slight initial angle variation: randomized ±15° from 45° using `Math.cos`/`Math.sin` unit vector with random sign flips for starting quadrant. Motion now frame-rate-independent — identical speed on 60Hz, 120Hz, and 144Hz displays. **(K2) Glow and presence:** Replaced single `drop-shadow` with layered glow: `drop-shadow(0 0 8px rgba(255,255,255,0.9)) drop-shadow(0 0 18px rgba(200,120,255,0.35))`. Added `akGlow` keyframe (3s infinite, pulses white+purple layers). Added `akBreath` keyframe (4s infinite, `scale(1)` → `scale(1.02)`). Desktop hover brighten via `@media (hover:hover) and (pointer:fine)`: stronger white+purple shadows on `:hover`. All three squash class overrides (`.ak-squish-x`, `.ak-squish-y`, `.ak-squish-xy`) updated to compose `akGlow` + `akBreath` + squash keyframe. **Preserved:** Bounce loop, wall collision, squash/stretch, tilt via `--ak-rot`, hover pause (desktop), click → `__akPortalExit`, kid image asset, z-index 999999, no nav/header/footer, section order unchanged. CSS + JS only within `custom_liquid` string. | Very Low | Verify bouncing kid: moves at consistent speed regardless of monitor refresh rate. Slight angle variation on page load (not always exactly 45°). Glow pulses gently (white + purple layers, 3s cadence). Subtle scale breathing (4s cadence). Desktop hover: glow brightens. Wall-hit squash/stretch still fires with glow maintained. Hover pauses bounce (desktop). Click triggers portal transition. No layout shift. No console errors. |
| 2026-04-09 | Landing page: Portal Phase 1 — background particles + click transition | `sections/ak-landing.liquid`, `templates/index.json` | First creative homepage pass. (1) **Background particles:** Added `ak-collection-bg` section to `templates/index.json` as first section in order array. Same proven full-viewport particle canvas already deployed on 18 collection pages and Life/Style. Fixed-position canvas at default z-index, renders behind `.ak-landing` splash (z-index: 1) and bouncing kid (z-index: 999999). Uses `visibilitychange` lifecycle. Particles are not visible while splash is fully opaque — they become visible during the exit transition fade and would persist if the splash were ever removed. (2) **Click transition:** Added 350ms cinematic exit transition to `ak-landing.liquid`. CSS: `.ak-landing` gets `transition: transform 350ms ease-in, opacity 350ms ease-in`. `.ak-landing--exiting` class applies `transform: scale(1.04); opacity: 0; pointer-events: none`. `@media (prefers-reduced-motion: reduce)` disables transition. JS: Second IIFE adds `doTransition()` function with double-navigation guard (`navigating` boolean). Click on `.ak-landing` calls `e.preventDefault()` + `doTransition()`. Reduced-motion users get instant navigation (no transition). `window.__akPortalExit = doTransition` exposed globally. (3) **Kid integration:** Bouncing kid click handler updated from `window.location.href = "/pages/mystery-box"` to `if (window.__akPortalExit) { window.__akPortalExit(); } else { window.location.href = "/pages/mystery-box"; }`. Kid now shares the same transition as the splash. Direct fallback preserved if `__akPortalExit` is somehow undefined. (4) **Section order:** `["ak-collection-bg", "ak-landing", "custom_liquid_xPURQD"]`. **Preserved:** MP4 video, static mobile image, autoplay JS, video error fallback, bouncing kid bounce/squash/hover-pause/tilt behavior, z-index layering, no nav/header/footer, Mystery Box routing. CSS-only transition + one proven section addition + one kid click handler line. | Very Low | Verify homepage: video plays on desktop, image on mobile. Click splash → 350ms scale+fade transition → navigates to `/pages/mystery-box`. Click bouncing kid → same transition fires. No double-navigation on rapid clicks. Bouncing kid bounces/squashes/pauses on hover exactly as before. Background particles render (visible during transition fade). No nav/header/footer. No layout shift. No console errors. Reduced-motion: transition skipped, instant navigation. All other pages unaffected. |
| 2026-04-09 | Landing page: route homepage entry to Mystery Box | `sections/ak-landing.liquid`, `templates/index.json` | Standalone routing pass. Changed both homepage click destinations from `/collections/master-collection` to `/pages/mystery-box`. (1) `ak-landing.liquid` line 1: `<a>` href updated. (2) `templates/index.json` bouncing kid section (`custom_liquid_xPURQD`): `window.location.href` updated. Two string replacements. Zero visual change. Zero structural change. Zero CSS/JS logic change. Video, image fallback, autoplay, bouncing kid behavior, z-index layering all preserved byte-for-byte. No particles, no transition animation, no creative homepage redesign. PageFly infrastructure untouched. | Negligible | Verify homepage: video plays on desktop, image on mobile, black background. Click splash (anywhere) → navigates to `/pages/mystery-box` (NOT `/collections/master-collection`). Click bouncing kid → navigates to `/pages/mystery-box`. Mystery Box page loads with ak-top, particles, display shell. Bouncing kid bounces/squashes/pauses on hover exactly as before. No console errors. All other pages unaffected. |
| 2026-04-09 | Landing page: extract homepage splash from PageFly | `sections/ak-landing.liquid` (new), `templates/index.json` | Phase L0 extraction. Replaced homepage dependency on `pagefly-home` section with theme-native `sections/ak-landing.liquid`. New section contains the exact visible HTML/CSS/JS from the PageFly section: fullscreen `<a class="ak-landing">` with MP4 video (autoplay, muted, loop) on desktop, static image fallback on mobile (≤767px), video error fallback, click-through to `/collections/master-collection`. CSS via `{%- style -%}` Liquid tag. JS IIFE handles autoplay with `canplay`/`playing`/`error` listeners. Bouncing kid logo section (`custom_liquid_xPURQD`) preserved byte-for-byte. `sections/pagefly-home.liquid` kept untouched as rollback. 4 hidden PageFly elements (button, video, 2 images) no longer in DOM. PageFly `pagefly-app-header` snippet no longer detects homepage (~24KB CSS/JS no longer loaded). PageFly infrastructure intentionally preserved for now. No nav, no particles, no redesign. | Very Low | Verify homepage: fullscreen video plays on desktop. Image on mobile. Click → `/collections/master-collection`. Bouncing logo bounces, squashes, pauses on hover, click → same destination. No PageFly CSS/JS in Network tab. No console errors. All other pages unaffected. |
| 2026-04-09 | Bad Music: finalize main page + 2025 page improvements | `sections/ak-bm-album-link.liquid`, `templates/page.badmusic.json`, `templates/page.badmusic-2025.json` | Consolidated Bad Music finalization across both pages. **(1) Cassette shelf redesign** (`ak-bm-album-link.liquid`): Single album-link module replaced with two-cassette shelf. Left = linked 2025 album with MP4 video (IO visibility-gated), right = coming-soon teaser (unlinked, hoverable). Each cassette built as layered 3D object: visible case edge thickness (top/right/bottom 7px), hinge spine (8px left edge), case interior, J-card art window, inner cassette body with label strip (ruled lines, 4 screws), tape window (48px reels with concentric rings/hub/spokes, oxide tape strip, guide posts), transparent front lid (specular overlay). Resting tilt `rotateX(-1.5deg) rotateY(1deg)`. Hover deepens tilt + magenta glow on live cassette. 5-layer depth shadow. Max-width 1060px / 94vw. Responsive single-column ≤768px. Reduced-motion safe. **(2) Main page heading hierarchy** (`page.badmusic.json`): "2025 (The Album)" caption moved above TV hero via CSS `order:-1` on `.ak-caption`. New `custom_liquid_bm_heading` section added between TV hero and cassette shelf with centered "BAD MUSIC" heading (Impact, magenta accent line). **(3) V1 bug fix** (both templates): `audio.load()` on card `mouseenter` guarded with `audio.readyState < 2 && audio.paused` — fixes playback reset when cursor re-enters a playing card. Duplicate `mouse*` event listeners removed from disc tilt system (pointer events provide full coverage). `bv-field` canvas `filter:contrast(1.1)` removed. **(4) V2 visual shell** (`page.badmusic-2025.json` only): Added `.bv-intro` album identity block above vinyl grid ("BAD MUSIC" kicker, "2025" hero title, "THE ALBUM — 12 TRACKS" subtitle, magenta accent line). Card material treatment (`rgba(12,12,12,.6)`, border, shadow). Grid max-width 1200px. Title/artist typography refined. Seek bar and time row polished. **Preserved:** TV hero (zero changes), BAD Vinyl interaction logic (zero changes), red banner, ak-top, ak-footer, ak-collection-bg, all other lanes. | Low | Verify main Bad Music: "2025 (The Album)" above TV. "BAD MUSIC" heading above cassettes. Two cassettes render (left linked, right coming-soon). TV hero plays exactly as before. Red banner scrolls. Particles visible. Mobile safe. Verify 2025 page: intro block renders. Cards have dark surface. Play a song → leave card → return → song continues (V1 fix). Seek/tilt/spin work. Mobile safe. No console errors. |
| 2026-04-09 | Bad Music: split album landing from 2025 vinyl destination | `sections/ak-bm-album-link.liquid` (new), `templates/page.badmusic.json`, `templates/page.badmusic-2025.json` (new) | Structural split of the Bad Music page into two pages. (1) **Main Bad Music page** now shows: ak-top → red banner → TV hero → album-cover MP4 module → ak-footer. The BAD Vinyl / CD-song interaction area is disabled (`"disabled": true`) — section data preserved for rollback. `ak-collection-bg` added as first section for full-viewport particle background continuity behind the album module area. (2) **New `ak-bm-album-link.liquid` section:** Self-contained linked album-cover module. `<a>` wrapping `<video>` (muted, loop, playsinline, preload=metadata) with IO visibility-gated playback (25% threshold). Pauses when offscreen. Reduced-motion safe (no autoplay, no hover lift). Links to `/pages/badmusic-2025`. Impact font label "2025 (The Album) — Listen Now". Dark transparent background (particles show through). Magenta accent border/glow on hover. Max-width 420px desktop, 320px mobile. No canvas. (3) **New `page.badmusic-2025.json` template:** Dedicated 2025 album destination page. Order: ak-collection-bg → main → ak-top → BAD Vinyl (`custom_liquid_QcCtY6`, active) → ak-footer. Full particle background continuity, universal nav, universal footer. BAD Vinyl section content byte-for-byte identical — only hosted in different template. **Preserved:** TV hero code (zero changes), BAD Vinyl logic (zero changes), red banner (zero changes), ak-top, ak-footer, ak-collection-bg section files. **Admin step required:** Create a Shopify page with handle `badmusic-2025` and assign template `page.badmusic-2025`. | Very Low | Verify main Bad Music: TV hero renders + plays exactly as before. Red banner scrolls. Album MP4 module visible between TV hero and footer. MP4 plays when visible, pauses offscreen. Hover lifts with magenta glow. Click navigates to `/pages/badmusic-2025`. No BAD Vinyl on main page. Particle background visible behind album area. ak-footer renders. Mobile 375/320px safe. No console errors. Verify 2025 page (after admin page creation): particles fill viewport. ak-top renders. BAD Vinyl interaction grid renders with all behavior (tilt, hover, click, audio, spin). ak-footer renders. Mobile safe. No console errors. |
| 2026-04-09 | Narrative Phase N1: visual shell + tweet card + composer polish | `templates/page.narrative.json` | CSS-only visual refinement of the Narrative page, scoped to `custom_liquid_7HLMyz`. (1) **Glow-box softened:** Border `1.5px solid rgba(255,51,255,0.45)` → `1px solid rgba(255,51,255,0.18)`. Padding `3rem 2.5rem 4rem` → `2.4rem 2rem 3rem`. `akBoxPulse` shadow opacities halved at 0%/100% (`.25/.12/.10` → `.10/.06/.05`) and 50% (`.32/.16/.14` → `.14/.08/.07`). Container breathes gently instead of strobing. (2) **Tweet card material:** Left border `4px solid var(--ak-purple)` → `3px solid rgba(192,132,252,0.5)` — thinner, semi-transparent. Background `rgba(0,0,0,0.55)` → `rgba(10,6,18,0.6)` — subtle purple-tinted dark. Added `border-bottom: 1px solid rgba(192,132,252,0.08)` card separation. Hover `::after` box-shadow `14px` → `10px`, opacity `0.6` → `0.3`. Tweet gap `2rem` → `1.6rem`. Avatar border opacity `0.4` → `0.25`, shadow `8px/0.35` → `6px/0.15`. (3) **Composer cohesion:** Border `2px solid var(--ak-purple)` → `1px solid rgba(192,132,252,0.3)`. Background `rgba(0,0,0,0.72)` → `rgba(10,6,18,0.65)` — matches card tint. Button border `2px solid var(--ak-accent)` → `1.5px solid rgba(57,255,20,0.5)`. Button hover shadow `14px+26px` → `10px+18px`. **Preserved:** All posting JS, tweet rendering logic, tweet data, scroll-reveal behavior, typing-loader tweet, Enter-to-post, particle canvas logic, ak-top, ak-footer, HTML structure. | Very Low | Verify glow-box border thin and subtle (not bold). Verify glow-box pulse gentle (not strobing). Verify tweet cards have purple-tinted dark background. Verify tweet left border thinner/semi-transparent. Verify subtle bottom separation line on cards. Verify hover glow softer. Verify avatar rings calmer. Verify composer border thinner/subtler. Verify composer background matches card tint. Verify button hover glow present but tighter. Type a post + Enter — post prepends correctly. Verify typing-loader dots animation. Scroll up/down — entrance animations fire. Verify particles render. Verify ak-top/ak-footer. Verify mobile 375px. No console errors. |
| 2026-04-09 | Life/Style Phase 7A: Adventures subtle 3D hover depth | `templates/page.json` | Added subtle CSS-only 3D hover tilt to Adventures non-hero cards, scoped to `custom_liquid_X46wQm`. (1) **Dock perspective:** `perspective:1200px` added to `#akmod-adventures-yt .ak-adv-dock` — same value as Channels' proven 3D system. (2) **Non-hero 3D treatment (inside `@media (hover:hover) and (pointer:fine)`):** `transform-style:preserve-3d` on `.ak-holo-card:not(:nth-child(1))`. Hover/focus-visible: `transform: rotateX(-2deg) rotateY(3deg) translateY(-3px)`. Existing `translateY(-3px)` hover lift folded into compound transform. (3) **Hero excluded:** Card 1 receives no `preserve-3d` or tilt — stays completely flat via `:not(:nth-child(1))` selector. (4) **Reduced-motion suppression:** `perspective:none` on dock, `transform-style:flat` on cards. (5) **No JS, no HTML, no `translateZ`.** Existing `transition: transform .28s cubic-bezier(.25,.46,.45,.94)` animates the tilt. Touch/mobile devices get zero 3D (capability-gated). **Preserved:** Adventures hero hierarchy, grid placement, card radius (10px), padding (12px), frame (8px warm), glass card system (backdrop-filter, shimmer, specular), playing states (neutral glow, lift, dimming), title animation (`akAdvFlicker`), canvas particles, YouTube embeds, Kid Radio (unchanged — no 3D), Channels (unchanged), ak-top, ak-footer. | Very Low | Hover cards 2–6 on desktop — confirm subtle tilt + lift. Hover hero card (card 1) — confirm NO tilt. Verify glass card shimmer/specular not broken by `preserve-3d`. Verify active state (mousedown) on cards 2–6. Play a video — playing card still lifts with neutral glow. Verify auto-focus dimming. Verify hero card grid unchanged (2col×2row). Verify Kid Radio unchanged (no tilt). Verify Channels 3D tilt unchanged. Verify mobile 375px — no tilt triggers. Verify title animation unchanged. Verify canvas particles render. Verify reduced-motion: no 3D tilt. No console errors. Count canvases: should be 5. |
| 2026-04-09 | Life/Style Phase 5B: Kid Radio wave-bar prominence + material divergence | `templates/page.json` | Strengthened Kid Radio's broadcast-deck identity through wave bar prominence and material divergence from Adventures. CSS-only, scoped to Kid Radio section (`custom_liquid_kkxqR3`). **Wave bar prominence:** (1) Height `28px` → `36px` — taller meter/control strip. (2) Padding `4px 6px 6px` → `6px 8px 8px` — more spacious meter area. (3) Background `rgba(0,0,0,.50)` → `rgba(0,0,0,.58)` — deeper, more defined strip. (4) Border-top `rgba(255,51,255,.10)` → `rgba(255,51,255,.16)` — stronger magenta separator between video and meter. **Material divergence:** (5) Shimmer `::before` opacity `.05` → `.025` — half intensity, calmer surface. (6) Shimmer animation `5.5s` → `8s` — slower cycle, less glassy. (7) Specular `::after` gradient start `rgba(255,255,255,.12)` → `rgba(255,255,255,.07)` — softer highlight. (8) Specular mid `rgba(255,255,255,.04)` → `rgba(255,255,255,.02)` — gentler falloff. (9) `backdrop-filter: saturate(210%)` → `saturate(170%)` — less vivid glass, more matte-console feel. **Adventures retains full shimmer/specular/saturation values** — the two sections now differ on shape (10px vs 20px), layout (hero hierarchy vs flat deck), AND material (glassy cinema vs matte console). **Preserved:** Kid Radio flat 3×2 grid, 20px card radius, 18px padding, wave bar JS behavior, playing states, magenta identity cues, title animation, canvas, embeds, Adventures, Channels, ak-top, ak-footer. | Very Low | Verify Kid Radio wave bar visibly taller (~36px). Verify wave bar reads as dedicated meter/control strip below each video. Verify wave bar magenta separator visible. Verify wave bar deeper background. Verify Kid Radio cards feel subtly less shimmery than Adventures. Verify Kid Radio shimmer slower/calmer. Verify Kid Radio specular highlight softer. Verify wave bar JS animation still works on play. Verify playing states unchanged (neutral glow, lift, dimming). Verify Kid Radio grid unchanged (flat 3×2). Verify Kid Radio card radius unchanged (20px). Verify Adventures visually unchanged. Verify mobile 375/320px. Verify no console errors. Count canvases: should be 5. |
| 2026-04-09 | Life/Style Phase 5A: Adventures featured-card hierarchy | `templates/page.json` | Broke the equal-weight 3×2 Adventures grid into a cinematic featured-card layout. CSS-only grid placement, scoped to Adventures section (`custom_liquid_X46wQm`). **Desktop (>980px):** Grid remains `repeat(3, minmax(0, 1fr))`. Card 1 (hero): `grid-column: 1 / 3; grid-row: 1 / 3` — spans 2 columns × 2 rows on the left. Card 2: `grid-column: 3; grid-row: 1` — top-right. Card 3: `grid-column: 3; grid-row: 2` — mid-right, stacked below card 2. Cards 4, 5, 6: auto-flow into row 3 as a clean 3-across supporting row. **Tablet (≤980px):** Cards 1–3 reset to `grid-column: auto; grid-row: auto`. Card 1 spans full width via `grid-column: 1 / -1`. Grid switches to `repeat(2, minmax(0, 1fr))`. **Mobile (≤540px):** Grid collapses to `1fr` — all cards stack single-column. **Kid Radio grid unchanged** — remains flat 3×2 equal-weight. Grid comment updated from "same as Kid Radio" to "Adventures featured-card hierarchy". **Preserved:** All HTML, JS, YouTube embeds, canvas particles, glass card styling, playing states, title animations, Phase 3C identity cues, Phase 4A shape divergence, Phase 4A.1 neutral play-state, Channels section, ak-top, ak-footer. | Very Low | Verify Adventures card 1 spans 2 columns × 2 rows on desktop (hero on left). Verify card 2 sits top-right. Verify card 3 sits directly below card 2. Verify cards 4, 5, 6 form a clean 3-across row below. Verify no orphaned single card. Verify all iframes maintain 16:9 aspect ratio. Verify glass card system works on all cards. Verify playing states work (neutral glow, lift, dimming). Verify Kid Radio grid unchanged (flat 3×2). Verify tablet (≤980px): clean 2-col fallback, card 1 full-width. Verify mobile (≤540px): clean single-column stack. Verify title animation unchanged. Verify canvas particles render. Verify no console errors. Count canvases: should be 5. |
| 2026-04-09 | Life/Style Phase 4A.1: neutralize play-state color wash | `templates/page.json` | Removed strong colored aura and per-card colored glow that appeared when a video played, replacing them with neutral/restrained emphasis. CSS-only, scoped by section ID. **Adventures:** (1) Section-wide aura `::before` disabled on play: `is-playing::before` opacity `.75` → `0`. (2) Playing-card `border-color`: `rgba(255,180,60,.35)` → `rgba(255,255,255,.18)`. (3) Playing-card colored box-shadow layers (3): `rgba(255,180,60, .15/.18/.12)` → `rgba(255,255,255, .06/.08/.04)`. (4) Playing-card `::after` specular tint: `rgba(255,180,60,.08)` → `rgba(255,255,255,.08)`. (5) Playing-frame `border-color`: `rgba(255,180,60,.25)` → `rgba(255,255,255,.12)`. (6) Playing-frame glow: `rgba(255,180,60,.1)` → `rgba(255,255,255,.04)`. **Kid Radio:** Same 6 changes with magenta `rgba(255,51,255,...)` → neutral white equivalents. **Preserved:** Playing-card lift (`translateY(-4px)`), auto-focus dimming (`.55` opacity on non-playing cards), inset highlights, dark drop shadows, at-rest warm frame (Adventures), at-rest magenta frame tint (Kid Radio), wave bar magenta accent, Phase 4A shape divergence, title animations, grid, canvas, embeds, Channels. | Very Low | Play a video in Adventures — section should NOT turn amber. Play a video in Kid Radio — section should NOT turn magenta. Active card should lift with neutral white glow. Non-playing cards should dim. Adventures at-rest warm frame border still visible. Kid Radio at-rest magenta frame tint still visible. Kid Radio wave bar magenta accent still visible. Verify mobile 375/320px. Verify no console errors. Count canvases: should be 5. |
| 2026-04-09 | Life/Style Phase 4A: Adventures + Kid Radio shape divergence | `templates/page.json` | Shape differentiation pass to visually separate Adventures (cinematic viewport) from Kid Radio (broadcast deck). CSS-only, scoped by section ID. (1) **Adventures card radius: 20px → 10px.** `.ak-holo-card` border-radius + `::before` + `::after` pseudo-elements all changed to 10px. Squared, cinematic feel. (2) **Adventures video-card padding: 14px → 12px.** `.ak-adv-video-card` padding tightened for compact viewport framing. (3) **Adventures frame radius: 14px → 8px.** `.ak-adv-frame` border-radius squared to match card. (4) **Adventures frame border: 1px neutral → 2px warm.** `.ak-adv-frame` border changed from `1px solid rgba(255,255,255,.10)` to `2px solid rgba(255,200,140,.08)`. Heavier warm-tinted frame. (5) **Kid Radio video-card padding: 14px → 18px.** `.ak-kr-video-card` padding increased for roomier broadcast-deck feel. (6) **Kid Radio wave bar height: 24px → 28px.** `.ak-kr-wave-bar` height increased — taller visualizer bar reinforces audio identity. (7) **Kid Radio wave bar border-top: neutral → magenta.** `.ak-kr-wave-bar` border-top changed from `1px solid rgba(255,255,255,.06)` to `1px solid rgba(255,51,255,.10)`. Magenta accent line. (8) **Kid Radio card radius unchanged at 20px.** Preserved rounded shape for contrast with Adventures. **Preserved:** All HTML, JS, YouTube embeds, canvas particles, grid layout, title animations, Phase 3C color values, Channels section. | Very Low | Verify Adventures cards read squared (10px radius). Verify Adventures cards feel tighter/compact (12px padding). Verify Adventures iframe frame has squared corners (8px) and visible warm border. Verify Kid Radio cards remain rounded (20px radius). Verify Kid Radio cards feel roomier (18px padding). Verify Kid Radio wave bar is taller (28px). Verify Kid Radio wave bar has subtle magenta top-line. Verify glass card system still works (shimmer, specular, hover lift). Verify playing states unchanged (amber/magenta). Verify mobile 375/320px. Verify no console errors. Count canvases: should be 5. |
| 2026-04-09 | Life/Style Phase 3C: Adventures + Kid Radio identity differentiation | `templates/page.json` | First visual identity pass to differentiate the two video sections. (1) **Adventures playing accent: magenta → amber.** All playing-state CSS changed from magenta (`255,51,255`) to warm amber/gold (`255,180,60`): `.is-playing-card` border-color, box-shadow (3 layers), `::after` specular tint, `.ak-adv-frame` playing border + glow, `:hover/:focus-visible` 1px ring. (2) **Adventures aura gradient: warm shift.** `::before` radial gradients changed: magenta → amber (`255,180,60,.22`), green → soft orange (`255,140,50,.12`), neutral white → warm white (`255,220,180,.07`). (3) **Kid Radio aura gradient: cool shift.** `::before` radial gradients changed: green → cyan-blue (`80,180,255,.14`), neutral white → cool white (`200,220,255,.07`). Magenta stays. (4) **Kid Radio frame border tint.** Rest-state `.ak-kr-frame` border: neutral `rgba(255,255,255,.10)` → subtle magenta `rgba(255,200,255,.08)`. (5) **Preserved:** All HTML structure, JS behavior, YouTube embeds, canvas parameters, wave bar, grid layout, title/header cleanup, auto-focus dimming, glass card system, Channels section. CSS-only color changes scoped by section ID. Zero HTML, zero JS. **Design note:** Ambient title animation (`akAdvFlicker`, `akKrSignal`) remains part of the intended final redesign direction — not temporary. | Very Low | Verify Adventures playing-card glow reads amber/warm (not magenta). Verify Adventures hover ring reads warm. Verify Adventures aura gradient reads warm when video plays. Verify Kid Radio playing-card glow still reads magenta. Verify Kid Radio aura reads cool (blue-magenta, not green). Verify Kid Radio iframe borders have very subtle magenta tint at rest. Verify wave bar unchanged. Verify auto-focus dimming works in both sections. Verify title animations unchanged. Verify accent underlines unchanged (amber/magenta). Verify Channels section visually unchanged. Verify mobile 375/320px. Verify no console errors. Count canvases: should be 5. |
| 2026-04-08 | Life/Style Phase 2: section headers, title simplification, helper text removal | `templates/page.json` | Full Adventures + Kid Radio header treatment. (1) **Banner card → section header:** Removed `ak-holo-card` class from both banner divs. Glass styling stripped: `background:transparent`, `backdrop-filter:none`, `box-shadow:none`, `border:none`, `border-radius:0`. Scanline/specular pseudo-elements hidden via `display:none`. `border-bottom: 1px solid rgba(255,255,255,.08)` section divider added. Banner padding: `28px 6px 10px`, margin: `0`. (2) **Title simplification:** Per-letter `<span>` markup replaced with plain text. Adventures: 26 spans → `THE ADVENTURES OF AMERIKID`. Kid Radio: 9 spans → `KID RADIO`. CSS changed from `inline-flex` + `gap` + `letter-spacing` clamp to simple `text-align:center` + `letter-spacing:0.08em`. This resolved persistent centering issues caused by trailing phantom space in the flex/span model. (3) **Title animation simplified:** Per-letter stagger animations (`akAdvFlicker`/`akKrSignal` with `calc(var(--i) * delay)`) replaced with whole-title gentle opacity pulse (Adventures: 4s, Kid Radio: 5s). Keyframes simplified to opacity-only. `prefers-reduced-motion` suppresses animation. (4) **Helper text removed:** "SELECT A VIDEO TO PLAY." removed from Adventures. "SELECT A PLAYLIST TO PLAY." removed from Kid Radio. Associated CSS (`.ak-adv-banner-meta`, `.ak-kr-banner-meta`, `▶`/`♪` pseudo-elements) removed. (5) **Accent underlines preserved:** Adventures warm amber `::after` (60px gradient). Kid Radio magenta `::after` (60px gradient). (6) **Preserved:** All video card glass styling, aura/playing states, auto-focus dim, wave bar, canvas particles, grid layout, YouTube behavior, Phase 1 shell softening. CSS + small HTML change, zero JS changes. | Very Low | Verify Adventures title "THE ADVENTURES OF AMERIKID" centered, clean spacing, no per-letter gaps. Verify Kid Radio title "KID RADIO" centered, clean spacing. Verify no helper text below titles. Verify accent underlines present (amber/magenta). Verify bottom border divider visible. Verify video cards still have full glass treatment. Verify gentle title animations running. Verify mobile 375/320px. Verify no console errors. |
| 2026-04-08 | Life/Style Phase 1: visual continuity + shell softening | `templates/page.json` | Transformed the Life/Style page from "three stacked island widgets" to "one cohesive page with continuous particle background." (1) **Background canvas:** Added `ak-collection-bg` section (proven, already deployed on 18 collection pages) as first section in order array. Provides full-viewport fixed-position particle canvas behind all content. CSS overrides force transparent backgrounds on Dawn section wrappers so particles show through gaps between modules. (2) **Shell softening (all 3 modules):** Adventures, Kid Radio, and Channels `.ak-social-holo` root containers changed: `background:#000` → `transparent`, `border-radius:22px` → `0`, `border:1px solid` → `none`, `box-shadow:0 18px 60px` → `none`, `margin:18px 0` → `0`. Modules now blend into the continuous particle field instead of floating as separate cards. (3) **Preserved:** All glass card styling on video/social cards (backdrop-filter, border, shadow, shimmer), all canvas particle parameters, YouTube behavior, wave bar, 3D tilt, title animations, grid layouts, playing states. Per-module canvases still run — they add local density inside content areas, layering over the background canvas via `mix-blend-mode:screen`. CSS-only changes plus one proven section addition. Zero JS changes. | Low | Verify particles fill entire viewport (not just inside modules). Verify no dead black gaps between Adventures, Kid Radio, and Channels. Verify module content still readable over particle background. Verify glass card styling on video cards still present (borders, shimmer, hover lift). Verify Channels 3D tilt still works. Verify wave bar visualizer still works. Verify YouTube playback and is-playing states. Verify title ambient animations running. Verify mobile 375/320px — no overflow, text readable. Verify ak-top particles still render (two canvases coexist). Verify ak-footer renders cleanly at bottom. Verify reduced-motion: background canvas hidden. Verify no new console errors. |
| 2026-04-08 | Life/Style: ambient title animations for Adventures + Kid Radio | `templates/page.json` | Replaced one-time load-in letter-scatter animations with always-on ambient treatments. **Adventures ("Film Reel Flicker"):** Removed `akAdvDrop` reveal (letters scattered from random positions with blur, staggered 0.06s, `forwards` fill). Replaced with `akAdvFlicker` — subtle continuous opacity pulse (1.0→0.82→0.92→1.0, 3.2s cycle, staggered 0.28s per letter via `--i`). Evokes film-projector light. **Kid Radio ("Signal Pulse"):** Removed `akKrDrop` reveal (same scatter pattern, staggered 0.08s). Replaced with `akKrSignal` — subtle continuous `scaleX` + opacity pulse (scaleX 1→1.04, opacity 1→0.85, 4.0s cycle, staggered 0.35s per letter). Evokes broadcast signal. Both: letters now `opacity:1` immediately (no load-in), no `translateZ`, no `filter:blur`, no scatter-position `nth-child` rules, no `will-change:transform`. `akAdvBreath`/`akKrBreath` no-op keyframes removed. `prefers-reduced-motion: reduce` suppresses title animation. Kid Radio `filter: none !important` guard removed (no longer needed without blur). Mobile word-wrap (540px) and font-size (600px) breakpoints preserved. CSS-only, zero JS, zero DOM, zero canvas changes. Channels/Social Dock untouched. | Very Low | Verify Adventures title "THE ADVENTURES OF AMERIKID" visible immediately (no scatter reveal). Verify subtle flicker animation running (letters gently pulse opacity). Verify Kid Radio title "KID RADIO" visible immediately. Verify subtle signal pulse running (letters gently breathe scaleX). Verify mobile 375/320px readability. Verify reduced-motion: no title animation. Verify no layout shift. Verify no new console errors. |
| 2026-04-08 | Mystery Box material polish (Stage 2.5) | `templates/page.mystery-box.json` | Material polish pass on the bunker monitor shell. Bezel gradient (flat `#0c0c0c` → subtle top-to-bottom `linear-gradient(180deg, #111, #0a0a0a)`). Asymmetric chin padding (8px uniform → 8/8/14/8 desktop, 6/6/10/6 mobile) for realistic monitor proportions. Bevel highlight peak opacity 0.10→0.13 for better readability. LED repositioned (bottom 2px→5px) to sit naturally in deeper chin. Screen-edge seam via `.ak-mb-stage::before` inset shadow for recessed well definition. Lock card kicker/title font-family set to Impact for brand consistency. All CSS-only, zero JS, zero DOM. Includes re-application of Stage 1 particle parity and Stage 2 shell (previously uncommitted). Also restores `ak-mb-release` and `ak-footer` section references. | Very Low | Verify bezel has subtle top-to-bottom gradient (not flat). Verify bottom bezel (chin) is slightly deeper than top/sides. Verify bevel highlight visible but not shiny. Verify LED sits centered in chin area. Verify screen content appears slightly recessed. Verify lock card text uses Impact font. Verify mobile 375/320px. Verify no layout shift. Verify no new console errors. |
| 2026-04-08 | Mystery Box release copy correction | `sections/ak-mb-release.liquid` | Fixed user-visible lock card subtitle from "1:00 AM PT" to "Midnight PT" to match the actual launch target (2026-04-09T07:00:00Z = Thu Apr 9, 12:00 AM America/Los_Angeles). Copy-only change — no logic, styling, or behavior changed. | Very Low | Verify lock card subtitle reads "Drops Thursday, April 9 at Midnight PT". |
| 2026-04-08 | Mystery Box bunker monitor shell (Stage 2) | `templates/page.mystery-box.json` | Replaced the generic frosted-glass iframe frame with a ruggedized bunker-monitor display shell. CSS-only changes inside the existing `custom_liquid` block — zero DOM changes, zero JS changes. Frame: `border-radius` 22px→6px, `backdrop-filter` removed (opaque housing), `background: #0c0c0c`, 8px `padding` as visible bezel, `box-sizing: border-box` added so JS-set height stays authoritative. Layered `box-shadow` for object depth (outer drop + inset recess). `::before` top-edge bevel highlight (1px gradient). `::after` power LED (5px green dot, bottom-center). Iframe gets `border-radius: 2px` + subtle inset shadow for recessed screen well. Lock card `border-radius` 20px→6px to match shell. Lock overlay `inset: 0` now covers content area (screen) inside the bezel padding — correct behavior. Mobile: 6px padding, 5px radius. All iframe behavior, lock overlay logic, release timer, URL construction, customer_id/demo passthrough, section order, particle system, and ak-top/ak-footer untouched. | Low | Verify frame reads as dark matte display housing with visible bezel. Verify subtle bevel highlight on top edge. Verify green LED dot at bottom-center. Verify iframe content fills screen area inside bezel. Verify lock overlay covers screen area (not bezel). Verify countdown timer visible inside lock card. Verify lock card corners match shell. Verify mobile 375/320px (slightly thinner bezel). Verify no layout shift or iframe reflow. Verify no new console errors. |
| 2026-04-08 | Mystery Box particle parity with ak-top (Stage 1) | `templates/page.mystery-box.json` | Brought the Mystery Box wrapper's particle canvas into visual/performance parity with ak-top. DPR capped to 1 (was `Math.min(dpr, 2)` — 4× fill-rate reduction on Retina). Dust cap reduced from desktop 1,500–3,400 to 400–800, mobile 180–520 to 120–300. Floater cap reduced from desktop 320–620 to 200–400, mobile 55–150 to 40–100. Dust `shadowBlur` removed (was 5 on accent particles). CSS filter reduced from `contrast(1.18) saturate(1.08)` to `contrast(1.1)`. Floater glowSize reduced from 7–18 to 7–16. All values now match ak-top exactly. Iframe, lock overlay, release timer, URL construction, customer_id/demo passthrough, section order, and all DOM completely untouched. 6th application of the proven particle reduction pattern. | Very Low | Verify Mystery Box page particle field renders and animates. Confirm particles are lighter/sparser — should now match ak-top header density visually. Confirm accent floater glow (purple/blue/yellow) still visible. Confirm shooting stars still fire with glow and sparks. Confirm iframe loads and is interactive. Confirm lock overlay still works (if before release time). Confirm countdown timer ticks. Confirm mobile 375/320px. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-08 | Mystery Box timer finalized to midnight launch + Shopify CLI docs | `sections/ak-mb-release.liquid`, `docs/source-of-truth.md`, `docs/known-fragile-areas.md`, `docs/runtime-test-checklist.md` | Reverted temporary test target (`Date.UTC(2026,3,8,6,55,0)` / Apr 7 11:55 PM PT) to final launch target: `Date.UTC(2026,3,9,7,0,0)` = **Thu Apr 9, 2026 12:00 AM** (midnight) America/Los_Angeles (`2026-04-09T07:00:00Z`). Updated all doc references from 1:00 AM / `08:00:00Z` to midnight / `07:00:00Z`. Added Shopify CLI push info section to source-of-truth.md with store domain, theme IDs, and exact push commands. No overlay logic, structure, or behavior changed. | Very Low | Verify `TARGET_UTC` in ak-mb-release.liquid reads `Date.UTC(2026, 3, 9, 7, 0, 0)`. Verify countdown timer reflects correct remaining time to midnight Apr 9 PT. Verify all doc references consistent. |
| 2026-04-07 | Mystery Box release overlay — scope fix + time correction | `sections/ak-mb-release.liquid` | Corrected release overlay from full-viewport (`position:fixed; z-index:9999`) to iframe-scoped. Section now renders NO visible HTML — it is a JS/CSS orchestrator that controls the existing `#akMbLock` overlay inside the embed's `.ak-mb-frame`. JS shows the existing lock overlay, injects countdown timer elements into the existing lock card, ticks every second, and auto-unlocks (hides lock, restores iframe pointer-events) when time passes. Nav/header/footer remain fully accessible. Release time corrected from 9:00 AM PT to **1:00 AM PT** (`Date.UTC(2026,3,9,8,0,0)` = `2026-04-09T08:00:00Z`). Embed section (`custom_liquid_mysterybox_embed`) completely untouched. **Client-side only.** | Low | Verify overlay covers only iframe frame area (not full viewport). Verify nav/header accessible. Verify footer accessible. Verify countdown ticks. Verify iframe interaction blocked. Verify auto-unlock (test by setting TARGET_UTC to past). Verify post-release: no overlay. Verify mobile. |
| 2026-04-07 | Fix Back to Top on Narrative page | `sections/ak-footer.liquid` | Narrative page's Twitter-feed section (`custom_liquid_7HLMyz`) injects unscoped `html, body { height:100%; overflow-x:hidden }` + `* { margin:0; padding:0 }`, which shifts the scroll container and strips section spacing. Fix: scroll-to-top JS now targets `window.scrollTo` + `document.body.scrollTop` + `document.documentElement.scrollTop` for cross-context coverage. Defensive `margin-top:0` added to `.ak-footer`. No other pages or sections changed. | Low | Verify Back to Top works on Narrative page. Verify still works on all 6 other footer pages. |
| 2026-04-07 | Back to Top button: circle → square | `sections/ak-footer.liquid` | CSS-only shape polish. `border-radius` changed from `50%` to `12px` on `.ak-btt-ring` (the button), `16px` on `.ak-btt-ring::before` (glow halo), and `12px` on `.ak-btt:focus-visible` (focus outline). Button now reads as a rounded square. All behavior, animation, rollout, and content unchanged. | Very Low | Verify button is visibly square (not circular). Verify hover glow, click animation, and scroll-to-top still work. Verify mobile sizing still correct. |
| 2026-04-07 | Footer redesign: Back to Top launchpad | `sections/ak-footer.liquid` | Full rewrite of the universal footer from a multi-link glass-tab nav block into a single branded Back to Top feature. (1) **Launchpad button:** Circular ring with upward chevron SVG, centered in a minimal black footer strip. (2) **Hover state:** Ring lifts `translateY(-3px)`, border turns magenta (`rgba(255,51,255,.35)`), radial glow halo appears, arrow brightens and lifts. (3) **Click animation:** Ring pulses outward (magenta ring expanding and fading via `akBttPulse`), arrow launches upward and returns (`akBttLaunch`), then `window.scrollTo({ top:0, behavior:'smooth' })`. (4) **"BACK TO TOP" label** in Impact micro-caps beneath the button. (5) **Copyright** `© YEAR AMERIKID` as the only other text. (6) **Gradient divider** above (same magenta-peak pattern). (7) **Reduced-motion safe:** All transitions/animations disabled, scroll uses `behavior:'auto'`. (8) Same 7-page template footprint preserved — no template JSON changes. Light inline JS (~12 lines) for click handler only. No canvas. | Very Low | Verify back-to-top button visible on all 7 pages (Life/Style, Products, Archive, Narrative, Bad Music, Mystery Box, Clubhouse). Verify hover shows magenta glow halo + ring lift + arrow brighten. Verify click triggers pulse animation + arrow launch + smooth scroll to top. Verify scroll actually reaches top. Verify reduced-motion: no animation, instant scroll. Verify button NOT on cart, product pages, or 18 collection pages. Verify Mystery Box iframe unaffected. Verify mobile 375/320px (smaller ring, readable label). Verify no console errors. |
| 2026-04-07 | Archive deck cassette-case skin | `sections/ak-archive.liquid` | CSS-only visual reskin of Archive deck cards from flat folder-cards to cassette-case/cassette-box objects. (1) **Case housing:** 8px padding on `.ak-archive-card-inner` exposes the `#111` → `#0e0e0e` gradient background as a visible plastic shell frame around the cover art. (2) **Cover art inset:** Image gets `border-radius:3px` and reduced height (`calc(100% - 8px)`) so it reads as an inserted art card inside the case. (3) **Plastic sheen:** `::before` pseudo-element adds a subtle diagonal highlight gradient across the case surface (`mix-blend-mode:screen`). (4) **Hinge line:** `::after` pseudo-element adds a thin 3px vertical accent on the left edge — the "case opens here" cue. (5) **Spine tabs:** Trailing card tabs widened and given vertical gradients + inset `box-shadow` ridge to read as stacked case spines instead of folder tabs. (6) **Snap ridge:** Subtle `box-shadow` on the image's top edge reads as the lid-closure line. All changes in a single commented CSS block (`CASSETTE CASE SKIN`). No HTML changes. No JS changes. Spring physics, drag model, stack constants, navigation, particle canvas, card order/links all untouched. Revert by removing the CSS block. | Low | Verify front card reads as a cassette case with visible dark shell around the cover image. Verify cover art has slight rounded corners and feels inset. Verify subtle diagonal sheen visible on front card. Verify thin hinge line on left edge. Verify trailing cards show spine-like tabs (wider, gradient). Verify stack composition preserved (5 trailing strips visible). Verify tap navigates, drag works, arrows work, keyboard works. Verify particles behind deck. Verify title labels readable. Verify mobile 375/320px (reduced padding, smaller tabs). Verify no console errors. |
| 2026-04-07 | Universal branded footer for top-level pages | `sections/ak-footer.liquid` (NEW), `templates/page.json`, `templates/collection.json`, `templates/page.narrative.json`, `templates/page.badmusic.json`, `templates/page.mystery-box.json`, `templates/page.club-hub.json`, `templates/list-collections.json` | New shared section `ak-footer` added to 7 top-level page templates. (1) **Branded endcap:** AMERIKID wordmark in Impact, "LOS ANGELES" micro-line, copyright with dynamic year. (2) **Glass-tab nav links:** 7 links matching ak-top's nav (Life/Style, Products, Archive, Narrative, Bad Music, Mystery Box, Clubhouse) with glass-morphism styling — semi-transparent borders, magenta hover glow, `is-current` state via Liquid `request.path`. (3) **Gradient divider** mirrors ak-top's divider (magenta peak, transparent edges). (4) **Template-scoped:** NOT global — added only to specific template JSONs as last section in order array. `footer.liquid`, `footer-group.json`, and `theme.liquid` untouched. (5) **No JS, no canvas.** Pure CSS + Liquid. (6) Mystery Box template handled with minimal insertion — footer appended after iframe embed section, no other changes. | Low | Verify footer visible on all 7 pages. Verify glass-tab links render and navigate correctly. Verify `is-current` highlight on active page. Verify magenta hover glow on tabs. Verify footer not visible on cart, product pages, or 18 collection pages. Verify Mystery Box iframe still loads and functions above footer. Verify Archive page footer visible below deck on scroll. Verify mobile 375/320px layout (tabs wrap cleanly). Verify no console errors. |
| 2026-04-07 | Collection page particle background: seamless starfield continuity | `sections/ak-collection-bg.liquid` (NEW), `templates/collection.kidzyn.json`, `collection.bucket.json`, `collection.aperikid.json`, `collection.loveamerica.json`, `collection.travellercap.json`, `collection.katskan.json`, `collection.stones.json`, `collection.siwyc.json`, `collection.solo.json`, `collection.weed.json`, `collection.watermelon.json`, `collection.skate.json`, `collection.uh-oh-ghoul-aid-tee.json`, `collection.bad-music.json`, `collection.kandy.json`, `collection.uhoh.json`, `collection.election.json`, `collection.gpr.json` | New shared section `ak-collection-bg` added to all 18 Archive-linked collection templates. (1) **Fixed-position viewport canvas** with ak-top's exact particle system (dust 400–800, floaters 200–400, shooters, DPR 1, `mix-blend-mode: screen`). Uses `position: fixed; inset: 0` so particles fill the entire viewport behind all page content. (2) **CSS overrides** force `html, body { background: #000 !important }`, `#MainContent { background: transparent !important }`, and `.gradient { background: transparent !important; background-image: none !important }` — making Dawn section backgrounds transparent so particles show through. (3) **Visibility lifecycle** via `document.visibilitychange` (not IO, since fixed canvas is always in viewport). (4) Section inserted first in each template's order array (before `ak-top`) so it renders first in DOM and stacks behind all content. Collection-specific content, ak-top, product-grid settings all untouched. | Low | Verify particles fill entire viewport on collection pages (not just header area). Verify no white gaps between sections. Verify text/images remain readable over particle background. Verify ak-top particles still render (two canvases coexist). Verify tab-away/tab-back pauses and resumes animation. Verify mobile 375/320px. Verify no console errors. Verify cart still works. |
| 2026-04-07 | Collection page normalization: ak-top migration + emoji removal | `templates/collection.kidzyn.json`, `collection.bucket.json`, `collection.aperikid.json`, `collection.loveamerica.json`, `collection.travellercap.json`, `collection.katskan.json`, `collection.stones.json`, `collection.siwyc.json`, `collection.solo.json`, `collection.weed.json`, `collection.watermelon.json`, `collection.skate.json`, `collection.uh-oh-ghoul-aid-tee.json`, `collection.bad-music.json`, `collection.kandy.json`, `collection.uhoh.json`, `collection.election.json`, `collection.gpr.json` | JSON-template-only normalization of 18 Archive-linked collection pages. (1) **Emoji banner removal (18 templates):** All emoji scrolling banner `custom_liquid` sections (`Jdbfj4`, `Gxbhbp`, `GRwzCK`, `V8cwFf`) set to `"disabled": true` across all 18 templates. (2) **ak-top migration (12 templates):** 12 templates that used an old CSS-only header/nav system (no particles, no canvas, no glass nav) now have `ak-top` section added and old header/nav `custom_liquid` sections disabled. Old header sections preserved with `"disabled": true` for rollback. `ak-top` inserted in order array after `banner`. Collection-specific content sections (image_with_text, collage, rich_text) and product-grid enable/disable settings completely untouched. No Liquid section code changes. | Low | Spot-check 1 page per bucket: (A) bad-music — already had ak-top, verify no emoji banner, content intact. (B1) kidzyn — verify ak-top renders with particles and glass nav, no old CSS header, no emoji. (B2) bucket — same as B1. (B3) travellercap — same as B1. Verify collection-specific images/text preserved on all checked pages. Verify cart still works. Verify mobile 375/320px. Verify no console errors. |
| 2026-04-07 | Archive polish: all-caps titles, upward bias, bottom line fix | `sections/ak-archive.liquid` | CSS-only polish. (1) `text-transform:uppercase` on `.ak-archive-title` — all card titles now render ALL CAPS. (2) `padding-bottom:clamp(30px,4vh,60px)` on `.ak-archive` — biases the deck slightly upward, reducing the gap below the nav/header. (3) `.ak-archive-section{background:#000}` — sets the section wrapper background to black, eliminating a 1px white body-background bleed at the bottom. Particle canvas, stack model, interaction model, card data all untouched. | Very Low | Verify all card titles render ALL CAPS. Verify deck sits slightly higher (less gap below nav). Verify no white line at bottom of Archive page. Verify no regressions. |
| 2026-04-07 | Archive deck v6: structural stack + interaction rewrite | `sections/ak-archive.liquid` | Root-cause fix for two persistent failures. (1) **Stack model replaced.** Previous v3–v5 used `position:absolute;inset:0` (every card filled the full stage) + `translateY` to peek — but the card was as tall as the stage, so trailing cards were hidden behind the front card. v6 fixes this structurally: cards are `bottom:0; height:76%` (shorter than stage), leaving 24% headroom above. Each trailing card shifts upward by `STRIP=26px * depth` via `translateY`. The visible strip is guaranteed because the card does not fill the stage. No perspective/translateZ/rotateX — the reference is a flat vertical fan. `SCALE_STEP=0.018` creates subtle narrowing. Opacity floor 0.55. `layout(false,true)` — initial layout is instant (no spring) so rest state is visible on first paint. (2) **Interaction model replaced.** Cards changed from `<a>` tags to `<div>` with `data-href`. Eliminates native anchor drag, native link behavior, and the fragile `didDrag`/`setTimeout` suppression chain. Navigation via `window.location.href` in click path. `pointerdown` calls `e.preventDefault()` + `setPointerCapture` on stage immediately — no pending-threshold model needed since there's no native anchor to protect. `didMove` flag (5px threshold) separates taps from drags: tap navigates, drag animates. Spring cancellation per card via `springStops{}` map — new springs cancel old ones, preventing orphaned rAF loops. `anim` safety timer (700ms) prevents permanent lock if a spring callback fails. Arrows use `e.preventDefault(); e.stopPropagation()` and are completely decoupled from drag state. Particle canvas byte-identical to v5. | Medium | Verify at rest: 4–5 trailing card strips clearly visible above front card. Verify stack reads as a deck from first frame. Verify front card dominant. Verify tapping front card navigates to collection URL. Verify drag: hold and pull — card follows pointer. Verify drag releases immediately on pointer up. Verify fast flick dismisses. Verify slow drag snaps back. Verify arrows always work on single click (never double-click needed). Verify keyboard arrows work. Verify deck wraps (18→1, 1→18). Verify particles render behind deck. Verify mobile 375/320px. Verify no console errors. |
| 2026-04-07 | (superseded by v6) Archive deck v5: large upward fan matching reference, reduced drag threshold | `sections/ak-archive.liquid` | Visual-comparison pass against 21st.dev reference screenshots. Two changes: (1) **Stack fan size doubled.** `TY_STEP` increased from -14 to -28 — each trailing card now exposes ~28px visible strip above the card in front of it, matching the reference's clearly fanned stack silhouette. `TX_STEP` removed (was 4, now 0) — reference stacks purely vertically with no lateral drift. `perspective-origin` moved from `center 60%` to `center 75%` so the vanishing point sits lower and the upward fan looks natural. All other stack params unchanged (TZ_STEP=70, SCALE_STEP=0.012, ROT_X_STEP=0.8). (2) **Drag threshold halved.** `DRAG_INTENT_PX` reduced from 8 to 4 — the dead zone before drag activates is halved, making the interaction feel more immediate and responsive. No other drag logic changed. Particle canvas, tabs, borders, arrows, cards, keyboard nav all untouched. | Low | Verify at rest: 4–5 trailing card strips clearly visible above front card as a fanned stack. Verify stack reads as a deck from first frame. Verify no lateral drift (cards centered). Verify front card dominant. Verify tap navigates. Verify drag feels immediate (no dead zone). Verify drag releases on pointer up. Verify fast flick dismisses. Verify slow drag snaps back. Verify arrows/keyboard work. Verify particles unaffected. Verify mobile 375/320px. Verify no console errors. |
| 2026-04-07 | (superseded by v5) Archive deck v4: visible upward stack, pending-drag threshold | `sections/ak-archive.liquid` | Targeted fix of two v3 failures. (1) **Stack visibility.** Trailing cards now stagger upward (TY_STEP=-14) and slightly rightward (TX_STEP=4) so 4–5 folder edges visibly peek above/beside the front card. Scale step reduced from 0.05 to 0.012 (perspective does the shrinking). RotateX reduced from 1.8 to 0.8 deg/depth. TranslateZ reduced from 80 to 70/depth. Transform-origin moved from `center 85%` to `center 40%`. Opacity floor raised from 0.30 to 0.50. Folder tabs taller (14→20px, mobile 11→16px), lighter (#2a→#18 gradient vs #1a→#0d). Trailing card-inner gets subtle 1px border `rgba(255,255,255,0.07)`. (2) **Drag lifecycle.** Pending-drag threshold model: `pointerdown` stores start position but does NOT call `setPointerCapture`. Only when `pointermove` exceeds 8px total displacement is drag committed and capture acquired. If `pointerup` fires before threshold, all state resets and native `<a>` click navigates normally. `resetDragState()` clears all flags. `lostpointercapture` handler added as safety net. `didDrag` survives via `setTimeout(0)` so click handler can consume it, then auto-clears. No more latent stuck state. Particle canvas, counter removal, arrows, cards, keyboard nav all untouched. | Low |
| 2026-04-07 | (superseded by v4) Archive deck v3: vanishing-point folder stack, pointer-captured drag, particle background | `sections/ak-archive.liquid` | Full rewrite of v2. Five changes: (1) **Counter/rail removed.** "01 / 18" counter, progress rail, tap zones all deleted. Only left/right arrow buttons remain. (2) **Vanishing-point stack.** CSS `perspective:1200px` on stage + `transform-style:preserve-3d` on cards. 6 visible cards (VIS=6). Each depth level applies `translateZ(-80px*depth)`, `translateY(18px*depth)`, `rotateX(1.8deg*depth)`, `scale(1-0.05*depth)` — cards recede into a vanishing point with progressive shadow and opacity. (3) **Folder styling.** Each card has a `.ak-archive-tab` element — a small dark tab that protrudes above trailing cards. Tab offset, width, and shade vary by depth. Front card tab hidden via CSS `opacity:0`. Inner content clipped via `.ak-archive-card-inner` with `border-radius:14px; overflow:hidden`. (4) **Pointer-captured drag.** Replaced mouse+touch listeners with PointerEvent API + `setPointerCapture`/`releasePointerCapture`. `pointerId` tracked to ignore stale events. `pointercancel` handled with spring snap-back. No "stuck holding" state possible — release always fires via capture. `didDrag` flag prevents link navigation during drag. (5) **Particle background.** Full-viewport canvas (`ak-archive-canvas`) behind deck using ak-top's proven pattern: dust (no shadowBlur), floaters (accent glow preserved), simplified shooting stars. DPR capped to 1. Density reduced ~15% below ak-top levels. `mix-blend-mode:screen`. IntersectionObserver cancel/restart (animationId=null on pause, lastFrameTime=0 on restart). `prefers-reduced-motion` hides canvas. All 18 cards, URLs, images, titles, order, schema preserved exactly. Template JSON unchanged. | Low |
| 2026-04-07 | (superseded) Archive deck v2 | `sections/ak-archive.liquid` | Full rewrite of the Archive card-stack section. v1 had too-narrow 5:7 portrait cards, barely-visible stack depth, linear drag, and 18 cluttered dots. v2 fixes all five issues: (1) **Wide format:** stage is `clamp(320px,72vw,920px)` at 16:10 aspect ratio (mobile 4:3, tiny 3:2) — dominant hero card. (2) **Visible stack:** 4 cards shown behind front card with 14px Y offset, 4.5% scale step, 1.2deg rotation per depth, progressive box-shadow, and opacity dimming. Stack is immediately readable as "more cards behind." (3) **Spring physics:** custom critically-damped spring engine (stiffness/damping per animation). Drag exit, snap-back, card entrance, and depth transitions all spring-driven. Velocity-tracked drag feeds into release physics. (4) **Weighted drag:** tracks pointer velocity in px/s. Drag rotates card (-0.04deg/px), scales down, fades opacity. Release uses velocity+distance to determine dismiss vs snap-back. Threshold 60px or 600px/s. Spring snap-back preserves momentum. (5) **Clean progress:** 18 dots replaced with "01 / 18" counter + segmented progress rail (white fill bar). (6) Prev/next buttons moved to left/right edges of viewport (vertically centered), integrated with the deck rather than clustered at bottom. Template JSON unchanged. All 18 cards, URLs, images, titles, order preserved exactly. Awaiting local runtime verification. | Low | Verify front card is wide and dominant (fills ~72vw on desktop). Verify 3-4 cards visibly stacked behind with offset, rotation, scale, and shadow depth. Verify clicking front card navigates to correct collection. Verify next button (right edge) advances with spring exit animation. Verify prev button (left edge) goes back with spring entrance. Verify tap zones work (left 25%, right 25%). Verify keyboard arrows work. Verify counter reads "01 / 18" and updates. Verify progress rail fills proportionally. Verify drag up on front card: card follows finger with rotation+scale, releases with spring to dismiss or snap back. Verify drag velocity matters (fast flick dismisses even short drag). Verify slow small drag snaps back via spring. Verify drag does not trigger link click. Verify stack wraps (18→1, 1→18). Verify no vertical scroll. Verify mobile 375px: card fills ~92vw at 4:3 ratio. Verify mobile 320px: card at 3:2 ratio. Verify ak-top unaffected. Verify no white flash. Verify no console errors. |
| 2026-04-07 | Narrative page: reduce input focus glow and glow-box intensity | `templates/page.narrative.json` | CSS-only polish in section `custom_liquid_7HLMyz`. (1) Input focus: removed green `drop-shadow(0 0 6px var(--ak-accent))` and changed focus color from `var(--ak-accent)` (green) to `var(--ak-purple)`, set `filter: none`. No more green inner glow on click. (2) Glow-box border: `3px solid var(--ak-accent)` → `1.5px solid rgba(255,51,255,0.45)` — thinner, semi-transparent, less neon. (3) Glow-box shadow: `0 0 20px/50px/inset 14px` → `0 0 10px/28px/inset 8px` all with rgba at 0.10–0.25 — roughly halved spread and opacity. (4) `backdrop-filter: saturate(160%)` → `saturate(140%)`. (5) Pulse animation slowed from 4s to 5s. (6) Pulse keyframes rewritten with rgba values at same reduced intensity as resting state, cycling subtly from 0.25→0.32 outer opacity. Feed card styles, tweet reveal IO, chat bar posting logic, canvas particle system, and all interactive behavior untouched. Awaiting local runtime verification. | Very Low | Verify clicking into "what's the narrative?" input shows no green glow. Verify input focus has subtle purple text color. Verify glow-box has a thinner, softer pink border. Verify glow-box shadow is present but restrained. Verify pulse animation still visible as subtle breathing. Verify feed cards still semi-transparent with blur. Verify particles visible behind feed. Verify posting still works. Verify tweet reveals still work. Verify no visual regression on mobile. Verify no new console errors. |
| 2026-04-07 | Narrative page: remove grid animation, fix white flashes, improve feed transparency | `templates/page.narrative.json` | CSS-only fix in section `custom_liquid_7HLMyz`. (1) Removed `.ak-twitter-feed-wrapper::before` — the repeating-linear-gradient grid layer animated via `@keyframes whaleDrift` (60s linear infinite vertical drift). This was the moving grid-line animation behind the Twitter-style feed. (2) Removed `@keyframes whaleDrift`. (3) Removed `.ak-twitter-feed-wrapper::after` — a tiled radial-gradient SVG vignette at `z-index:-1` that sat between the canvas (`z-index:-3`) and content, darkening the particle field unnecessarily. Removing it lets particles show more cleanly through the semi-transparent feed cards. (4) Strengthened body background from `background: var(--ak-bg)` to `background: #000 !important` in the inline `html, body` rule. The global theme body background is white (scheme-1 `#ffffff`), which flashed through during fast scroll and overscroll bounce. The `!important` ensures the black background wins the specificity war with the theme's `body { background-color: rgb(var(--color-background)); }`. (5) Section `padding_top`/`padding_bottom` reduced from 4 to 0, eliminating any wrapper gap that could expose the body background. Canvas particle system, feed card styles (`backdrop-filter: blur`, semi-transparent backgrounds), tweet reveal IO, chat bar, posting logic, tweet data, and all interactive behavior completely untouched. Canvas still runs at original density — density/IO optimization deferred to separate Phase 2 pass. Awaiting local runtime verification. | Low | Verify no moving grid-line animation behind feed. Verify no white flashes during normal scroll up/down. Verify no white flashes during overscroll bounce (top and bottom). Verify particle canvas visible behind semi-transparent feed cards and glow-box. Verify glow-box border/shadow/pulse animation intact. Verify tweet reveal animations (scroll-triggered) still work. Verify chat bar input and posting still work. Verify canvas particles animate smoothly. Verify no visual regression on desktop. Verify no visual regression on mobile (320/375/414px). Verify ak-top header/nav unaffected. Verify no new console errors. |
| 2026-04-06 | Bad Music: density match + IO cancellation for both canvases | `templates/page.badmusic.json` | Retuned both Bad Music particle canvases (`ak-bg-canvas` in 3D Retro TV section `custom_liquid_dx4GNc`, `bv-field` in BAD Vinyl section `custom_liquid_QcCtY6`) to match the `ak-top` starfield environment and added true offscreen cancellation. Per canvas: (1) DPR capped to 1 (was `Math.min(dpr, 2)`), 4× fill-rate reduction on Retina. (2) 3-tier `densityScale()` added (0.28/0.38/0.85 at 520/750px breakpoints). (3) Dust cap reduced from TV 1,200–2,400 / Vinyl 1,100–5,200 to 400–800 desktop / 160–450 mobile / 120–300 tiny. (4) Floater cap reduced from TV 220–420 / Vinyl 180–650 to 200–400 desktop / 50–130 mobile / 40–100 tiny. (5) Floater glowSize reduced from TV 5–12 / Vinyl 6–14×sizeMul to 7–16. (6) Dust accent shadowBlur removed (TV had 3.5, Vinyl had 4). (7) Dust alpha/sizes tightened to match ak-top feel. (8) CSS: TV `filter: contrast(1.08) saturate(1.12)` → `contrast(1.1)`. Vinyl outer `filter:saturate(1.02) contrast(1.02)` → `contrast(1.1)`. (9) `IntersectionObserver` added to both with true cancel/restart — rAF loop stops entirely when offscreen, restarts cleanly on re-entry. Neither canvas had any IO previously. TV canvas `opacity: .95` preserved. TV YouTube/countdown/tilt/play logic completely untouched. Vinyl audio/seek/spin/tilt logic completely untouched. Red banner section untouched. Disabled sections untouched. Floater glow, shooter glow, shooting star systems, colors, blend modes all preserved. Awaiting local runtime verification. | Low | Verify TV hero starfield renders and animates with lighter particles. Verify Vinyl grid starfield renders and animates with lighter particles. Confirm particle density visually matches ak-top header/nav zone. Confirm dust is lighter but present. Confirm accent floater glow still visible but tighter. Confirm shooting stars still fire in both areas. Confirm TV YouTube play button works. Confirm TV countdown displays (if before release). Confirm TV CRT scanlines visible. Confirm TV tilt/hover works on desktop. Confirm Vinyl cards render (2-col desktop, 1-col mobile). Confirm Vinyl audio playback works. Confirm Vinyl seek bar works. Confirm Vinyl spin animation works. Confirm red scrolling banner unaffected. Scroll TV offscreen and back — canvas stops and restarts cleanly. Scroll Vinyl offscreen and back — canvas stops and restarts cleanly. Confirm ak-top header/nav unaffected. Confirm mobile (320/375/414px) intact. Confirm no new console errors. |
| 2026-04-06 | Cart footer: lift canvas to section wrapper | `sections/main-cart-footer.liquid` | Moved the `ak-cart-footer-field` canvas from inside `.cart__footer` (the rounded card box) to directly inside `#main-cart-footer` (the section wrapper). Previously the canvas was trapped inside the card's `border-radius:22px` + `overflow:hidden`, clipping particles at the corners and making the starfield look contained inside the box. Now the canvas fills the full section area and the footer card floats as a glass card on top of the starfield. CSS changes: (1) `#main-cart-footer` gets `position:relative; isolation:isolate; overflow:hidden` for canvas containment. (2) Canvas targeting moved from `.cart__footer .ak-cart-footer-field` to `#main-cart-footer > .ak-cart-footer-field`. (3) `.page-width` gets `position:relative; z-index:1` to layer above canvas. (4) `isolation:isolate` removed from `.cart__footer` (no longer needed). (5) Content z-index override rule removed (no longer needed). HTML: canvas element moved to be a direct child of `#main-cart-footer`. JS: no changes — `canvas.parentElement` now correctly points to the section wrapper for sizing. Card border, background, border-radius, hover effects, HUD scanline, content structure, checkout button, totals, discounts, tax notes, and all commerce logic unchanged. Density tuning from prior pass preserved. Awaiting local runtime verification. | Low | Verify cart footer starfield fills the full section area (particles visible in margins around the card, not clipped at corners). Confirm footer card floats as a glass card over the starfield. Confirm estimated total renders with purple value. Confirm checkout button works. Confirm dynamic checkout buttons render. Confirm card hover effect works (border glow, translateY). Confirm HUD scanline overlay still visible on card. Confirm bottom breathing room preserved. Confirm cart items section unaffected. Confirm mobile (320/375/414px) intact. Confirm empty cart state works. Confirm no new console errors. |
| 2026-04-06 | Cart canvas density match to ak-top + bottom spacing | `sections/main-cart-items.liquid`, `sections/main-cart-footer.liquid` | Retuned both cart canvases (`ak-cart-field` in items section, `ak-cart-footer-field` in footer section) to match the `ak-top` starfield environment. Per canvas: (1) DPR capped to 1. (2) Dust cap reduced from 1,200–20,000 to 400–800 desktop / 120–450 mobile / 120–300 tiny. (3) Floater cap reduced from 240–1,400 to 200–400 desktop / 40–130 mobile. (4) Floater glowSize reduced from 14–34 to 7–16. (5) Dust/floater particle sizes tightened. (6) Dust accent shadowBlur removed. (7) CSS `saturate(1.10)` removed from both canvas filters (kept `contrast(1.10)`). (8) IO upgraded from flag-only to true cancel/restart. (9) densityScale upgraded to 3-tier (tiny/mobile/desktop). Bottom spacing added: `.cart__footer` margin-bottom changed from 0 to `clamp(40px, 6vw, 80px)`. Container border-radius and overflow intentionally not changed in this pass. Cart form, quantity inputs, remove links, checkout button, dynamic checkout buttons, discounts, shipping/tax logic, hover effects, and all commerce functionality unchanged. Awaiting local runtime verification. | Low | Verify cart items starfield renders and animates. Verify footer starfield renders and animates. Confirm particle density visually matches ak-top header/nav zone. Confirm dust is lighter but present. Confirm accent floater glow still visible but tighter. Confirm shooting stars still fire in both areas. Confirm adequate breathing room below checkout area. Confirm cart title visible. Confirm line item cards render correctly. Confirm quantity inputs work. Confirm remove links work. Confirm estimated total renders with purple value. Confirm checkout button works. Confirm dynamic checkout buttons render. Confirm card/footer hover effects work. Confirm empty cart state works. Confirm mobile (320/375/414px) intact. Scroll offscreen and back — confirm both canvases stop and restart cleanly. Confirm ak-top unaffected. Confirm other pages unaffected. Confirm no new console errors. |
| 2026-04-06 | Clubhouse background density match to ak-top | `snippets/club-hub.liquid` | Retuned the `ak-clubhub-field` canvas to match the `ak-top` starfield environment. (1) DPR capped to 1 (was `Math.min(devicePixelRatio, 2)`), 4× fill-rate reduction on Retina. (2) Dust cap reduced from 1,200–20,000 to 400–800 desktop / 120–450 mobile, matching ak-top exactly. (3) Floater cap reduced from 240–1,400 to 200–400 desktop / 40–130 mobile, matching ak-top. (4) Floater glowSize reduced from 14–34 to 7–16, matching ak-top. (5) Dust particle sizes tightened to match ak-top's lighter feel. (6) Dust accent `shadowBlur = 3` removed (no dust shadowBlur, matching ak-top). (7) CSS `saturate(1.10)` removed from canvas filter (kept `contrast(1.10)` to match ak-top). (8) IO observer upgraded from flag-only skip to true cancel/restart — rAF loop stops entirely when offscreen, restarts cleanly on re-entry. `densityScale()` now uses ak-top's 3-tier breakpoint pattern (tiny/mobile/desktop). Floater accent glow, shooter glow, shooting star system, card styling, login/join controls, customer logic, profile form, metafields, and all member functionality unchanged. Awaiting local runtime verification. | Low | Verify Clubhouse page starfield renders and animates. Confirm particle density visually matches the ak-top header/nav zone above. Confirm dust is lighter but present. Confirm accent floater glow (purple/blue/yellow) still visible but tighter. Confirm shooting stars still fire. Confirm ID card renders with border, gradient, backdrop-filter blur. Confirm logged-out state: Login/Join buttons visible and functional. Confirm logged-in state: customer data, photo, metafields render correctly. Confirm profile edit form still submits. Scroll Clubhouse offscreen and back — confirm animation stops and restarts with no flash/pop/timing-jump. Confirm ak-top header/nav unaffected. Confirm other pages unaffected (club-hub CSS is scoped via `.ak-clubhub-page`). Confirm no new console errors. |
| 2026-04-06 | Adventures module heading fix (v6 — wrapper reset) | `templates/page.json` | Fixes AMERIKID letters disappearing after v5 markup regrouping. Root cause: the broad selector `.ak-adv-title span` matched the new `.ak-word-amerikid` wrapper span, applying `opacity:0`, `transform`, `filter:blur(2px)`, and `akAdvDrop`/`akAdvBreath` animations to the wrapper itself. With `display:contents` the wrapper generates no box, so the animation couldn't drive `opacity` back to 1 — AMERIKID stayed invisible. Fix: added `opacity:1 !important; transform:none !important; filter:none !important; animation:none !important;` to the existing `.ak-word-amerikid` base rule. The broad `.ak-adv-title span` selector still correctly targets all 26 letter spans (including the 8 nested inside the wrapper) for per-letter animation. The wrapper is now explicitly excluded from animation inheritance. V5 markup grouping, mobile `inline-flex` behavior, inline `--sx/--sy/--sr` values, and all other CSS unchanged. Supersedes v5. Awaiting local runtime verification. | Low | Verify at 320, 375, 390, 414px: AMERIKID letters visible, heading reads "THE ADVENTURES OF" on line 1, "AMERIKID" on line 2, both centered, AMERIKID never splits. Verify desktop heading unchanged (single line at 1200px+). Verify letter drop animation still plays on all 26 letters individually. Verify AMERIKID letters animate individually (not as a block). Verify breathing animation still pulses. Verify banner card glass effect intact. Verify video grid below unaffected. Verify no new console errors. |
| 2026-04-05 | Life & Style content canvas optimization | `templates/page.json` | Applied proven canvas optimization pattern to all 3 inline content canvases in the default page template (Adventures `custom_liquid_X46wQm`, Kid Radio `custom_liquid_kkxqR3`, Social Dock `custom_liquid_PRGR7i`). Per module: (1) DPR capped to 1 (was `Math.min(devicePixelRatio, 2)`), 4× fill-rate reduction on Retina. (2) Dust cap reduced from 1,600–3,400 to 800–2,000. (3) Floater cap reduced from 280–560 to 200–400. (4) Dust `shadowBlur` removed (accent dust glow eliminated, floater/shooter glow preserved). (5) CSS `filter: contrast(1.1)` removed (per-frame GPU filter pass eliminated). (6) IntersectionObserver offscreen cancellation added — rAF loop stops entirely when module scrolls out of viewport and restarts cleanly on re-entry. These modules previously had zero offscreen cancellation. `mix-blend-mode: screen`, floater glow, shooter glow, colors, module layouts, YouTube embeds, video docks, social links all unchanged. Applied via Python script for reliable JSON-escaped string manipulation. | Low | Verify Life & Style page loads with all 3 content modules (Adventures, Kid Radio, Social Dock) rendering starfield backgrounds. Confirm particles animate when visible. Confirm dust is lighter but present. Confirm accent floater glow still visible. Confirm shooting stars still fire. Scroll each module out of view and back — confirm animation stops and restarts with no flash/pop/timing-jump. Confirm YouTube video grid still functional. Confirm video dock still functional. Confirm social links still functional. Confirm mobile layout unaffected. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-05 | Product purchase panel CSS polish | `sections/main-product.liquid` | CSS-only changes in the `{%- style -%}` block to improve product info panel visual hierarchy. (1) Price scaled up from 1.6/1.8rem to 2.4/3rem mobile/desktop with letter-spacing. (2) Add to Cart button overridden to filled white/dark-text primary visual weight with subtle purple hover glow. (3) Description separated from buy area via border-top, extra padding-top, and reduced opacity. (4) Spacing rhythm refined: tighter title-to-price, grouped purchase controls, breathing room before description. (5) Stacked gallery grid tightened on desktop with 0.6rem gap and 3px border-radius on media containers. No Liquid, JS, JSON, gallery settings, or transparency overrides changed. | Low | Verify price is visibly larger on mobile and desktop. Confirm Add to Cart button appears as a filled white button with dark text. Confirm subtle purple glow on Add to Cart hover. Confirm description has a thin white separator line above it and appears slightly dimmed. Confirm spacing between title/price/variants/buy feels grouped. Confirm stacked images on desktop appear slightly tighter with subtle rounded corners. Confirm lightbox still opens on image click. Confirm variant picker still works. Confirm dynamic checkout button still renders. Confirm canvas particles still render. Confirm no layout drift. Confirm no new console errors. |
| 2026-04-05 | Reorder product info blocks for purchase flow | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Reordered product info block_order from title → description → custom_liquid_KbTzPh → price → variant_picker → quantity_selector → buy_buttons → share to title → price → variant_picker → quantity_selector → buy_buttons → description → share. Moves price and purchase controls directly below title, pushes description below buy buttons. Removed the empty `custom_liquid_KbTzPh` block (empty string, rendered nothing) from both block_order and blocks. Gallery settings, section settings, section order, main-product.liquid, CSS, and JS all untouched. | Very Low | Verify product page shows: title, then price, then variant picker, then quantity, then Add to Cart / Buy buttons, then description, then share. Confirm description text still renders. Confirm variant picker still works. Confirm Add to Cart and dynamic checkout buttons still function. Confirm gallery (stacked + lightbox) unaffected. Confirm canvas particles still render. Confirm no layout drift. Confirm no new console errors. |
| 2026-04-05 | Disable back button section on product pages | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Added `"disabled": true` to the `custom_liquid_6wWQFN` (back button) section in both product templates. This removes the full ak-back-field canvas particle system (dust, floaters, shooters) and BACK button from the product page render, eliminating one canvas and its rAF loop. Section code is preserved in the JSON — can be re-enabled by removing the `disabled` flag. Gallery settings, product info blocks, main-product section, related-products, and all other sections unchanged. | Very Low | Verify product pages no longer show the BACK button section between the product area and related products. Confirm no ak-back-field canvas in the DOM. Confirm product gallery (stacked + lightbox), form, variants, cart buttons, and related products all still work. Confirm ak-product-field canvas still renders. Confirm no layout drift. Confirm no new console errors. |
| 2026-04-05 | Product gallery: restore stacked layout, keep lightbox | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Partial rollback: changed `gallery_layout` from `"thumbnail"` back to `"stacked"` while keeping `image_zoom` as `"lightbox"`. The thumbnail layout removed the visible stacked supporting images below the main product image, which the user wants. Stacked layout shows all product images vertically; lightbox enables click-to-zoom on each image. No Liquid, CSS, or JS files changed — settings-only. | Very Low | Verify all product images display vertically below the main image (stacked layout). Confirm clicking any product image opens a full-screen lightbox modal. Confirm lightbox can be closed. Confirm product form (variants, add to cart) still works. Confirm canvas particles still render behind media. Confirm mobile gallery slider still works. Confirm back button section unaffected. Confirm no new console errors. |
| 2026-04-05 | Restore product gallery: thumbnail layout + lightbox zoom | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Changed `gallery_layout` from `"stacked"` to `"thumbnail"` and `image_zoom` from `"hover"` to `"lightbox"` in both product templates. Stacked layout was hiding the thumbnail list on desktop (`display: none` via `section-main-product.css`). Hover zoom was hiding the lightbox button on hover-capable devices. Thumbnail layout restores a clickable thumbnail grid below the main image. Lightbox restores click-to-open full-screen modal on product images. No Liquid, CSS, or JS files changed — settings-only. Product page styling, canvas particles, transparency overrides, back button, and all other sections unchanged. | Low | Verify product page shows clickable thumbnail grid below main image. Confirm clicking a thumbnail switches the active image. Confirm clicking the main image opens a full-screen lightbox modal. Confirm lightbox can be closed. Confirm product form (variants, add to cart) still works. Confirm canvas particles still render behind media. Confirm mobile gallery slider still works. Confirm back button section unaffected. Confirm no new console errors. |
| 2026-04-05 | Revert collection-card image ratio: portrait → square | `templates/collection.json` | Reverted `image_ratio` from `"portrait"` back to `"square"` on the default collection template. The portrait change was intended to reduce crop severity on one apparel product image, but user prefers the original square grid layout overall. The crop issue is specific to one product image and should be addressed in the Shopify admin rather than theme-wide. | Very Low | Verify collection-card images are 1:1 square again. Confirm grid layout, hover zoom, title styling, sold-out banner, pagination, and particle canvas are all unaffected. Confirm no new console errors. |
| 2026-04-05 | Collection-card image ratio: square → portrait | `templates/collection.json` | Changed `image_ratio` from `"square"` to `"portrait"` on the default collection template. This changes the card media container from a 1:1 aspect ratio (`--ratio-percent: 100%`) to a 4:5 aspect ratio (`--ratio-percent: 125%`), reducing crop severity on non-square product images (especially taller apparel photos). Cards still have uniform height across the grid. Hover zoom (`scale(1.03)`), card layout, spacing, sold-out banner, title styling, transparency overrides, particle canvas, and pagination all unchanged. Only the default `collection.json` template is changed — other collection templates retain their current `image_ratio` settings. | Very Low | Verify collection-card images are now taller (4:5 ratio instead of 1:1 square). Confirm product images that were previously cropped (e.g., long-sleeve apparel) show more of the garment. Confirm cards still have uniform height within each row. Confirm hover zoom effect still works. Confirm grid layout (2-column, max-width 480px) is unaffected. Confirm sold-out banner, title styling, and pagination are unaffected. Confirm particle canvas renders behind cards. Confirm other collection templates are unaffected. Confirm no new console errors. |
| 2026-04-05 | Collection-card title visual cleanup | `sections/main-collection-product-grid.liquid` | Added `font-family: Impact, sans-serif !important` and `text-transform: uppercase` to the existing collection-card title CSS rule. Added a new rule overriding `component-card.css` hover underline (`text-decoration: none !important`). All changes are in the section's `{%- style -%}` block, scoped to when this section renders on collection pages. Card layout, spacing, sold-out banner, transparency overrides, zoom behavior, pagination, and particle canvas all unchanged. | Very Low | Verify collection-card titles render in Impact font. Confirm titles are uppercase. Confirm no underline appears on hover. Confirm card layout, spacing, images, sold-out banner, and pagination are unaffected. Confirm particle canvas renders behind cards. Confirm product-detail pages, cart, and other page types are unaffected. Confirm no new console errors. |
| 2026-04-05 | Remove collection-grid scroll-trigger reveal animations | `sections/main-collection-product-grid.liquid` | Removed `scroll-trigger animate--slide-in` and `data-cascade` from both the `#ProductGridContainer` wrapper and each product card `<li>` in the collection grid. Previously, 37 elements (1 container + 36 cards) had double-stacked slide-in animations (opacity 0.01 + translateY 2rem → visible), creating a "waterfall pop-in over particles" effect on the dark canvas-backed page. Cards now appear immediately at full opacity. Liquid-only change — `animations.js`, `base.css`, and the global `animations_reveal_on_scroll` setting are untouched. All other sections keep their scroll-trigger behavior. | Low | Verify collection page product cards appear immediately without slide-in animation. Confirm cards are fully visible on load (no invisible/opacity:0.01 state). Confirm grid layout, card spacing, hover styling, sold-out banner, and pagination are unaffected. Confirm no layout drift. Confirm particle canvas still renders behind cards. Confirm other page types (product, cart, mystery-box) still have their normal scroll-trigger animations. Confirm no new console errors. |
| 2026-04-05 | ak-collection-field `filter: contrast(1.1)` removal | `sections/main-collection-product-grid.liquid` | Removed `filter: contrast(1.1)` from the ak-collection-field canvas CSS. This was a per-frame GPU filter pass applied to the full canvas area (~1.5M pixels at DPR 1) 60 times per second while visible. Particle colors, glow sizes, `mix-blend-mode: screen`, dust counts, floater counts, shooter behavior, card/grid layout, and all other CSS unchanged. | Very Low | Verify collection page starfield still renders and animates. Confirm particles appear very slightly less "punchy" but otherwise identical. Confirm accent floater glow (purple/blue/yellow) still visible. Confirm shooting stars still fire. Confirm product grid layout, card hover, and pagination unaffected. Confirm no layout drift. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-03-19 | Documentation Bootstrap | `AGENTS.md`, `docs/*` | Establish AI continuity and source of truth before beginning optimization pass. | None | N/A |
| 2026-03-19 | Runtime Findings Sync | `docs/*` | Update documentation with verified runtime audit findings (PageFly active, EComposer remnants, canvas redundancy). | None | N/A |
| 2026-03-19 | ak-product-field offscreen cancellation | `sections/main-product.liquid` | Stop scheduling `requestAnimationFrame` when the product canvas is offscreen. Restart cleanly when it re-enters the viewport. Reduces idle GPU/CPU cost on product pages when users scroll below the hero area. | Low | Verify canvas animates normally when visible, stops when scrolled out of view, resumes with no flash/pop/reseed on scroll-back. Check desktop, mobile, reduced-motion. |
| 2026-03-20 | ak-collection-field offscreen cancellation | `sections/main-collection-product-grid.liquid` | Stop scheduling `requestAnimationFrame` when the collection canvas is offscreen. Restart cleanly when it re-enters the viewport. Same proven pattern as `ak-product-field`. Reduces idle GPU/CPU cost on all collection pages when users scroll past the product grid. | Low | Verify collection canvas animates normally when visible, stops when scrolled out of view, resumes with no flash/pop/reseed/timing-jump on scroll-back. Check desktop, mobile, reduced-motion. Confirm no layout drift or product grid functionality changes. |
| 2026-03-20 | ak-back-field offscreen cancellation | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Upgrade existing partial `IntersectionObserver` to true rAF cancellation. The loop previously kept scheduling frames when offscreen (just skipping draws); now it stops entirely and restarts cleanly on re-entry. Same proven pattern as `ak-product-field` and `ak-collection-field`. Reduces idle GPU/CPU cost on product pages when users scroll past the back-button section. | Low | Verify back-field canvas animates normally when visible, stops when scrolled out of view, resumes with no flash/pop/reseed/timing-jump on scroll-back. Check desktop, mobile, reduced-motion. Confirm back button still functions. Confirm no layout drift or product page functionality changes. |
| 2026-03-26 | holo-rail-field offscreen cancellation | All 18 JSON templates containing the holo-rail `custom_liquid` section | Stop scheduling `requestAnimationFrame` when the holo-rail header canvas scrolls out of viewport. Restart cleanly on re-entry. Same proven IO+cancel/restart pattern. Two code variants patched: 10 pretty-printed files (function `step`, var `lastFrameTime`) and 8 minified files (function `stp`, var `lft`). Reduces idle GPU/CPU cost on every page when users scroll past the header area. Header uses `position:relative`, not sticky — IO correctly detects offscreen. | Low | Verify holo-rail canvas animates normally when header is visible, stops when scrolled out of view, resumes with no flash/pop/reseed/timing-jump on scroll-back. Check product, collection, cart, mystery-box pages. Check desktop and mobile (375px). Confirm canvas count unchanged. Confirm no new console errors. Confirm nav drawer unaffected (separate section). |
| 2026-03-27 | aknav-field offscreen cancellation | All 18 JSON templates containing the aknav `custom_liquid` section | Stop scheduling `requestAnimationFrame` when the nav starfield canvas scrolls out of viewport. Restart cleanly on re-entry. Same proven IO+cancel/restart pattern. Four code variants patched: 8 files (V1, `hardSeedOnce`/`softResizeKeepPositions`), 6 files (V2, simple `reseed`), 2 files (V3, `reseed`/`aknav-divider`), 2 files (V4, identical to V1). Completes 5/5 canvas offscreen cancellation. Canvas is purely decorative (`aria-hidden`); drawer is detached to `document.body`; all interactive event listeners (drawer, tilt, glow) are independent of rAF loop. | Medium | Verify nav starfield animates when visible, stops when scrolled out of view, resumes with no flash/pop/reseed/timing-jump on scroll-back. Click MENU — confirm drawer opens/closes. Confirm tile hover tilt and glow tracking still work. Check product, collection, cart, mystery-box pages. Check desktop and mobile (375px). Confirm canvas count unchanged. Confirm no new console errors. Confirm holo-rail and ak-back-field canvases unaffected. |
| 2026-03-27 | Remove inactive EComposer global renders | `layout/theme.liquid` | Remove `{% render 'ecom_header' %}` (line 4) and `{% render 'ecom_footer' %}` (line 389) from the main layout. EComposer is confirmed inactive — not driving any page layouts. The header snippet fires 6 preconnect/prefetch links to `cdn.ecomposer.app` plus 167+ lines of conditional JS on every page load, all for an unused builder. The footer snippet is a comment stub. Snippet files preserved in repo. Note: `theme.pagefly.liquid` and `layout/ecom.liquid` still render these snippets — not touched in this pass. | Very Low | Verify no visual or layout changes on any page. Confirm `cdn.ecomposer.app` no longer appears in Network tab preconnects. Check console for any new errors. Confirm PageFly homepage still renders correctly. Confirm no missing styles or scripts. |
| 2026-03-27 | Remove unused Gameball integration | `layout/theme.liquid`, `layout/theme.pagefly.liquid`, `snippets/gameball.liquid` (deleted), `config/settings_data.json` | Store owner confirmed Gameball is unused — no active loyalty, no referrals, no widget rendering (widget was already broken due to css.json 404). Removed `{% include 'gameball' %}` from both layouts (theme.liquid line 419, theme.pagefly.liquid line 324). Deleted the snippet file. Disabled the `gb-widget` app embed block in settings_data.json. Eliminates 4 Gameball script loads (gb-init-shopify.js, gb_shopify.js, gameball-init.min.js, gameball.widget.min.js), 22 duplicate hidden inputs, referral cookie/form JS, and SignalR dependency from every page. Note: the Gameball Shopify app may still need to be uninstalled from the admin. | Low | Verify zero gameball-related DOM elements, scripts, and network requests across homepage, product, collection, and cart pages. Confirm no layout drift. Confirm canvases still render. Confirm no new console errors. Confirm account/signup surfaces unaffected (store uses external customer accounts — no theme-rendered registration form). |
| 2026-04-03 | Remove empty PopClips carousel blocks and orphan apps sections | `templates/page.club-hub.json`, `templates/page.json`, `templates/page.badmusic.json` | Removed 6 empty PopClips carousel app blocks (all with `published_carousel_id: ""`) and 3 orphan empty `apps` sections from 3 page templates. These rendered zero visible output but still triggered additional JS/CSS loads (`popclips-carousel-D1sx3lyq.js`, `Loader.ce`, etc.) and wasted API calls to `storefront?carouselId=custom-popclips-media-api` on affected pages. The global Videeo app embed SDK in `config/settings_data.json` is NOT touched — it still loads on every page. Disabling it is the next optional step pending business confirmation. | Very Low | Verify club-hub, badmusic, and default page template pages load with no visual change. Confirm no new console errors. Confirm no PopClips carousel elements in DOM on those pages. Confirm homepage, product, collection, cart, and mystery-box pages are unaffected. |
| 2026-04-04 | ak-product-field visible-cost reduction (Phase 1) | `sections/main-product.liquid` | Reduced dust particle cap from 3,000–17,000 to 800–2,000 and removed `ctx.shadowBlur` from dust draw calls. Accent floater glow, shooter glow, colors, blend mode, canvas structure, layout, and all other behavior unchanged. This is Phase 1 of visible-canvas optimization — not yet propagated to other canvas systems. | Low | Verify product page starfield still renders and animates. Confirm accent floater glow (purple/blue/yellow) is still visible. Confirm dust field still appears but is lighter. Confirm no layout drift. Confirm product form, gallery, variants, cart buttons, and back button area unaffected. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-04 | ak-top visual continuity refinement (cart only) | `sections/ak-top.liquid` | Removed the frosted-glass treatment from the nav area (`backdrop-filter`, `border`, `box-shadow`, `border-radius`, white-tinted background) that was creating a separate "dark slab" below the header. Nav area is now fully transparent — particles show through it the same way they show through the header, creating one continuous background language. Added a subtle gradient divider (`<hr>`) between header and nav for minimal structural definition. Added a bottom-fade pseudo-element on the wrap for smooth transition to page content. No particle, interaction, or layout changes. | Low | Verify cart page top area reads as one seamless zone rather than two stacked blocks. Confirm nav tile text is still readable (text-shadows provide contrast). Confirm shooting stars, dust, and floater glow all render continuously across full height. Confirm all interactions unchanged: cart tilt/oil-fill, logo tilt, drawer open/close, tile hover. Confirm mobile (375px). Confirm no new console errors. |
| 2026-04-04 | Cart-only prototype: combined top-of-page system (ak-top) | `sections/ak-top.liquid` (new), `templates/cart.json` | Created `ak-top.liquid` — a single shared section replacing two separate `custom_liquid` sections (holo-rail + aknav). Combines header bar (logo, cart button) and nav panel (tiles, drawer, glow) behind ONE canvas instead of two. Dust particles capped at 400–800 (was 1,500–3,400), dust `shadowBlur` removed, floater accent glow preserved, full shooting star system with sparks/fragmentation preserved. Cart button has 3D tilt, oil-fill animation, and `/cart.js` polling. Nav drawer detached to `document.body`. IntersectionObserver offscreen cancellation included. Migrated `cart.json` only as prototype — removed `custom_liquid_YpGePJ` (holo-rail, 33,762 chars) and `custom_liquid_RHPdJm` (aknav, 47,871 chars), added one `ak-top` section reference. Template went from ~100KB to ~5KB. Remaining 17 templates unchanged pending visual verification. | Medium | Verify cart page loads with header bar (logo + cart count), nav panel (tiles + drawer), and single canvas background. Confirm shooting stars fire and fragment. Confirm dust is lighter but present. Confirm cart button tilt and oil-fill update. Confirm MENU drawer opens/closes, escape key works, outside-click closes. Confirm tile hover tilt and glow tracking. Confirm mobile layout (375px). Confirm no new console errors. Confirm other pages (product, collection, homepage) are unaffected (still using old sections). |
| 2026-04-04 | Site-wide ak-top rollout (17 templates) | `templates/collection.json`, `templates/collection.bad-music.json`, `templates/collection.election.json`, `templates/collection.gp-template-bk-default.json`, `templates/collection.gpr.json`, `templates/collection.kandy.json`, `templates/collection.uh-oh-ghoul-aid-tee.json`, `templates/collection.uhoh.json`, `templates/list-collections.json`, `templates/page.badmusic.json`, `templates/page.club-hub.json`, `templates/page.contact.json`, `templates/page.json`, `templates/page.narrative.json`, `templates/page.mystery-box.json`, `templates/product.json`, `templates/product.gp-template-bk-default.json`, `docs/*` | Replaced the old two-section pattern (holo-rail `custom_liquid` + aknav `custom_liquid`) with single `ak-top` section reference in all 17 remaining templates. Combined with previous cart.json migration, all 18 two-section templates now use ak-top. Removed ~1,310KB of inline custom_liquid across 17 files. Each template now renders one canvas instead of two for the top-of-page area. Mystery-box template preserved its `custom_liquid_mysterybox_embed` section intact. 15 aknav-only collection templates intentionally skipped (different structural pattern). Homepage templates untouched. | Medium | Verify all migrated page types load with header bar (logo + cart), nav panel (tiles + drawer), and single canvas background. Confirm shooting stars fire. Confirm cart button tilt and count update. Confirm MENU drawer opens/closes on all page types. Confirm tile hover tilt and glow tracking. Confirm mystery-box iframe still loads and is interactive. Confirm mobile layout (375px) on product, collection, page, and mystery-box. Confirm no new console errors. Confirm homepage is unaffected. |
| 2026-04-05 | ak-collection-field visible-cost reduction (Phase 1) | `sections/main-collection-product-grid.liquid` | Combined DPR cap, dust reduction, floater reduction, glowSize reduction, and dust shadowBlur removal for ak-collection-field. DPR capped to 1 (was `Math.min(devicePixelRatio, 2)`). Dust cap reduced from 3,600–20,000 to 800–2,000. Floater cap reduced from 520–1,800 to 200–800. Accent floater glowSize reduced from 14–32 to 8–18. Dust accent `shadowBlur = 4` block replaced with `shadowBlur = 0`. Floater accent glow (`p.glowSize * twk2`), shooter glow (`16 * s.depth`), shooter gradients, colors, blend mode (`lighter`), CSS, layout, product grid, card styling, and all other behavior unchanged. Same proven pattern as ak-product-field Phases 1–3 and ak-back-field reduction. | Low | Verify collection page starfield still renders and animates. Confirm dust field appears but is lighter. Confirm accent floater glow (purple/blue/yellow) still visible but lighter/tighter. Confirm shooting stars still fire with full glow. Confirm product grid layout, card hover, and pagination unaffected. Confirm no layout drift. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-04 | ak-back-field visible-cost reduction | `templates/product.json`, `templates/product.gp-template-bk-default.json` | Reduced ak-back-field dust particle cap from desktop 1,200–20,000 / mobile 420–6,500 to desktop 400–2,000 / mobile 200–1,000. Reduced floater cap from desktop 240–1,400 / mobile 70–520 to desktop 120–600 / mobile 50–300. Removed `ctx.shadowBlur` from dust draw calls. Floater accent glow (`p.glowSize * twk2`) and shooter head glow (`14 * s.depth`) preserved. Button styling, layout, back-button behavior, blend mode, colors all unchanged. Same proven pattern as ak-product-field Phase 1. gp-template variant (no mobile branch) received equivalent reductions: dust 400–2,000, floaters 120–600. | Low | Verify back-field canvas still renders and animates on product pages. Confirm dust field appears but is lighter. Confirm floater accent glow (purple/blue/yellow) still visible. Confirm shooting stars still fire. Confirm BACK button still functions (history.back). Confirm no layout drift. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-04 | ak-product-field floater cost reduction (Phase 3) | `sections/main-product.liquid` | Reduced ak-product-field floater particle cap from 440–1,530 to 200–800 and accent floater glowSize range from 14–32 to 8–18. This reduces both the number of floater draw calls per frame and the per-call `shadowBlur` expense on accent floaters (~24% of total). Dust counts (800–2,000), shooter rendering, shooter glow (`16 * s.depth`), floater colors, floater shapes (dot/pill/blob), floater alpha ranges, blend mode (`lighter`), `filter: contrast(1.1)`, DPR cap (1), CSS, and layout all unchanged. Accent floater glow behavior is preserved — just lighter and tighter. | Low | Verify product page starfield still renders and animates. Confirm accent floater glow (purple/blue/yellow) still visible but lighter/tighter. Confirm dust field unchanged from Phase 1. Confirm shooting stars still fire with full glow. Confirm no layout drift. Confirm product form, gallery, variants, cart buttons, and back button area unaffected. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab. |
| 2026-04-04 | ak-product-field DPR cap to 1 | `sections/main-product.liquid` | Capped the ak-product-field canvas device pixel ratio from `Math.min(window.devicePixelRatio \|\| 1, 2)` to a fixed `1` in both the initial declaration (line 908) and the `resize()` reassignment (line 933). On DPR 2 screens this reduces the physical canvas from ~2548×2408 (~6.1M pixels) to ~1274×1204 (~1.5M pixels) — a 4× reduction in fill rate, clear cost, and compositor blending work per frame. Particle counts, glow sizes, colors, blend mode (`lighter`), `filter: contrast(1.1)`, CSS, layout, shooting stars, floater accent glow, and all other behavior unchanged. Particle effects are inherently soft/blurry and tolerate resolution reduction well. | Low | Verify product page starfield still renders and animates on both DPR 1 and DPR 2 screens. Confirm particles do not appear jagged or broken. Confirm accent floater glow (purple/blue/yellow) still visible. Confirm shooting stars still fire. Confirm dust field still present. Confirm no layout drift. Confirm product form, gallery, variants, cart buttons, and back button area unaffected. Confirm no new console errors. Compare GPU/frame cost in DevTools Performance tab — expect material improvement on Retina screens. |
| 2026-04-02 | Unlock Mystery Box static teaser lock | `templates/page.mystery-box.json` | Changed `LOCKED_DEFAULT = true` to `LOCKED_DEFAULT = false` in the `custom_liquid_mysterybox_embed` section. This removes the static blur overlay and pointer-events block, exposing the Vercel mystery crate iframe for interaction. No dynamic entitlement gating exists — this was a hardcoded pre-launch lock. Iframe URL construction (`customer_id`, `demo` passthrough), canvas particle system, sizing logic, and init guard are all unchanged. Unpublished theme only. | Low | Verify Mystery Box page loads with iframe interactive (no blur, no overlay). Confirm canvas particle background still renders. Confirm iframe src includes `customer_id` when logged in and `demo=1` when URL param present. Confirm no layout drift. Confirm no new console errors beyond existing `crate.glb` 404 and THREE.js warnings. |
