# Source of Truth

## Repo Purpose
This repository holds the unpublished working copy of the live Amerikid Shopify theme. It serves as a staging ground for performance optimization and cleanup before pushing updates to the live store.

## High-Level Theme Description
This theme is based on Shopify Dawn but contains heavy custom aesthetic overrides, most notably a global "Impact" font style, neon elements, and custom interactive "starfield" canvas animations. It also shows traces of multiple page builder apps coexisting.

## Navigation Label (2026-06-02)
The site-wide nav/menu tile that links to the club-hub page is labeled **MONIKER** (visible copy in `sections/ak-top.liquid` — desktop `.ak-top-label` tile + mobile drawer link). This is a visible label only: the underlying route/handle remains `/pages/club-hub` and the active-state logic (`request.path == '/pages/club-hub'`) is unchanged. Renamed from "CLUBHOUSE" → "MONIKER".

The **CandyFacts desktop nav icon is intentionally enlarged** (2026-06-02) in `sections/ak-top.liquid` (`.ak-top-candy-img` `transform: scale(1.52)`, `.ak-top-candy` padding `clamp(4px,0.8vw,8px)`). Visual-only: the route remains `/pages/mystery-box`, the active-state logic and BAD icon are unchanged, and the mobile drawer is unchanged (still the shared 120 px `.ak-top-mobile-image img` rule).

The **footer back-to-top control is a larger text-only button** (2026-06-02) reading exactly `CLICK HERE TO GET TOP`, in `sections/ak-footer.liquid` (`.ak-btt-text` desktop `1.6rem` / mobile `1.2rem`, `.ak-btt` padding `8px 14px`). CSS-only enlargement: `id="AkBackToTop"`, `aria-label="Back to top"`, the scroll-to-top JS, `:focus-visible` outline, hover `#ff33ff`, and reduced-motion behavior are unchanged.

The **footer back-to-top resting text and the product-page description/helper text are intentionally full white** (2026-06-04). Footer `.ak-btt` resting color is now `#fff` (was a muted off-white `rgba(255,255,255,.58)`); hover/focus `#ff33ff` is preserved. On product pages the `#MainProduct-{{ section.id }} .product__description` rule in `sections/main-product.liquid` is now `opacity:1` + `color:rgb(255,255,255)` (was `opacity:0.85` over an inherited `rgba(255,255,255,0.75)`), so the helper/body copy below the buy buttons (Shopify Admin product-description content rendered via `{{ product.description }}`, e.g. the CandyFacts combo's `PURCHASED SPINS…` / `HOW THE CANDYFACTS MYSTERY BOX WORKS` lines) reads full white. The white `color` override is **scoped to `.product__description` only** — the product title/price/variant text deliberately keeps the broad inherited `rgba(255,255,255,0.75)`. CSS-only; product form, Add to Cart, Shop Pay/dynamic payment buttons, and layout untouched.

The current **NEW product-card badge** visual is the **larger 3D red comic-burst sticker** (2026-06-02) in `assets/base.css` (`.card-wrapper .badge--new.ak-badge-new`: desktop shape `9.6rem` / text `3rem` so the red outgrows the word, mobile `6.6rem` / `2rem`; 3D depth from CSS-only highlight + dark-red-underside gradients, thicker text stroke, deeper black comic drop-shadow). Still controlled by the Shopify product tag `new` (case-insensitive); the old hardcoded magenta `grid__item--new` system remains removed and the sold-out → NEW → sale priority is unchanged.

The **cart** (`/cart`) is a custom AmeriKid layout where the **item card** (`sections/main-cart-items.liquid`) and the **checkout summary panel** (`sections/main-cart-footer.liquid`) are styled as **one unified black-neon system** (2026-06-02): shared rounded panel + HUD scanline, the green QTY pill balanced with the checkout button, and the magenta checkout button filling a 600px inner column as the primary CTA with Shop Pay/PayPal/GPay centered beneath. All CSS-only — cart/checkout logic, cart AJAX, `name="updates[]"`, and the Shopify-rendered dynamic checkout button internals are untouched.

## Active Experiment — Notebook Paper (2026-06-07, expanded 2026-06-09)
There is an in-progress, **unpublished, non-live** visual experiment that reskins pages as a notebook / legal-pad paper look (ink-on-paper text over pale ruled yellow). **Current notebook proof pages: Club Hub / Moniker (`/pages/club-hub`), Life/Style (`/pages/the-collective`), Products (`/collections/master-collection`), Narrative (`/pages/narrative`), Bad Music (`/pages/2025`), Bad Music 2025 album (`/pages/badmusic-2025`), and Archive (`/collections`).**
- **Branch:** `experiment/notebook-paper-background`
- **Baseline tag (pre-experiment known-good):** `baseline-before-notebook-bg`
- **Experiment theme:** `186464731416` — **"Notebook Paper Experiment - DO NOT PUBLISH"**
- **Checkpoint commits:** `2a54e79` (initial proof) → `experiment: checkpoint club hub notebook nav` (2026-06-08, mask-based nav accepted) → `experiment: checkpoint club hub notebook margins` (2026-06-08, right blue rule + even margins + cart nudge accepted) → `experiment: checkpoint club hub notebook doodles` (2026-06-08, decorative doodle composition + dollar swap + footer/paper-ending band accepted) → `experiment: checkpoint notebook page polish` (2026-06-09, Club Hub MONIKER active glow + side-margin doodles + Life/Style notebook enablement / header doodles / responsive star behavior / title-divider+accent-dash removal accepted).
- **File map (2026-06-09):** `snippets/club-hub.liquid` holds the Club Hub notebook paper, the 10-doodle composition (8 original + 2 side-margin), the **MONIKER active glow** (Phase 3.17: `.ak-clubhub-page .ak-top-tile.is-active::before` → purple `#ff33ff` wordmark + triple drop-shadow magenta halo, paint-only, no layout shift), and the **side-margin doodles** (Phase 3.18). `snippets/ak-notebook-lifestyle.liquid` is the **Life/Style notebook host** (Phases 4.0–4.7): rendered from ONE repurposed spare custom-liquid render slot in `templates/page.json` (`custom_liquid_izr6z3`, previously a disabled fastbanner, now `{% render 'ak-notebook-lifestyle' %}`, `padding_top` 0). It adds the page class `.ak-lifestyle-page` via a **guarded hook** — only when a Life/Style module root (`#akmod-adventures-yt` / `#ak-kidradio-holo` / `#ak-chan-holo`) exists in the DOM — so other pages sharing the default `page.json` are never affected.
- **Club Hub side-margin doodles (2026-06-09):** two new spans in the existing `.ak-clubhub-doodles` wrapper — left **eyes** (t640, beside the member card, outside the red rule) and right **cross** (t845, outside the blue rule; deliberately below the FIXED cart's max-scroll viewport slot so they never coincide at any scroll position on a 900px-tall window) — width `clamp(64px,5.8vw,90px)`, individually rotated, **gated to `min-width:1200px`** because the outer strip (`--ak-paper-side-gap`, 82px floor) is too tight below that for the rotated bounding box to clear the rules. Both inherit the multiply / pointer-events:none / page-anchored / ≤768px-hide base treatment; both duplicate assets are differentiated by placement, size, and rotation per the duplicate-doodle rule.
- **Life/Style notebook state (2026-06-09):** legal-pad paper backdrop painted behind the existing dark glass/video modules (scrapbook look — **NOT a module reskin**; module internals untouched); mask-PNG nav + ink header/cart/footer ported from the accepted Club Hub treatment and re-scoped to `.ak-lifestyle-page`; **active LIFE/STYLE wordmark purple + magenta glow** (Phase 4.3 Option B — Option A ink-plus-halo was measured too subtle on yellow paper). A **13-span page-specific doodle map** (`.ak-lifestyle-doodles` — its own composition, NOT the Club Hub map): 3 header doodles (hdr-left star / hdr-mid / hdr-right) + 10 lower/sidebar marks, with **responsive gates**: lower/sidebar 8 hidden ≤1399px; chan-l/chan-r/hdr-mid/hdr-right hidden ≤1279px, leaving **star-only (hdr-left) in the 1024–1279px band**; everything hidden ≤1023px/mobile. **Title cleanup accepted:** the full-width faint banner divider lines under all three section titles are removed (Phase 4.6 `border-bottom:none`), and the small amber/magenta accent-underline dashes under Adventures/Kid Radio are removed (Phase 4.7 `background:none`, the 60×2 box kept so zero layout shift). Title text, ambient title animations (`akAdvFlicker`/`akKrSignal`/`akChanTuneIn`), YouTube embeds, glass cards, 3D tilt, wave bar, nav routes, and footer CTA are preserved.
- **Products notebook proof (2026-06-09, checkpoint `experiment: checkpoint products notebook proof`):** the PRODUCTS nav route is **`/collections/master-collection`**, rendered by the **shared default `templates/collection.json`** (also serving `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve`). `snippets/ak-notebook-products.liquid` is the **Products notebook host** (Phase 5.0), rendered from one repurposed spare slot (`custom_liquid_Jdbfj4`, previously a disabled fastbanner) behind the **load-bearing Liquid handle guard** `{% if collection.handle == 'master-collection' %}` — **the guard must stay; sibling collections must never receive notebook styling** (verified: they render no snippet/class and stay black). The snippet adds `.ak-products-page` to `<html>` and carries: the accepted paper, the mask-PNG nav + ink header/cart/footer port, the **PRODUCTS active wordmark purple `#ff33ff` + glow**, **ink product titles** (`#15120b`, magenta hover/focus, color-only), and the **v3 product cutout shadow** — `drop-shadow` on the **img** (true transparent-PNG silhouette alpha) with **BOTH `.card__media` AND `.media` opened to `overflow:visible`** (page-scoped; Dawn's `.card__media` in `component-card.css:77` is the real outer clipper — v1 on `.media` and v2 with only `.media` opened were both rejected as clipped; v3 is the visually approved structure). **No product scaling. No doodles on Products yet** (deferred until paper + shadow + font direction are evaluated). **Do NOT edit `sections/main-collection-product-grid.liquid`, `snippets/card-product.liquid`, or `assets/component-card.css` for this proof** — all Products notebook styling lives in the snippet. Known accepted asset limits: the FreeVitaly hem and combo right edge are cropped at the source-PNG edge (soft straight shadow band; fixable only by re-exported PNGs).
- **Narrative notebook Stage 1 (2026-06-12, checkpoint `experiment: add narrative notebook proof`, OWNER VERDICT PENDING):** `/pages/narrative` (the fake-Twitter feed → `templates/page.narrative.json` → active feed section `custom_liquid_7HLMyz`, ~1,139 lines inline CSS/HTML/JS, **NOT edited**) is the FOURTH notebook page. **Host: `snippets/ak-notebook-narrative.liquid`** (the house pattern), rendered from the repurposed disabled spare fastbanner slot **`custom_liquid_izr6z3`** behind a `{% if page.handle == 'narrative' %}` Liquid guard; the snippet adds `.ak-narrative-page` to `<html>` via the standard one-line classList hook (the ONLY JS — feed JS untouched). Carries the accepted paper + mask-PNG nav + ink header/cart/footer + **NARRATIVE active wordmark purple `#ff33ff` + magenta glow**. **CRITICAL SPECIFICITY NOTE:** the active feed section declares its own `html,body{background:#000 !important}` LATER in the DOM than the snippet, so the paper rules are written as `html.ak-narrative-page` / `html.ak-narrative-page body` — they win on specificity, not order; do not "simplify" them to bare `body`. **Fonts:** duplicated scoped `@font-face` pair (Due Date + Tenada `unicode-range:U+23`); **tweet text (`.ak-tweet-text`) + composer input (`#akTweetInput`) compute `'AK Tenada','AK Due Date',Impact,…`** — every `#` on the page (23 hardcoded `baseTweets` + user-typed posts) is a plain text node, so Tenada serves all of them with zero DOM wrapping/JS; **`.ak-handle` and `.ak-time` deliberately STAY Impact** (Due Date's cmap lacks `@` — every `@AMERIKID.CA`/`@YOU` handle — and `·`, the JS time separator; fontTools-verified); footer CTA = Due Date (**Stage 3** of the footer-font lane, all four notebook pages now carry it). **Material decision:** the glow-box / tweet cards / composer keep their dark-glass material over the paper (the Life/Style scrapbook precedent); at runtime the **outer glow-box (`rgba(0,0,0,.55)`) and composer bar (`rgba(10,6,18,.65)`) read muddy-olive over the yellow** (the tweet cards themselves stay acceptably dark) — a page-scoped alpha raise (~0.85–0.92) is the documented candidate fix, **NOT applied; separate owner-review lane**. NO doodles yet (standing rule). Posting behavior verified live post-push: Enter + button posts, typing-loader stays on top, dots animate, scroll-reveal both directions, orphan `#akTweetContainer` untouched, `akfeed-field` inert. **Doc correction recorded:** the feed's `*` reset is wrapper-scoped (`.ak-twitter-feed-wrapper, .ak-twitter-feed-wrapper *`) in current code — earlier "global `*` reset" notes were stale.
- **Narrative notebook Stage 1.5 — Feed Material De-Mudding (2026-06-12, checkpoint `experiment: refine narrative feed material`, IMPLEMENTED + owner-approved direction):** the documented muddy-olive read on the outer feed shell + composer was fixed with a **CSS-only, page-scoped pass in `snippets/ak-notebook-narrative.liquid` ONLY** — **Direction B "Dark Ink Sticker Hybrid"**, chosen from a read-only DevTools prototype over the larger light-sticker-feed rewrite (A) and the paper-note-card conversion (C). Root cause: `.ak-glow-box` (`rgba(0,0,0,0.55)` + `backdrop-filter:blur(10px) saturate(1.4)`) and `.ak-input-wrapper` (`rgba(10,6,18,0.65)` + `blur(10px)`) sit directly on the yellow paper, so the translucent dark glass + `saturate` averaged the yellow into the material → khaki/olive. Fix: raise both to `rgba(13,9,20,0.93)`, reduce `backdrop-filter` to `blur(2px)` (drop the `saturate`), add `border:2px solid #14110a`, and add a HARD offset paper shadow via **`filter:drop-shadow(...)` (7px glow-box / 5px composer)** — drop-shadow NOT `box-shadow`, **so the feed's `akBoxPulse` box-shadow breathing on `.ak-glow-box` survives untouched**; `.ak-tweet` firmed to `rgba(8,5,14,0.82)` + ink bottom hairline. **No `!important`** — the active feed section (`custom_liquid_7HLMyz`, NOT edited) declares those three selectors WITHOUT `!important`, so the page-scoped two-class selectors win on SPECIFICITY (0,2,0 > 0,1,0). The three changed selectors are `.ak-narrative-page .ak-glow-box`, `.ak-narrative-page .ak-input-wrapper`, `.ak-narrative-page .ak-tweet`. **NO text/handle/time/name/button recolor, NO size/spacing/tweet-order/keyframe change; `.ak-handle`/`.ak-time` stay Impact; the Due Date/Tenada stack, posting JS, typing-loader, scroll-reveal, inert `akfeed-field`, and orphan `#akTweetContainer` are all untouched; no doodles.** This is NOT the light-feed rewrite and NOT the paper-note-card conversion (both evaluated and not taken). Verified live at 1440/1024/768/375/320: olive gone, paper stays yellow, `akBoxPulse` intact, posting/dots/reveal/footer-CTA/drawer/cart all work, no overflow, console baseline; full 13-route notebook leak battery clean. **Revert: delete the labeled `STAGE 1.5 — FEED MATERIAL DE-MUDDING` block in the snippet.**
- **Bad Music notebook (Stage BM-1) (2026-06-15, checkpoint `experiment: add bad music notebook`, IMPLEMENTED):** Bad Music (`/pages/2025` → `templates/page.badmusic.json`) is the **FIFTH notebook page**. Host: `snippets/ak-notebook-badmusic.liquid`, rendered from the repurposed disabled spare fastbanner slot `custom_liquid_RnLA6b` (position 3, right after `ak-top` — early, no FOUC; the late `custom_liquid_izr6z3` was NOT used) behind a `{% if page.handle == '2025' %}` guard; adds `.ak-badmusic-page`. "Scrapbook / taped media on paper" (dark modules stay dark ON the paper, Life/Style precedent — NOT a module reskin). Carries: paper (yellow + rules + bands; `html.ak-badmusic-page` beats the global `#000` net; `#MainContent`/`.gradient`/`.shopify-section`/`.ak-top-wrap` forced transparent → paper on desktop+mobile, no black top band); mask-PNG ink nav + header/cart port with **BAD active `#ff33ff` + glow** (BAD is the active tile; glow from base ak-top); MENU trigger ink; **footer CTA Due Date**; **"BAD MUSIC" heading → ink Due Date** (inline styles overridden with `!important`, content preserved); **right "Coming Soon" case de-mudded** (`.ak-cas--soon .ak-cas__case` opacity .72→1; link/MP4/IO untouched); **red banner kept red + scrolling as a taped strip** (ink borders + offset, NO rotation, copy untouched); **TV hero kept `#000`** (filmstrip; player/YouTube/scanlines/caption untouched). **No doodles** (Stage BM-2 next). `sections/ak-top.liquid`, `sections/ak-footer.liquid`, `sections/ak-bm-album-link.liquid` (cassette behaviour) untouched; `/pages/badmusic-2025` + individual archive pages stay dark. Verified live + full leak battery clean. **Revert: delete the snippet + restore `custom_liquid_RnLA6b` to disabled fastbanner.** **(Two BM-1 specifics below were SUPERSEDED by BM-1.1 — the "red banner kept red + scrolling as a taped strip" was REMOVED, and the "TV hero kept `#000`" was DE-SLABBED to transparent; see the BM-1.1 bullet.)**
- **Bad Music notebook Stage BM-1.1 — visual correction (2026-06-15, checkpoint `experiment: bad music correction`, IMPLEMENTED):** owner-feedback correction of BM-1 (BM-1 was **technically clean / passed QA but visually left two wrong areas**). Two surgical fixes, code in `templates/page.badmusic.json` + `snippets/ak-notebook-badmusic.liquid` ONLY. **(1) Red moving banner REMOVED** by setting `"disabled": true` on section `custom_liquid_JpKeak` in the template (NOT CSS-hidden; the block + `order` entry are kept for a clean revert) — the now-dead BM-1 `#akrb` "taped strip" CSS was deleted from the snippet. **(2) TV "2025 (The Album)" full-width black slab REMOVED** via a labeled page-scoped `STAGE BM-1.1 CORRECTION` block: `#amerikid-tv-hero` → `background:transparent; min-height:0; padding:clamp(24px,4vw,64px) 0` (was `#000`/~100svh), `.ak-caption` → ink `#15120b`, and a subtle hard offset paper shadow on `.ak-tv` (`box-shadow:14px 14px 0 0 rgba(21,18,11,0.85); border-radius:6px`, reduced to `7px 7px` in a page-scoped `@media (max-width:480px)`). The TV now sits on the notebook paper. **The ACTUAL TV/video/player is preserved** — `.ak-tv` markup/JS (player facade, YouTube, scanlines, static, play button, caption text) untouched; only paint/spacing changed (live-verified: clicking the screen still builds the YouTube iframe + OSD "PLAYING"). **HARD HAZARD: never add width/max-width/layout constraints to `#amerikid-tv-hero`/`.ak-bg`/`.ak-stack`/`.ak-tv` and never dispatch resize / rely on resize recompute — the TV sizing/resize path can HANG THE RENDERER (the "constrain widths" Option A was tried + rejected).** Only background/min-height/padding/caption-color/box-shadow/border-radius are safe. `sections/ak-top.liquid`/`ak-footer.liquid`/`ak-bm-album-link.liquid`, the TV custom-liquid section, cart/product/Mystery Box/Archive, and `/pages/badmusic-2025` UNTOUCHED. `/pages/badmusic-2025` stays dark but is the **required next scope**. No doodles (Stage BM-2 pending). Live (127.0.0.1:9292 dev preview, desktop 1440/1280/1024 + fresh mobile 390/375/320) + 15-route leak battery clean. **Revert BM-1.1: remove `"disabled": true` from `custom_liquid_JpKeak` (banner returns) + delete the `STAGE BM-1.1 CORRECTION` block (TV returns to its #000 slab).**
- **Bad Music 2025 album page notebook (Stage BM2025-1) (2026-06-15, checkpoint `experiment: bad music 2025 notebook`, IMPLEMENTED):** `/pages/badmusic-2025` (the BAD Vinyl album / CD-player page, linked from the left album case on `/pages/2025` → `templates/page.badmusic-2025.json` → active section `custom_liquid_QcCtY6`, ~37KB inline, **NOT edited**) is the **SIXTH notebook page**, using read-only-prototype **Option B — dark record-sleeve/player cards on notebook paper**. **No spare disabled host slot existed** here (the only disabled section, `ak-collection-bg`, is a particle canvas, NOT reused), so **a NEW host slot was added**: `custom_liquid_bm2025host` in the template (`{% if page.handle == 'badmusic-2025' %}{% render 'ak-notebook-badmusic-2025' %}{% endif %}`), placed in `order` **immediately after `ak-top`, before `custom_liquid_QcCtY6`** (early → no FOUC). New host `snippets/ak-notebook-badmusic-2025.liquid` adds **`.ak-badmusic-2025-page`** (own class, distinct from `/pages/2025`'s `.ak-badmusic-page` — different DOM). Carries: paper (yellow + rules + bands; `html.ak-badmusic-2025-page` beats the global `#000`; `#MainContent`/`.gradient`/`.shopify-section`/`.ak-top-wrap` transparent → no black top band); mask-PNG ink nav + header/cart with **BAD wordmark purple `#ff33ff` + glow AT REST** (BAD is NOT `.is-active` here — ak-top keys active off `request.path '/pages/2025'` — so BAD is page-scoped as the section indicator; other tiles ink, hover→purple); MENU trigger ink; **footer CTA Due Date**. **`#bad-vinyl` de-slabbed** (`background:transparent; min-height:0; padding`; own `overflow:hidden` left intact → clips card offset shadows); `.cinema-bars` (black letterbox) + inert `.bv-field` canvas hidden. **Intro inked:** huge "2025" → ink `#15120b` Due Date (stroke/glow removed); "BAD MUSIC" kicker + "THE ALBUM — 12 TRACKS" sub inked; accent rule inked (content/sizes/layout preserved). **Cards (Option B):** `.vinyl-card` → opaque `rgba(13,9,20,0.93)` + `2px solid #14110a` + hard offset shadow `8px 8px 0 0 rgba(21,18,11,0.85)` (was translucent `rgba(12,12,12,.6)` → muddy-olive on yellow); magenta border on `.vinyl-card.is-playing`; **inner card text NOT recolored** (stays white on the dark card). **The BAD Vinyl player is preserved exactly** — `custom_liquid_QcCtY6` HTML/CSS/JS untouched (audio, play/pause, single-track-at-a-time, `vinylSpin`/`startSpin`, `initDiscTilt`, `enforceCols`, seek/scrubber drag, progress ring `--p`, `hyperLaunch` FX, hover-preload `audio.load` guard, track previews). **HARD HAZARD: never add width/max-width to `#bad-vinyl`/`.vinyl-grid`/`.vinyl-card`/`.vinyl-disc` (they feed `enforceCols` + the disc-tilt `getBoundingClientRect`); paint/spacing only.** No doodles (later stage). `sections/ak-top.liquid`/`ak-footer.liquid`, `templates/page.badmusic.json`, `snippets/ak-notebook-badmusic.liquid`, `sections/ak-bm-album-link.liquid`, cart/product/Mystery Box/Archive UNTOUCHED; `/pages/2025` BM-1.1 unchanged. Live (127.0.0.1:9292) at 1440/1280/1024/768 + fresh 390/375/320 + 15-route leak battery clean; player live-verified (single-track + seek + progress). **Revert: delete the `custom_liquid_bm2025host` section + its `order` entry in `templates/page.badmusic-2025.json` + delete the snippet.**
- **Bad Music notebook typography polish (Stage BM-TYPO-1) (2026-06-15, checkpoint `experiment: bad music typography polish`, IMPLEMENTED):** CSS-only, **font-family-ONLY** pass (no color/size/layout/spacing/markup/animation change) closing the remaining Impact pockets on BOTH Bad Music pages, page-scoped in the two notebook snippets ONLY (labeled `STAGE BM-TYPO-1` block each). **/pages/2025:** `.ak-caption` "2025 (The Album)" → **Due Date** (BM-1.1 ink `#15120b` unchanged). **/pages/badmusic-2025:** added a scoped `@font-face 'AK Tenada'` (`unicode-range:U+23`, reusing the existing `assets/ak-tenada.woff2`); `#bad-vinyl .vinyl-title` track titles → **`'AK Tenada','AK Due Date',Impact,…'`** (`#`→stencil Tenada, words→Due Date; every `#` is a plain text node → ZERO DOM wrapping, the Narrative/Products mechanism; **white card colour unchanged**); `.bv-intro__kicker` + `.bv-intro__sub` + `.vinyl-artist` → **Due Date**. **KEPT Impact deliberately:** `.time-row` (Due Date lacks `:`/`•`), the `.badge` play/pause icon, and ALL cassette micro-text (`sections/ak-bm-album-link.liquid` NOT edited — a page-scoped snippet override exists but the dark cassette micro-labels read best in Impact at 8–22px). `custom_liquid_QcCtY6` + all BAD Vinyl player JS untouched (CSS-only). **KNOWN COSMETIC CAVEAT (owner verdict pending):** Due Date is wider than Impact, so on desktop the long titles wrap more — `#FREEVITALY`/`#FASHIONING` 1→2 lines (an orphan letter), `#SUMMER OF THE HOODIE` 2→3 lines (taller cards); NO clipping/overflow at any width; mobile titles (24–28px) wrap fine. Inherent to the wider font; an OPTIONAL later `letter-spacing`/`font-size` micro-tune on long `.vinyl-title`s would reduce it (out of this font-only stage's scope). Live (127.0.0.1:9292) 1440/1280/1024/768 + fresh 390/375/320; player live-verified (single-track/seek/progress); 15-route leak battery clean (`STAGE BM-TYPO-1` only on the 2 Bad Music pages; `ak-tenada.woff2` only on Products/Narrative/badmusic-2025). **Revert: delete the `STAGE BM-TYPO-1` block in each snippet (Tenada `@font-face` is inside the 2025 snippet's block).** **(The BM-TYPO-1 long-title wrap caveat was RESOLVED by BM-TYPO-2 below.)**
- **Bad Music typography micro-tune (Stage BM-TYPO-2 — long-title wrap fix) (2026-06-15, checkpoint `experiment: tune bad music title wraps`, IMPLEMENTED):** fixes the awkward desktop/tablet wrapping BM-TYPO-1's wider Due Date font caused on `/pages/badmusic-2025`. CSS-only, **`.vinyl-title` ONLY**, ONE labeled `STAGE BM-TYPO-2` block appended to `snippets/ak-notebook-badmusic-2025.liquid`: `@media (min-width:768px){ .ak-badmusic-2025-page #bad-vinyl .vinyl-title{ font-size:clamp(40px,4.8vw,66px) !important; letter-spacing:-0.01em !important; line-height:.92 !important; } }` — trims the desktop title from the base 72px (`clamp(28px,6vw,72px)`) to 66px@1440 and condenses Due Date's wide tracking slightly so `#FREEVITALY`/`#FASHIONING` fit 1 line and `#SUMMER OF THE HOODIE` a clean 2 lines, with short titles still bold (picked from a live 4-candidate DevTools comparison). **BM-TYPO-1 preserved:** the `'AK Tenada','AK Due Date',…'` stack (`#`→Tenada, words→Due Date), white-on-dark colour, card material, kicker/sub/artist Due Date, `.time-row` Impact — all unchanged (this is size/tracking/line-height ONLY, no font-family/color/markup). Scoped to `min-width:768px` (the 2-column grid where the wrap occurs); mobile ≤767px (1-column) keeps the base sizing (already fits); the clamp 40px floor protects the narrow 768px cards. NO width/max-width on `#bad-vinyl`/`.vinyl-grid`/`.vinyl-card`/`.vinyl-disc`; `custom_liquid_QcCtY6` + player JS untouched. Live (127.0.0.1:9292) 1440/1280/1024/768 (tune applies, long titles fit) + fresh 390/375/320 (base, unchanged); player live-verified; leak clean (`STAGE BM-TYPO-2` only on `/pages/badmusic-2025`). **Revert: delete the `STAGE BM-TYPO-2` block (titles return to BM-TYPO-1 sizing).**
- **Bad Music doodle pass (Stage BM-DOODLE-1 — "mixtape margins") (2026-06-15, checkpoint `experiment: add bad music doodles`, IMPLEMENTED):** both Bad Music pages join the doodled notebook pages (Club Hub + Life/Style + Products + Narrative). Decorative ink-doodle margin layer using **EXISTING assets ONLY** (hashtag/star/dollar/kid `…/files/ak-doodle-*.png?v=1780967556`; the `#`-title + `$` music subset — distinct from the other four maps), ONE labeled `STAGE BM-DOODLE-1` block + one `aria-hidden` wrapper per snippet. **PAGE-ANCHORED** (`#MainContent{position:relative}`, absolute, `z-index:0`, `pointer-events:none`, `mix-blend-mode:multiply`, behind content; `.ak-footer`+`#bad-vinyl` lifted to `z-index:1` so content occludes strays — z-index is paint-only, not a width/layout constraint; the layer is a sibling of `#bad-vinyl`, never inside its `overflow:hidden`). **/pages/2025 = 5 marks** (top band ×2, cassette-shelf L/R gutters, footer band); **/pages/badmusic-2025 = 6 marks** (top band ×2, record-grid L/R gutters ×3, footer band). **Width gates (tuned because the cassette shelf is 94vw/max-1060 and the grid is max-1200 — their side gutters only open wide enough above 1200):** `/pages/2025` cassette-side ≥1366 / top+footer ≥1024; `/pages/badmusic-2025` grid-side ≥1440 / top+footer ≥1024; **whole layer hidden ≤1023** (no mobile/tablet). Measured ZERO overlap with the TV/caption/heading/cassettes/album-link/Coming-Soon/CTA/cart/nav (page 1) and the vinyl cards/discs/seek/titles/intro/CTA (page 2); hit-tests confirm the TV screen + vinyl discs stay reachable; player + drawer + cart + album link live-verified intact. **No width/max-width on `#amerikid-tv-hero`/`.ak-bg`/`.ak-stack`/`.ak-tv`/`#bad-vinyl`/`.vinyl-grid`/`.vinyl-card`/`.vinyl-disc`; `custom_liquid_QcCtY6`/player JS/sections/templates untouched; no new assets.** Live (127.0.0.1:9292) + 13-route leak battery clean (each page's wrapper only on its own route). **Revert: per page delete the `<div class="ak-badmusic[-2025]-doodles">` wrapper + the `STAGE BM-DOODLE-1` block (+ the `#MainContent{position:relative}`/`z-index:1` lift lines).** *(Future option, NOT this pass: commission new hand-drawn music doodle assets — cassette/record/note/headphones/play-triangle — for a literal music-room look, needing an upload pass.)*
- **Bad Music doodle scale correction (Stage BM-DOODLE-2) (2026-06-15, checkpoint `experiment: enlarge bad music doodles`, IMPLEMENTED):** owner said the BM-DOODLE-1 doodles were too small vs Club Hub. CSS-only size/position/gate update to the existing doodle blocks (no new assets). **Club Hub reference: its main doodles render ~155–249px @1440; BM-DOODLE-1 was ~50–88px.** BM-DOODLE-2 raises the Bad Music doodles to **Club-Hub scale (~150–211px @1440** via the `--s` clamps), moving the big ones into the roomier open paper (top header band + footer band) and the side gutters. **Off-LEFT bleed (negative `left:`) is measured-SAFE (clipped → no overflow); off-RIGHT bleed CAUSES overflow** — so left-side doodles bleed partly off the left margin for scale, right-side presence comes from the top-right + footer-right zones (page-2 `hash-grid-right-mid` → moved to footer `hash-footer-right`). **Footer doodles capped ~150px** so the 158px footer band contains them clear of the cassettes/grid above. **Gates retuned for the bigger doodles: whole layer hidden ≤1199px; top+footer doodles ≥1200px; cassette-side (page 1) AND grid-side (page 2) doodles ≥1440px.** Still decorative/`aria-hidden`/`pointer-events:none`/multiply/page-anchored/behind content; players + TV + nav + cart + discs all live-verified intact (hit-tests reachable); no overflow; no new assets; no JS/section/template/width-constraint edits. Live (127.0.0.1:9292, 1536→320) + 12-route leak battery clean. **Revert: restore the BM-DOODLE-1 `--s`/position/gate values in each snippet's doodle block (or delete the wrapper + block).**
- **Archive notebook paper (Stage ARCH-1, paper-only) (2026-06-16, checkpoint `experiment: archive notebook paper`, IMPLEMENTED):** the main Archive route **`/collections`** (`templates/list-collections.json` → visible section `sections/ak-archive.liquid`, the v6 cassette-case folder-deck) is the **SEVENTH notebook page**, using the accepted **"dark media ON the paper"** direction — the cassette deck stays a DARK object laid on the yellow legal-pad paper (NOT a deck reskin; the prototype read it as "cassettes on a notebook page"). **No spare/disabled custom-liquid host slot existed** (list-collections.json had ZERO custom-liquid sections), so **a NEW host slot was added**: `custom_liquid_akArch1` in the template (`{% if template contains 'list-collections' %}{% render 'ak-notebook-archive' %}{% endif %}` — the `template` route guard, since a collections-list route has no `page.handle`), placed in `order` **right after `ak-top`, before `ak-archive`** (early → no flash of the dark deck). New host `snippets/ak-notebook-archive.liquid` adds **`.ak-archive-page`** to `<html>` (one-line classList hook; the only JS). Carries: paper (yellow + red/blue margin rules + ruled lines + bands; `html.ak-archive-page` beats the global `#000` net); **the deck declares opaque `#000` on TWO elements — the section wrapper `.ak-archive-section` (schema `"class"`) and the inner full-viewport stage `.ak-archive` — both forced transparent (PAINT-ONLY; `.ak-archive` keeps `height:100vh`/`overflow:hidden`/flex)** so the paper shows behind the cassette fan; `#MainContent`/`.gradient`/`.shopify-section`/`.ak-top-wrap` transparent → no black top band desktop+mobile; the inert (guard-disabled) particle canvas `.ak-archive-canvas` hidden (`display:none`; element stays in DOM → section `getElementById` + early-return path unchanged). Mask-PNG ink nav + header/cart with **ARCHIVE wordmark purple `#ff33ff` + magenta glow** (ARCHIVE IS `.is-active` here — a MASKED tile, so its `::before` is painted by the shared `.ak-top-tile.is-active::before` rule, the same proven mechanism as NARRATIVE-active; also surfaces in the mobile drawer ARCHIVE link); MENU trigger ink; **footer CTA Due Date**. **Archive arrows `.ak-archive-prev`/`.ak-archive-next`** → PAINT-ONLY ink-tinted dark chips (`rgba(21,18,11,.62)` rest / `.78` hover, purple hover border) so the white chevron keeps contrast on yellow in rest AND hover (the base near-transparent hover would wash out on paper); **arrow geometry (position/size/z-index/transform) untouched**. **Cassette cards stay DARK** (`.ak-archive-card-inner` already opaque `#111` + cassette-skin gradient → no muddy-translucent problem, no de-mud); **card title `#fff` NOT recolored**. **No doodles** (deferred — the deck is a full-viewport `overflow:hidden` surface and the arrows occupy the L/R gutters; if ever wanted, limit to the header/footer bands outside the deck). **HARD HAZARD: never add width/max-width/layout constraints to `.ak-archive`/`.ak-archive-section`/`.ak-archive-stage`/`.ak-archive-card`/`.ak-archive-card-inner`, and never touch the section JS** — `sections/ak-archive.liquid` is byte-identical (spring physics, drag/tap, arrows, keyboard, the 18 `data-href` links, the particle guard, the cassette skin, deck geometry all preserved; the deck is a spring-physics drag surface that reads `getBoundingClientRect`). `sections/ak-top.liquid`/`ak-footer.liquid`, individual collection pages, products, cart, Mystery Box UNTOUCHED. Live (127.0.0.1:9292) at 1440/1280/1024 + fresh 390/375/320: paper behind the deck (no black band), `.ak-archive` `overflow:hidden` preserved, ARCHIVE `::before` `rgb(255,51,255)`, 18 cassettes dark + titles white, arrows ink chips, footer reachable below the 100vh deck with the Due Date CTA, no horizontal overflow; **deck behavior live-verified (NEXT/PREV arrows, keyboard ArrowRight/Left, vertical fling drag→advance, short drag snap-back, tap→`data-href` navigation);** 14-route leak battery clean (`ak-archive-page` only on `/collections`; individual collections + the other 6 notebook pages + home/cart/product/Mystery Box carry no archive marker; product ATC + Shop Pay render). **Revert: delete the `custom_liquid_akArch1` section + its `order` entry in `templates/list-collections.json` (restores the 4-section dark Archive) + delete `snippets/ak-notebook-archive.liquid`; `sections/ak-archive.liquid` never changes either way.**
- **Archive doodles (Stage ARCH-2) (2026-06-16, checkpoint `experiment: add archive doodles`, IMPLEMENTED — owner-approved after ARCH-1):** the Archive page joins the doodled notebook pages (Club Hub + Life/Style + Products + Narrative + both Bad Music pages). CSS+markup ONLY in `snippets/ak-notebook-archive.liquid` (ONE labeled `STAGE ARCH-2` block + ONE `<div class="ak-archive-doodles" aria-hidden>` wrapper of 5 `<span class="ak-ad">`); **`templates/list-collections.json` NOT changed** (the ARCH-1 host slot already renders the snippet). **Concept = "archive labels / storage-box marginalia / catalog marks"** — EXISTING ink assets only (star/hashtag/cross/kid `…/files/ak-doodle-*.png?v=1780967556`; **NO dollar**, deliberately distinct from the Bad Music `$`+`#` music subset; no new/AI assets, no uploads). **5 marks, OUTER paper zones ONLY:** `star-top-left` + `hash-top-right` (top header band, the hash sits below the fixed cart), `star-edge-left` (off-LEFT paper edge in the open band between the nav and the deck top — duplicate star differentiated by placement/size/rotation), `kid-footer-left` + `cross-footer-right` (footer band, flanking the CTA). **Page-anchored** (absolute inside `.ak-archive-page #MainContent{position:relative}`, scrolls with the paper — NOT viewport-fixed), `pointer-events:none`, `mix-blend-mode:multiply`, `z-index:0` BEHIND content (`.ak-archive-section` + `.ak-footer` lifted to `z-index:1` — **paint-only z-index, NOT a width/layout constraint**; the layer is a SIBLING, never inside `.ak-archive`'s `overflow:hidden`; deck geometry byte-untouched). **NEVER over the deck / cards / stage / arrows / nav / KID logo / cart / MENU / footer CTA** (getBoundingClientRect overlap math = ZERO at every shown width). **Restrained-large scale** (the "doodles too small" lesson): mains `clamp(126–128px,9.4–9.6vw,150–170px)`, accents `clamp(106–120px,7.7–8.6vw,136–156px)` (footer pair capped ~150/136 for the ~157px band). **Off-LEFT bleed only** (clipped → no overflow; off-RIGHT would overflow). **Gates:** whole layer hidden ≤1199px (no tablet/mobile doodles); off-left edge doodle ≥1440px; the 4 band doodles ≥1200px. **`sections/ak-archive.liquid` byte-untouched** (deck JS / spring / drag / tap / arrows / keyboard / 18 `data-href` links / particle guard preserved); deck behavior re-verified WITH doodles present (NEXT/keyboard/drag-dismiss/tap→`/collections/bad-music`; every doodle hit-test passes through → pointer-events:none confirmed). Verified at 1536/1440/1366/1280/1200 + fresh 1024/768/390/375/320; 14-route leak battery clean (wrapper/marks/class only on `/collections`; individual collections + the other 6 notebook pages + home/cart/product/Mystery Box carry no archive-doodle markup). **Revert: delete the `<div class="ak-archive-doodles">` wrapper + the `STAGE ARCH-2` block in the snippet (ARCH-1 paper returns doodle-free).**
- **Cart notebook paper (Stage CART-1, paper-only) (2026-06-16, checkpoint `experiment: cart notebook paper`, IMPLEMENTED):** the cart route **`/cart`** (`templates/cart.json` → `sections/main-cart-items.liquid` + `sections/main-cart-footer.liquid`) is the **EIGHTH notebook surface**, using the accepted **"dark objects ON the paper"** direction — the cart **item cards** (`.cart-item-card`) and the **checkout summary panel** (`.cart__footer`) stay DARK objects on the yellow legal-pad paper. **Paint-only, CSS-only, NO doodles** (cart is permanently out of doodle scope per the standing rule). **No new host slot:** the EXISTING **disabled `custom_liquid_z76fRN`** (the spare fastbanner already in `order` right after `ak-top`, before `cart-items` → early, no FOUC) was repurposed — `custom_liquid` set to `{% if template == 'cart' %}{% render 'ak-notebook-cart' %}{% endif %}`, `"disabled": true` removed, padding zeroed; **the `order` array and `cart-items`/`cart-footer` settings are unchanged.** New host `snippets/ak-notebook-cart.liquid` adds **`.ak-cart-page`** to `<html>` (one-line classList hook; the only JS). Carries: accepted paper (`html.ak-cart-page` beats the global `#000` net); `#MainContent`/`.gradient`/`.shopify-section`/`.ak-top-wrap` **and the items wrapper `.cart-glow-box` (which paints itself opaque `#000`)** forced transparent → no black band desktop+mobile and the `.cart-item-card`s float as dark objects on the paper; the two inert (already JS-disabled) cart canvases hidden page-scoped (`.ak-cart-field`/`.ak-cart-footer-field` `display:none`; **IIFEs NOT touched**). Mask-PNG ink nav + header/cart port re-scoped to `.ak-cart-page` (the Archive lineage). **`/cart` is NOT one of the seven nav destinations → NO tile is `.is-active` here, so NO wordmark glows** (every wordmark ink, hover/focus→purple; CDP-verified the ARCHIVE `::before` is ink `rgb(21,18,11)` on cart vs purple on `/collections`). **NO footer block / NO Due Date `@font-face`** — cart.json has **no `ak-footer`**, so there is no back-to-top CTA to restyle and no `#` → no Tenada. **De-mud — FRAGILE specificity:** cart cards via `.ak-cart-page .cart-item-card` (0,2,0) beat the section's `.cart-item-card` (0,1,0) at REST with **NO `!important`** so the section's `.cart-item-card:hover` (0,2,0, declared LATER in the DOM) still wins on hover (magenta lift preserved); the checkout panel needs the ID — `.ak-cart-page #main-cart-footer .cart__footer` (1,2,0) is **REQUIRED** to beat the section's `#main-cart-footer .cart__footer` (1,1,0). Both raised to `rgba(13,9,20,0.93)` + `2px solid #14110a` + hard offset paper shadow (`8px` desktop / `5px` ≤480px). Internal **green QTY pill, red REMOVE, white title/variant/price** (cards) and **purple estimated-total, magenta checkout button, dynamic checkout buttons, cart note, tax/shipping copy, discounts, `#cart-errors`** (panel) are NOT recolored — they sit on the dark objects and stay legible. Title bar ("CART"/Continue-shopping) + empty-cart copy ("DUDE BUY SOME SHIT."/"HAVE AN ACCOUNT?") inked `#15120b`; the purple Continue-shopping + LOG IN dark pills left as-is. **HARD PRESERVATION — `sections/main-cart-items.liquid` + `sections/main-cart-footer.liquid` are BYTE-IDENTICAL:** the cart `<form id="cart">`, `name="updates[]"` inputs, the qty change→`form.submit()` handler, `item.url_to_remove` remove links, `<button name="checkout" form="cart">`, dynamic/additional checkout buttons, cart note, discounts, `#cart-errors`, empty-cart logic, and the inert particle IIFEs are untouched (paint-only by specificity); `cart.js`/`quantity-popover.js`/`<cart-items>`, `ak-top.liquid`, `ak-footer.liquid`, `layout/theme.liquid`, component-cart CSS, products, Mystery Box, checkout, live theme, normal dev theme also untouched; NO width/max-width/layout constraints added. **Filled-cart QA done** (normal product `#FreeVitaly Longsleeve`, no checkout): item card + qty `updates[]` + native qty submit ($65→$130) + remove (→empty) + dynamic buttons verified; MENU drawer opens/closes; no overflow 1440/1280/1024/390/375/320; 11-route leak battery clean (`.ak-cart-page` only on `/cart`). **Revert: in `templates/cart.json` restore `custom_liquid_z76fRN` to its disabled fastbanner (`"disabled": true` + the original emoji-marquee custom_liquid from git history) + delete `snippets/ak-notebook-cart.liquid` (`git revert` for full restore); the two cart sections never change either way.**
- **Cart notebook typography (Stage TYPO-1A) (2026-06-16, checkpoint `experiment: tune cart notebook typography`, IMPLEMENTED):** **font-family-ONLY** follow-on to CART-1 (which was paint-only). **This SUPERSEDES the CART-1 "NO Due Date `@font-face`" note above** — TYPO-1A now declares one. From the typography audit, `/cart` mixed **Impact** (the `main-cart-items.liquid` `!important` force list), **Fjalla One** (Dawn's default heading font on the item cards + the entire checkout panel), and **Arial** (`.quantity-input`) — none the notebook voice. CSS-only, page-scoped to `.ak-cart-page`, in **`snippets/ak-notebook-cart.liquid` ONLY** (ONE labeled `STAGE TYPO-1A` block). Adds a scoped `@font-face 'AK Due Date'` reusing the EXISTING `assets/ak-due-date.woff2` (**NO new file / NO upload**) and moves the cart DISPLAY text to `'AK Due Date', Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif`. **`!important` ONLY on the 7 Impact-force-list selectors** (`.cart__empty-text`, `.cart__login-title`, `.title--primary`, `.underlined-link`, `.custom-continue-button`, `.cart__login-paragraph a`, `.cart__remove-link` — `.ak-cart-page X` 0,2,0 + `!important` beats the section's `X` 0,1,0 + `!important`); **NO `!important` on the 6 Fjalla-One selectors** (`.cart-item-title`, `.cart-meta-label`, `.cart-meta-value`, `#main-cart-footer .totals__total`/`.totals__total-value`/`.cart__checkout-button` — specificity alone beats Dawn's default). **Deliberately KEPT readable / NOT converted:** `.quantity-input` (typed number → Arial), `.tax-note` (legal copy → Fjalla One), `.cart-item-variant` (small meta → Fjalla One). **Tenada `unicode-range:U+23` for the leading `#` on product titles was evaluated at runtime and NOT added** — the `#` falls back to Impact and reads acceptably (the Bad-Music `$`-fallback precedent); a one-block follow-up can add `AK Tenada` (reuse `assets/ak-tenada.woff2`) + prepend it to the `.cart-item-title` stack for full `#`-stencil parity with Products if the owner wants it. **NO size/letter-spacing/line-height/layout/grid/flex/button-dimension/animation/markup change** (font-family only — verified no clipping/overflow at any width, so no micro-tune needed). **`sections/main-cart-items.liquid` + `sections/main-cart-footer.liquid` stay BYTE-IDENTICAL** (override by specificity); cart form/`updates[]`/qty-submit/remove/checkout/dynamic-buttons/note/discounts/`#cart-errors` + `cart.js`/`quantity-popover.js`/`<cart-items>` untouched; CART-1 paper + de-mud unchanged. **QA:** `AK Due Date` reports `status:loaded` on `/cart`; empty + filled (normal `#FreeVitaly Longsleeve`, no checkout) at 1440/1280/1024/390/375/320 — all targeted selectors compute AK Due Date, kept-readable stay Fjalla/Arial, no overflow, qty-submit ($65→$130)/remove/dynamic-buttons intact, test item removed; 11-route leak battery clean (`STAGE TYPO-1A`/`ak-cart-page` only on `/cart`). **Revert: delete the `STAGE TYPO-1A` block (incl. the `@font-face`) in `snippets/ak-notebook-cart.liquid` → fonts return to the CART-1 state (Impact + Fjalla One + Arial).**
- **Club Hub notebook typography (Stage TYPO-1B) (2026-06-16, checkpoint `experiment: tune club hub notebook typography`, IMPLEMENTED):** **font-family-ONLY** pass. Club Hub forces Impact GLOBALLY via `.ak-clubhub, .ak-clubhub *{ font-family:Impact… !important }` (near the top of `snippets/club-hub.liquid`). TYPO-1B adds ONE labeled `STAGE TYPO-1B` block to `snippets/club-hub.liquid` ONLY, moving the **visible DISPLAY tier** to `'AK Due Date', Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif`: **`.ak-clubhub-page .ak-id-title`** (member-ID title ~28px) + **`.ak-clubhub-page .ak-value`** (value lines ~18px). **`!important` is REQUIRED** (the global force is `!important`; the override wins on specificity — 0,2,0 > 0,1,0). **Reuses the `AK Due Date` `@font-face` already declared in this file** (same `assets/ak-due-date.woff2` — NO new file/upload). **Deliberately KEPT readable in Impact (NOT converted):** `.ak-id-photo-mark`, `.ak-id-hint`, `.ak-id-sub`, `.ak-id-btn` (LOG IN/JOIN), `.ak-id-photo-note`, `.ak-label`, and logged-in drops-form inputs/helper text (the Bad-Music meta precedent; the runtime check showed the remaining Impact does NOT dominate, so the default "small text stays readable" held). **Footer CTA `.ak-btt-text` was already Due Date and is UNCHANGED.** The global Impact force is **NOT removed** — only selectively overridden. **NO size/letter-spacing/line-height/spacing/layout/card/button/animation/doodle/markup/behaviour change** (the diff is one CSS comment + one rule; the drops form/captcha + LOG IN/JOIN buttons are untouched). **QA:** `AK Due Date` `status:loaded`; tally under `.ak-clubhub` = Due Date ×4 (title + 3 values) / Impact ×9 (small); no overflow at 1440/1280/390/375/320; doodles/paper/nav/footer-CTA unchanged. Logged-in drops state not runtime-tested (no customer session) — unchanged by construction (only `.ak-id-title`/`.ak-value` targeted). **Revert: delete the `STAGE TYPO-1B` block in `snippets/club-hub.liquid` → the title + value lines return to the global Impact.**
- **Narrative notebook Stage 1.9 — Doodle Map "Margin Reactions" (2026-06-12, checkpoint `experiment: add narrative doodle map`, IMPLEMENTED):** the Narrative page gets its own decorative doodle layer in `snippets/ak-notebook-narrative.liquid` (one `<div class="ak-narrative-doodles" aria-hidden>` + one labeled CSS block). **Concept = social-reaction marginalia** (hashtags/eyes/star/cross/kid), narrowed to the SOCIAL subset — **NO dice/dollar** — and DELIBERATELY DIFFERENT from Club Hub's diagonal scatter and Life/Style's edge rails (sparse + asymmetric). **7 marks:** 3 header (flank the KID logo, above the nav) + 3 feed-margin (outer strips, hashtag-led) + 1 bottom kid (footer band, left of the CTA, `bottom:`-anchored). Existing ink PNGs only (v=1780967556). **Page-anchored** (absolute inside `#MainContent{position:relative}`, scrolls with the paper), **behind content** (feed wrapper + footer lifted to `z-index:1`, doodle layer `z-index:0` → white cards occlude any stray; the feed wrapper's `isolation:isolate`/`overflow:hidden` respected as a sibling), `mix-blend-mode:multiply`, `pointer-events:none`. **Gates:** ≥1400 all 7; 1280-1399 header trio + kid; 1024-1279 far-left star only; ≤1023 whole layer hidden. Measured zero overlap with logo/nav/cart/composer/glow-box/CTA/copyright. Stage 1.7 material, Stage 1.8 input cleanup, handles, `.ak-time`, tweet cards, posting JS, and `custom_liquid_7HLMyz` are all untouched. **Revert: delete the `<div class="ak-narrative-doodles">` wrapper + the labeled `STAGE 1.9` CSS block.** Narrative now joins Club Hub + Life/Style + Products as a doodled notebook page (each its own page-specific map, per the standing doodle rules).
- **Narrative notebook Stage 1.8 — Feed Cleanup (2026-06-12, checkpoint `experiment: clean up narrative feed`, IMPLEMENTED — owner feedback):** cleanup of the Stage 1.7 white sticker feed, CSS-only in `snippets/ak-notebook-narrative.liquid`. **Removed** the three Stage 1.7 social pseudo cues (`.ak-input-wrapper::before` "NARRATIVE FEED", `.ak-input-wrapper::after` live-dot, `.ak-handle::after` verified ✔). **Handles** forest green `#1c7c2e` → official `#ff33ff` + `'AK Due Date',Impact,…` (the `@` falls back to Impact — Due Date's cmap lacks `@`; "AMERIKID.CA" in Due Date stencil; judged acceptable). **Input focus white-box fix:** root cause = Dawn's GLOBAL `:focus-visible{box-shadow:var(--focused-base-box-shadow)}` in `base.css` (solid white ring + dark halo); killed PAGE-SCOPED only and replaced with a magenta `:focus-within` active border + glow, magenta `::selection`, and an a11y-preserving magenta `#akTweetButton:focus-visible` (Dawn's ring also hit the button). **Dawn's global focus is NOT touched** (scoped to `.ak-narrative-page`; leak-verified other routes' focus rings unaffected). No `!important`. `#akTweetButton::after` ripple, `.ak-time` (Impact/warm-grey), the Due Date/Tenada tweet-text stack, `custom_liquid_7HLMyz`, and posting JS are all untouched; no doodles. Verified live at 1440/1024/768/375/320 + full 13-route leak battery. **Revert: restore the three deleted pseudo rules + the green handle, and delete the four focus rules.** **Doodles are the next separate pass (read-only analysis done; "Margin Reactions" social-subset concept proposed — distinct from Club Hub/Life-Style).**
- **Narrative notebook Stage 1.7 — Light Sticker Feed v3 (2026-06-12, checkpoint `experiment: add narrative light sticker feed`, IMPLEMENTED — owner-preferred):** the fake-Twitter feed was flipped from the dark sticker material to **pale printed STICKER LABELS on the notebook paper** — the owner's choice over the dark printed-overlay (Direction A) and the kraft-brown paper-slip variant after the read-only prototype. CSS-only, ONE appended labeled block in `snippets/ak-notebook-narrative.liquid`; it **OVERRIDES the committed Stage 1.5 dark material with higher specificity (`html.ak-narrative-page X` 0,2,1 > Stage 1.5 `.ak-narrative-page X` 0,2,0)** and leaves the Stage 1.5/1.6 lines intact so revert is a single-block delete. Shell → warm cream `#fdf3c9`; composer → `#fffdf3`; cards → near-white `#fffef8` + ink border + official-purple `#ff33ff` left tab + hard offset shadow. The magenta `akBoxPulse` is **neutralized PAGE-SCOPED** (`animation:none`+`box-shadow:none` on `html.ak-narrative-page .ak-glow-box` — it reads sickly on cream; the global `@keyframes` is NOT edited; paper depth comes from `filter:drop-shadow`). Tweet text white → ink `#14110a` (Due Date/Tenada stack + sizes untouched → `#` still Tenada); handles → forest green `#1c7c2e`, timestamps → warm-grey `#6b6358` (both **STAY Impact**); loader dots become ink on white (still blink); avatars paper-tinted; composer input/placeholder/button synced to the `#ff33ff` family for legibility on cream. Pseudo-only social cues inherit to new posts: "NARRATIVE FEED" header (`.ak-input-wrapper::before`) + magenta live-dot (`::after`) + magenta verified ✔ (`.ak-handle::after`); **no faux engagement row, no pinned tab** (kept clean). `.ak-tweet::after` hover glow left intact (no `overflow:hidden` added → not clipped). `--ak-purple` stays `#ff33ff`; no lavender; `custom_liquid_7HLMyz` + posting JS + DOM untouched; no new assets; no doodles. Verified live at 1440/1024/768/375/320, posting + new-post inheritance + leak battery all clean. **Revert: delete the labeled `STAGE 1.7 — LIGHT STICKER FEED V3` block (returns to the Stage 1.5/1.6 dark sticker feed), or `git revert`.**
- **Narrative notebook Stage 1.6 — Official Purple Sync (2026-06-12, checkpoint `experiment: sync narrative feed to official purple`, IMPLEMENTED):** the feed's off-brand **lavender** purple was synced to the **official AmeriKid purple `#ff33ff`** (the header/nav active hue), CSS-only in `snippets/ak-notebook-narrative.liquid` ONLY — **Prototype 1 (dark sticker + official purple)**; the white-sticker-feed v2 was prototyped and remains a valid larger alternative but was NOT implemented. The feed declares `--ak-purple:#c084fc` (`rgb(192,132,252)`, lavender) which drove the composer input text/caret/placeholder, the (empty) `.ak-name`, and the `.ak-tweet::after` hover glow; the Stage 1.5 `.ak-tweet` left edge was also hardcoded lavender. **Two edits, no `!important`:** (1) added `--ak-purple:#ff33ff;` inside the existing `html.ak-narrative-page{…}` block — `html.ak-narrative-page` (0,1,1) beats the feed's later `:root{--ak-purple:#c084fc}` (0,1,0), and inheritance re-points all `var(--ak-purple)` consumers at once; (2) changed `.ak-narrative-page .ak-tweet` `border-left-color` lavender `rgba(192,132,252,0.65)` → official `rgba(255,51,255,0.65)` (same alpha). Already-correct `#ff33ff` values (glow-box `akBoxPulse`, avatar glow, `.ak-tweet:hover` border) and ALL Stage 1.5 dark-sticker material are untouched; handles stay neon green, timestamps grey, tweet text white, all Impact/Due Date/Tenada behavior unchanged; `custom_liquid_7HLMyz` + posting JS untouched; no doodles. Verified live at 1440/1024/768/375/320 (no lavender remains; new runtime posts inherit the magenta edge; full 13-route leak battery clean). **Revert: remove the `--ak-purple:#ff33ff;` line and restore the tweet `border-left-color` to `rgba(192,132,252,0.65)`.**
- **Fonts (in progress, NOT final — updated 2026-06-11):** the owner wants to **move away from Impact** as the site's secondary/sub-font. The font-analysis shortlist was tested on the Products grid titles via the guarded `--ak-products-label-font` variable (Impact in-stack as live fallback): **Permanent Marker rejected** (too close to the handwritten nav PNGs), **Sedgwick Ave Display rejected** (too graffiti/loud), **Black Ops One rejected by owner** (2026-06-10), **Londrina Solid was interim-only and is now removed**. **Current proof (Phase 6.3, 2026-06-11, OWNER VERDICT PENDING): "Due Date"** (Dieter Steffmann / Typographer Mediengestaltung, v2.47) — the same distressed military-stencil direction as the owner-liked but license-blocked STENCILED/Dizzy, **commercially clean** (1001Fonts FFC license: commercial use + WOFF/WOFF2 conversion + web embedding expressly permitted; fsType 0). **First font file in the repo: `assets/ak-due-date.woff2`** (30.7KB), served via scoped `@font-face` (`font-family:'AK Due Date'`, `font-display:swap`) inside `snippets/ak-notebook-products.liquid` behind the master-collection guard; title size **3.3rem** (+10% browser-preview winner — at 3rem the wider letterforms split "T-Shirt" as `T-`/`SHIRT`). **Known glyph gap: `#` is not in Due Date — since 2026-06-11 it is served by "Tenada"** (Tenada Corp., SIL OFL 1.1, official site en.tenada.co.kr, NO Reserved Font Name) via a second scoped `@font-face` with **`unicode-range:U+23`** placed FIRST in the stack (`'AK Tenada','AK Due Date',Impact,…`) — the browser uses Tenada for `#` only and Due Date for everything else, **CSS-only, no DOM wrapping/JS** (`snippets/card-product.liquid` untouched). The asset `assets/ak-tenada.woff2` is a **1.3KB OFL-compliant subset (the `#` glyph only)** of the ~1MB Korean ExtraBold family; OFL expressly permits subsetting (unlike Due Date's FFC, which **forbids modifying that typeface** — format conversion only). **Impact must stay in the stack** as the last-resort `#` fallback. **Recorded for later (NOT implemented): the hashtag-heavy Archive notebook redesign should reuse this same Tenada-hash utility; no global hashtag replacement system without analysis + explicit approval.** **Footer CTA adoption (Stages 1+2 DONE, 2026-06-12):** the footer "CLICK HERE TO GET TOP" renders in Due Date on **all three notebook pages** — Products (Stage 1, 2026-06-11: one `.ak-products-page .ak-btt-text` rule reusing the Products snippet's @font-face) and Life/Style + Club Hub (Stage 2, 2026-06-12: each snippet carries its own **duplicated** `@font-face 'AK Due Date'` + one scoped `.ak-btt-text` rule — duplication is the deliberate house pattern; same `assets/ak-due-date.woff2`). No Tenada (no `#` in the text); no size/tracking tuning needed (one-line verified at 375/320 on all three). **`sections/ak-footer.liquid` is intentionally NOT edited** — it is shared by 8 routes and all dark/non-notebook footers keep Impact. **A global footer font change is REJECTED/DEFERRED unless explicitly approved.** Broader Impact replacement still happens only via staged, explicitly-approved passes.
- **Products doodles (Phase 7.1, owner feedback pending — 2026-06-09):** the Products page carries the **"Catalog margins v2"** layer — 8 page-anchored doodles at Club Hub scale (header star+cross flanking the KID logo; bumped margin hashtag/eyes that may cross the painted rules but never card boxes; gutter dollar+dice; bottom-anchored kid + mirrored star on the CTA flanks; star is the only duplicate, differentiated by `scaleX(-1)` + ~35% size + rotation). **Page-anchored on purpose** — glued to the scrolling paper; viewport-fixed was analyzed and rejected (fixed ink slides across the ruled lines and over products during scroll). Hidden <1200px; gutter pair <1366px. **8 is not a hard cap** — the page is long and more may be added if the owner likes the direction. Removal contract: one wrapper + one CSS block; every doodle is one self-contained rule (easy to remove/replace/resize/swap if the owner rejects them).
- **Life/Style handwritten PNG titles (Phase 4.8, checkpointed — 2026-06-09):** the visible text of THE ADVENTURES OF AMERIKID / KID RADIO / CHANNELS is replaced by Declan's handwritten CDN PNGs (`adventures_of_amerikid.png` 710×156 / `kid_radio.png` 425×59 / `channels.png` 408×53) via **CSS-only `::before` rules in `snippets/ak-notebook-lifestyle.liquid`** — `templates/page.json` and the title markup are NOT edited; the real text stays in the DOM (collapsed via `font-size:0` in the notebook scope only) for screen readers; the dark-theme text titles remain intact outside the scope. The element-level `akAdvFlicker`/`akKrSignal` ambience transfers to the PNGs automatically; **Channels is static** (its per-letter `akChanTuneIn` cannot apply to a single image). Sizes are capped at native (never upscaled). **Adventures was tuned bigger per the owner: `height:clamp(72px, 8.4vw, 125px)`** (~550×121 @1440); Kid Radio and Channels are acceptable for now and unchanged. 2× PNG re-exports would improve Retina sharpness later (same filenames slot in with zero code change).
- **Notebook nav dividers — REJECTED BY OWNER AND REMOVED (2026-06-10):** the hand-drawn dotted/marker nav divider system (one marker-blob line under the seven-button nav cluster + its approved draw-on load animation) was built and iterated on Life/Style, Products, and Club Hub — then **rejected by the owner** ("does not like the dotted/marker line") and **fully removed** from all three snippets in the owner-feedback cleanup commit. The `.ak-top-nav-area::after` pseudo is free again on every page; `sections/ak-top.liquid` was never edited. **Future notebook pages must NOT automatically receive a dotted divider** — re-adding any version requires explicit owner approval first. The full implementation spec (accepted v3 filled-blob technique, per-page seeds 23/7/41, pass-2 clip-path draw animation with its timing/easing/reduced-motion rules) is recoverable from git history at commit `a8bbf18` if the owner ever changes course. **Products grid font status (updated 2026-06-11):** Permanent Marker REJECTED; Sedgwick Ave Display REJECTED; **Black Ops One REJECTED by owner** (was the Google/OFL stand-in for the stencil direction — do not retry it); Londrina Solid interim proof REMOVED; **current proof = "Due Date" (Phase 6.3, FFC commercial-use license, self-hosted `assets/ak-due-date.woff2`, owner verdict pending — see the Fonts bullet above)**. **Owner-liked direction: his two supplied stencil fonts.** "STENCILED PERSONAL USE" (Billy Argel): owner likes the bold military-stencil look but **will not buy the commercial license right now** — the file is personal-use-only + fsType-2 (embedding forbidden) with broken demo glyphs (digits render as star ornaments; #/().-/& fall back to Impact) and **MUST NOT be uploaded to Shopify or added to the repo**. "d Dizzy Stencil" (wep): owner-liked direction with full glyph coverage but lighter weight; **license unconfirmed — browser-only local preview ONLY until licensing is confirmed; no theme upload, no final/live use**. **Parked idea only (not active work): hidden notes / easter-egg system** across the seven main pages — recorded as a future direction, nothing implemented.
- **Queued ideas (NOT implemented):** a **nav highlighter animation** (queued after the Products doodles are accepted). (The divider system — line + draw animation — was later rejected by the owner and removed 2026-06-10; see the nav-dividers bullet above.)
- **Doodle rules (standing):** doodles are **experimental and must remain easy to remove** if the owner dislikes them — each page's system is one deletable `aria-hidden` wrapper + one deletable CSS block. Future doodle scope is **ONLY the seven main pages**: Life/Style, Products, Archive, Narrative, Bad Music, CandyFacts / Mystery Box, Club Hub / Moniker. **No doodles are planned for cart, individual product pages, or individual archive pages.** Products doodles are deferred until paper + shadow + font direction are evaluated.
- **Future direction (recorded, NOT implemented):** replace the text titles THE ADVENTURES OF AMERIKID / KID RADIO / CHANNELS with **handwritten PNG title images**. After Life/Style is polished, the **next major notebook page target is Products — before Cart** — so the owner can evaluate product-page copy under the notebook background. **Mystery Box / CandyFacts may or may not stay dark: treat it as last/optional, not automatic.**
- **Margins (2026-06-08):** the paper now has **two** vertical margin rules — red left + blue right — both driven by one shared **`--ak-paper-side-gap: clamp(82px, 7vw, 128px)`** so they sit evenly inset (~101px from each edge), 2px each (red `rgba(218,40,50,.74)`, blue `rgba(44,120,180,.60)`). A tiny Club-Hub-only cart nudge (`.ak-top-cart-container{ transform:translateX(clamp(4px,.6vw,10px)) }`) keeps the blue rule from crowding the cart (~10px gap). Horizontal ruled lines + yellow paper unchanged.
- **Doodles + footer/paper-ending (2026-06-08):** a **decorative doodle layer** was added — a small DOM wrapper `<div class="ak-clubhub-doodles" aria-hidden="true">` (8 `<span>` items) inserted just before `.ak-clubhub` (a DOM layer is required because CSS background layers cannot be rotated individually). Doodles are **decorative, page-anchored** (absolute inside the page-anchored `#MainContent`, so they scroll WITH the paper), **`aria-hidden`, `pointer-events:none`, full opacity**, each **individually rotated**, soaked into the paper via `mix-blend-mode:multiply`, painted **behind content** (wrapper `z-index:0`; `#MainContent` + `.ak-footer` lifted to `z-index:1`; `ak-top` keeps `z-index:999`), and **hidden on mobile** (`@media (max-width:768px){ display:none }`). The upper-right former **money** doodle now uses **`ak-doodle-dollar.png`**; `ak-doodle-money.png` no longer appears on Club Hub. **`ak-doodle-dollar.png` is used twice on this page intentionally**, the two differentiated (upper-right: larger, right/top, `rotate(9deg)`; lower-middle: lower, smaller, `rotate(4deg)`). **Duplicate doodle asset rule: a duplicate asset is acceptable ONLY when it is visually differentiated by placement, size, and rotation.** The footer/paper ending was fixed with a blank **bottom paper band** — `--ak-paper-bottom-gap: clamp(130px,12vw,190px)` + a `body` background layer `linear-gradient(180deg, transparent calc(100% - var(--ak-paper-bottom-gap)), #f5dd4f calc(100% - var(--ak-paper-bottom-gap) + 36px))` placed ABOVE the ruled lines but BELOW the red/blue vertical rules — so the horizontal ruled lines **fade out into a clean bottom margin** (the page no longer ends halfway through a ruled line), the **red/blue side rules still run full height**, and the footer **"CLICK HERE TO GET TOP" CTA now sits on the clean bottom band** (CTA text / `id="AkBackToTop"` / `aria-label` / scroll / hover-focus unchanged; `ak-footer.liquid` not edited).
- **Future site-wide notebook expansion** should be driven by **per-page doodle maps** (page-specific / pseudo-random sets), **NOT this exact fixed Club Hub composition repeated identically on every page**. Recorded as a code comment in `snippets/club-hub.liquid` and **not implemented** — any expansion needs separate explicit approval (it inverts the locked black-neon identity).
- **Current accepted state (2026-06-08):** notebook paper (yellow + red **and blue** vertical margin rules + blue horizontal ruled lines + a **blank bottom paper band** so the page ends on clean margin, not mid-line) · ink-on-paper header/nav/card/footer · **mask-based PNG nav (Phase 3.9)** — the five text nav tiles render their wordmark PNGs as a `mask` on the anchor `::before` (ink `#15120b` at rest → `#ff33ff` on hover/focus/press), the clickable `<a>` is **untransformed** (the static handwritten tilt lives only on the non-interactive `::before`), label text kept in DOM (`color:transparent`) for a11y, `.is-active` excluded so the current page stays ink at rest · BAD MUSIC + CANDYFACTS enlarged sticker buttons (tilt+scale on the inner graphic, anchor untransformed) · **decorative PNG doodle composition** (page-anchored, aria-hidden, pointer-events:none, full opacity, individually rotated, hidden on mobile; dollar asset used twice, differentiated) — the earlier **CSS scribble doodle layer stays removed** (these are the separate PNG doodles). The mask rewrite fixed a Safari hover jolt structurally (Chrome-verified; **Declan verifies real Safari**).
- **Scope (updated 2026-06-15):** **Club Hub + Life/Style + Products + Narrative + Bad Music + Bad Music 2025 only, and NOT live** (Bad Music `/pages/2025` added Stage BM-1 + the BM-1.1 visual correction — red banner disabled via `custom_liquid_JpKeak "disabled": true`; TV hero de-slabbed to transparent — via `snippets/ak-notebook-badmusic.liquid` rendered from the repurposed disabled slot `custom_liquid_RnLA6b` in `templates/page.badmusic.json` behind a `page.handle == '2025'` guard; `/pages/badmusic-2025` added Stage BM2025-1 — the **sixth** notebook page — via `snippets/ak-notebook-badmusic-2025.liquid` rendered from a NEW host slot `custom_liquid_bm2025host` in `templates/page.badmusic-2025.json` (placed after `ak-top`, before the BAD Vinyl section `custom_liquid_QcCtY6`, which is NOT edited) behind a `page.handle == 'badmusic-2025'` guard; individual archive pages stay dark; Archive main / Mystery Box / cart / product pages remain dark, pending separate passes). CSS + tiny **decorative `aria-hidden` / `pointer-events:none` DOM doodle wrappers** (Club Hub + Life/Style + Products; Narrative has none yet) + six guarded page-class hooks (no canvas), additive, scoped to `.ak-clubhub-page` inside `snippets/club-hub.liquid`, `.ak-lifestyle-page` inside `snippets/ak-notebook-lifestyle.liquid`, `.ak-products-page` inside `snippets/ak-notebook-products.liquid`, `.ak-narrative-page` inside `snippets/ak-notebook-narrative.liquid`, `.ak-badmusic-page` inside `snippets/ak-notebook-badmusic.liquid`, and `.ak-badmusic-2025-page` inside `snippets/ak-notebook-badmusic-2025.liquid` (`sections/ak-top.liquid` and `sections/ak-footer.liquid` source files NOT edited; `templates/page.json`, `templates/collection.json`, and `templates/page.narrative.json` each only repurpose one disabled spare custom-liquid slot — the collection slot behind a `collection.handle == 'master-collection'` guard, the narrative slot behind a `page.handle == 'narrative'` guard). Live theme `184615043352` and normal dev theme `185910067480` are untouched. Any expansion beyond the seven main pages needs separate explicit approval (it inverts the locked black-neon identity).

## Current Known Priorities
1. Establish cross-AI continuity and reliable documentation (Complete).
2. Reduce lag and unnecessary fat (bloat, large global assets, duplicated scripts) without altering visual output.
3. Consolidate duplicated navigation code currently hardcoded across multiple JSON templates.
4. Future Phase: Mystery Crate integration work.

## Particle Removal Strategy (2026-05-18)
The current particle-removal program is **pilot-first**, not site-wide. After a prior 21-template batch attempt (`ak-collection-bg` disabled across the full set) produced white-background regressions, a read-only DevTools postmortem identified that `sections/ak-collection-bg.liquid` was carrying critical black-background CSS in addition to drawing particles — see [[known-fragile-areas]] for details. The current approach lifts that 3-rule CSS package into `layout/theme.liquid` as a permanent global safety net, then disables `ak-collection-bg` one template at a time with a development-theme observation window between batches. The 2026-05-18 pilot covered a single low-traffic collection (`templates/collection.weed.json`). The 2026-05-19 batch 2 added `templates/collection.kandy.json` and `templates/collection.skate.json`. The 2026-05-19 batch 3 added `templates/collection.stones.json` and `templates/collection.aperikid.json`. All five use the same one-line `"disabled": true` mechanism. A sitemap-anchored handle→template-suffix probe (2026-05-19) confirmed that file name does not always equal storefront handle (`/collections/skate-tables` renders via `templates/collection.skate.json`), so future batches must verify each template's real route before flipping. The 2026-05-19 batch 4 added `templates/collection.bucket.json` and `templates/collection.gpr.json`. The 2026-05-19 batch 5 added `templates/collection.solo.json` and `templates/collection.watermelon.json`. The 2026-05-19 batch 6 added `templates/collection.uh-oh-ghoul-aid-tee.json` (→ /collections/uh-oh-ghoul-aid-tee) and `templates/collection.election.json` (→ /collections/2024-election-tee). The 2026-05-19 batch 7 added `templates/collection.kidzyn.json` (→ /collections/kid-zyn) and `templates/collection.loveamerica.json` (→ /collections/love-amerikid). The 2026-05-19 batch 8 added `templates/collection.katskan.json` (→ /collections/kat-skan-tees) and `templates/collection.travellercap.json` (→ /collections/traveller-cap). The 2026-05-19 batch 9 (final named-collection batch) added `templates/collection.uhoh.json` (→ /collections/uh-oh-tee) and `templates/collection.siwyc.json` (→ /collections/say-it-with-your-chest-1). **The full collection-template `ak-collection-bg` removal lane is now COMPLETE on dev:** every published Collection that used `ak-collection-bg` — including the Bad Music collection (`collection.bad-music.json` → /collections/bad-music), which was the last one, flipped 2026-05-19 after local-preview verification — is now particle-disabled on dev. Full disabled set (18 collection templates + `index.json`): Weed, Kandy, Skate-Tables, Stones, Aperikid, Bucket, GPR, Solo, Watermelon, UH-OH Ghoul-Aid Tee, 2024 Election Tee, Kid Zyn, Love Amerikid, Kat Skan Tees, Traveller Cap, UH-OH Tee, Say It With Your Chest, Bad Music collection (`templates/index.json` had `ak-collection-bg` disabled as part of the homepage-prototype lane, not the particle-removal rollout). **Homepage reverted to pre-prototype (2026-05-22):** the game-landing prototype was removed from `templates/index.json` (the old `ak-landing` splash + bouncing-kid `custom_liquid_xPURQD` re-enabled), while `ak-collection-bg` was **kept disabled** so the homepage stays particle-free (hybrid revert — restores the old homepage without reintroducing the homepage particle background). `sections/ak-home-game-landing.liquid` is left in the repo but dormant (no longer referenced by `index.json`). This removed the prototype from the live-promotion delta; the homepage is now the original splash + bouncing-kid on plain black. `ak-collection-bg` is now **DISABLED on every template that ever used it** — the section is no longer active anywhere on dev (2026-05-19). **`templates/page.json` (`/pages/the-collective`, Life/Style) is now fully de-particled (2026-05-19):** `ak-collection-bg` is disabled AND all three module ambient particle canvases — Adventures `.ak-adv-field` (`custom_liquid_X46wQm`), Kid Radio `.ak-kr-field` (`custom_liquid_kkxqR3`), Channels `.ak-chan-field` (`custom_liquid_PRGR7i`) — are neutralized by flipping each particle guard to `if(false && canvas && !reduced)` (the same code path as reduced-motion). The canvases remain in the DOM but draw nothing; all Life/Style content and interactions are preserved: Adventures/Kid Radio/Channels modules, YouTube embeds/facades, glass cards (`.ak-holo-card`), playing states, Adventures 3D tilt, Kid Radio wave bar, module layouts, the ambient **title animations** (`akAdvFlicker`/`akKrSignal`/`akChanTuneIn` — intended final design), footer, nav/cart. `ak-top` header particles are untouched (saved for last). **`templates/page.badmusic.json` (`/pages/2025`, the TV + cassette page) is now fully de-particled (2026-05-19):** `ak-collection-bg` is disabled AND the TV-hero ambient `ak-bg-canvas` (dust/floaters/shooting stars in the isolated "Ambient background particles" IIFE inside `custom_liquid_dx4GNc`) is neutralized via an early `return;`. The `ak-bg-canvas` element remains in the DOM but draws nothing; the TV hero (screen/bezel/`ak-yt-holder` YouTube player/poster/controls/timeline/scanlines), 2025 caption, BAD MUSIC heading, cassette shelf (`ak-bm-album-link`), left cassette link to `/pages/badmusic-2025`, right Coming Soon cassette, and cassette MP4 are all preserved. **`templates/page.badmusic-2025.json` (`/pages/badmusic-2025`, the vinyl album page) is now fully de-particled (2026-05-19):** `ak-collection-bg` is disabled AND the page-specific `bv-field` ambient canvas (dust/floaters/shooting stars in `custom_liquid_QcCtY6`) is neutralized via an early `return;` in `initBadVinylAmbient()`. The `bv-field` canvas element remains in the DOM but draws nothing; the full vinyl UI/interactions (12 records, album art, intro, seek, spin, hover/tilt, play/pause) are preserved. **Separate later lanes, each requiring its own pilot (Bad Music collection + both Bad Music pages + Life/Style + default/master collection grid are now COMPLETE on dev and no longer in this list):** NONE — **all particle-background lanes are now COMPLETE on dev (2026-05-22).** Every ambient particle/starfield canvas across the storefront (collections, products, Bad Music, Life/Style, Narrative, Archive, Club Hub, cart, and the site-wide `ak-top` header) is neutralized on the development theme. The Mystery Box's theme-side ambient `ak-mb-canvas` is now **hidden via a CSS-only rule** (2026-05-22): `#ak-mysterybox-embed .ak-mb-canvas { display: none !important; }` was added to the embed's `<style>` block so the particle background no longer renders visually (no compositor cost). **No JavaScript was touched** — the canvas JS shares the embed's single main IIFE and combined guard (`if(!embed || !iframe || !canvas || !frame) return;`) with the iframe + `customer_id` handoff, so a JS guard-disable/early-return is unsafe and was explicitly NOT used. The **Mystery Crate iframe, app, `customer_id`/purchased/preview/email-link flow, prestart, and release shell remain fully PROTECTED and untouched** (verified: iframe loads with the Vercel src, prestart/lock present). The `ak-mb-canvas` element stays in the DOM (its rAF JS still runs harmlessly) but is `display:none`. No further particle-removal lanes remain. The next step would be a live-promotion readiness review (read-only) — only when the user explicitly approves; live theme `184615043352` remains untouched. **Default/master collection grid de-particled (2026-05-19):** the `ak-collection-field` ambient particle canvas in `sections/main-collection-product-grid.liquid` is neutralized via an early `return;` at the top of its standalone canvas-only IIFE. This silences particles on every default-template collection page (`/collections/master-collection`, `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve`, any suffix-less collection) in one shared-section edit; the `<canvas class="ak-collection-field">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). Dawn product grid, cards, images, prices, sold-out badges, links, hover, facets/filtering, sorting, pagination, layout, and section settings/schema are all unaffected. Named collections are unaffected because their `main-collection-product-grid` section is disabled (they never rendered `ak-collection-field`). **Product pages de-particled (2026-05-19):** the `ak-product-field` ambient particle canvas in `sections/main-product.liquid` is neutralized via an early `return;` at the top of its dedicated standalone IIFE (`<script id="ak-product-particles-{{ section.id }}">`, labeled "BEGIN: Ambient Particle System"). This silences particles on every product page (all products use this one shared section) in a single edit; the `<canvas class="ak-product-field">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). The full purchase flow is preserved: media gallery, gallery images, lightbox, variant selectors, variant image switching, quantity, product form, Add to Cart, dynamic checkout/payment buttons, price, pickup availability, accordions, sticky behavior, hover, layout, related products. `ak-back-field` was already disabled in the product templates (`custom_liquid_6wWQFN`, 2026-04-05) and remains untouched/disabled. `sections/main-product.liquid` is the theme's most fragile file and must NOT be refactored — only this one-line `return;` was added. **Narrative page de-particled (2026-05-21):** the `akfeed-field` ambient particle canvas in `templates/page.narrative.json` (`custom_liquid_7HLMyz`, `/pages/narrative`) is neutralized via an early `return;` at the top of its dedicated labeled "PARTICLE SYSTEM SCRIPT" IIFE. This was the theme's largest (1304×5543) and only un-optimized canvas; the `<canvas class="akfeed-field">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). The full feed/composer is preserved (verified by a live post test that prepended a card): tweet cards, `baseTweets`, `makeTweet`, `postTweet`, composer input/button, Enter-to-post, typing-loader/dots, scroll-reveal entrance, footer. The posting logic lives in a separate `<script>` block and was untouched. **Archive page de-particled (2026-05-21):** the `ak-archive-canvas` (`#AkArchiveCanvas`) ambient particle canvas in `sections/ak-archive.liquid` (`/collections`, `list-collections.json`) is neutralized via a **guard-disable**, not a top-of-script return: the existing particle guard `if(!cv||prm) return;` (line 729) was changed to `if(!cv||prm||true) return;`, which reuses the section's own already-tested reduced-motion no-particle path. **Critical:** the Archive section's single `<script>` contains BOTH the card-stack deck engine AND the particle code; the deck engine (`springTo`/`springStops`/`layout()`/drag/`pointerdown`/tap→`window.location.href`/`data-href`/arrows/keyboard/anim-lock) initializes BEFORE this guard, and the particle code is a nested IIFE that follows it — so a top-of-IIFE early return would break the deck and must NOT be used. The `<canvas id="AkArchiveCanvas">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). Verified on dev: 18 deck cards + 18 `data-href` links + arrows + footer intact, canvas blank. **Clubhouse de-particled (2026-05-22):** the `ak-clubhub-field` (`#ak-clubhub-field`) ambient particle canvas in `snippets/club-hub.liquid` (`/pages/club-hub` → `page.club-hub.json` → `custom_liquid_4tbt7q` = `{% render 'club-hub' %}`) is neutralized via an early `return;` at the top of its dedicated labeled "Ambient particles + shooting stars system" standalone IIFE. The `<canvas id="ak-clubhub-field">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). The Clubhouse content/forms are preserved: the drops form + captcha logic (`dropsForm`/`dropsBtn`/`findToken`/auto-submit/`hCaptcha`), links, buttons, inputs, content blocks, footer all live in separate `<script>` blocks and were untouched (drops form/captcha render conditionally by customer/drops state). Verified on both local working-tree and dev theme: canvas blank, `.ak-clubhub` content + links + footer intact, `ak-top` present. **Cart de-particled (2026-05-22):** the cart page's TWO ambient particle canvases — `ak-cart-field` (`#ak-cart-canvas-{id}` in `sections/main-cart-items.liquid`) and `ak-cart-footer-field` (`#ak-cart-footer-canvas-{id}` in `sections/main-cart-footer.liquid`) — are each neutralized via an early `return;` at the top of their dedicated labeled standalone canvas-only IIFEs. Both `<canvas>` elements stay in the DOM but draw nothing (default 300×150, 0 pixels). Verified on dev in BOTH empty and item-present states (a harmless test item was added then cleared): line items, quantity controls, remove buttons, cart form, subtotal/totals, checkout button, dynamic/additional checkout buttons, cart notes, discounts, cart-errors, empty-cart state, AJAX cart updates all preserved. The cart drawer (`snippets/cart-drawer.liquid`) and cart notification (`snippets/cart-notification.liquid`) have NO canvas refs and were untouched. **ak-top header de-particled (2026-05-22) — FINAL LANE:** the site-wide header/nav canvas `ak-top-canvas` (`#ak-top-cv-{id}` in `sections/ak-top.liquid`, included by 31 templates) is neutralized via a guard-disable: the particle guard `if(!cv || prm) return;` (line 550) was changed to `if(!cv || prm || true) return;`, reusing the section's own already-tested reduced-motion no-particle path. **NOT a top-of-IIFE early return:** the single main IIFE contains BOTH the header/nav/cart/menu/drawer/label behavior (which initializes BEFORE the guard) AND the particle code (after it); the guard-disable silences only the particle canvas. The `<canvas id="ak-top-cv-{id}">` element stays in the DOM but draws nothing (default 300×150, 0 pixels). Verified on dev: header shell, nav labels, MENU drawer open/close, cart button, `cart:updated` count, `akTopGlint` sheen, `akTopCartBob` bounce all preserved; Mystery Box's protected `ak-mb-canvas` still drawing and untouched. **With ak-top complete, every non-protected ambient particle background across the storefront is now off on dev.** A live-theme promotion of the accumulated set is a future step, only after sustained clean dev-theme observation. Mystery Crate is protected from all of these lanes.

## Locked / Non-Negotiable
- The visual identity, animations, and "feel" of the website must not change.
- Mystery Crate logic is critical and must not be broken during cleanup.

## Optimization Goal
Make the theme less glitchy, faster, leaner, and more maintainable while preserving the exact current experience.

## Mystery Crate Note
The Mystery Crate is currently implemented as an iframe embed pointing to an external Vercel app. The wrapper logic contains fragile styling and its own canvas animation. 
- **Verified Runtime Finding:** The Vercel app is currently returning a 404 for `crate.glb`. This is an external issue but affects the "glitchy" feel.
- **Static Lock Removed (2026-04-02):** The wrapper's `LOCKED_DEFAULT` boolean was changed from `true` to `false` on the unpublished theme. The blur overlay and pointer-events block are now disabled, exposing the iframe for interaction. No dynamic entitlement gating exists — the wrapper does not check `customer.tags`, `crate_spins:*`, or metafields. It passes `customer_id` and `demo` to the iframe but no entitlement data. Future entitlement gating will require new Liquid conditionals and potentially new iframe params.
- **Guest CTA Handoff Fix (2026-04-09):** `custom_liquid_mysterybox_embed`'s iframe-src IIFE now also reads `customer_id` from the page URL query params as a fallback when the Liquid `{{ customer.id }}` variable is empty. Priority order is Liquid-first, URL-fallback second (signed-in users are never overridden by URL). The existing empty-check gate is preserved, so direct guest visits with no URL param still produce a no-param src and preview as before. The `demo=1` passthrough is unchanged. `syclid` is intentionally not forwarded. This restores the order-confirmation email CTA path for guests, whose URL already carries `customer_id` but whose Liquid customer context is empty. Crate repo unchanged. Replay-protection for finalized email links is a separate crate-side lane, not addressed here.
- **Display Shell (2026-04-08, Stage 2 + 2.5):** The Mystery Box iframe frame has been restyled from glassmorphism to a ruggedized bunker-monitor display shell with material polish. Housing uses subtle top-to-bottom gradient (`linear-gradient(180deg, #111, #0a0a0a)`), asymmetric chin padding (8/8/14/8 desktop, 6/6/10/6 mobile), squared corners (`border-radius: 6px`), layered shadow for object depth, top-edge bevel highlight (`::before`, peak 0.13 opacity), power LED (`::after`, positioned in chin at `bottom: 5px`). Screen-edge seam defined via `.ak-mb-stage::before` inset shadow (`z-index: 3`). Lock overlay covers screen area inside bezel (`box-sizing: border-box` + `padding`). Lock card kicker/title use Impact font. CSS-only — zero DOM, zero JS.
- **Wrapper Particle Parity (2026-04-08):** The Mystery Box embed canvas (`ak-mb-canvas`) has been brought into exact particle parity with `ak-top`. DPR capped to 1, dust 400–800 desktop / 120–300 mobile, floaters 200–400 desktop / 40–100 mobile, dust `shadowBlur` removed, CSS filter reduced to `contrast(1.1)`, floater glowSize 7–16. Shooters/meteors unchanged. This is the same proven pattern applied to ak-product-field, ak-collection-field, ak-back-field, and ak-top.
- **Prestart Wrapper (2026-04-15):** `sections/ak-mb-prestart.liquid` is a JS/CSS orchestrator that injects a branded entry overlay into `.ak-mb-frame` at runtime as a sibling of `#akMbIframe` and `#akMbLock`. Renders no Liquid DOM — all content built via JS `innerHTML`. Z-index 4 in the frame ladder (canvas 0 → stage 2 → seam 3 → prestart 4 → lock 5 → bevel+LED 6). Two states based on URL detection: `isPurchased = new URLSearchParams(window.location.search).has('customer_id')` — deliberately URL-only, NOT using Liquid `{{ customer.id }}`, to avoid showing purchased-access UI to unrelated signed-in customers. **Public state** (no `customer_id` param): single-CTA layout — `START / MYSTERY BOX` (`<button>`, `--primary`, `id="akMbPrestartStart"`, dismisses overlay, exposes crate spin). Subtitle `START // SPIN`. 3 briefing lines: start here to spin the crate, the combo pack includes 1 Mystery Crate hat + 1 CandyFacts shirt (inline-linked `COMBO PACK`), buy the combo pack first and access 2 spins through your email confirmation link. The inline `COMBO PACK` text in brief line 2 is an `<a>` linking to the combo product page, styled with magenta accent color and underline. No secondary button. Disclaimer `START HERE · OR COMBO PACK · 2 SPINS`. **Purchased state** (email-link arrivals with `customer_id`): ACCESS SPINS / PURCHASED ACCESS single centered CTA, 2 briefing lines, disclaimer `PURCHASED ACCESS · 2 SPINS · NON-REFUNDABLE`. Dismiss targets `#akMbPrestartStart` by ID (position-independent). Composition scaled to fill most of the iframe area: inner max-width 680px, title 72px Impact, inner gap 22px. Release gate: `Date.UTC(2026, 3, 9, 7, 0, 0)` — now past, so prestart is active. CSS-only visual language: matte bunker housing with single purple accent (`#ff33ff`). **Phase 1.5 Atmospheric Styling (2026-04-15):** Ambient CSS animations bring the overlay to life as a bunker-monitor terminal. Seven additive layers: scanline scroll (12s linear, `background-position-y`), grain drift (14s linear, `background-position`), vignette breathing (7s, opacity pulse), title glow pulse (6s, `text-shadow` bloom to `rgba(255,51,255,0.18)` at 32px + 60px spread), CTA glow pulse (`::after` pseudo, 5s, `box-shadow` bloom), plate top-edge flicker (`::after` pseudo, 6s, irregular opacity pattern with micro-dip), root atmospheric haze (`::before` pseudo, radial gradient pulse, 8s). State differentiation via CSS modifier class on root div (`ak-mb-prestart--public` vs `ak-mb-prestart--purchased`): public reads colder/eerier with default slower cadences, purchased reads more energized with faster durations (3.5–10s range) and slightly brighter haze/glow. Both states remain in the `#ff33ff` purple family — no new accent colors. All animations compositor-friendly (`opacity`, `transform`, `background-position`) except title `text-shadow` (scoped single element, negligible paint cost). Reduced-motion: all Phase 1.5 animations killed. Zero JS animation loops added — all CSS `@keyframes` only. **Phase 2.0 Visibility Tuning (2026-04-15):** Four value-level CSS adjustments to make Phase 1.5 effects actually perceptible in real browsing. (1) CTA glow fix: removed `overflow:hidden` from `.ak-mb-prestart__cta` and changed `::after` `z-index` from `-1` to `0` — glow pulse now renders visibly around button instead of being hidden behind opaque background and clipped. (2) Scanline visibility: stripe opacity bumped from `rgba(255,255,255,0.038)` to `rgba(255,255,255,0.07)` — now above casual perception threshold on most displays. (3) Root haze visibility: resting gradient bumped from `rgba(255,51,255,0.04)` to `rgba(255,51,255,0.08)`, keyframe range widened to `0.06→0.18` (was `0.04→0.09`), purchased override bumped proportionally to `0.10` (was `0.06`). (4) Plate flicker visibility: `::after` height `1px→2px`, background `rgba(255,51,255,0.32)→0.55`, keyframe range widened to `0.12→0.58` (was `0.08→0.38`), reduced-motion static opacity updated to `0.28`. No new keyframes, no new DOM, no JS changes, no copy changes. **Phase 3.0 Shell Continuity (2026-04-15):** Persistent atmospheric + telemetry layer mounted directly on `.ak-mb-frame` so the bunker-monitor read survives prestart dismiss. New DOM `.ak-mb-shell-fx` is injected by the prestart section's JS as the first child of `.ak-mb-frame` (z-index 3, below prestart at 4, below lock at 5, below bevel/LED at 6). Idempotent via `frame.dataset.akMbShellFxInit` guard. Six persistent children: (1) **scanline continuity** — `.ak-mb-shell-fx__scanline` at stage inset 8/8/14/8 (desktop) / 6/6/10/6 (mobile), stripe opacity `rgba(255,255,255,0.04)` — deliberately subtler than prestart's 0.07 so it doesn't fight iframe content readability, reuses `akMbScanScroll` keyframe at 14s linear. (2) **grain continuity** — `.ak-mb-shell-fx__grain` at same inset, SVG fractal-noise texture at opacity 0.045 (no blend mode to stay neutral over arbitrary iframe colors), reuses `akMbGrainDrift` keyframe at 18s linear. (3) **phosphor hairline** — `.ak-mb-shell-fx__hairline` 1px magenta line at top edge of stage (8px inset), `rgba(255,51,255,0.22)` with `akMbShellHair` breathing keyframe (7s ease-in-out, 0.55↔1.0 opacity). (4) **corner register marks** — `.ak-mb-shell-fx__reg--tl/tr/bl/br` L-brackets 8×8px (desktop) / 6×6px (mobile) positioned 3px inside each stage corner, magenta lines at `rgba(255,51,255,0.55)` with 0.42 element opacity, no animation. (5) **chin telemetry** — `.ak-mb-shell-fx__chin` mono-caps status strip at `left:12px; bottom:3px` (desktop) / `left:8px; bottom:2px` (mobile), 8.5px/7.5px, purple `rgba(255,51,255,0.56)`, with 6×8px blinking cursor (`akMbChinCursor` 1.1s steps(1)). Initial wording: `CRATE // LINK ACTIVE` (public state) or `CRATE // ACCESS GRANTED` (purchased state). Text swaps to `CRATE // ONLINE` in the existing `dismiss()` timeout callback once the overlay is removed. (6) **LED life** — `.ak-mb-frame::after` overridden via cascade (same selector, loads after the embed's CSS so it wins) with stacked animations: `akMbLedBreathe` 5s ease-in-out on opacity (0.68↔1.0) + `akMbLedFlicker` 27s linear on `filter:brightness()` — two animations target different properties so both apply without one losing to the other. Flicker pattern: brightness(1) → brightness(0.28) at 98.8% → brightness(1) → brightness(0.58) at 99.3% → brightness(1), giving one rare ~150ms double-blink per 27s. No LED color/size/position change. All new rules live in `sections/ak-mb-prestart.liquid`. Zero edits to `templates/page.mystery-box.json`, zero edits to `sections/ak-mb-release.liquid`, zero iframe interaction, zero audio. Reduced-motion kills all Phase 3.0 animations, hairline snaps to 0.55 opacity, chin cursor stays solid. Mobile breakpoint at 480px matches chin/padding geometry. All effects pointer-events:none so iframe interactivity is preserved. Release gate and `isPurchased` detection are unchanged and shared with prestart. **Phase 4.0 Tier 1 — Hierarchy refinements (2026-04-15):** Pure CSS typographic/bookend/focus pass over the existing prestart overlay. Five coordinated refinements, zero DOM change, zero JS change, zero behavior change, zero copy change. (1) **Kicker authority** — `.ak-mb-prestart__kicker` picks up 1px magenta hairlines above/below via `border-top`/`border-bottom`, padding `4px 14px 3px`, alpha raised from 0.42 to 0.62, tracking tightened from 0.34em to 0.32em. Reads as a stamped document header rather than a caption. (2) **Subtitle bookends** — leading/trailing 4×8px magenta rectangles added via `.ak-mb-prestart__subtitle::before`/`::after` with 5px box-shadow glow. Bridges title→plate with visible terminal hardware. (3) **Briefing-line hierarchy** — `:nth-child()` selectors on `.ak-mb-prestart__plate-body .ak-mb-prestart__brief-line` create a three-tier read on the public state (line 1 = imperative, line 2 = offer, line 3 = gate): line 1 `14px` / alpha `0.96` / chevron glow `0 0 6px rgba(255,51,255,0.6)`; line 2 unchanged at `12.5px` / `0.82`; line 3 `11px` / alpha `0.6`. Purchased state (2 lines) benefits from the same line-1 emphasis with no change to line 2. Link styling on `COMBO PACK` unchanged. (4) **Disclaimer bookends** — `.ak-mb-prestart__disclaimer::before` renders a 6px magenta pulse dot left-anchored at `14px`, inset ring shadow identical in family to `.ak-mb-prestart__serial-dot`, reuses `akMbPrestartPulse` 4s keyframe. `::after` renders a 4×6px magenta bar right-anchored at `14px`. Now mirrors the top serial strip as a matching console footer bookend. (5) **Keyboard focus echo** — `.ak-mb-prestart__inner:has(.ak-mb-prestart__cta--primary:focus-visible)` selector brightens `.ak-mb-prestart__plate::after` to `rgba(255,51,255,0.92)` and lifts subtitle border-top to `rgba(255,51,255,0.82)` / color to `#ff66ff`. Subtitle carries `transition:border-color 160ms ease-out,color 160ms ease-out` so the response is smooth in both directions. Desktop keyboard users get a full-screen coordinated response matching the hover treatment. Responsive: all new rules have matching sizes at `@media (max-width:640px)` and `@media (max-width:380px)` breakpoints. Reduced-motion: `.ak-mb-prestart__disclaimer::before` animation disabled and static opacity pinned to 0.85 inside the existing reduced-motion block; focus-echo rules animate no keyframes (color/opacity/border-color only) so they remain active for reduced-motion users. All changes live in `sections/ak-mb-prestart.liquid` only. Zero edits to `templates/page.mystery-box.json`, zero edits to `sections/ak-mb-release.liquid`, zero iframe/crate-repo interaction, zero Shopify admin or Flow impact. **Phase 5.0 — Dossier / Eyes-Only framing (2026-04-15):** Additive cosmetic framing layer over the existing overlay. Six coordinated devices, zero behavior change, zero copy change, zero state change, purple `#ff33ff` only. (1) **Overlay envelope** — new `.ak-mb-prestart__envelope` absolute-positioned element at `inset:16px` (desktop) / `12px` (640) / `10px` (380), z-index 2 (above watermarks, below inner content), 1px rule at `rgba(255,51,255,0.16)` with inset + outer box-shadow. Four corner bracket children `.ak-mb-prestart__env-corner--tl/tr/bl/br` render L-shaped magenta brackets via `::before` (horizontal 2px arm) + `::after` (vertical 2px arm), 22×22px desktop / 18×18 640 / 14×14 380, with `0 0 6px rgba(255,51,255,0.45)` glow. Deliberately larger and crisper than shell-fx register marks so the overlay reads as a separate document layer. (2) **Watermark document bands** — two `.ak-mb-prestart__watermark--top`/`--bottom` siblings at z-index 1 (behind envelope, above haze), sans-serif 700-weight uppercase, `rgba(255,51,255,0.05)` alpha, 14px (desktop) / 11px (640) / 9px (380) with `0.5em` tracking. Content: `CLASSIFIED // MYSTERY BOX` repeating (top), `EYES ONLY // SPIN PROTOCOL` repeating (bottom). Purely decorative, `aria-hidden="true"`, `user-select:none`. Very low opacity so the classified-document read is felt but never fights hero legibility. (3) **Title rails** — new `.ak-mb-prestart__title-frame` wrapper around the h2, `max-width:560px` (desktop) / `420px` (640) / `300px` (380), padding `14px 0 10px` with `::before`/`::after` absolute 1px magenta hairlines (linear gradient, transparent→0.35→0.55→0.35→transparent) pinning the title top and bottom. Reads as a mastered callout on the page, not floating type. (4) **Subtitle cartridge** — `.ak-mb-prestart__subtitle::before`/`::after` re-specified from Tier 1 inline magenta rectangles to absolutely-positioned 10px-wide bracket hardware: left bracket has `border-right:none`, right bracket has `border-left:none`, `height:calc(100% - 6px)`, `border:1px solid rgba(255,51,255,0.72)`, `0 0 6px rgba(255,51,255,0.3)` glow, `position:absolute; top:50%; transform:translateY(-50%)`. Subtitle padding becomes `7px 30px 6px` to make room. Overrides (not adds to) the Tier 1 subtitle rectangles via cascade order — Tier 1 declarations are shadowed. Reads as a status cartridge bracketing `START // SPIN` / `ENTER // ACCESS`. (5) **Plate memo right tab** — new `.ak-mb-prestart__plate-tab-r` absolutely positioned at `top:-7px; right:18px` (desktop) / `14px` (640) / `12px` (380), 10px mono letter-spaced 0.3em decorative three-middots glyph balancing the existing top-left `HOW IT WORKS` / `YOUR ACCESS` label. Same background/type/color family as `.ak-mb-prestart__plate-label`, text-shadow `0 0 5px rgba(255,51,255,0.4)`. Plate now reads as a two-terminal memo strip rather than a single-anchor panel. Internal dashed divider added between briefing lines via `.ak-mb-prestart__plate-body .ak-mb-prestart__brief-line:not(:last-child){ padding-bottom:6px; border-bottom:1px dashed rgba(255,51,255,0.1); }`. (6) **Disclaimer signature ruling** — `.ak-mb-prestart__disclaimer` picks up a very fine repeating vertical tick pattern via `repeating-linear-gradient(90deg, transparent 0, transparent 6px, rgba(255,51,255,0.055) 6px, rgba(255,51,255,0.055) 7px)` layered atop the existing dark gradient, and `border-top` color raised to `rgba(255,51,255,0.28)`. Pulse-dot + right bar bookends from Tier 1 are preserved on top. Turns the disclaimer into a designed console/signature footer. Composition: watermarks + envelope sit behind the existing atmospheric layers (haze, grain, scanline) and behind inner content, so they never compete with hero legibility. All new elements are `aria-hidden="true"`, none animate, so Phase 5.0 is compositor-free and requires no reduced-motion additions. All changes live in `sections/ak-mb-prestart.liquid` only. Zero edits to `templates/page.mystery-box.json`, zero edits to `sections/ak-mb-release.liquid`, zero iframe/crate-repo interaction, zero Shopify admin or Flow impact. Held for possible later pass: large rotated `AUTHORIZED` / `OPEN FILE` stamp, CTA rail extensions reaching the envelope, stronger purchased-only elevation package, boot-sequence staged mount.
- **Release Overlay Added (2026-04-07, corrected same day):** `sections/ak-mb-release.liquid` is a JS/CSS orchestrator that controls the existing `#akMbLock` overlay inside the embed section. It renders no visible HTML of its own — it shows the existing lock overlay, injects a countdown timer into the lock card, and auto-unlocks when time passes. Overlay covers only the iframe/frame area, not the full viewport. Nav/header/footer remain accessible. Target release: **2026-04-09T07:00:00Z (Thu Apr 9, 12:00 AM America/Los_Angeles)**. **Client-side enforcement only** — not server-side. Bypassable via JS disable or clock manipulation. Stronger enforcement would require the Vercel app or a Shopify app proxy checking server time. After the release date passes, the section's JS immediately returns (no-op) but should be removed from `page.mystery-box.json` for cleanliness.

## Verified Runtime Findings (2026-03-19)
- **Active Builder:** **PageFly** app is still installed but no longer drives the homepage (extracted 2026-04-09). The homepage now uses theme-native `sections/ak-landing.liquid`. `sections/pagefly-home.liquid` is preserved as rollback. `layout/theme.liquid` still includes `pagefly-app-header` but it no longer triggers on the homepage (no `shopify-section-pagefly-home` in `content_for_layout`). PageFly snippets, assets, and `theme.pagefly.liquid` remain in place — removal deferred until confirmed no other page uses PageFly.
  - **Confirmed Extraction (2026-04-09, Landing Page L0):** Homepage template (`templates/index.json`) updated from `pagefly-home` section to `ak-landing` section. `sections/ak-landing.liquid` contains the exact visible content from `pagefly-home.liquid`: fullscreen `<a class="ak-landing">` with MP4 video (autoplay, muted, loop, playsinline) on desktop, static image fallback on mobile (≤767px), video error fallback to image, click-through to `/pages/mystery-box`. CSS: `position:fixed; inset:0; z-index:1`, viewport height via `100svh`/`100dvh`/`100vh` cascade. JS: autoplay IIFE with `canplay`/`playing`/`error` listeners. Bouncing kid logo (`custom_liquid_xPURQD`) preserved as second section — `position:fixed; z-index:999999`, rAF bounce loop, squash/stretch on wall hits, hover pause (desktop), click → `/pages/mystery-box`. Dawn header/footer groups render nothing (both `sections/header.liquid` and `sections/footer.liquid` are 0 bytes).
  - **Confirmed Portal Phase 1 (2026-04-09):** Background particles added via `ak-collection-bg` section (proven, deployed on 18+ pages). Click transition added: 350ms `scale(1.04)` + fade-out on `.ak-landing` before navigating to `/pages/mystery-box`. Double-navigation guard. `prefers-reduced-motion` skips transition. `window.__akPortalExit` exposed for bouncing kid to call same transition. Kid click handler updated to use `__akPortalExit` with direct fallback. Section order: `ak-collection-bg` → `ak-landing` → `custom_liquid_xPURQD`. No ak-top, no ak-footer. Homepage now has 1 canvas (`ak-collection-bg`, fixed, `visibilitychange` lifecycle). Particles render behind the z-index:1 splash — visible only during exit transition fade.
  - **Confirmed K1 Motion Quality (2026-04-09):** Bouncing kid (`custom_liquid_xPURQD`) movement upgraded from per-frame pixel increment (`dx=2.2, dy=2.2`) to delta-time-based system. `SPEED=132` px/sec (equivalent to 2.2 px/frame at 60fps). `requestAnimationFrame(now)` provides high-resolution timestamp; `lastTime` tracks previous frame. Delta capped at 100ms to prevent position jumps after tab-switch or background. Initial direction randomized: angle varies ±15° from 45° via `(Math.PI/4) + (Math.random()-0.5) * (Math.PI/6)`, with random sign flips for starting quadrant. Movement is now frame-rate-independent — identical perceived speed on 60Hz, 120Hz, and 144Hz displays. Bounce loop, wall collision, squash/stretch, tilt, hover pause all preserved.
  - **Confirmed K2 Glow and Presence (2026-04-09):** Bouncing kid visual presence upgraded. Base filter changed to layered `drop-shadow`: `0 0 8px rgba(255,255,255,0.9)` (white core) + `0 0 18px rgba(200,120,255,0.35)` (purple/magenta halo). `akGlow` keyframe (3s infinite) pulses both shadow layers between base and intensified values. `akBreath` keyframe (4s infinite) scales `1` → `1.02` for subtle breathing. Desktop hover brighten via `@media (hover:hover) and (pointer:fine)` applies stronger white+purple shadows. All three squash class overrides (`.ak-squish-x`, `.ak-squish-y`, `.ak-squish-xy`) compose `akGlow` + `akBreath` + squash keyframe in CSS `animation` shorthand. CSS-only within `custom_liquid` string. Kid image asset unchanged.
- **App Bloat:** **Videeo** and **JustSell** are initializing globally on every page (Home, Product, Collection, Mystery Box). Empty PopClips carousel app blocks have been removed from `page.club-hub.json`, `page.json`, and `page.badmusic.json` (2026-04-03). The global Videeo app embed SDK remains enabled in `config/settings_data.json` — disabling it is the next step pending business confirmation that Videeo/PopClips is unused. **Gameball** has been fully removed from theme code (2026-03-27): hardcoded snippet includes removed from both layouts, snippet file deleted, app embed disabled in settings_data.json. Note: the Gameball Shopify app may still need to be uninstalled from the Shopify admin separately.
- **Redundant Canvas Systems:** Product pages and Mystery Box pages are running up to 5 and 4 concurrent `<canvas>` elements respectively (e.g., `aknav-field`, `ak-product-field`, `ak-back-field`, `holo-rail-field`). This is a major source of GPU lag.
  - **Confirmed Optimization (2026-03-19):** `ak-product-field` now uses true offscreen cancellation — its `requestAnimationFrame` loop stops entirely when scrolled out of viewport and restarts on re-entry.
  - **Confirmed Optimization (2026-03-20):** `ak-collection-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the collection product grid scrolls out of viewport and restarts on re-entry.
  - **Confirmed Optimization (2026-03-20):** `ak-back-field` now uses the same true offscreen cancellation pattern. Its existing partial `IntersectionObserver` (which previously kept scheduling rAF frames while offscreen) was upgraded to fully cancel the loop when offscreen and restart cleanly on re-entry.
  - **Confirmed Optimization (2026-03-26):** `holo-rail-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the header/rail scrolls out of viewport and restarts on re-entry. Patched across all 18 JSON templates (two code variants: 10 pretty-printed, 8 minified).
  - **Confirmed Optimization (2026-03-27):** `aknav-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the nav starfield scrolls out of viewport and restarts on re-entry. Patched across all 18 JSON templates (four code variants). All 5/5 known canvas systems now have offscreen cancellation.
  - **Confirmed Optimization (2026-04-05):** `ak-collection-field` visible-cost reduction (Phase 1). DPR capped to 1. Dust cap reduced from 3,600–20,000 to 800–2,000. Floater cap reduced from 520–1,800 to 200–800. Accent floater glowSize reduced from 14–32 to 8–18. Dust shadowBlur removed. Floater glow, shooter glow, colors, blend mode, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-05):** `ak-collection-field` CSS `filter: contrast(1.1)` removed (Phase 2). Eliminates a per-frame GPU filter pass applied to the full canvas area (~1.5M pixels at DPR 1) every animation frame. `mix-blend-mode: screen`, particle counts, glow, shooters, colors, blend mode, layout all unchanged.
  - **Confirmed Polish (2026-04-05):** Product purchase panel CSS polish applied. Price scaled to 2.4rem mobile / 3rem desktop. Add to Cart button overridden to filled primary visual weight (white bg, dark text) with subtle purple hover glow. Description contained via border-top separator and 0.85 opacity. Spacing rhythm refined across title/price/variants/buy/description. Stacked gallery grid tightened on desktop (0.6rem gap, 3px border-radius). All CSS-only in `{%- style -%}` block of main-product.liquid.
  - **Confirmed Cleanup (2026-04-05):** Product info block order reordered in both product templates: title → price → variant_picker → quantity_selector → buy_buttons → description → share. Empty `custom_liquid_KbTzPh` block removed. Moves purchase controls directly below title for stronger conversion flow.
  - **Confirmed Cleanup (2026-04-05):** Back button section (`custom_liquid_6wWQFN`) disabled in both product templates via `"disabled": true`. Eliminates the ak-back-field canvas (dust, floaters, shooters) and BACK button from product pages. Section code preserved — re-enable by removing the `disabled` flag.
  - **Confirmed Cleanup (2026-04-05):** Product gallery settings: final state is `gallery_layout: "stacked"` + `image_zoom: "lightbox"` in both product templates. Tried `"thumbnail"` layout but it removed visible stacked images — user prefers stacked. Tried `"hover"` zoom but it hid the lightbox button on desktop — user prefers lightbox. Current combo shows all images stacked vertically with click-to-lightbox on each.
  - **Confirmed Cleanup (2026-04-05):** Collection-card image ratio: tried `"portrait"` (4:5), reverted back to `"square"` (1:1). User prefers the square grid layout. The crop issue on one apparel product should be addressed via product image re-crop in Shopify admin, not theme-wide ratio change.
  - **Confirmed Cleanup (2026-04-05):** Collection-card title visual cleanup. Titles now render in Impact, uppercase, with no hover underline. CSS added to the section `{%- style -%}` block in `main-collection-product-grid.liquid`. Card layout, zoom, spacing, sold-out badge, and pagination unchanged.
  - **Confirmed Optimization (2026-04-05):** Life & Style content canvas optimization. All 3 inline content canvases in `templates/page.json` (Adventures `custom_liquid_X46wQm`, Kid Radio `custom_liquid_kkxqR3`, Social Dock `custom_liquid_PRGR7i`) received the proven optimization package: DPR capped to 1, dust 1,600–3,400 → 800–2,000, floaters 280–560 → 200–400, dust shadowBlur removed, `filter: contrast(1.1)` removed, IntersectionObserver offscreen cancellation added. These 3 modules previously had zero offscreen cancellation — rAF loops ran continuously even when scrolled out of view. All 5 site-wide canvas systems + 3 Life & Style content canvases now have offscreen cancellation and visible-cost reductions.
  - **Confirmed Structural Split (2026-04-09, Bad Music):** Bad Music page split into two pages. Main Bad Music page (`templates/page.badmusic.json`) keeps TV hero + cassette shelf module (`sections/ak-bm-album-link.liquid`). BAD Vinyl (`custom_liquid_QcCtY6`) disabled on main page (`"disabled": true`, data preserved). `ak-collection-bg` added for particle background continuity behind cassette shelf. New dedicated 2025 album page template (`templates/page.badmusic-2025.json`) hosts BAD Vinyl as active content with ak-collection-bg + ak-top + ak-footer. **Admin step required:** A Shopify page with handle `badmusic-2025` must be created in admin and assigned template `page.badmusic-2025` for the destination URL to resolve.
  - **Confirmed Redesign (2026-04-09, Bad Music cassette shelf):** `sections/ak-bm-album-link.liquid` rebuilt as a two-cassette 3D shelf. Each cassette has full physical construction: visible case edge divs (top/right/bottom 7px), hinge spine `::before` (8px left), transparent lid with specular, inner cassette body with label window, dual reels (48px), guide posts, tape path, and screws. Resting tilt `rotateX(-1.5deg) rotateY(1deg)` with hover lift `rotateX(-2deg) rotateY(2deg) translateY(-4px)` gated by `@media (hover:hover) and (pointer:fine)`. Left cassette: linked to `/pages/badmusic-2025`, has MP4 video with IO visibility-gated playback. Right cassette: unlinked, "Coming Soon" overlay. Grid: `max-width:1060px; width:94vw; grid-template-columns:1fr 1fr; gap:36px`. Reduced-motion safe.
  - **Confirmed Bug Fix (2026-04-09, Bad Music V1):** BAD Vinyl playback bug fixed in both `page.badmusic-2025.json` and disabled copy in `page.badmusic.json`. `audio.load()` was called unconditionally on every vinyl card `mouseenter`, resetting playback. Fixed with `audio.readyState < 2 && audio.paused` guard. Duplicate mouse event listeners removed (pointer events already cover mouse). Canvas `filter:contrast(1.1)` removed from `bv-field` (CD art filter on `.cd-art` preserved).
  - **Confirmed Polish (2026-04-09, Bad Music V2):** 2025 album page visual shell added to `page.badmusic-2025.json`. Album identity intro block (`bv-intro`) with kicker/title/subtitle inserted above `#vinyl-grid`. Grid max-width 1200px. Card material `rgba(12,12,12,.6)`. Title reduced to `clamp(28px,6vw,72px)`. Artist reduced to `clamp(14px,2.8vw,22px)`. Seek bar 6px height. Time row Impact font. Mobile responsive adjustments. CSS-only.
  - **Confirmed Polish (2026-04-09, Bad Music heading hierarchy):** Main Bad Music page heading hierarchy added. "2025 (The Album)" caption moved above TV hero via CSS `order:-1` on `.ak-caption`. New `custom_liquid_bm_heading` section added with "BAD MUSIC" heading (Impact, `clamp(32px,7vw,64px)`) above cassette shelf. Thin magenta gradient accent line below heading.
  - **Confirmed Redesign (2026-04-09, Narrative N1):** Narrative page visual shell + tweet card + composer polish. CSS-only, scoped to `custom_liquid_7HLMyz` in `templates/page.narrative.json`. (1) **Glow-box softened:** Border thinned from `1.5px` to `1px`, opacity from `0.45` to `0.18`. Padding tightened. `akBoxPulse` shadow opacities halved at both keyframe stops — container breathes gently instead of strobing. (2) **Tweet card material:** Left border thinned from `4px` to `3px` with semi-transparent purple. Background shifted from pure black `rgba(0,0,0,0.55)` to purple-tinted dark `rgba(10,6,18,0.6)`. Bottom border added for card separation. Hover glow halved (opacity `0.6` → `0.3`, shadow `14px` → `10px`). Tweet gap tightened `2rem` → `1.6rem`. Avatar border/glow softened. (3) **Composer cohesion:** Border thinned from `2px` to `1px` with semi-transparent purple — echoes card language. Background shifted to match card tint `rgba(10,6,18,0.65)`. Button border softened. Button hover glow tightened. **Preserved:** All posting JS, tweet rendering logic, tweet data array, scroll-reveal behavior, typing-loader tweet, Enter-to-post, particle canvas logic (no optimization yet), ak-top, ak-footer, HTML structure. Duplicate HTML container and global `*` reset left for future passes.
  - **Confirmed Redesign (2026-04-09, Phase 7A):** Life & Style Adventures subtle 3D hover depth. Added `perspective:1200px` to Adventures dock container (`#akmod-adventures-yt .ak-adv-dock`). Inside `@media (hover:hover) and (pointer:fine)`, non-hero cards receive `transform-style:preserve-3d` and hover/focus-visible `transform: rotateX(-2deg) rotateY(3deg) translateY(-3px)`. Hero card (card 1) excluded from tilt via `:not(:nth-child(1))` — stays flat. Existing `translateY(-3px)` hover lift folded into compound transform. Existing `transition: transform .28s cubic-bezier(.25,.46,.45,.94)` animates the tilt smoothly. Reduced-motion suppression: `perspective:none` on dock, `transform-style:flat` on cards. CSS-only, scoped to `custom_liquid_X46wQm`. Zero HTML, zero JS. Same `perspective:1200px` value as Channels' proven 3D tilt system. Kid Radio unchanged (no 3D). Adventures hero hierarchy, glass card system, playing states, title animation, canvas, embeds, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 5B):** Life & Style Kid Radio wave-bar prominence + material divergence. Wave bar strengthened as broadcast-deck identity feature: height `28px` → `36px`, padding `4px 6px 6px` → `6px 8px 8px`, background `rgba(0,0,0,.50)` → `rgba(0,0,0,.58)`, border-top `rgba(255,51,255,.10)` → `rgba(255,51,255,.16)`. Material diverged from Adventures: shimmer `::before` opacity `.05` → `.025`, shimmer animation `5.5s` → `8s`, specular `::after` gradient `rgba(255,255,255,.12/.04)` → `rgba(255,255,255,.07/.02)`, `backdrop-filter: saturate(210%)` → `saturate(170%)`. Kid Radio now reads as matte-console vs Adventures' glassy cinema. Adventures retains full shimmer/specular/saturation. CSS-only, scoped to `custom_liquid_kkxqR3`. Zero HTML, zero JS. Kid Radio flat 3×2 grid, 20px radius, 18px padding, wave bar JS, playing states, title animation, canvas, embeds, Adventures, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 5A):** Life & Style Adventures featured-card hierarchy. Broke the equal-weight 3×2 Adventures grid into a cinematic featured-card layout using CSS grid placement only, scoped to `custom_liquid_X46wQm`. Desktop (>980px): Card 1 (hero) `grid-column: 1 / 3; grid-row: 1 / 3` — spans 2 columns × 2 rows on the left. Card 2 `grid-column: 3; grid-row: 1` — top-right. Card 3 `grid-column: 3; grid-row: 2` — mid-right. Cards 4–6 auto-flow into row 3 as 3-across supporting row. Tablet (≤980px): cards 1–3 reset to auto placement, card 1 spans full width (`1 / -1`), grid switches to 2-col. Mobile (≤540px): single-column stack. Kid Radio grid unchanged (flat 3×2 equal-weight). CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, playing states, title animations, Phase 3C/4A/4A.1 values, canvas, embeds, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 4A.1):** Life & Style play-state color wash neutralized for Adventures + Kid Radio. Section-wide aura `::before` disabled on play (`opacity:0`, was `.75`) in both sections. Playing-card `border-color` neutralized to `rgba(255,255,255,.18)` (was amber `rgba(255,180,60,.35)` / magenta `rgba(255,51,255,.35)`). Playing-card colored box-shadow layers neutralized to white equivalents (`rgba(255,255,255, .06/.08/.04)`). Playing-card `::after` specular tint neutralized to `rgba(255,255,255,.08)`. Playing-frame `border-color` neutralized to `rgba(255,255,255,.12)`. Playing-frame glow neutralized to `rgba(255,255,255,.04)`. Playing emphasis now comes from lift (`translateY(-4px)`), auto-focus dimming, neutral glow, and contrast — not colored wash. At-rest identity cues preserved: Adventures warm frame `rgba(255,200,140,.08)`, hover ring `rgba(255,180,60,.08)`, Kid Radio magenta frame tint `rgba(255,200,255,.08)`, wave bar magenta accent. CSS-only, scoped by section ID. Zero HTML, zero JS.
  - **Confirmed Redesign (2026-04-09, Phase 4A):** Life & Style shape divergence for Adventures + Kid Radio. Adventures cards squared: `border-radius:10px` (was 20px) on `.ak-holo-card` + `::before` + `::after`. Adventures video-card padding tightened: `12px` (was 14px). Adventures frame squared: `border-radius:8px` (was 14px). Adventures frame border heavier warm: `2px solid rgba(255,200,140,.08)` (was `1px solid rgba(255,255,255,.10)`). Kid Radio video-card padding roomier: `18px` (was 14px). Kid Radio wave bar taller: `height:28px` (was 24px). Kid Radio wave bar magenta accent: `border-top:1px solid rgba(255,51,255,.10)` (was `rgba(255,255,255,.06)`). Kid Radio card radius unchanged at 20px. CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, grid, canvas, YouTube behavior, title animations, Phase 3C color values all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 3C):** Life & Style identity differentiation for Adventures + Kid Radio. Adventures playing-state accent changed from magenta (`255,51,255`) to warm amber/gold (`255,180,60`) across all playing-state CSS (border-color, box-shadow, specular tint, frame glow, hover ring). Adventures aura gradient shifted warm (amber + soft orange + warm white). Kid Radio aura gradient shifted cool (green → cyan-blue, white → cool white; magenta retained). Kid Radio rest-state frame border tinted subtle magenta (`rgba(255,200,255,.08)`). CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, grid, canvas, wave bar, YouTube behavior, title animations all preserved. **Design note:** Ambient title animations (`akAdvFlicker`, `akKrSignal`) remain part of the intended final redesign direction — not temporary.
  - **Confirmed Redesign (2026-04-08, Phase 2):** Life & Style section headers, title simplification, helper text removal. (1) **Banner card → header:** `ak-holo-card` class removed from `.ak-adv-banner` and `.ak-kr-banner`. Glass styling stripped. `border-bottom` divider added. Banner `padding: 28px 6px 10px`, `margin: 0`. (2) **Title simplification:** Per-letter `<span>` markup abandoned — replaced with plain text (`THE ADVENTURES OF AMERIKID`, `KID RADIO`). CSS changed from `inline-flex` + `gap` + `letter-spacing` clamp to `text-align:center` + `letter-spacing:0.08em`. Resolves persistent centering issues from trailing phantom space in the flex/span model. (3) **Title animation simplified:** Per-letter stagger replaced with whole-title gentle opacity pulse (`akAdvFlicker` 4s, `akKrSignal` 5s). Opacity-only keyframes. (4) **Helper text removed:** "SELECT A VIDEO TO PLAY." and "SELECT A PLAYLIST TO PLAY." removed along with their CSS (`.ak-adv-banner-meta`, `.ak-kr-banner-meta`, pseudo-elements). (5) **Accent underlines preserved:** Adventures warm amber, Kid Radio magenta (60px gradient `::after`). All video card glass, aura, wave bar, canvas, grid, YouTube preserved.
  - **Confirmed Redesign (2026-04-08, Phase 1):** Life & Style visual continuity pass. Added `ak-collection-bg` section to `page.json` — same proven full-viewport particle background already running on 18 collection pages. CSS overrides force transparent Dawn section backgrounds so particles show through all content. All 3 module shells (Adventures, Kid Radio, Channels) softened: `background:transparent`, `border-radius:0`, `border:none`, `box-shadow:none`, `margin:0`. Modules now blend into the continuous particle field. Per-module canvases still run and add local density via `mix-blend-mode:screen`. Glass card styling on video/social cards preserved. Page now reads as one immersive environment instead of three stacked island widgets. 5 concurrent canvases on page (background + ak-top + 3 modules) — all with offscreen cancellation or visibility lifecycle. CSS-only plus one proven section addition. Zero JS changes.
  - **Confirmed Polish (2026-04-08):** Life & Style title animations — original per-letter scatter animations removed, replaced with whole-title gentle opacity pulses. Adventures: `akAdvFlicker` (4s, opacity 1→0.85). Kid Radio: `akKrSignal` (5s, opacity 1→0.82). Both titles are now plain text (no per-letter spans). `prefers-reduced-motion` suppresses animation. Channels/Social Dock title animation (`akChanTuneIn`) untouched.
  - **Confirmed Optimization (2026-04-05):** Collection-grid scroll-trigger reveal animations removed. Both `#ProductGridContainer` and each product card `<li>` had `scroll-trigger animate--slide-in` + `data-cascade` — 37 animated elements (1 container + 36 cards) starting at `opacity: 0.01` with `translateY(2rem)`. This created a double-stacked "waterfall pop-in" over the particle canvas. Removed from Liquid only. Cards now appear instantly. `animations.js`, `base.css`, and `animations_reveal_on_scroll` global setting untouched. All other sections retain scroll-trigger behavior.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` visible-cost reduction (Phase 1). Dust particle cap reduced from 3,000–17,000 to 800–2,000 and `ctx.shadowBlur` removed from dust draw calls. Floater glow, shooter glow, colors, blend mode, and all other behavior unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` floater cost reduction (Phase 3). Floater cap reduced from 440–1,530 to 200–800. Accent floater glowSize reduced from 14–32 to 8–18. Floater glow behavior preserved (just lighter/tighter). Dust, shooters, DPR, blend mode, filter, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` DPR cap to 1 (Phase 2). Canvas device pixel ratio hardcoded to 1 (was `Math.min(devicePixelRatio, 2)`). On DPR 2 screens, physical canvas drops from ~6.1M pixels to ~1.5M pixels — 4× fill-rate reduction. Particle counts, glow, shooters, blend mode, filter, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-back-field` visible-cost reduction. Dust cap reduced from desktop 1,200–20,000 / mobile 420–6,500 to desktop 400–2,000 / mobile 200–1,000. Floater cap reduced from desktop 240–1,400 / mobile 70–520 to desktop 120–600 / mobile 50–300. Dust `shadowBlur` removed. Floater and shooter glow preserved. Applied to both product templates.
  - **Combined Top-of-Page System (2026-04-04):** `sections/ak-top.liquid` created — a single shared section replacing the two adjacent `custom_liquid` sections (holo-rail + aknav) with one canvas, one header bar, and one nav panel. Dust capped at 400–800, no dust shadowBlur, floater glow and full shooting star system preserved. Visual continuity refinement applied same day: frosted-glass effect stripped from nav area so particles render continuously across full section height. **Site-wide rollout (2026-04-04):** All 17 remaining two-section templates migrated to ak-top (total 18/18 including cart). Removed ~1,310KB of inline custom_liquid.
  - **Collection Page Normalization (2026-04-07):** 12 collection templates that used an old CSS-only header/nav system (no canvas, no particles, no glass nav) migrated to ak-top. Old header/nav `custom_liquid` sections preserved with `"disabled": true`. All emoji scrolling banner sections disabled across all 18 Archive-linked collection templates. All 18 Archive-linked collection pages now use ak-top for uniform header/nav/particle continuity.
  - **Collection Page Particle Background (2026-04-07):** New shared section `sections/ak-collection-bg.liquid` provides full-viewport ambient particle background for all 18 Archive-linked collection pages. Fixed-position canvas with ak-top's exact particle system (dust, floaters, shooters, DPR 1). CSS overrides force black body and transparent Dawn section backgrounds so particles show through all content. Added as first section in each template's order array. Uses `visibilitychange` lifecycle (not IO) since fixed canvas is always in viewport.
  - **Universal Footer / Back to Top (2026-04-07):** `sections/ak-footer.liquid` provides a branded Back to Top launchpad for 7 top-level pages: Life/Style, Products, Archive, Narrative, Bad Music, Mystery Box, Clubhouse. Template-scoped (NOT global) — added as last section in each template's order array. Contains: circular ring button with upward chevron SVG, magenta glow hover state, pulse + launch click animation, smooth scroll to top, "BACK TO TOP" micro-label in Impact, copyright line, gradient divider. Light inline JS (~12 lines) for click handler. No canvas. Reduced-motion safe. `footer.liquid`, `footer-group.json`, and `theme.liquid` untouched. Footer deliberately excluded from cart, product pages, and all 18 Archive-linked collection pages. (Supersedes the earlier glass-tab nav footer concept.)
  - **Confirmed Optimization (2026-04-16):** `ak-collection-field` desktop floater ceiling reduced 800 → 600 (next smallest-safe collection-page compositor pass). In `sections/main-collection-product-grid.liquid` line 504, floater count formula changed from `Math.round(clamp((area / 1600) * s, 200, 800))` to `Math.round(clamp((area / 1600) * s, 200, 600))`. Desktop-only impact because mobile's `densityScale()` of 0.62 keeps the floater calculation below the new 600 cap already — mobile visual density is unchanged. ~25 % reduction in accent-glowing floaters on desktop large viewports where the prior cap was hit. Matches `ak-back-field`'s proven desktop floater ceiling of 600 (shipped 2026-04-04). Dust ceiling, DPR 1, `mix-blend-mode: screen`, shooter cadence, accent `glowSize`, `IntersectionObserver` cancel/restart, reduced-motion hide, color palette, all CSS, all other formulas unchanged. Mystery Crate files and commerce logic untouched.
  - **Confirmed Optimization (2026-05-12):** `ak-top` glint pseudo-element `filter:blur(.15px)` removed (next smallest-safe site-wide scroll-lag pass). Post-deploy validation of the `ak-top-canvas` DPR cap showed site-wide lag persisted: ak-top routes 24–43 FPS, homepage (no ak-top) 72.5 FPS — confirming ak-top as the dominant shared compositor cost. Live in-page isolation measured `akTopGlint` killed → +7.9 FPS on `/collections/master-collection`. `.ak-top-label::after` runs 5 simultaneous infinite instances stacking `background-position` animation + `mix-blend-mode: screen` + `filter:blur(.15px)`. The 0.15 px blur is below the perceptual threshold on every modern display but still routes the layer through the GPU filter pipeline every frame on 5 simultaneous pseudos. Only the `filter:blur(.15px);` declaration removed from `sections/ak-top.liquid` line 197. **Preserved unchanged:** `akTopGlint` keyframe animation (still 2.85s linear infinite), `background-position` sweep `-140% → 160%`, opacity progression, `mix-blend-mode: screen`, `-webkit-background-clip:text`, linear-gradient sheen, hover state `filter:blur(0px)` override (left in place, now vacuous but harmless), `.ak-top-label::before` (the holographic backdrop), particle counts, DPR cap=1, `akTopCartBob`, nav drawer, cart button, mobile drawer, every other selector and rule in `sections/ak-top.liquid`. Affects every page using `ak-top` (18+ templates). Mystery Crate files, `page.mystery-box.json`, `_preview_hat_id`, customer flow, iframe internals untouched. The `ak-product-field` `filter: contrast(1.1)` removal (page-specific) was identified in the same audit but deliberately not bundled — it remains a separate lane.
  - **Confirmed Optimization (2026-05-12):** `ak-top-canvas` DPR cap reduced from 2 to 1 (highest-confidence measured compositor pass). Live Chrome DevTools isolation on `/collections/master-collection` showed hiding `ak-top-canvas` recovered +23.8 FPS (~8.8 ms/frame) vs only +2.3 FPS for `ak-collection-field`. Root cause: `sections/ak-top.liquid` lines 555 and 574 both read `dpr = Math.min(window.devicePixelRatio || 1, 2)` — the only major canvas in the theme without a DPR=1 hardcap. On retina hardware that meant a 1600×1530 backing buffer (~2.45 MP) instead of 800×765 (~0.61 MP) — a 4× fill-rate cost on the always-on header that runs across every page using `ak-top` (18+ templates). Both expressions changed to literal `1`, matching the proven pattern used on `ak-product-field` (Phase 2 2026-04-04), `ak-collection-field` (Phase 1 2026-04-05), `ak-archive-canvas`, `ak-collection-bg`, `ak-mb-canvas`. Particle counts (dust 400–800, floaters 200–400), `akTopGlint` (5 simultaneous instances on `.ak-top-label::after`), `akTopCartBob`, `mix-blend-mode: screen`, shooter cadence, accent `glowSize`, `IntersectionObserver` cancel/restart, `ResizeObserver`, reduced-motion branch, nav drawer, cart button, glass nav, layout, all CSS — all unchanged. **Measurement disqualified the previously planned `ak-collection-field` dust ceiling 2000 → 1500:** the entire `ak-collection-field` canvas contributes only ~1.3 ms/frame, so a 25 % dust trim would be ~0.3 ms/frame — below measurement noise. Mystery Crate files, `page.mystery-box.json`, `_preview_hat_id`, customer flow, iframe internals, and all commerce logic untouched.
  - **Confirmed Optimization (2026-04-16):** `ak-top-canvas` CSS `filter: contrast(1.1)` removed (Phase 2 — smallest-safe collection-page compositor pass). Eliminates a per-frame GPU filter pass on the always-on top canvas that renders on every collection page and 17 other ak-top-using templates. `mix-blend-mode: screen`, particle counts, glow, shooters, colors, blend mode, DPR cap, IntersectionObserver cancel/restart, `akTopCartBob` + `akTopGlint` animations, nav drawer, cart button, glass nav, layout all unchanged. Driven by real-browser Web Vitals `onINP` measurement showing 1640–1656ms interaction delay on `/collections/master-collection` dominated by presentation delay (paint/compositor), not JS processing. Matches the precedent set by `ak-collection-field` (Phase 2, 2026-04-05), `bv-field` (2026-04-09), and Life/Style content canvases (2026-04-05). Mystery Box wrapper, Mystery Crate iframe, `ak-mb-canvas` filter, and all commerce/customer logic untouched.
- **Builder Remnants:** **EComposer** and **GemPages** are inactive in terms of page layout. EComposer render calls (`ecom_header`, `ecom_footer`) have been removed from `layout/theme.liquid` (2026-03-27), eliminating 6 preconnect/prefetch links to `cdn.ecomposer.app` and 167+ lines of conditional JS on every page. Note: `theme.pagefly.liquid` and `layout/ecom.liquid` still render these snippets. GemPages assets (`gp-global.css`) were not observed loading at runtime.

## Needs Verification
- Whether all the `image-banner-[name].liquid` sections are genuinely required or if they can be consolidated.
- How much of `global.js` and `animations.js` is custom versus stock Dawn.
- If `assets/gp-global.css` is truly orphaned (it appears so from runtime traces).

## Shopify CLI Push Info

- **Store domain:** `iacxyv-w5.myshopify.com`
- **Live theme ID:** `184615043352` (`Copy of club amerikid update`)
- **Development theme ID (active, used by this workflow):** `185910067480` — all particle-removal batches push here. (A separate older dev theme `184629133592` (`Development (cff5cd-Declans-MacBook-Pro)`) also exists but is NOT the target for this workflow.)

**Commands:**
```bash
# List all themes
shopify theme list --store iacxyv-w5.myshopify.com

# Push to live theme (CAUTION — this is the live storefront)
shopify theme push --theme 184615043352 --path . --store iacxyv-w5.myshopify.com

# Push to development theme (safe for testing) — active workflow dev theme
shopify theme push --theme 185910067480 --nodelete --store iacxyv-w5.myshopify.com
```

## Product Card Badges
- **NEW badge source of truth (2026-05-28):** Shopify product tag `new` (case-insensitive) drives the comic-style NEW badge on every product card across the theme. The previous hardcoded handle allow-list (`candyfacts-mystery-box-combo`, `candyfacts-zombie-slayer-trucker-hat`, `candyfacts-label-t-shirt`) and its `grid__item--new` magenta-glow `::after` system in `sections/main-collection-product-grid.liquid` have been removed. Tagging products `new` in Shopify Admin is now the only mechanism. Sold-out remains highest priority; NEW second; on-sale third. Lives in `snippets/card-product.liquid` (Liquid + class) + `assets/base.css` (red comic-burst clip-path styling).

## AI Workflow Expectations
1. Read `AGENTS.md` and this file.
2. Read `docs/known-fragile-areas.md` and `docs/theme-architecture.md` if touching layout or specific pages.
3. Propose changes with risk assessment.
4. Execute approved changes.
5. Log changes in `docs/change-log.md`.
