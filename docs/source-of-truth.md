# Source of Truth

## Repo Purpose
This repository holds the unpublished working copy of the live Amerikid Shopify theme. It serves as a staging ground for performance optimization and cleanup before pushing updates to the live store.

## High-Level Theme Description
This theme is based on Shopify Dawn but contains heavy custom aesthetic overrides, most notably a global "Impact" font style, neon elements, and custom interactive "starfield" canvas animations. It also shows traces of multiple page builder apps coexisting.

## Current Known Priorities
1. Establish cross-AI continuity and reliable documentation (Complete).
2. Reduce lag and unnecessary fat (bloat, large global assets, duplicated scripts) without altering visual output.
3. Consolidate duplicated navigation code currently hardcoded across multiple JSON templates.
4. Future Phase: Mystery Crate integration work.

## Locked / Non-Negotiable
- The visual identity, animations, and "feel" of the website must not change.
- Mystery Crate logic is critical and must not be broken during cleanup.

## Optimization Goal
Make the theme less glitchy, faster, leaner, and more maintainable while preserving the exact current experience.

## Mystery Crate Note
The Mystery Crate is currently implemented as an iframe embed pointing to an external Vercel app. The wrapper logic contains fragile styling and its own canvas animation. 
- **Verified Runtime Finding:** The Vercel app is currently returning a 404 for `crate.glb`. This is an external issue but affects the "glitchy" feel.
- **Static Lock Removed (2026-04-02):** The wrapper's `LOCKED_DEFAULT` boolean was changed from `true` to `false` on the unpublished theme. The blur overlay and pointer-events block are now disabled, exposing the iframe for interaction. No dynamic entitlement gating exists — the wrapper does not check `customer.tags`, `crate_spins:*`, or metafields. It passes `customer_id` and `demo` to the iframe but no entitlement data. Future entitlement gating will require new Liquid conditionals and potentially new iframe params.
- **Display Shell (2026-04-08, Stage 2 + 2.5):** The Mystery Box iframe frame has been restyled from glassmorphism to a ruggedized bunker-monitor display shell with material polish. Housing uses subtle top-to-bottom gradient (`linear-gradient(180deg, #111, #0a0a0a)`), asymmetric chin padding (8/8/14/8 desktop, 6/6/10/6 mobile), squared corners (`border-radius: 6px`), layered shadow for object depth, top-edge bevel highlight (`::before`, peak 0.13 opacity), power LED (`::after`, positioned in chin at `bottom: 5px`). Screen-edge seam defined via `.ak-mb-stage::before` inset shadow (`z-index: 3`). Lock overlay covers screen area inside bezel (`box-sizing: border-box` + `padding`). Lock card kicker/title use Impact font. CSS-only — zero DOM, zero JS.
- **Wrapper Particle Parity (2026-04-08):** The Mystery Box embed canvas (`ak-mb-canvas`) has been brought into exact particle parity with `ak-top`. DPR capped to 1, dust 400–800 desktop / 120–300 mobile, floaters 200–400 desktop / 40–100 mobile, dust `shadowBlur` removed, CSS filter reduced to `contrast(1.1)`, floater glowSize 7–16. Shooters/meteors unchanged. This is the same proven pattern applied to ak-product-field, ak-collection-field, ak-back-field, and ak-top.
- **Release Overlay Added (2026-04-07, corrected same day):** `sections/ak-mb-release.liquid` is a JS/CSS orchestrator that controls the existing `#akMbLock` overlay inside the embed section. It renders no visible HTML of its own — it shows the existing lock overlay, injects a countdown timer into the lock card, and auto-unlocks when time passes. Overlay covers only the iframe/frame area, not the full viewport. Nav/header/footer remain accessible. Target release: **2026-04-09T07:00:00Z (Thu Apr 9, 12:00 AM America/Los_Angeles)**. **Client-side enforcement only** — not server-side. Bypassable via JS disable or clock manipulation. Stronger enforcement would require the Vercel app or a Shopify app proxy checking server time. After the release date passes, the section's JS immediately returns (no-op) but should be removed from `page.mystery-box.json` for cleanliness.

## Verified Runtime Findings (2026-03-19)
- **Active Builder:** **PageFly** is confirmed active and driving the homepage.
- **App Bloat:** **Videeo** and **JustSell** are initializing globally on every page (Home, Product, Collection, Mystery Box). Empty PopClips carousel app blocks have been removed from `page.club-hub.json`, `page.json`, and `page.badmusic.json` (2026-04-03). The global Videeo app embed SDK remains enabled in `config/settings_data.json` — disabling it is the next step pending business confirmation that Videeo/PopClips is unused. **Gameball** has been fully removed from theme code (2026-03-27): hardcoded snippet includes removed from both layouts, snippet file deleted, app embed disabled in settings_data.json. Note: the Gameball Shopify app may still need to be uninstalled from the Shopify admin separately.
- **Redundant Canvas Systems:** Product pages and Mystery Box pages are running up to 5 and 4 concurrent `<canvas>` elements respectively (e.g., `aknav-field`, `ak-product-field`, `ak-back-field`, `holo-rail-field`). This is a major source of GPU lag.
  - **Confirmed Optimization (2026-03-19):** `ak-product-field` now uses true offscreen cancellation — its `requestAnimationFrame` loop stops entirely when scrolled out of viewport and restarts on re-entry.
  - **Confirmed Optimization (2026-03-20):** `ak-collection-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the collection product grid scrolls out of viewport and restarts on re-entry.
  - **Confirmed Optimization (2026-03-20):** `ak-back-field` now uses the same true offscreen cancellation pattern. Its existing partial `IntersectionObserver` (which previously kept scheduling rAF frames while offscreen) was upgraded to fully cancel the loop when offscreen and restart cleanly on re-entry.
  - **Confirmed Optimization (2026-03-26):** `holo-rail-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the header/rail scrolls out of viewport and restarts on re-entry. Patched across all 18 JSON templates (two code variants: 10 pretty-printed, 8 minified).
  - **Confirmed Optimization (2026-03-27):** `aknav-field` now uses the same true offscreen cancellation pattern. Its rAF loop stops entirely when the nav starfield scrolls out of viewport and restarts on re-entry. Patched across all 18 JSON templates (four code variants). All 5/5 known canvas systems now have offscreen cancellation.
  - **Confirmed Optimization (2026-04-05):** `ak-collection-field` visible-cost reduction (Phase 1). DPR capped to 1. Dust cap reduced from 3,600–20,000 to 800–2,000. Floater cap reduced from 520–1,800 to 200–800. Accent floater glowSize reduced from 14–32 to 8–18. Dust shadowBlur removed. Floater glow, shooter glow, colors, blend mode, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-05):** `ak-collection-field` CSS `filter: contrast(1.1)` removed (Phase 2). Eliminates a per-frame GPU filter pass applied to the full canvas area (~1.5M pixels at DPR 1) every animation frame. `mix-blend-mode: screen`, particle counts, glow, shooters, colors, blend mode, layout all unchanged.
  - **Confirmed Polish (2026-04-05):** Product purchase panel CSS polish applied. Price scaled to 2.4rem mobile / 3rem desktop. Add to Cart button overridden to filled primary visual weight (white bg, dark text) with subtle purple hover glow. Description contained via border-top separator and 0.85 opacity. Spacing rhythm refined across title/price/variants/buy/description. Stacked gallery grid tightened on desktop (0.6rem gap, 3px border-radius). All CSS-only in `{%- style -%}` block of main-product.liquid.
  - **Confirmed Cleanup (2026-04-05):** Product info block order reordered in both product templates: title → price → variant_picker → quantity_selector → buy_buttons → description → share. Empty `custom_liquid_KbTzPh` block removed. Moves purchase controls directly below title for stronger conversion flow.
  - **Confirmed Cleanup (2026-04-05):** Back button section (`custom_liquid_6wWQFN`) disabled in both product templates via `"disabled": true`. Eliminates the ak-back-field canvas (dust, floaters, shooters) and BACK button from product pages. Section code preserved — re-enable by removing the `disabled` flag.
  - **Confirmed Cleanup (2026-04-05):** Product gallery settings: final state is `gallery_layout: "stacked"` + `image_zoom: "lightbox"` in both product templates. Tried `"thumbnail"` layout but it removed visible stacked images — user prefers stacked. Tried `"hover"` zoom but it hid the lightbox button on desktop — user prefers lightbox. Current combo shows all images stacked vertically with click-to-lightbox on each.
  - **Confirmed Cleanup (2026-04-05):** Collection-card image ratio: tried `"portrait"` (4:5), reverted back to `"square"` (1:1). User prefers the square grid layout. The crop issue on one apparel product should be addressed via product image re-crop in Shopify admin, not theme-wide ratio change.
  - **Confirmed Cleanup (2026-04-05):** Collection-card title visual cleanup. Titles now render in Impact, uppercase, with no hover underline. CSS added to the section `{%- style -%}` block in `main-collection-product-grid.liquid`. Card layout, zoom, spacing, sold-out badge, and pagination unchanged.
  - **Confirmed Optimization (2026-04-05):** Life & Style content canvas optimization. All 3 inline content canvases in `templates/page.json` (Adventures `custom_liquid_X46wQm`, Kid Radio `custom_liquid_kkxqR3`, Social Dock `custom_liquid_PRGR7i`) received the proven optimization package: DPR capped to 1, dust 1,600–3,400 → 800–2,000, floaters 280–560 → 200–400, dust shadowBlur removed, `filter: contrast(1.1)` removed, IntersectionObserver offscreen cancellation added. These 3 modules previously had zero offscreen cancellation — rAF loops ran continuously even when scrolled out of view. All 5 site-wide canvas systems + 3 Life & Style content canvases now have offscreen cancellation and visible-cost reductions.
  - **Confirmed Redesign (2026-04-09, Narrative N1):** Narrative page visual shell + tweet card + composer polish. CSS-only, scoped to `custom_liquid_7HLMyz` in `templates/page.narrative.json`. (1) **Glow-box softened:** Border thinned from `1.5px` to `1px`, opacity from `0.45` to `0.18`. Padding tightened. `akBoxPulse` shadow opacities halved at both keyframe stops — container breathes gently instead of strobing. (2) **Tweet card material:** Left border thinned from `4px` to `3px` with semi-transparent purple. Background shifted from pure black `rgba(0,0,0,0.55)` to purple-tinted dark `rgba(10,6,18,0.6)`. Bottom border added for card separation. Hover glow halved (opacity `0.6` → `0.3`, shadow `14px` → `10px`). Tweet gap tightened `2rem` → `1.6rem`. Avatar border/glow softened. (3) **Composer cohesion:** Border thinned from `2px` to `1px` with semi-transparent purple — echoes card language. Background shifted to match card tint `rgba(10,6,18,0.65)`. Button border softened. Button hover glow tightened. **Preserved:** All posting JS, tweet rendering logic, tweet data array, scroll-reveal behavior, typing-loader tweet, Enter-to-post, particle canvas logic (no optimization yet), ak-top, ak-footer, HTML structure. Duplicate HTML container and global `*` reset left for future passes.
  - **Confirmed Redesign (2026-04-09, Phase 7A):** Life & Style Adventures subtle 3D hover depth. Added `perspective:1200px` to Adventures dock container (`#akmod-adventures-yt .ak-adv-dock`). Inside `@media (hover:hover) and (pointer:fine)`, non-hero cards receive `transform-style:preserve-3d` and hover/focus-visible `transform: rotateX(-2deg) rotateY(3deg) translateY(-3px)`. Hero card (card 1) excluded from tilt via `:not(:nth-child(1))` — stays flat. Existing `translateY(-3px)` hover lift folded into compound transform. Existing `transition: transform .28s cubic-bezier(.25,.46,.45,.94)` animates the tilt smoothly. Reduced-motion suppression: `perspective:none` on dock, `transform-style:flat` on cards. CSS-only, scoped to `custom_liquid_X46wQm`. Zero HTML, zero JS. Same `perspective:1200px` value as Channels' proven 3D tilt system. Kid Radio unchanged (no 3D). Adventures hero hierarchy, glass card system, playing states, title animation, canvas, embeds, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 5B):** Life & Style Kid Radio wave-bar prominence + material divergence. Wave bar strengthened as broadcast-deck identity feature: height `28px` → `36px`, padding `4px 6px 6px` → `6px 8px 8px`, background `rgba(0,0,0,.50)` → `rgba(0,0,0,.58)`, border-top `rgba(255,51,255,.10)` → `rgba(255,51,255,.16)`. Material diverged from Adventures: shimmer `::before` opacity `.05` → `.025`, shimmer animation `5.5s` → `8s`, specular `::after` gradient `rgba(255,255,255,.12/.04)` → `rgba(255,255,255,.07/.02)`, `backdrop-filter: saturate(210%)` → `saturate(170%)`. Kid Radio now reads as matte-console vs Adventures' glassy cinema. Adventures retains full shimmer/specular/saturation. CSS-only, scoped to `custom_liquid_kkxqR3`. Zero HTML, zero JS. Kid Radio flat 3×2 grid, 20px radius, 18px padding, wave bar JS, playing states, title animation, canvas, embeds, Adventures, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 5A):** Life & Style Adventures featured-card hierarchy. Broke the equal-weight 3×2 Adventures grid into a cinematic featured-card layout using CSS grid placement only, scoped to `custom_liquid_X46wQm`. Desktop (>980px): Card 1 (hero) `grid-column: 1 / 3; grid-row: 1 / 3` — spans 2 columns × 2 rows on the left. Card 2 `grid-column: 3; grid-row: 1` — top-right. Card 3 `grid-column: 3; grid-row: 2` — mid-right. Cards 4–6 auto-flow into row 3 as 3-across supporting row. Tablet (≤980px): cards 1–3 reset to auto placement, card 1 spans full width (`1 / -1`), grid switches to 2-col. Mobile (≤540px): single-column stack. Kid Radio grid unchanged (flat 3×2 equal-weight). CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, playing states, title animations, Phase 3C/4A/4A.1 values, canvas, embeds, Channels, ak-top, ak-footer all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 4A.1):** Life & Style play-state color wash neutralized for Adventures + Kid Radio. Section-wide aura `::before` disabled on play (`opacity:0`, was `.75`) in both sections. Playing-card `border-color` neutralized to `rgba(255,255,255,.18)` (was amber `rgba(255,180,60,.35)` / magenta `rgba(255,51,255,.35)`). Playing-card colored box-shadow layers neutralized to white equivalents (`rgba(255,255,255, .06/.08/.04)`). Playing-card `::after` specular tint neutralized to `rgba(255,255,255,.08)`. Playing-frame `border-color` neutralized to `rgba(255,255,255,.12)`. Playing-frame glow neutralized to `rgba(255,255,255,.04)`. Playing emphasis now comes from lift (`translateY(-4px)`), auto-focus dimming, neutral glow, and contrast — not colored wash. At-rest identity cues preserved: Adventures warm frame `rgba(255,200,140,.08)`, hover ring `rgba(255,180,60,.08)`, Kid Radio magenta frame tint `rgba(255,200,255,.08)`, wave bar magenta accent. CSS-only, scoped by section ID. Zero HTML, zero JS.
  - **Confirmed Redesign (2026-04-09, Phase 4A):** Life & Style shape divergence for Adventures + Kid Radio. Adventures cards squared: `border-radius:10px` (was 20px) on `.ak-holo-card` + `::before` + `::after`. Adventures video-card padding tightened: `12px` (was 14px). Adventures frame squared: `border-radius:8px` (was 14px). Adventures frame border heavier warm: `2px solid rgba(255,200,140,.08)` (was `1px solid rgba(255,255,255,.10)`). Kid Radio video-card padding roomier: `18px` (was 14px). Kid Radio wave bar taller: `height:28px` (was 24px). Kid Radio wave bar magenta accent: `border-top:1px solid rgba(255,51,255,.10)` (was `rgba(255,255,255,.06)`). Kid Radio card radius unchanged at 20px. CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, grid, canvas, YouTube behavior, title animations, Phase 3C color values all preserved.
  - **Confirmed Redesign (2026-04-09, Phase 3C):** Life & Style identity differentiation for Adventures + Kid Radio. Adventures playing-state accent changed from magenta (`255,51,255`) to warm amber/gold (`255,180,60`) across all playing-state CSS (border-color, box-shadow, specular tint, frame glow, hover ring). Adventures aura gradient shifted warm (amber + soft orange + warm white). Kid Radio aura gradient shifted cool (green → cyan-blue, white → cool white; magenta retained). Kid Radio rest-state frame border tinted subtle magenta (`rgba(255,200,255,.08)`). CSS-only, scoped by section ID. Zero HTML, zero JS. Glass card system, grid, canvas, wave bar, YouTube behavior, title animations all preserved. **Design note:** Ambient title animations (`akAdvFlicker`, `akKrSignal`) remain part of the intended final redesign direction — not temporary.
  - **Confirmed Redesign (2026-04-08, Phase 2):** Life & Style section headers, title simplification, helper text removal. (1) **Banner card → header:** `ak-holo-card` class removed from `.ak-adv-banner` and `.ak-kr-banner`. Glass styling stripped. `border-bottom` divider added. Banner `padding: 28px 6px 10px`, `margin: 0`. (2) **Title simplification:** Per-letter `<span>` markup abandoned — replaced with plain text (`THE ADVENTURES OF AMERIKID`, `KID RADIO`). CSS changed from `inline-flex` + `gap` + `letter-spacing` clamp to `text-align:center` + `letter-spacing:0.08em`. Resolves persistent centering issues from trailing phantom space in the flex/span model. (3) **Title animation simplified:** Per-letter stagger replaced with whole-title gentle opacity pulse (`akAdvFlicker` 4s, `akKrSignal` 5s). Opacity-only keyframes. (4) **Helper text removed:** "SELECT A VIDEO TO PLAY." and "SELECT A PLAYLIST TO PLAY." removed along with their CSS (`.ak-adv-banner-meta`, `.ak-kr-banner-meta`, pseudo-elements). (5) **Accent underlines preserved:** Adventures warm amber, Kid Radio magenta (60px gradient `::after`). All video card glass, aura, wave bar, canvas, grid, YouTube preserved.
  - **Confirmed Redesign (2026-04-08, Phase 1):** Life & Style visual continuity pass. Added `ak-collection-bg` section to `page.json` — same proven full-viewport particle background already running on 18 collection pages. CSS overrides force transparent Dawn section backgrounds so particles show through all content. All 3 module shells (Adventures, Kid Radio, Channels) softened: `background:transparent`, `border-radius:0`, `border:none`, `box-shadow:none`, `margin:0`. Modules now blend into the continuous particle field. Per-module canvases still run and add local density via `mix-blend-mode:screen`. Glass card styling on video/social cards preserved. Page now reads as one immersive environment instead of three stacked island widgets. 5 concurrent canvases on page (background + ak-top + 3 modules) — all with offscreen cancellation or visibility lifecycle. CSS-only plus one proven section addition. Zero JS changes.
  - **Confirmed Polish (2026-04-08):** Life & Style title animations — original per-letter scatter animations removed, replaced with whole-title gentle opacity pulses. Adventures: `akAdvFlicker` (4s, opacity 1→0.85). Kid Radio: `akKrSignal` (5s, opacity 1→0.82). Both titles are now plain text (no per-letter spans). `prefers-reduced-motion` suppresses animation. Channels/Social Dock title animation (`akChanTuneIn`) untouched.
  - **Confirmed Optimization (2026-04-05):** Collection-grid scroll-trigger reveal animations removed. Both `#ProductGridContainer` and each product card `<li>` had `scroll-trigger animate--slide-in` + `data-cascade` — 37 animated elements (1 container + 36 cards) starting at `opacity: 0.01` with `translateY(2rem)`. This created a double-stacked "waterfall pop-in" over the particle canvas. Removed from Liquid only. Cards now appear instantly. `animations.js`, `base.css`, and `animations_reveal_on_scroll` global setting untouched. All other sections retain scroll-trigger behavior.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` visible-cost reduction (Phase 1). Dust particle cap reduced from 3,000–17,000 to 800–2,000 and `ctx.shadowBlur` removed from dust draw calls. Floater glow, shooter glow, colors, blend mode, and all other behavior unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` floater cost reduction (Phase 3). Floater cap reduced from 440–1,530 to 200–800. Accent floater glowSize reduced from 14–32 to 8–18. Floater glow behavior preserved (just lighter/tighter). Dust, shooters, DPR, blend mode, filter, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-product-field` DPR cap to 1 (Phase 2). Canvas device pixel ratio hardcoded to 1 (was `Math.min(devicePixelRatio, 2)`). On DPR 2 screens, physical canvas drops from ~6.1M pixels to ~1.5M pixels — 4× fill-rate reduction. Particle counts, glow, shooters, blend mode, filter, CSS, layout all unchanged.
  - **Confirmed Optimization (2026-04-04):** `ak-back-field` visible-cost reduction. Dust cap reduced from desktop 1,200–20,000 / mobile 420–6,500 to desktop 400–2,000 / mobile 200–1,000. Floater cap reduced from desktop 240–1,400 / mobile 70–520 to desktop 120–600 / mobile 50–300. Dust `shadowBlur` removed. Floater and shooter glow preserved. Applied to both product templates.
  - **Combined Top-of-Page System (2026-04-04):** `sections/ak-top.liquid` created — a single shared section replacing the two adjacent `custom_liquid` sections (holo-rail + aknav) with one canvas, one header bar, and one nav panel. Dust capped at 400–800, no dust shadowBlur, floater glow and full shooting star system preserved. Visual continuity refinement applied same day: frosted-glass effect stripped from nav area so particles render continuously across full section height. **Site-wide rollout (2026-04-04):** All 17 remaining two-section templates migrated to ak-top (total 18/18 including cart). Removed ~1,310KB of inline custom_liquid.
  - **Collection Page Normalization (2026-04-07):** 12 collection templates that used an old CSS-only header/nav system (no canvas, no particles, no glass nav) migrated to ak-top. Old header/nav `custom_liquid` sections preserved with `"disabled": true`. All emoji scrolling banner sections disabled across all 18 Archive-linked collection templates. All 18 Archive-linked collection pages now use ak-top for uniform header/nav/particle continuity.
  - **Collection Page Particle Background (2026-04-07):** New shared section `sections/ak-collection-bg.liquid` provides full-viewport ambient particle background for all 18 Archive-linked collection pages. Fixed-position canvas with ak-top's exact particle system (dust, floaters, shooters, DPR 1). CSS overrides force black body and transparent Dawn section backgrounds so particles show through all content. Added as first section in each template's order array. Uses `visibilitychange` lifecycle (not IO) since fixed canvas is always in viewport.
  - **Universal Footer / Back to Top (2026-04-07):** `sections/ak-footer.liquid` provides a branded Back to Top launchpad for 7 top-level pages: Life/Style, Products, Archive, Narrative, Bad Music, Mystery Box, Clubhouse. Template-scoped (NOT global) — added as last section in each template's order array. Contains: circular ring button with upward chevron SVG, magenta glow hover state, pulse + launch click animation, smooth scroll to top, "BACK TO TOP" micro-label in Impact, copyright line, gradient divider. Light inline JS (~12 lines) for click handler. No canvas. Reduced-motion safe. `footer.liquid`, `footer-group.json`, and `theme.liquid` untouched. Footer deliberately excluded from cart, product pages, and all 18 Archive-linked collection pages. (Supersedes the earlier glass-tab nav footer concept.)
- **Builder Remnants:** **EComposer** and **GemPages** are inactive in terms of page layout. EComposer render calls (`ecom_header`, `ecom_footer`) have been removed from `layout/theme.liquid` (2026-03-27), eliminating 6 preconnect/prefetch links to `cdn.ecomposer.app` and 167+ lines of conditional JS on every page. Note: `theme.pagefly.liquid` and `layout/ecom.liquid` still render these snippets. GemPages assets (`gp-global.css`) were not observed loading at runtime.

## Needs Verification
- Whether all the `image-banner-[name].liquid` sections are genuinely required or if they can be consolidated.
- How much of `global.js` and `animations.js` is custom versus stock Dawn.
- If `assets/gp-global.css` is truly orphaned (it appears so from runtime traces).

## Shopify CLI Push Info

- **Store domain:** `iacxyv-w5.myshopify.com`
- **Live theme ID:** `184615043352` (`Copy of club amerikid update`)
- **Development theme ID:** `184629133592` (`Development (cff5cd-Declans-MacBook-Pro)`)

**Commands:**
```bash
# List all themes
shopify theme list --store iacxyv-w5.myshopify.com

# Push to live theme (CAUTION — this is the live storefront)
shopify theme push --theme 184615043352 --path . --store iacxyv-w5.myshopify.com

# Push to development theme (safe for testing)
shopify theme push --theme 184629133592 --path . --store iacxyv-w5.myshopify.com
```

## AI Workflow Expectations
1. Read `AGENTS.md` and this file.
2. Read `docs/known-fragile-areas.md` and `docs/theme-architecture.md` if touching layout or specific pages.
3. Propose changes with risk assessment.
4. Execute approved changes.
5. Log changes in `docs/change-log.md`.
