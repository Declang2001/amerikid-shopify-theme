# Runtime Test Checklist

Use this checklist to verify the storefront after any approved change is implemented.

## Notebook Paper Experiment (theme 186464731416 ONLY — branch `experiment/notebook-paper-background`)
Run this section ONLY against the unpublished experiment theme (`?preview_theme_id=186464731416`). The live theme and normal dev theme do not carry the notebook look.
- [ ] **Club Hub (`/pages/club-hub`) desktop:** Verify yellow legal-pad paper + red left / blue right vertical rules + blue horizontal ruled lines + clean top/bottom blank bands. Verify all **10 doodles** render (8-piece composition + 2 side-margin: left **eyes** beside the member card outside the red rule, right **cross** outside the blue rule) with NO overlap on the KID logo, nav wordmarks, MONIKER glow, BAD MUSIC, CANDYFACTS, cart (including the fixed cart's position when scrolled to the bottom), member/passport card, LOG IN / JOIN, footer CTA, or copyright. Verify the side-margin doodles stay OUTSIDE the red/blue rules at 1536/1440/1366/1280 and are **hidden below 1200px**. Verify **MONIKER active glow** (purple wordmark + magenta halo at rest; other tiles stay ink). Verify mask nav hover → purple, MENU drawer (mobile width) opens/closes, cart visible/clickable, member card + LOG IN/JOIN intact, "CLICK HERE TO GET TOP" scrolls to top, no horizontal overflow at 1536/1440/1366/1280/1024.
- [ ] **Club Hub mobile (≤768px / 375/320):** the whole doodle wrapper is `display:none`; paper + content + drawer + cart + CTA intact; no horizontal overflow.
- [ ] **Life/Style (`/pages/the-collective`) desktop:** Verify paper backdrop behind the dark glass/video modules. Verify **active LIFE/STYLE wordmark is purple with magenta glow** at rest. Verify the 13-doodle page-specific map at ≥1400px (3 header + 10 lower/sidebar), no overlaps with logo/nav/cart/modules/footer. Verify **responsive doodle gates**: ≤1399px the lower/sidebar 8 hide; 1024–1279px is **star-only** (hdr-left); ≤1023px all hidden. Verify the full-width title divider lines stay REMOVED and the small accent dashes under Adventures/Kid Radio stay REMOVED (no layout shift). Verify **title animations** still run and **YouTube embeds** still load/play in Adventures and Kid Radio; glass cards/3D tilt/wave bar intact. Verify footer CTA works. No horizontal overflow.
- [ ] **Life/Style mobile (375/320):** doodles hidden, modules/text readable, no overflow.
- [ ] **Products (`/collections/master-collection`) desktop:** Verify the notebook paper proof renders ONLY here (`.ak-products-page` on `<html>`; yellow paper + red/blue rules + ruled lines + clean top/bottom bands). Verify **PRODUCTS active wordmark is purple + magenta glow** at rest. Verify product titles are **ink** (`#15120b`) and readable, with magenta hover/focus. Verify the **v3 cutout shadows wrap the actual product silhouettes** — check at CLOSE ZOOM (not grid overview; this caught two false passes): combo right/bottom, FreeVitaly bottom, both raglans' hems, KID tray sides, skate table right, bucket hat, KANDY hoodie hood — **no squared-off CSS clipping at any image-box edge**. Verify `.card__media` AND `.card__media .media` BOTH compute `overflow:visible` (only on this page) and the drop-shadow filter sits on the **img**. Slow-hover several cards: **no shadow flicker/disappear/reappear**. Verify NEW badges unchanged/unshadowed; product links go to the correct products; grid layout/sold-out behavior untouched; footer CTA scrolls; no doodles (deferred); no horizontal overflow at 1536/1440/1366/1280/1024. Known accepted asset limits: FreeVitaly hem + combo right edge end in a soft straight band (source-PNG crop, not CSS).
- [ ] **Products mobile (375/320):** paper + ink titles + shadows intact, single-column grid, no overflow, drawer works.
- [ ] **Products sibling-collection guard (every Products pass):** `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve` must stay BLACK with NO `.ak-products-page` class and NO `ak-notebook-products` snippet in the DOM (the `collection.handle == 'master-collection'` Liquid guard in `templates/collection.json` is load-bearing).
- [ ] **Products Phase 7.1 doodles (experiment theme):** at ≥1366px all **8** doodles render (header star+cross beside the KID logo, margin hashtag/eyes, gutter dollar+dice, bottom kid + mirrored star); at 1280–1365px the **gutter pair hides** (6 visible); below 1200px **all hide**. Verify zero overlap with the KID logo, nav tile boxes, cart (rest AND max-scroll band), product photos, v3 shadow halos, titles, NEW badges, footer CTA, and copyright — check at close zoom, not grid overview. Margins may cross the painted red/blue rules but never card boxes. Bottom pair stays bottom-anchored above the footer regardless of title wrapping. Mirrored bottom star must read distinct from the header star. No horizontal overflow at any width (the rotated bounding boxes have bitten before).
- [ ] **Products font proof (Phase 6.3 "Due Date", until the owner's verdict):** grid titles render in **AK Due Date** (distressed stencil) at **3.3rem** with Impact as the in-stack fallback; the font loads from **`/cdn/.../assets/ak-due-date.woff2` (theme asset — NOT Shopify Files, NOT Google Fonts; no Londrina request remains)**; special characters render — digits, parentheses, periods, hyphens, slash, `&`, `!` in Due Date and **`#` in stencil Tenada (`#CandyFACTS`/`#FreeVitaly` — Due Date has no `#` glyph; the `'AK Tenada'` `unicode-range:U+23` @font-face serves it; `assets/ak-tenada.woff2` ~1.3KB loads from theme assets on Products ONLY; if it ever fails, `#` falls back to Impact)**; verify the title stack computes `'AK Tenada','AK Due Date',Impact,…` and that NO ak-tenada request appears on any non-Products route; **no `T-`/`SHIRT` hyphen split** ("CandyFACTS Label T-Shirt" must break on word boundaries at every width — this is why the size is 3.3rem, not 3rem); no font request on any other route.
- [ ] **Life/Style PNG titles (experiment theme):** all three handwritten PNGs render centered and ≤ native size (Adventures `clamp(72px,8.4vw,125px)` ≈ 550×121 @1440; Kid Radio 425×59; Channels 408×53); the real heading text remains in the DOM (collapsed only in notebook scope); **Adventures + Kid Radio still pulse** (element-level flicker transfers to the PNGs); **Channels is static** with no per-letter artifact; Adventures clears the video grid below (~10px + banner padding) and the side doodles; Kid Radio/Channels spacing reads natural (their PNGs are shorter than the old text lines); CHANNELS stays clear of the chan-l/chan-r flank doodles; mobile (≤768) titles readable, no overflow; YouTube embeds/wave bar/channel cards unaffected.
- [ ] **Dotted/marker nav divider REMOVED (owner rejection, 2026-06-10) — verify it stays gone:** on Life/Style (`/pages/the-collective`), Products (`/collections/master-collection`), and Club Hub (`/pages/club-hub`), `.ak-top-nav-area::after` must compute `content:none` and `animation-name:none` — NO marker/dotted line under the nav, NO draw-on animation, at any width. Nav tiles/hover/active, cart, and the MENU drawer must work normally. **Products font checks:** grid titles render the Phase 6.3 Due Date proof (`AK Due Date`, Impact fallback) — **NOT Black Ops One** (rejected; no `family=Black+Ops+One` request in the network tab), **no Google Fonts request from Products** (Londrina removed 2026-06-11), and **no owner-font requests anywhere** (no dDizzyStencil/STENCILED files — those exist only as browser-local previews and must never appear as theme/network resources). Mystery Box / cart / product pages unchanged (iframe loads, Add to Cart + Shop Pay render).
- [ ] **Notebook leak battery (run after ANY notebook-snippet change):** sibling collections (`/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve`) stay black with no notebook classes/fonts/doodles; `/`, `/cart`, a product page (Add to Cart + Shop Pay render), `/pages/mystery-box` (Vercel iframe loads) unchanged; Club Hub and Life/Style and Products each keep their own systems with no cross-page class/asset leaks.
- [ ] **Active nav glow generally:** on each notebook page, ONLY the current page's tile glows at rest; hover/focus still flips other wordmarks purple.
- [ ] **Leak checks (every notebook pass):** `/`, `/cart`, `/collections/master-collection`, a product page (e.g. `/products/candyfacts-mystery-box-combo`), and `/pages/mystery-box` must show NO `.ak-clubhub-page` / `.ak-lifestyle-page` class on `<html>`, NO doodle wrappers in the DOM, normal black backgrounds, product Add to Cart + dynamic checkout rendering, cart intact, and the **Mystery Box Vercel iframe loading** with its customer flow untouched. No new console errors beyond the pre-existing `video-player` registry error / favicon 404 / crate THREE.js warnings.

## Core Pages
- [ ] **Homepage:** Verify fullscreen MP4 video plays automatically on desktop (black background → video fades in). Verify static fallback image displays on mobile (≤767px, video hidden). Verify clicking anywhere on the splash triggers a 350ms scale+fade transition, then navigates to `/pages/mystery-box`. Verify bouncing kid logo appears, bounces, squashes on wall hits, pauses on hover (desktop). Verify logo click triggers the same transition (not a direct navigate). Verify no double-navigation if splash and kid are clicked rapidly. Verify logo renders above the splash (z-index layering). Verify background particles render during the exit transition fade (visible as splash fades out). Verify reduced-motion: transition skipped, instant navigation. Verify no PageFly CSS/JS loading in Network tab (no `pagefly-animation.css`, no `cdn.pagefly.io` requests). Verify no layout shift or flash on load. Verify kid moves at consistent speed regardless of monitor refresh rate (K1 delta-time). Verify kid starting direction has slight variation between page loads (not always exactly 45°). Verify kid glow pulses gently with white + purple/magenta layers (K2 akGlow, 3s cadence). Verify kid has subtle scale breathing (K2 akBreath, 4s cadence). Verify kid glow brightens on desktop hover. Verify wall-hit squash/stretch still fires with glow maintained through animation. No new console errors.
- [ ] **Collection Pages:** Check grid layout, image loading, and that the Impact font is still applied where expected. **Default-template collections (`/collections/master-collection`, `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve`):** verify NO visible particle background — `ak-collection-field` is neutralized (2026-05-19) so the `<canvas class="ak-collection-field">` element is present in the DOM but inert/blank (default 300×150, 0 drawn pixels); background is plain solid black; product cards/images/titles/prices/sold-out badges/links/hover/facets/sorting/pagination/layout all render normally; `ak-top` header particles intact. **Named collections** (e.g. `/collections/uh-oh-tee`): only `ak-top` canvas — grid is disabled, so no `ak-collection-field` and no `ak-cbg-cv`.
- [ ] **Product Pages:** Check `main-product` layout. Verify NO visible particle background — `ak-product-field` is neutralized (2026-05-19) so the `<canvas class="ak-product-field">` element is present in the DOM but inert/blank (default 300×150, 0 drawn pixels); background is plain solid black. Verify `ak-back-field` is absent/disabled. Verify the full purchase flow intact: media gallery + images, lightbox, variant selector, variant image switching, quantity, product form, Add to Cart (cart updates), price, dynamic checkout/payment buttons, pickup availability + accordions if present, sticky behavior, hover, layout, related products (lazy-loaded on scroll). Verify `ak-top` header particles still render. **Count canvases: should not increase (ak-top + inert ak-product-field).** **Description/helper text full white (2026-06-04):** verify the product description/helper text below the buy buttons (`.product__description` — e.g. on `/products/candyfacts-mystery-box-combo` the `PURCHASED SPINS…` / `HOW THE CANDYFACTS MYSTERY BOX WORKS` copy, which is Shopify Admin product-description content) renders **full white** (`opacity:1` + `color:rgb(255,255,255)`). Verify the product **title/price/variant and other product-column text are NOT changed** (they keep the inherited `rgba(255,255,255,0.75)` — the white override is scoped to `.product__description` only). Verify Add to Cart + Shop Pay/dynamic payment buttons still render and layout is unchanged. Verify readable at 375/320px. No new console errors.
- [ ] **Archive Page (`/collections`):** Verify NO particle background (disabled 2026-05-21 via guard-disable) — background is plain solid black; the `ak-archive-canvas`/`#AkArchiveCanvas` element is present in the DOM but inert/blank (default 300×150, 0 drawn pixels), no dust/floaters/shooting stars. Verify `ak-top` header particles still render. Verify cards read as cassette cases — visible dark shell housing around cover art, subtle plastic sheen, hinge line on left edge, spine-style tabs on trailing cards. Verify stack composition preserved (5 trailing strips visible). Verify tap navigates, drag works, arrows work, keyboard works (deck engine unaffected — it initializes before the particle guard). Verify title labels readable over inset art. Verify mobile 375/320px (reduced padding, smaller tabs).
- [ ] **Life/Style Page (`/pages/the-collective`):** Verify NO `ak-collection-bg` particle background (disabled 2026-05-19) — background is plain solid black; no `ak-cbg-cv-…` canvas in the DOM. Verify all three module ambient canvases (`.ak-adv-field`, `.ak-kr-field`, `.ak-chan-field`) draw NO particles (neutralized 2026-05-19 via `if(false && canvas && !reduced)` guards — elements present in the DOM but blank: default 300×150, 0 drawn pixels). Verify module content reads cleanly on solid black (modules are transparent containers over black — no dead-gap regression since plain black is the intended state). Verify module content readable. Verify glass card styling on video cards still present (borders, shimmer, hover lift, playing states). Verify Adventures title "THE ADVENTURES OF AMERIKID" renders as plain centered text with clean spacing (no per-letter gaps). Verify Kid Radio title "KID RADIO" renders as plain centered text with clean spacing. Verify no helper text below either title (no "SELECT A VIDEO/PLAYLIST TO PLAY."). Verify Adventures has amber accent underline below title. Verify Kid Radio has magenta accent underline below title. Verify both titles are centered relative to video cards below. Verify gentle whole-title ambient animation running on both titles. Verify Channels title animation unchanged. Verify Channels 3D tilt on desktop. Verify Adventures featured-card hierarchy on desktop: card 1 spans 2 columns × 2 rows on left (hero), card 2 top-right, card 3 mid-right, cards 4/5/6 clean 3-across row below. Verify no orphaned single card in Adventures. Verify Adventures cards read squared (10px radius, not rounded). Verify Adventures cards feel compact (12px padding). Verify Adventures iframe frame has squared corners (8px) and visible warm border (2px). Verify Adventures cards 2–6 show subtle 3D hover tilt on desktop (rotateX/rotateY + lift). Verify Adventures hero card (card 1) does NOT tilt on hover — stays flat. Verify Adventures glass card shimmer/specular not broken by 3D tilt (no flicker, no missing blur). Verify Kid Radio cards remain rounded (20px radius). Verify Kid Radio grid unchanged (flat 3×2 equal-weight, no hierarchy). Verify Kid Radio has NO 3D tilt. Verify Kid Radio cards feel roomier (18px padding). Verify Kid Radio wave bar is taller (36px, reads as dedicated meter strip). Verify Kid Radio wave bar has stronger magenta top-line separator. Verify Kid Radio cards feel subtly less shimmery/glassy than Adventures (matte-console material divergence). Verify Kid Radio shimmer animation slower/calmer than Adventures. Verify Adventures playing-card glow reads neutral white (not amber — Phase 4A.1 neutralized). Verify Adventures hover ring reads warm at rest. Verify NO section-wide color wash when Adventures video plays. Verify Kid Radio playing-card glow reads neutral white (not magenta — Phase 4A.1 neutralized). Verify NO section-wide color wash when Kid Radio video plays. Verify Kid Radio iframe borders have very subtle magenta tint at rest. Verify active card lifts and non-playing cards dim on play. Verify YouTube embeds load and play in both Adventures and Kid Radio. Verify wave bar visualizer on Kid Radio. Verify ak-top particles still render above background canvas. Verify ak-footer renders cleanly at bottom. Verify tablet (≤980px): Adventures card 1 full-width, remaining cards flow cleanly in 2-col grid. Verify mobile 375/320px — no overflow, text readable, modules flow seamlessly, Adventures cards stack single-column, no 3D tilt on touch devices. Verify reduced-motion: background canvas hidden, title animations disabled, Adventures 3D tilt disabled (perspective:none, transform-style:flat). Verify no layout shift. Verify no new console errors. **Count canvases: should be 4 in DOM (ak-top + 3 inert module fields `.ak-adv-field`/`.ak-kr-field`/`.ak-chan-field`), with `ak-cbg-cv` absent (ak-collection-bg disabled 2026-05-19) and all 3 module fields blank/inert.**
- [ ] **Bad Music Page (`/pages/2025`):** Verify NO `ak-collection-bg` particle background (disabled 2026-05-19) — background is plain solid black; no `ak-cbg-cv-…` canvas in the DOM. Verify the TV hero's own ambient `ak-bg-canvas` draws NO particles (neutralized 2026-05-19 via early `return;` in the "Ambient background particles" IIFE inside `custom_liquid_dx4GNc` — the `<canvas class="ak-bg-canvas">` element is present but blank: default 300×150 size, 0 drawn pixels, no dust/floaters/shooting stars). Verify ak-top renders correctly. Verify "2025 (The Album)" caption appears ABOVE the TV hero (not below). Verify red BAD MUSIC banner scrolls normally. Verify 3D retro TV hero renders and plays exactly as before. Verify "BAD MUSIC" heading appears between TV hero and cassette shelf (Impact font, centered, magenta accent line). Verify two-cassette shelf visible: left cassette (2025 album, MP4 video) and right cassette ("Coming Soon" overlay). Verify cassettes read as 3D physical objects — visible case edges, transparent lid with specular, inner cassette body with reels, tape path, label window. Verify left cassette MP4 plays when shelf scrolls into view and pauses when offscreen. Verify hover lifts cassette with deeper tilt on desktop (pointer:fine only). Verify left cassette click navigates to `/pages/badmusic-2025`. Verify right cassette is not clickable (no link). Verify BAD Vinyl / CD interaction area is NOT visible on this page. Verify background behind cassette shelf is plain solid black (no ambient particles — the previously-visible particle field was removed 2026-05-19). Verify ak-footer renders correctly. Verify mobile 375/320px — cassettes stack or fit, no overflow, text readable. No new console errors.
- [ ] **Bad Music 2025 Page (`/pages/badmusic-2025`):** Verify NO `ak-collection-bg` particle background (disabled 2026-05-19) — background is plain solid black; no `ak-cbg-cv-…` canvas in the DOM. Verify the vinyl section's own `bv-field` ambient canvas draws NO particles (neutralized 2026-05-19 via early `return;` in `initBadVinylAmbient()` — the `<canvas class="bv-field">` element is present in the DOM but blank: default 300×150 size, 0 drawn pixels, no dust/floaters/shooting stars). Verify ak-top renders correctly. Verify album intro block visible above vinyl grid ("BAD MUSIC" kicker, "2025" title, "THE ALBUM — 12 TRACKS" subtitle). Verify BAD Vinyl / CD interaction grid renders with all behavior (tilt, hover, click, audio, seek, spin). Verify hovering a vinyl card does NOT reset audio playback (V1 bug fix: audio.load guard). Verify card material reads as dark semi-transparent (`rgba(12,12,12,.6)`). Verify seek bar visible (6px height). Verify time display uses Impact font. Verify ak-footer renders correctly. Verify mobile 375/320px — no overflow, intro block responsive, grid stacks. No console errors.
- [ ] **Narrative Page:** Verify glow-box border reads as thin, subtle magenta frame (not bold/aggressive). Verify glow-box pulse breathes gently (not strobing). Verify tweet cards have purple-tinted dark background (not pure black). Verify tweet left border thinner and semi-transparent. Verify subtle bottom separation line between cards. Verify hover glow softer (not blast). Verify avatar rings calmer. Verify composer border thinner and subtler. Verify composer background matches card tint language. Verify button hover glow present but tighter. Type a post and hit Enter — post prepends correctly. Verify typing-loader tweet (first tweet) still shows dots animation. Scroll down/up — tweet entrance animations still fire. Verify NO particle background (disabled 2026-05-21) — background is plain solid black; the `akfeed-field` canvas is present in the DOM but inert/blank (default 300×150, 0 drawn pixels), no dust/floaters/shooting stars. Verify ak-top renders correctly. Verify ak-footer renders correctly. Verify mobile 375/320px — no overflow, text readable. No new console errors.
- [ ] **Custom Landing Pages:** Spot check pages like "The Collective" or specific collection drops for missing assets.
- [ ] **Mystery Crate Page:** Verify the theme-side ambient particle canvas is hidden — `.ak-mb-canvas` should compute `display:none` (no visible particle background behind the embed; 2026-05-22 CSS-only hide), while the Mystery Crate iframe/app and customer flow remain fully intact (the JS was NOT touched). Verify the iframe loads the Vercel app. **Check console for crate.glb status.** Verify display shell: frame should read as a dark matte monitor housing with subtle top-to-bottom gradient, asymmetric bezel (8px top/sides, 14px chin on desktop; 6px/10px on mobile), squared corners, subtle top-edge bevel highlight, and dim green power LED centered in chin area. Iframe content should fill the screen area inside the bezel with a slight recess (inset shadow). Verify release overlay: before release time, the lock overlay should cover the screen area inside the bezel (not the bezel itself, not the full viewport). Lock card corners should match the shell (squared, not rounded). Nav/header (ak-top) must remain accessible. Footer must remain accessible. Countdown timer (DD:HH:MM:SS) should be visible inside the lock card. Timer should tick every second. Interaction with iframe should be blocked. To test auto-unlock: temporarily set `TARGET_UTC` to a past time in `ak-mb-release.liquid` and reload — lock overlay should not appear. To test live unlock: set target ~10 seconds in the future and watch the lock hide. Verify mobile 375/320px (smaller digits, thinner bezel, card fits). After release date (2026-04-09T07:00:00Z / Thu Apr 9, 12:00 AM PT): overlay should not appear at all. **Customer ID handoff (2026-04-09):** Signed-in visit → DevTools Elements: `#akMbIframe` src should be `https://amerikid-mystery-crate.vercel.app?customer_id=<your id>` → crate enters purchased mode. Guest email-CTA path → open a real order-confirmation CTA link (`/pages/mystery-box?customer_id=...&syclid=...`) in a logged-out browser → iframe src should now include that `customer_id` → crate enters purchased mode (previously fell into preview). Guest direct visit (no query params, logged out) → iframe src should be bare `https://amerikid-mystery-crate.vercel.app` → crate previews as before. `/pages/mystery-box?demo=1` → iframe src should include `demo=1` (and `customer_id` if signed-in or supplied via URL). **Public prestart single-CTA (2026-04-15):** Public visit (no `customer_id` URL param) → prestart overlay shows one button: `START / MYSTERY BOX` (primary, centered). Verify START dismisses overlay (200ms fade), restores iframe pointer-events, crate spin flow becomes interactive. Verify no secondary button visible. Verify subtitle reads `START // SPIN`. Verify 3 briefing lines: `START HERE TO SPIN THE CRATE.` / `THE COMBO PACK INCLUDES 1 MYSTERY CRATE HAT + 1 CANDYFACTS SHIRT.` (where `COMBO PACK` is a magenta underlined link to `https://amerikid.ca/products/candyfacts-mystery-box-combo`) / `BUY THE COMBO PACK FIRST. ACCESS 2 SPINS THROUGH YOUR EMAIL CONFIRMATION LINK.`. Verify no "free" or "preview" wording anywhere in public state. Verify inline COMBO PACK link navigates to combo product page. Verify link hover brightens to lighter magenta and underline thickens. Verify disclaimer reads `START HERE · OR COMBO PACK · 2 SPINS`. Verify purchased prestart (`?customer_id=test`) unchanged: single `ACCESS SPINS / PURCHASED ACCESS` button, dismiss works. Verify mobile 375/320px — button centered, no overflow. **Phase 1.5 atmospheric styling (2026-04-15):** Verify scanlines visibly scroll downward (slow, subtle — not jarring). Verify grain texture drifts (barely perceptible lateral motion). Verify vignette breathes (edge darkness gently pulses). Verify MYSTERY BOX title has a gentle magenta glow pulse (text-shadow blooms and recedes on ~6s cycle). Verify primary CTA has a faint outer glow pulse (not distracting, reads as alive). Verify plate top edges have subtle brightness flicker (irregular, not uniform). Verify faint purple atmospheric haze visible behind content (radial gradient, barely there). Verify public state (`ak-mb-prestart--public` class on root) reads colder/eerier — slower animation cadences. Verify purchased state (`ak-mb-prestart--purchased` class on root) reads more energized — faster cadences, slightly brighter haze/glow. Verify both states use only purple (`#ff33ff`) family — no green, amber, or other accent colors. Verify reduced-motion: all Phase 1.5 ambient animations killed (no scrolling scanlines, no grain drift, no breathing, no glow pulses, no flicker, no haze). Verify no new console errors. Verify no layout shift from new animations. Verify hover states on CTA still work cleanly over the glow pulse. **Phase 2.0 visibility tuning (2026-04-15):** Verify CTA glow pulse is now actually visible — a faint magenta halo should pulse around the primary button (was previously hidden behind opaque background). Verify glow does not cover or obscure button label text. Verify CTA hover/active/focus states still work correctly over the glow. Verify scanlines are now clearly perceptible as horizontal stripes scrolling downward (should read as CRT monitor lines, not just vague shimmer). Verify scanlines do not reduce text readability. Verify purple atmospheric haze is now visible as a soft central purple tint in the background (not foggy, but clearly present). Verify haze pulses gently (8s cycle public, 5s purchased). Verify plate top-edge flicker is now visible as a 2px magenta accent line that flickers irregularly (reads as terminal power-line). Verify purchased state shows proportionally brighter haze than public. Verify no content clipping on CTA button after overflow:hidden removal (button text, `::before` gradient sheen should still render cleanly within bounds). **Phase 3.0 shell continuity (2026-04-15):** Verify `.ak-mb-shell-fx` is mounted as the first child of `.ak-mb-frame` (DevTools Elements). **During prestart overlay:** shell-fx layers are visually hidden beneath the prestart overlay's opaque gradient background — that is correct. **After clicking START:** once the overlay fades and is removed, the shell-fx layers should be clearly visible over the iframe content. Verify subtle horizontal scanlines continue scrolling over the iframe (more subtle than the prestart scanlines — about half the opacity). Verify very faint grain texture is present over the iframe (barely perceptible drift). Verify a thin magenta phosphor hairline sits just inside the top bezel edge and breathes gently (7s cycle, 0.55↔1.0 opacity). Verify four small magenta L-bracket register marks sit inset from each screen corner (top-left, top-right, bottom-left, bottom-right). Verify chin telemetry strip reads `CRATE // LINK ACTIVE` (public state, no `customer_id` URL param) or `CRATE // ACCESS GRANTED` (purchased state, `?customer_id=test`). Verify the chin cursor (6×8px magenta square) blinks next to the telemetry text (~1.1s cadence). Verify clicking START swaps chin wording to `CRATE // ONLINE` once the overlay is removed (200ms after click). Verify chin telemetry does not overlap or visually fight the centered green power LED (LED is centered in chin, telemetry is left-aligned). Verify LED now breathes (5s cycle, 0.68↔1.0 opacity) — reads as a powered machine instead of a static pixel. Verify a rare brief LED flicker (dims briefly via `filter:brightness()`) fires roughly every 27 seconds — should feel restrained, not flashy. Verify iframe remains fully interactive (pointer events pass through shell-fx because `pointer-events:none`). Verify clicking anywhere on the iframe still reaches the crate. Verify shell-fx scanlines/grain paint inside the screen area only (not into the bezel). Verify on mobile 375/320px: shell-fx insets match chin padding (6/6/10/6), register marks shrink to 6×6, chin text uses 7.5px, all layers still render cleanly. Verify purple-only palette — no green/amber/cyan accents added. Verify reduced-motion: all Phase 3.0 animations stopped (no scanline scroll, no grain drift, no hairline pulse, no LED breathing or flicker, no chin cursor blink — cursor stays solid and hairline holds at 0.55 opacity). Verify no new console errors. Verify no new canvases added (shell-fx is DOM + CSS only). Verify no layout shift on mount. **Phase 4.0 Tier 1 hierarchy refinements (2026-04-15):** Verify the `— AMERIKID —` kicker above `MYSTERY BOX` now sits between 1px magenta hairlines (top + bottom), reads as a stamped document header rather than a soft caption, and holds higher alpha than before. Verify `START // SPIN` (public) and `ENTER // ACCESS` (purchased) subtitles now have a small magenta 4×8px block to the left of the text and another to the right, both with subtle glow — reads as terminal hardware flanking a status label. Verify the HOW IT WORKS plate's three public briefing lines now read in three distinct tiers: line 1 (`START HERE TO SPIN THE CRATE.`) larger (14px desktop) and brightest with a visibly glowing magenta chevron; line 2 (combo pack copy with inline magenta `COMBO PACK` link) at the prior baseline; line 3 (`BUY THE COMBO PACK FIRST. ACCESS 2 SPINS THROUGH YOUR EMAIL CONFIRMATION LINK.`) smaller (11px desktop) and dimmer. Verify purchased state's 2 briefing lines get the same line-1 emphasis on the first line with the second line unchanged. Verify the `COMBO PACK` inline link still navigates to the combo product page and still brightens on hover. Verify the bottom disclaimer strip now has a small magenta pulse dot anchored 14px from the left (mirrors the top serial strip's dot, same pulse cadence) and a small magenta bar anchored 14px from the right — now reads as a matched console footer bookend instead of a flat text strip. Verify disclaimer copy unchanged (`START HERE · OR COMBO PACK · 2 SPINS` public / `PURCHASED ACCESS · 2 SPINS · NON-REFUNDABLE` purchased). Verify keyboard focus echo: tab to the START / ACCESS SPINS CTA → the plate's top magenta accent line brightens to near-full magenta AND the subtitle's top border brightens to strong magenta and the subtitle text brightens toward `#ff66ff` in a smooth 160ms transition. Verify blurring focus reverses the echo smoothly. Verify hover state on CTA still works and still produces its own box-shadow glow. Verify mobile 640/380px breakpoints — kicker/subtitle bookends/brief-line tiers/disclaimer dots all scale proportionally, no overflow on 375/320px. Verify reduced-motion: disclaimer bookend dot holds steady (no pulse), focus echo still operates (color/border only, no animation). Verify no new DOM nodes added (Tier 1 is pseudo-element-only), no new canvases, no new console errors, no layout shift from the new hairlines or bookends. **Phase 5.0 Dossier / Eyes-Only framing (2026-04-15):** Verify an inset 1px magenta rule frames the prestart content area with ~16px padding from all four edges of the overlay (desktop), ~12px on 640px, ~10px on 380px. Verify four magenta L-shaped corner brackets sit at the four corners of that inset rule — larger and crisper than the shell-fx corner register marks behind the iframe. Verify a very faint repeating `CLASSIFIED // MYSTERY BOX` watermark runs horizontally across the top of the overlay, behind the inner content but above the atmospheric haze — readable only on inspection, should not fight hero legibility. Verify a matching `EYES ONLY // SPIN PROTOCOL` watermark runs across the bottom. Verify both watermarks are purple-only, very low alpha, sans-serif, wide tracking, uppercase. Verify the `MYSTERY BOX` title is now flanked by two full-width-ish magenta hairlines (top and bottom, gradient fading to transparent at the edges) — reads as a mastered title block, not floating type. Verify `START // SPIN` (public) and `ENTER // ACCESS` (purchased) subtitles now sit inside a status-cartridge bracket — a pair of thin magenta L-brackets with inner glow flanking the label, replacing the Tier 1 inline rectangles. Verify subtitle copy unchanged. Verify the HOW IT WORKS / YOUR ACCESS plate now shows a balancing decorative `· · ·` tab at the top-right of the plate (same typographic family as the existing top-left label) — plate reads as a two-terminal memo strip. Verify subtle dashed magenta dividers appear between briefing lines inside the plate (not between line content and the plate's bottom edge — only between consecutive brief lines). Verify the disclaimer strip picks up a fine vertical tick pattern in the background and a slightly brighter top border — reads as a designed console/signature footer. Verify disclaimer's Tier 1 pulse-dot (left) and bar (right) remain intact on top of the new tick background. Verify purple `#ff33ff` palette only — no gold/amber/green/cyan added. Verify z-index ladder: watermarks sit behind envelope, envelope sits behind inner content, so hero legibility never fights framing. Verify all new elements are `aria-hidden="true"` — screen readers read only the existing kicker/title/subtitle/plate/CTA/disclaimer. Verify Phase 5.0 adds no new animations — reduced-motion users see the same framing as everyone else (static envelope, static watermarks, static title rails, static cartridge, static plate tab, static disclaimer ticks). Verify button behavior unchanged: START still dismisses, ACCESS SPINS still dismisses, iframe pointer-events restore on dismiss, chin telemetry still swaps to `CRATE // ONLINE`. Verify 640px/380px responsive: envelope inset tightens, corner brackets shrink (18→14), watermark type shrinks (14→11→9), title rails shorten (max-width 560→420→300), subtitle cartridge padding tightens, plate right-tab shifts inward. Verify no overflow or clipping on 375/320px. Verify no new console errors, no new canvases, no layout shift from the new envelope/watermarks/rails.

## Product Cards
- [ ] **NEW badge — enlarged + 3D (2026-06-02):** The red comic NEW badge is **intentionally larger** with stronger 3D comic styling (`.card-wrapper .badge--new.ak-badge-new` in `assets/base.css`: desktop shape `9.6rem`, text held at `3rem`; mobile shape `6.6rem`, text `2rem`). Verify the badge is larger than the prior pass. Verify the **red burst shape is visibly larger than the `NEW` text** (more red around the word). Verify the **3D comic styling**: bright top-left highlight, darker red along the bottom edge, thicker black letter stroke, and a deeper black comic drop-shadow offset down/right. Verify the badge remains readable. Verify it still sits on top of the product image (top-left). Verify ONLY products tagged `new` show it; products without `new` do not. Verify the old hardcoded magenta `grid__item--new` system is still gone. Verify sold-out and sale badges are unchanged. Verify the badge does NOT cover the product title, price, or Add-to-Cart button. Verify mobile 375/320 px does not clip badly or break the grid (no horizontal overflow). No images/SVG/JS/animation added.
- [ ] **NEW badge (2026-05-28):** Verify the comic-style red NEW badge appears on product cards ONLY for products that have the Shopify product tag `new` (case-insensitive). Confirmed surfaces: default `collection.json` grid (`/collections/master-collection`, `/collections/all`, `/collections/candyfacts`, `/collections/free-vitaly-longsleeve`), `featured-collection` sections, `collage` tiles, `main-search` results, related products in `main-product`. Verify the OLD hardcoded handle-based NEW system (`grid__item--new` class on `<li>`, magenta-glow `::after` "NEW" pseudo-element on `.card-wrapper`) is fully GONE — no `.grid__item--new` nodes in the DOM, no `::after` content `"NEW"` on `.card-wrapper`. Verify on `/collections/candyfacts`: only `candyfacts-mystery-box-combo` shows the red badge (it has the `new` tag); `candyfacts-zombie-slayer-trucker-hat` and `candyfacts-label-t-shirt` do NOT (they were in the old hardcoded list, no longer marked). Verify priority chain holds — sold-out wins over NEW, NEW wins over sale. Visual: ~6.6rem red comic burst with 16-spike clip-path silhouette, cream/yellow inner radial glow, cream-white `NEW` text in Impact uppercase with thick black stroke (`text-shadow`), layered black `drop-shadow` for sticker depth, `transform: rotate(-12deg)` tilt, top-left of card (Dawn `.card__badge.top.left` grid placement). Mobile ≤480 px: shrinks to ~4.6rem with scaled stroke/shadow, still readable. Verify badge does not overlap product title, price, or Add-to-Cart button. Verify product grid layout, hover, sold-out diagonal banner, and sale badge are unchanged on un-tagged products. Verify console has no new errors. Tagging a product `new` in Shopify Admin makes the badge appear on its cards; removing the tag makes it disappear.

## Navigation and Interactivity
- [ ] **Mobile Menu / Drawer:** Click the "MENU" label. Ensure the drawer opens and closes properly (and click-outside closes). Check that the close "X" is positioned correctly. Note: the `ak-top` header particle canvas is disabled (2026-05-22) but the menu trigger / `openDrawer` / `closeDrawer` / click-outside behavior is unaffected (it initializes before the particle guard).
- [ ] **Footer back-to-top — enlarged (2026-06-02):** The footer text button is **intentionally larger** (`.ak-btt-text` desktop `font-size:1.6rem`, mobile `1.2rem`; `.ak-btt` padding `8px 14px`). Verify the visible copy is exactly `CLICK HERE TO GET TOP`. Verify the button is visibly larger than before. Verify the text **stays on one line** at 375/320 px (no wrap, no footer overflow). Verify click still scrolls to top (smooth; instant under `prefers-reduced-motion`). Verify the `:focus-visible` magenta outline still appears on keyboard tab. Verify the resting text is **full white** (`#fff`, 2026-06-04). Verify hover/focus turns the text `#ff33ff`. Verify click still scrolls to top. Verify on all 8 footer routes (`/pages/the-collective`, `/pages/club-hub`, `/pages/narrative`, `/pages/2025`, `/pages/badmusic-2025`, `/pages/mystery-box`, `/collections`, `/collections/master-collection`). Verify the Mystery Box iframe still loads on `/pages/mystery-box` (no customer-flow interaction).
- [ ] **CandyFacts nav icon — enlarged (2026-06-02):** The CandyFacts image button in the site-wide `ak-top` nav (right tile of the BAD+Candy duo, linking to `/pages/mystery-box`) is **intentionally enlarged on desktop** (`.ak-top-candy-img` `transform: scale(1.52)`, `.ak-top-candy` padding `clamp(4px,0.8vw,8px)`; ~241×195 @1440). Verify CandyFacts desktop icon is visibly larger than BAD. Verify the **BAD icon is unchanged**. Verify **MONIKER does not move or get overlapped** (clearance ~11/8/8 px @1440/1280/1024; at ≤980 px MONIKER is on its own row). Verify CandyFacts still links to `/pages/mystery-box` and its active state still fires on `/pages/mystery-box`. Verify the **mobile drawer is unchanged** (BAD + Candy still 120 px via the shared rule — not modified unless later approved). Verify the Mystery Box iframe still loads (no customer-flow interaction). Note: in the 768–980 px band the wide candy image's bounding box overlaps BAD's box (pre-existing geometry, transparent margins reduce visible collision, no page overflow).
- [ ] **Nav/Menu label — MONIKER (2026-06-02):** The site-wide nav tile linking to `/pages/club-hub` reads **MONIKER** (renamed from CLUBHOUSE; visible label-only). Verify the desktop nav tile reads MONIKER. Verify the mobile drawer link reads MONIKER. Verify both still link to `/pages/club-hub`. Verify MONIKER shows the active highlight on `/pages/club-hub`. Verify the MENU drawer still opens/closes (and click-outside closes). Route/handle (`/pages/club-hub`) and active-state logic (`request.path == '/pages/club-hub'`) are unchanged. Other nav labels (LIFE/STYLE, PRODUCTS, ARCHIVE, NARRATIVE, BAD Music, Candy Facts) unchanged.
- [ ] **Clubhouse Page (`/pages/club-hub`):** Verify NO particle background (disabled 2026-05-22) — background is plain solid black; the `ak-clubhub-field`/`#ak-clubhub-field` element is present in the DOM but inert/blank (default 300×150, 0 drawn pixels), no dust/floaters/shooting stars. Verify `ak-top` header particles still render. Verify `.ak-clubhub` content/cards/copy/links/footer render. In a logged-in / drops-active state, verify the drops form + captcha (hCaptcha) + buttons still surface and submit — `dropsForm`/`dropsBtn`/`findToken`/auto-submit are in separate untouched scripts. Verify mobile 375/320px. No new console errors.
- [ ] **Header/Footer:** Check for missing elements or misaligned global navigation. **Header particle background (`ak-top`):** verify NO particle field behind the header (disabled site-wide 2026-05-22) — the `ak-top-canvas` / `#ak-top-cv-{id}` element is present in the DOM but inert/blank (default 300×150, 0 drawn pixels); header background is plain solid black. Verify the header shell, nav labels, `akTopGlint` sheen, `akTopCartBob` cart bounce, cart button + live cart count, and the MENU drawer all still work. Verify ak-footer back-to-top control renders on Life/Style (`/pages/the-collective`), Clubhouse (`/pages/club-hub`), Narrative (`/pages/narrative`), Bad Music (`/pages/2025`), Bad Music 2025 (`/pages/badmusic-2025`), Mystery Box (`/pages/mystery-box`), Archive (`/collections`), and default-template collections (e.g. `/collections/master-collection`, `/collections/all`, `/collections/candyfacts`). Verify footer does NOT appear on cart, individual product pages, or named-collection pages. **Verify the control is now a text-only button reading exactly `CLICK HERE TO GET TOP`** (Impact, uppercase, 0.95rem desktop / 0.85rem ≤680 px, letter-spacing 0.18em / 0.16em mobile) — the previous 64×64 ring + SVG chevron + small "Back to Top" caption + launch-pulse animation are GONE (2026-05-28). Verify resting color reads as **full white (`#fff`)** (2026-06-04; was a muted off-white `rgba(255,255,255,.58)`). Verify hover and keyboard focus turn the text AmeriKid magenta `#ff33ff` with no layout shift. Verify `:focus-visible` shows a magenta outline ring around the button when tabbed via keyboard. Verify click scrolls smoothly to the top (and instantly under `prefers-reduced-motion: reduce`). Verify mobile 375 / 320 px — the single line does not wrap badly or overflow the footer. Verify no `is-launching` class is added on click (the JS toggle was removed; only the `scrollTo` + body/document fallbacks remain).
- [ ] **Cart visual system — unified panels (2026-06-02):** **Test WITH an item in the cart** (empty cart is sanity only — verify it renders). Add one safe test product (cart AJAX or add-to-cart), open `/cart`. Verify the item card and the checkout summary panel read as **one cohesive AmeriKid system** (same black-neon shell). Verify the summary panel's **HUD/scanline overlay now renders** (the curly-quote `content` bug is fixed). Verify the **QTY pill is balanced** with the checkout button (desktop `.quantity-input` 120×50, mobile 48px) and readable. Verify the **QTY value is editable and updating it updates the cart/totals**. Verify **REMOVE** works. Verify estimated total renders. Verify the **checkout button is primary and fills the 600px inner column** (magenta). Verify **Shop Pay / PayPal / GPay render unchanged internally** (centered beneath, ~396px Shopify-native). Verify mobile **375/320 px** has no overflow/clip and the item panel + summary panel **align at the same width** (both ~342px @375). **Clear the test item afterward. Do not place an order or enter checkout.** No new console errors (only pre-existing `video-player` registry warning).
- [ ] **Cart Drawer / Cart Page (`/cart`):** Verify NO particle background on the cart page (disabled 2026-05-22) — background is plain solid black; both cart canvases (`ak-cart-field` / `#ak-cart-canvas-{id}` and `ak-cart-footer-field` / `#ak-cart-footer-canvas-{id}`) are present in the DOM but inert/blank (default 300×150, 0 drawn pixels), no dust/floaters/shooting stars. Verify `ak-top` header particles still render. Verify empty-cart state renders. Add an item to the cart: verify line item, quantity controls, remove button, subtotal/totals, checkout button, dynamic/additional checkout buttons render; change quantity and remove work (AJAX). Verify the AJAX cart drawer slides out from product add-to-cart without breaking the layout (drawer has no canvas — untouched). Verify cart notification works. No new console errors.
- [ ] **Animations:** Ensure hover effects (glows, neon borders) on buttons and the continuous wrapper glow in the nav still function.

## Technical Checks
- [ ] **Console Errors:** Open browser developer tools. Ensure no **new** 404 errors or Javascript errors are present. Monitor `video-player` registry error.
- [ ] **Builder Regressions:** Verify that PageFly pages still function. Check that removing EComposer/GemPages remnants didn't break global styles (if applicable).
- [ ] **Asset Loading:** Check Network tab for `cdn.ecomposer.app` or `gp-global.css` calls.
- [ ] **Responsive Design:** Resize the viewport down to 320px. Ensure neon borders, canvases, and text do not break the horizontal bounds.
